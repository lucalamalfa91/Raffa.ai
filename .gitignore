# Local AFI checkout — junctions, not copies
.claude/marketplace/plugins/afi
.claude/skills/afi
.claude/skills/env
.claude/skills/scan
.claude/skills/query
.claude/settings.local.json

# Helix fan-out checkouts and process-local secrets
worktrees/
.helix/.env
.helix/node_modules/
.helix/.git/
.helix/.git.bak
.helix/.git.nest.bak
.helix/reports/plan/gates/

# Local az ad federated-credential dumps (not repo secrets, not source)
fic-dev.json
fic-demo.json

# Secrets and local environment files (never commit real credentials) —
# task E01/F01/US01/T01, parent story AC-4.
.env
.env.*
!.env.example
!.env.*.example
*.pem
*.key
*.pfx
*.p12
*.cer
appsettings.*.local.json
local.settings.json
*.tfvars
!*.tfvars.example
.terraform/
*.tfstate
*.tfstate.*

# Python bytecode cache — task E01/F01/US01/T02 introduces the first
# Python scripts/tests at the repo root.
__pycache__/
*.pyc

# .NET build artifacts — task E01/F04/US01/T01 scaffolds the backend solution.
[Bb]in/
[Oo]bj/
*.user
*.suo
*.nupkg
*.snupkg

# Node build artifacts (web/, and future mobile/) — task E01/F07/US01/T01
# scaffolds the web client.
node_modules/
web/dist/

# Playwright e2e run artifacts (web/e2e/day1.spec.ts) — task E08/F04/US01/T01
# adds the §20 Day-1 browser-walk integration gate. The HTML report / traces
# / videos are a local or CI run's own output, never source.
web/test-results/
web/playwright-report/
web/blob-report/
web/playwright/.cache/
