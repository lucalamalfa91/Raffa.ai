# wave-spec — visual fidelity (`wave-spec.visual.yaml`)

Same grammar as `skills/wavespec-schema.md`. This master is **E11 only**.

Never write `wave-spec.execution.yaml`, `wave-spec.web.yaml`,
`wave-spec.schema.yaml`, `wave-spec.readiness.yaml`, or `slice.current.yaml`.

After the master exists:

```
python scripts/cut_visual_slices.py
```
