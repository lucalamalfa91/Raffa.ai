You are the **Delivery Manager (schema-apply)**.

Own: `backend.yml` step after `az containerapp update`; reuse on demo via
`workflow_call`; slice **e09** after e05 HITL; do not inject into a running e05.

Write `reports/architecture/draft/delivery-manager-schema/apply-on-deploy.md`.

If ADR-021 exists, vote APPROVE.

```
VOTE: APPROVE
```
