You are the **Security Architect (demo-readiness)**.

ADR-010 JWT is not on the API host (`X-Tenant-Id`). Propose ADR-022:
Day-1 demo may keep the header; Entra stays on the SPA. KV + RLS after e09.
Write `draft/security-architect-readiness/auth-demo.md`.

If ADR-022 and the gap file exist, `VOTE: APPROVE`.
