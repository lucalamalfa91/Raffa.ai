# Wave w17 — council decisions

Skeleton written by `next-intake`; rows completed by the involved seats at
`next-council-close`; verified by `next-council-gate`. Source:
`reports/context/waves/w17-requirements.md`.

Wave theme: **the product officializes what it knows, shows the page it read it
from, and answers where you can save.** Baseline `d3d2d24` (`helix/w17`); the
wave base **is** `origin/main` @ `d3d2d24` — `0 0` ahead/behind, so there is no
base row and no "merge main first" open question.

Rows below are the **eleven in-wave items** of `w17-requirements.md` §5, in
selection order: the two residual head items, then the auto-accept rule that the
review-screen set renders, then the 360 answer wires, then the viewer chain.

> **Parameter note.** `reports/plan/next-run.json` resolves this run as `w17`
> (`previous=w16`); the workflow input string carried a stale `w15`, which the
> disk contradicts (`w15`/`w16` requirements, decision records and slices all
> exist). See `w17-requirements.md` §1.

> **Four items are NOT at this table** — **NW-75, NW-23, NW-25, NW-74** overflow
> to the **head of W18** in the raw file's own overflow order
> (`w17-todo.md` §5). They are not decided here, not decomposed, and not
> demoted. **NW-74 keeps `should`**; the written reason for queuing it is in
> `w17-requirements.md` §5.

> **The cap binds.** Fifteen items estimate at ≈ 28–30 tasks against 20. If the
> council cannot land NW-63 whole (**OQ-w17-001**), the deferred half becomes the
> **head of W18 ahead of the four overflow items** — a `must`'s remainder
> outranks a `should`/`could` overflow.

| Item | Seat(s) | Decision | ADR action | File |
|---|---|---|---|---|
| NW-72 | product-owner, software-architect, client-architect, ux-ui-designer | **PO** — IN as the unmet AC-1 of the still-`active` `E04/F03/US01` (no banner, no `superseded:` line). The w16 clause 4 money fence is **lifted and replaced**, not deleted: realized money renders **only** from `RealizedSavings` rows **grouped by currency, never summed across them**; the estimate-summed `Realized` range stays a defect to fix, never to render; an outcome with `savingsPropagated: null` enters no total. The cell is labelled **Verified** (spec `:447`) and implies no currency. **SA** — `Realized` **stops summing estimates** (`SavingsKpiCalculator.cs:103-104`, whose own comment `:46-54` already calls this a gap) and reads the `RealizedSavings` rows, which **exist and are written** (`RealizedSavings.cs:26`, `SavingsOpportunityService.cs:325`) but have had **no reader anywhere**. `SavingsRangeByCurrency` is the wrong shape — an estimate is a band, a realized figure is **one number** (`:45`) — so a new `RealizedSavingsByCurrency(Currency, Amount, Count)` replaces it and `SavingsKpiSummary.Realized` (`:59`) **changes type: a deliberate wire break on one field**, flagged to client-architect rather than smuggled through. `Count` rides along because "€X verified across N outcomes" is the honest label. **No cross-currency total** — no conversion service exists (`:40-44`) — and the client already renders per-currency `lines[]` (`savingsViewModel.ts:91`), so **no new cell shape is owed**. The calculator **stays pure**: a second input sequence, never a service (`SavingsKpiQueryService.cs:32-36` projects both). `RealizedAmount` stays PATCH-only; `savingsPropagated: null` enters no total. Stale-comment sweep travels with the task. **CA — no new cell shape is owed, and the declared wire break cannot land silently.** The KPI renders **currency-grouped money from the server** and the client **never sums across currencies**; absent → the existing `—` idiom (`KpiRow.tsx:49`), never a fabricated `0`. `KpiCellView.lines` is already `string[]` and `savingsViewModel.ts:91` already renders one formatted line per currency for `annualSpendAnalyzed`, so realized money reuses that idiom verbatim. On SA's **declared** break of `SavingsKpiSummary.Realized`: verified that `client.ts:985` derives `SavingsKpiSummaryBody` from the **generated** `GetSavingsKpisResponses` — not a hand-declared interface — and `package.json:13` runs `npm run generate:api && tsc --noEmit && vite build`, **regeneration then typecheck in one command**, so a break on a generated type **fails the build rather than reaching a screen**. That property is what makes a declared break safe, and it is the reason it must stay declared rather than discovered. ⚠ **`client.ts` is a two-writer file, not one** (**OQ-w17-cl-01**): NW-72 wants at most a one-line derived alias beside `:986-987` while NW-63 adds a parameter to a method — ADR-012 w16 clause 25 applies, and the free escape is to **drop the alias** and index the generated body inline, which removes NW-72 from the file entirely. ADR-012 w17 clause 37. **UX — a fourth equal cell, not a second line in the third; no new component and no new token.** `design-system.md:45` specifies a full-width row of **equal** cells, so a fourth is the band's own shape; folding verified money into "Savings identified" is refused by `design-system.md:48`'s facts-vs-AI rule — a **verified amount** and a **pre-negotiation estimate** are different kinds of money and must never share a cell. **Label: "Savings verified"** — PO's clause 1 requires spec `:447`'s word *Verified* on screen and it is preserved verbatim, but the band's other three labels are noun phrases (`savingsViewModel.ts:85-87`) and a bare "Verified" reads as a **status, not a quantity**, making it the only cell in the row that is not a thing counted. The required word is PO's; the parallelism is this seat's (**OQ-w17-ux-04**, owed back to product-owner, not asserted over it). **No new cell shape is owed and none is invented**: `KpiCellView.lines` (`:63-64`) is already `readonly string[]` rendering **one formatted line per currency**, with `:91` the exact precedent, so only the **closed key union at `:61`** widens by one literal — which is why SA's currency-grouped shape lands with zero design cost. **Zero state**: `lines: []` → `KpiRow.tsx` renders **`—`** (`:49`) with meta **"no verified savings recorded yet"**, reusing `:98`'s own idiom for absent annual spend — **never a fabricated `0`**, which asserts that outcomes were recorded and came to nothing, a different and false claim. Provenance rides the `meta` slot ("from N recorded outcomes"); the `· N realized` fragment leaves the identified cell (`:110`), and the w16 money-fence comment at `:92` is swept by the same task or the tree contradicts the screen. **Divergence ratified**: `screens-v2.md:134-135` fixes a KPI **triple** with no realized-money slot at all. ADR-020 w17 §15, §17. | **PO**: amend ADR-001 (w17 clause 1); **SA**: amend **ADR-028** (w17 footer); **CA**: amend **ADR-012** (w17 clause 37); **UX**: amend **ADR-020** (w17 §15, §17) | `ADR-001-scope-r0-r4.md`, `ADR-028-server-side-state.md`, `ADR-012-web-stack.md`, `ADR-020-web-screen-inventory.md` |
| NW-73 | delivery-manager, software-architect, security-architect, cloud-architect | **SA** — a new console **`Raffa.Tools`** under `backend/src/`, registered in `Raffa.slnx`: a **third composition root** with no table, no endpoint, no business rule, referenced by nothing. It **calls** `DocumentReprocessService.ReprocessAsync` (`:62`) per document and **must not** re-implement `RequeueClassificationJobAsync` (`:138-176`) — the `ClaimedAt = null` reset (`:168`) is what lets the Worker re-deliver, and `AttemptCount` is deliberately preserved (`:135-136`); a psql equivalent silently drops both (raw-file correction 5). It composes the service's seven dependencies (`:37-44`) via the owning modules' own `ServiceCollectionExtensions`, never hand-built. Bounded concurrency + continue-on-error; **re-runs are safe** because `MessageId` collapses duplicates. In `DependencyDirectionTests` it joins the **all-projects** array (`:39-53`) and **must not** be added to the domain-module array (`:20-33`) or the allow-list dictionary (`:62-69`) — it is a host, not a module with a dependency budget. ⚠ **The Admin gate does not travel with the service** (`:30-31`): a console bypasses `DocumentsEndpointExtensions.cs:506-509` entirely, so its only authorization is the principal's RBAC + the Postgres credential (**OQ-w17-sa-02** — security rules). **CLOUD — the wave's entire Azure delta is one RBAC row.** A **third** topic-scoped `azurerm_role_assignment` in `modules/servicebus` granting **`Azure Service Bus Data Sender`** — never `Receive`, never `Manage`, never namespace scope, never an authorization rule or SAS key (`main.tf:64-70` is the module's own standing rule) — carrying the **same `lifecycle { ignore_changes = … }`** both existing grants carry (`:80-86`, `:95-101`): ARM rejects in-place updates to a role assignment, so omitting it plans clean today and fails a *later, unrelated* apply. **OQ-w17-ca-01 is ruled (A), the GitHub runner — and the reason inverts security's "zero new rights" framing**: the workload identity holds **Sender *and* Receiver** (`:71-87`, `:89-102`), so a Container Apps Job (C) would let the console **receive from `document-processing`, the one subscription the Worker depends on**, while (A) grants **Send only** to a principal that exists only inside CI. (C) adds zero rights and gives the **wider** capability; (A) adds one right and gives the **narrower** one. **Variable name yields to delivery-manager**: `ci_deploy_principal_id`, matching `modules/keyvault/variables.tf:34`, so one grep finds every grant to that principal; **required**, with **both roots wired in the same PR** — one line each, the data source already exists (`dev:223-225`→`:236`, `demo:245-247`→`:258`). **Wiring is not applying**: `dev` applies on merge, **`demo` only at its next promotion**. **Zero new environment keys** (the console is not a container app) and **`AZURE_CLIENT_ID` must not be set on a runner** — it selects the user-assigned MI and aims `DefaultAzureCredential` at an identity absent from that host. **$0.00/month on both environments.** Apply is **HCP VCS on merge**, an operator action at the HITL gate, **never a wave task**; the gate confirms **two** applies (this one **and** ADR-016 clause 33's outstanding PR #118 — OQ-w17-dm-03). ⚠ **`infra/README.md:331-336` and `:417-421` are falsified by this change** — the latter asserts "the two new V2 operator workflows need **no new Azure grant**", which is exactly what NW-73 is — and both are updated in the same infra PR. **SEC — the Admin gate is *relocated to the CI plane*, not bypassed, and the RLS binding is what a task gets silently wrong.** ADR-022 §4's "owed to W17" is **discharged**: a CI principal **may** hold **`Azure Service Bus Data Sender`, topic-scoped, and nothing else**, because the message is a **pointer, not content** (`ServiceBusExtractionQueuePublisher.cs:24`) and the Worker **re-reads all authority from the database under RLS** when it claims the job, with `MessageId` collapsing duplicates (`:50`) — so a forged or replayed message cannot make the Worker read what the sender could not already reach. **Send crosses no confidentiality boundary; Receive would** (it drains the Worker's deliveries *and* reads ids from the envelope). Receiver, `Manage`, namespace scope, SAS key, an app role on `azuread_application.api` and a `workspace_membership` row for the SP are each refused for their own recorded reason. **OQ-w17-ca-01's (A) is adopted, and it corrects this seat's own lane**: verified first-hand, the workload identity holds **Sender *and* Receiver** (`modules/servicebus/main.tf:71-87`, `:89-102`), so (C) would grant the **wider** capability — counting *rights added* was the wrong measure, and this seat's own Send/Receive asymmetry condemns its own stated preference. **The decisive product finding is S17-3**: the console is the **first code path that enumerates a tenant with no HTTP caller**, and `DocumentsContractsDbContextOptions.Configure`'s **optional third argument** (`:50-66`) means the two-argument form — the one `TenantRlsMigrationCheckTests.cs:32` shows a reader — leaves `app.tenant_id` unset, so the policy's `nullif(...)` is NULL, **every query returns zero rows**, and the console **exits green having done nothing**, indistinguishable from an empty worklist. Five binding rules: three-argument `Configure` inside `BeginScope`; **never** a raw `NpgsqlConnection`, a hand-written `SET app.tenant_id` or `psql`; **one tenant per run from an explicit GUID, no all-tenants mode**; **zero rows under a valid tenant exits non-zero**; the application's own `postgres-connection` secret, **never a superuser or `BYPASSRLS`** (`FORCE` binds the table owner but not `BYPASSRLS` — the only escape left). Actor is the fixed literal **`system:bulk-reprocess`** and **no CI-controlled string is ever interpolated into `AuditEvent.Actor`** (the append-only trigger makes a wrong actor permanent); human attribution rides in `Detail`. **NW-73 is the product's first *mutating* operator workflow** — its predecessor's own comment reads *"this job reports; it does not mutate"* (`verify-tenant-corpus.yml:8`) — so it inherits that file's gating idiom **at least in full**: `workflow_dispatch` only (never `push`, never `schedule`), **required** `tenant_id` input, top-level `permissions: contents: read`, a concurrency group keyed by environment+tenant, and **`environment: ${{ inputs.target_environment }}`** so demo's required reviewers gate it (`:15-16`, `:52`); on `dev` the gate is repository write plus an explicit dispatch, recorded honestly rather than described as reviewer-gated. Log rule: tenant id, document ids, counts, statuses, job ids and file names (`:143` precedent) — **never** chunk text, extracted values, document bytes or any vault value. **DM — the console runs on the GitHub runner, the wave is two PRs, and the `demo` promotion job does not apply.** **Where it runs** (ADR-016 w17 clause 36): `dotnet run` on the runner, **not** a Container Apps Job. `backend.yml:66-74` runs `dotnet restore/build/test` on **`Raffa.slnx`**, so a console registered there is built and tested with **no `backend.yml` edit**; `:130-144` builds exactly **two** Dockerfiles, so the console is not containerised, not pushed to ACR, not a Container App — **zero deploy-path change, zero new image, zero new environment key**. The refused Container Apps Job is recorded so W18 does not re-open it as "the cheaper option", and its one attraction — "no new role" — is **backwards**: cloud-architect and security-architect each verified that the workload identity holds Sender **and Receiver** (`modules/servicebus/main.tf:71-87`, `:89-102`), so the Job grants the **wider** capability. **The infra change is four files and the variable is required** (clause 37): `modules/servicebus/{variables,main}.tf` plus **both** env roots, each gaining **one line** (`dev:223-225,236`; `demo:245-247,258`) with no new data source. Required + both roots is not symmetry for its own sake — **`demo-promote.yml:128-136` calls `infra.yml` with `target_environment: demo`, so a dev-only wiring breaks the promotion path** one tag later, in the job nobody runs weekly. ⚠ **Adopted from cloud-architect and absent from this seat's draft**: the new assignment carries the same **`lifecycle { ignore_changes = … }`** both existing grants carry (verified at `:80-86`, `:95-101`) — ARM rejects in-place role-assignment updates, so omitting it plans clean today and fails a *later, unrelated* apply. ⚠ **The CI gate no other seat carries** (clause 38): `AuthenticationSeamAbsenceTests.cs:62-68` scans **`.github/workflows/**`** and runs inside `backend.yml`'s `dotnet test`, the required check on `main` — **this seat's file set is inside a backend test**, and a red `main` is no `dev` deploy (ADR-014 w15 clause 3), so a careless workflow line kills the **wave's** deploy, not just its own job. It flags an auth-disabling key, a fixed passcode/token, and **a branch keyed on an environment name that skips a credential check**; it does *not* flag connection strings (`:146` asserts a Postgres string *with a password* clean). Rule: the workflow **copies `verify-tenant-corpus.yml`'s credential idiom verbatim** (`azure-login` `:67-72` → `az keyvault secret show` `:112` → `pg_connection_string_env.py` → masked `PG*` `:119-134`), which is **proven green against this scanner today**. **The two operator jobs compose** (clause 39): `verify-tenant-corpus.yml:147-169` already computes the worklist (`Failed`, or an `embedding` still starting with `%PDF`) — **that is the console's input set and the console runs the same predicate, never a second one**, or "what needs reprocessing" and "what got reprocessed" drift apart silently; this makes A17-S2 self-proving (verify → console → verify, worklist 0 and `%PDF` gone, `:171-184`). `workflow_dispatch` only, required `tenant_id`, `environment:` so demo's reviewers gate it; **it is never added to `demo-promote.yml`**, whose header `:43-50` forbids steps that move data. **Two PRs** (ADR-014 w17 clause 2): PR 1 is the Terraform task **alone** → merge → **HCP applies** (`infra.yml:114-136` deliberately does not) → PR 2 is the wave. ⚠ **A17-S2 cannot pass before that apply** — the console authenticates and then fails to send — an **operator sequencing fact, not a task**, and the gate confirms **two** applies (this one and clause 33's still-outstanding PR #118). ⚠ **The `demo` finding, which corrects cloud-architect's framing** (clause 40): product-owner's clause 9 overrules this seat's draft assumption and clears the backlog at the w17 gate — accepted without reservation — but **the promotion job applies nothing**. `demo-promote.yml` → `infra.yml`, whose apply job is a **step-summary echo** named **"terraform apply skipped (demo)"** printing *"Do not run `terraform apply` from GitHub Actions against a VCS-connected workspace"* (`:114-136`); `promote-backend`/`promote-web` then `needs: promote-infra` (`:140`, `:151`), which the file's own comment (`:123-127`) says waits on a green infra **plan**. **So a `demo-v*` tag can go green end to end while `demo` lacks this row's role assignment.** Clearing the backlog is therefore **two acts in order**: the **HCP VCS apply on `raffa-demo`, confirmed in the HCP UI** — never inferred from a green `promote-infra` — then the tag push, its number **read** at the gate (`git tag -l "demo-v*"`), never assumed. ⚠ **The flags, corrected and already answered on disk**: clause 9 leaves "whether **w17's two flags** default `false` on `demo`" here — they are **not w17's**, they are **w15's**, they are **Terraform variables rather than application flags**, and they **already default `false`** (`demo/variables.tf:45-49`, `:51-55`); there are **three**, not two (`guest_role_assignment_managed` `:65-67`), and `:57-64` records why the third is false — the Graph `User.Invite.All` grant is written **out of band**, because on `dev` it returned `Authorization_RequestDenied` **and failed the whole run, Service Bus and ACS included**. **Ruling: w17 flips none of the three**, exactly as `:41-44` prescribes (a one-line PR after demo's own post-promotion acceptance), so **clearing the promotion backlog does not clear the invitation walk** — stated in the acceptance doc rather than discovered by a `demo` walker. ⚠ **Correction against this seat's own lane, on the record**: the draft told software-architect that `DependencyDirectionTests` is a set of "named allow-lists" and that a new `Raffa.Tools` is "**not scanned** and adds no test burden". **That is wrong** — `AllRaffaProjects` (`:36-53`) is a `HashSet` that already carries the hosts `Raffa.Api` (`:49`) and `Raffa.Worker` (`:50`), and software-architect's ruling that the console joins it is right. The *reason* is stronger than convention and is this seat's contribution to it: that set is the **membership filter on the violation check** — `.Where(r => AllRaffaProjects.Contains(r) && !allowed.Contains(r))` (`:103`, and again `ArchitectureRuleEnforcementTests.cs:33`, `:83`) — so a project **absent** from it is **invisible to the theory**, and a later domain module referencing the console (a host) would be **silently legal with the suite green**. It is therefore a **DoD line, not an implementer's option**: the burden is not a test that fails, it is a test that **quietly stops protecting** — the same silent-drift shape security-architect found in `TenantRlsMigrationCheckTests`, client-architect in `countOf`'s structural typing and ux-ui-designer in the KPI arity, reached this round from the fourth direction. | **SA**: amend **ADR-002** (w17 clause 1), **ADR-027** (w17 clause 3), **ADR-021** (w17 clause 3 — the CI prose sweep is assigned here because `.github/workflows/**` is NW-73's alone); **CLOUD**: amend **ADR-005** (w17 §15–§18, §22) and **ADR-007** (w17 §5–§8); `none` on **ADR-006** (region unchanged) and **ADR-008** (no Foundry change); **SEC**: amend **ADR-022** (w17 clauses 1–3, 5), **ADR-011** (w17 clauses 20, 23), **ADR-009** (w17 clauses 1, 4); `none` on **ADR-010** (no token, claim, app registration or federated credential change — the console reuses the deploy principal's existing federated credential) and **ADR-025** (no membership, role or invitation change; the SP gets no `workspace_membership` row); **DM**: amend **ADR-016** (w17 clauses **36–42**, continuing w16's 35 — the runner, the grant's promotion-path constraints, the workflow's CI-scanner gate, the composed worklist, the `demo` promotion ruling, the final-integration list and the known-gaps table) and **ADR-014** (w17 clauses **1–7**, numbering restarted — the one-file CI set, the two-PR shape, the standing gate stamp, W17-A1, the close-record replacement, `backend.yml` parked, and the wave order); `none` on **ADR-015** — the new right is an Azure **data-plane role** on an existing principal, so no federated credential, GitHub secret, subject claim or Graph right changes; `none` on **ADR-005/ADR-007** from this seat — the module body is cloud-architect's and is not contested | `ADR-002-dotnet-solution.md`, `ADR-027-async-document-processing.md`, `ADR-021-schema-apply.md`, `ADR-005-azure-skus.md`, `ADR-007-terraform-layout.md`, `ADR-022-day1-demo-auth-and-fixture.md`, `ADR-011-secrets-and-rag.md`, `ADR-009-tenancy.md`, `ADR-016-promotion-dev-demo.md`, `ADR-014-git-flow.md` |
| NW-71 | product-owner, software-architect, security-architect, client-architect, ux-ui-designer | **PO** — one bar at **90 %**, every field, the five critical ones included, **no always-review list**. Supersedes five oracle lines on the record (spec `:333-335` and `:341`'s threshold half; pilot `:32`, `:46`, `:55`, `:122`); **not** superseded: the *criticality* of the five fields, and the threshold-relative rules at pilot `:65` / `:55` (unlock), which survive re-parameterized. Consequential use is fenced on **"not accepted"** rather than "< 80 %". **A percentage beside a decision is floored, never rounded up across the bar**, or omitted. **The fixture changes, never the threshold** (OQ-w17-po-01). **SA — the server decides and persists; the web renders.** Raw `confidence >= 0.90` on the stored double, **no rounding before the compare** (OQ-w17-002): rounding first makes `0.895` accept in the payload and review in the audit row — the record disagreeing with itself. **One** `ExtractionConfidencePolicy` in `Raffa.Documents.Contracts`, consumed by **both** `StagedExtractionService.DetermineDocumentStatus` (`:863`, applied `:236`) **and** `DocumentQueryService.IsWeak` (`:46-52`); today these are duplicated pairs (`:78,88` vs `:33,42`), so changing one **desyncs the badge from `needs_review`** (raw-file correction 4). State lands on the **existing `extraction_evidence`** — already one row per (contract, field) carrying `Confidence` (`ExtractionEvidence.cs:38,48`) — as two nullable columns `decision` + `decided_at`, **no SQL enum** (`FieldName` is deliberately not one, `:36-37`), **no new table** ⇒ zero new isolation surface. **Not a boolean**: the wire carries **three** states (`auto_accepted` / `human_accepted` / `review_required`), which is the shape ADR-001 w17 clause 8 requires and a boolean would collapse — `review_required` **is** that clause's third state, named for what it needs rather than for what it lacks, and it reuses the stored vocabulary instead of inventing a fourth word (derivation table in the ADR-003 w17 footer). The threshold is exposed **once** so the web never hardcodes 90 and the legend is server-fed. `ContractEvidenceSchemaTests.cs:49-76` is a column-by-column proof and **must gain both columns**. **SEC — zero new isolation surface, no new authorization surface, and the trail must distinguish the two acceptances.** A column on `extraction_evidence` — already `ENABLE`+`FORCE`+`tenant_isolation` (`documents-contracts.sql:798-800`) and already per-field keyed (`:770`) — needs **no new policy, guard, test or migration ordering**; three seats reached that table independently (this one from isolation, SA from the wire, CA from the read-back), which is why it is recorded rather than assumed. **OQ-w17-sec-03 is closed by SA's column ruling.** Had a **new table** been chosen, w16 ADR-009 clause 2's four requirements bind, and w17 records the failure mode they defend against: `TenantRlsMigrationCheckTests.cs:37-44` discovers its table list **from the EF model by `TenantScopedEntity` subclass**, and the "guards the guard" assert at `:46-49` catches only an **empty** list — never a **missing member** — so a non-deriving entity is **never checked and the suite stays green**. The audit row is **one per document per extraction run**, actor **`system:extraction`** (ADR-011 clause 16, no HTTP caller), carrying **field names and confidence numbers, never a field value** — the table is append-only, so a value written once **cannot be removed**; precedent `DocumentValidationService.cs:124-135`. **NW-71 adds no authorization surface and must not**: the decision is **server-computed and read-only on the wire** — a client-supplied decision on the validate/PATCH path is **rejected 400 and never persisted**, because letting a display band become an authorization decision is exactly the error ADR-022 §3 forbids on the other screen. Manual override keeps **today's** gate — no new role, no Admin-gating of review. An auto-accept is attributed to `system:extraction`, a human validation to the caller's resolved subject: the **audit half of ADR-001 w17 clause 8**, and the reason three states cannot collapse into a boolean in the trail either. Tests: another tenant never sees the decision state; a client-asserted decision is rejected and changes nothing; the audit row asserts on a **value string being absent**. **CA — the flagship read-back, and this seat's own lane proposed a wire shape that would have defeated it.** The web **renders the server's persisted decision and never recomputes it**: `semantics.ts` becomes **label-only** (`getConfidenceTag` paints a label, it no longer decides acceptance) and `isConfidenceBlocking` (`reviewViewModel.ts:294`) reads the decision instead of `< 80`. **The store this retires is `acceptedThisSession`** (`useReviewSession.ts:92`, folded into the rows at `:145`), and the warrant was verified first-hand at this table: **`accept()` already has two branches and they are not equal** — `:177-182` routes a row whose `proposalPending` is true through a real `PATCH` + `load()` read-back, while `:184` is `setAcceptedThisSession(...)` with **no network call at all**. Two kinds of "accepted", painted identically, the second vanishing on reload; NW-71 adds a **third**. **One screen, three durabilities, one paint** — the exact cache-instead-of-a-GET ADR-012 §1 forbids, and the product has retired one of these before (NW-10's `sessionStorage` tracker, `navItems.ts:85-90`). **The read-back is free**: `useReviewSession.ts:124` already calls `getContractEvidence` on every `load()`, so a decision stored on `extraction_evidence` needs **zero new endpoints and zero new fetches** — the third independent route to that table. **Correction against this seat's own lane, recorded because it matters more than the ruling**: the lane proposed a **two-value** enum (`auto_accepted | needs_review`); SA's **three** states are adopted, because with two a human acceptance has nowhere to live on the wire and would have to be either painted as `auto_accepted` — which ADR-001 w17 clause 8 forbids — or **left in `acceptedThisSession`**, the store the clause was retiring. A two-value enum does not simplify this design; it **forces the session store to survive**. **Writer math** (a correction to the file list, not to the ordering): NW-71 **need not open `contract360ViewModel.ts`** — its consumer there, `:574`, sits inside `computeNeedsAttention`, a function constraint 3 already assigns to **NW-65**; if `getConfidenceTag` keeps its signature the three 360 call sites compile untouched and the two that must disappear are deleted by the items that own them. ⚠ **New defect (OQ-w17-cl-02)**: that same function gates on `tag.variant !== "neutral"` (`:575`), inferring a decision from a **colour** against `semantics.ts:45-46`'s explicit rule, so collapsing the bands **silently changes which facts the list shows with nobody editing the line**. ADR-012 w17 clauses 34 and 36. **UX — two treatments over three decisions, and the accent leaves confidence.** ADR-019 w17 clause 7 replaces the locked mapping's three confidence rows (`:100-102`) — the **first change to them since this ADR was accepted**: `auto_accepted` → `.tag-neutral` "**Accepted automatically · NN%**"; `human_accepted` → `.tag-neutral` "**Accepted by you**", with **no percentage**, because a human decision has no confidence and printing the model's score beside a person's judgement asserts that the judgement is uncertain; `review_required` → `.tag-outline` "Review · NN%". **The two accepted states are distinguished by the label, never by the variant.** PO's clause 8 requires them distinguishable, `## Accessibility baseline` (`:124-125`) requires the **text** to be the carrier, and minting a third variant would build the next instance of OQ-w17-cl-02's colour-as-decision defect into the system on purpose. **`.tag-accent` is released from confidence entirely** — it was the retired middle band's treatment on **four** surfaces (`WhyClauses.tsx:56`, `FactTable.tsx:35`, `contract360ViewModel.ts:574`, `reviewViewModel.ts:282`) — so the one-accent rule gets **stronger**, and retiring that band is the whole point of moving to one bar. **Legend: two items, server-fed** (`ReviewHeader.tsx:89,92,95` — the web never hardcodes `90`), and **the title stops naming a threshold**: `screens-v2.md:83`'s "N facts below 80% — you decide" becomes "**N facts need you — you decide**", because that sentence has now gone stale **twice** for naming the bar, and the count is what the user acts on. ⚠ **Floor, never round** (ADR-019 w17 clause 8): `semantics.ts:33`'s `Math.round` renders a `review_required` field at `0.895` as "**Review · 90 %**" — a tag contradicting itself inside one string. This seat reached that line from the semantic-mapping side and client-architect from the call site, in the same round. ⚠ **Correction on the record** (ADR-019 w17 clause 10, consistent with PO's note): the w14 footer §6 declined to ratify the bands on the grounds that `semantics.ts:9-13` cites a 2026-09-10 HITL "≥90%" ruling — **verified first-hand on `d3d2d24`, that docstring cites spec §7.3's three bands and contains no `90`, no HITL date and no supersession sentence**. §6's conclusion was right and its evidence is not on main (the 90 % comment lives only in an uncommitted operator stash); the body is **not edited**, the correction is a footer clause, and §6's naming of *NW-65* as the reconciling item is corrected to **NW-71 (threshold) + NW-65 (layout)** so the trace lands on real tasks. Tests rewritten, not re-run: `semantics.test.ts:15-37`, `reviewViewModel.test.ts:192`, `ReviewRoute.test.tsx:492`. **Vocabulary fence**: `officialized` is an ADR word and **never appears on a screen**. | **PO**: amend ADR-001 (w17 clause 2). ⚠ The correction to **ADR-019's w14 footer §6** is recorded in that clause, **not** by editing ADR-019 (ux-ui-designer's). **SA**: amend **ADR-003** (w17 clause 1), **ADR-024** (w17 §A), **ADR-027** (w17 clause 2), **ADR-021** (w17 clause 1); **SEC**: amend **ADR-009** (w17 clause 2), **ADR-011** (w17 clause 21); **CA**: amend **ADR-012** (w17 clauses 34, 36 — the client renders the server's decision and `acceptedThisSession` retires); **UX**: amend **ADR-019** (w17 clauses 7, 8, 10, 11 — the confidence rows, floor-not-round, the §6 correction, and where each treatment may appear) and **ADR-020** (w17 §13) | `ADR-012-web-stack.md`, `ADR-001-scope-r0-r4.md`, `ADR-003-data-store.md`, `ADR-024-ask-raffa-v2.md`, `ADR-027-async-document-processing.md`, `ADR-021-schema-apply.md`, `ADR-009-tenancy.md`, `ADR-011-secrets-and-rag.md`, `ADR-019-web-design-system.md`, `ADR-020-web-screen-inventory.md` |
| NW-20 | software-architect, product-owner | **PO** — IN but bounded. "Activity" for V1 is the **provenance timeline of already-persisted events** (upload, processing completed, validated, reprocessed, contract correction, renewal action, negotiation tick, savings outcome), each carrying when · actor (`system:<component>` for pipeline events) · what changed, and **never the value of an extracted fact**. Out: supplier outreach (§1.2, pilot `:153`), notes, anything needing a new table. If the projection does not fit the wave, **remove the member and delete the record type** — **an unconditional `[]` with no decision is not acceptable**; `benchmark` has no such option (NW-62 consumes it). *SA owns the payload shape.* **SA** — both members are memberless records reserved for R3/R4 (`Contract360Result.cs:209-223`) and are filled by **host composition in `Raffa.Api`**, never by `Contract360QueryService`: `DependencyDirectionTests.cs:63` fences `Raffa.Documents.Contracts` to exactly `[SharedKernel, AiGateway]`, so it may reference neither Audit nor Benchmark (`PortfolioEndpointExtensions.cs:133-146` is the existing precedent, where `supplierName` is already joined that way). **`benchmark`** uses the **same** `IBenchmarkService` call and the **same** resolved `(supplier name, geography)` key as NW-62 — one resolution, two consumers; resolving twice and getting two answers on one screen is the defect. **`activity`** has no domain concept and this seat will not invent one: the only honest existing source is the **audit trail**, projected read-only through the host with an action whitelist — a **security question before it is an architecture one**, because a 360 tab is a wider audience than ADR-011's w16 reader ruling contemplated (**OQ-w17-sa-03**). If security refuses, the member is **removed and the record type deleted**, matching PO clause 3; never an unconditional `[]`. Either way both record types gain members ⇒ contract delta. **SEC (ruling OQ-w17-sa-03, the re-review ADR-011 clause 14c reserved)** — `activity` is **permitted as a contract-scoped provenance projection and refused as an audit reader**. The widening is verified, not suspected: `/api/audit` is **live-`Admin`-only** (ADR-011 w16 clause 14) while `GET /api/contracts/{id}` is gated by `ICallerContext.ResolveTenantAsync` — **membership, with no Admin check** (`ContractsEndpointExtensions.cs:77`, `:101-105`) — so a naive projection moves an Admin-only read onto an **any-member** surface. Five conditions make it a *different read* rather than the same read with a filter, and they are in ADR-011 w17 clause 22. **SA round 2 — security permitted, so the conditional in the SA half above resolves: both members land and their shapes are named** (ADR-024 w17 clause 11), because a permitted member with no named shape is still a guess. `Contract360ActivityEntry(OccurredAt, Action, ActorLabel)` — three members, each from a **bounded** audit column (`Timestamp`; `Action` admitted only from the closed allow-list, `varchar(100)`, `AuditEventConfiguration.cs:21`; `Actor` `varchar(200)` `:20` resolved to a **display label**, never a raw GUID) — which is exactly PO clause 3's *when · actor · what changed*, because that clause's own list (upload, processing completed, validated, reprocessed, correction, renewal action, negotiation tick, savings outcome) **is a list of action names**. ⚠ **`AuditEvent.Detail` is never projected**, and this is what makes security's condition 3 checkable rather than aspirational: it is `text` with **deliberately no `HasMaxLength`** — *"unbounded free-form context"* (`AuditEventConfiguration.cs:24`) — and writers **already** interpolate contract-derived dates into it (`Detail: "…; milestoneDate={…:yyyy-MM-dd}"`, `RenewalAlertService.cs:254-255`, again `RenewalThresholdScheduler.cs:148-149`) on a `Contract` resource. The obvious implementation — project the one human-readable column — therefore breaks *names, never values* **on day one**, and the table is append-only, so it would be permanent. `Contract360BenchmarkEntry(Metric, Status, Position?, AdapterName?, SampleSize?, AsOf?)`, with the abstain carried **as an entry** (`Status = insufficient_data`), never as `[]` — `[]` is today's defect (`Contract360QueryService.cs:211`) and cannot be told apart from "never wired". | **PO**: amend ADR-001 (w17 clause 3); **SA**: amend **ADR-024** (w17 clauses 10–11), **ADR-002** (w17 clause 3); **SEC**: amend **ADR-011** (w17 clause 22) | `ADR-001-scope-r0-r4.md`, `ADR-024-ask-raffa-v2.md`, `ADR-002-dotnet-solution.md`, `ADR-011-secrets-and-rag.md` |
| NW-22 | software-architect, product-owner | **PO** — IN. A market claim has exactly **two** honest shapes: a **representative** position (above / in line with / below market) carrying **adapter, sample size and as-of date**, or an explicit **"insufficient market data"**. Never a percentile alone, never "market" unqualified, never "Not determined" (ADR-001 `:94-96`; spec `:538`, `:496`). *SA owns the field and the adapter call.* **SA — this corrects the intake's framing ("inject and wire").** **Do not inject `IBenchmarkService` into `RenewalPipelineBuilder`**: the type's own doc comment states it is *"Pure and synchronous — no database call, no HTTP call, no LLM call (Appendix C rule 6)"* (`Raffa.Renewals/Application/RenewalPipelineBuilder.cs:9-11`), while the service is async and adapter-backed (`MarketFeedBenchmarkAdapter.cs:98-102` reads `market_record`). **Permission was never the blocker — a resolved key was**: `DependencyDirectionTests.cs:65` allow-lists `Raffa.Renewals → [SharedKernel, Raffa.Benchmark]`, and the **port** `IBenchmarkService` does live in `Raffa.Benchmark` — while the **adapter** lives in `Raffa.Market`, which is **not** allow-listed. So the module may depend on the port; only the host may compose the adapter. **Ruling**: `RenewalsEndpointExtensions` resolves the band per candidate and passes it **into** the builder on `RenewalDashboardCandidate` — the DTO that exists precisely so the builder never sees a real `Contract` (`:16-20`) — the same pattern as `InsightsEndpointExtensions.cs:33-36`. The builder then fills `MarketPosition` (and `AnnualUpliftPercent` where the band supports it) instead of the hardcoded nulls at `:91-92`, and sweeps the stale comment at `:89-90`. `PotentialSavingsRange` stays null unless the band yields one — an honest null is not a defect. Tests stay unit tests with **no new fake service**, which is the point of keeping the builder pure. | **PO**: amend ADR-001 (w17 clause 4). **SA**: `none` on the ADR-002 allow-list — already permitted, and the reason is now recorded; the **purity rule** is written into the **ADR-002 w17 clause 2** footer so a later task does not "fix" this by injection. Wire shape in **ADR-024** w17 clause 6 | `ADR-001-scope-r0-r4.md`, `ADR-002-dotnet-solution.md`, `ADR-024-ask-raffa-v2.md` |
| NW-62 | product-owner, software-architect, client-architect, ux-ui-designer | **PO** — IN **at the R3/R4 gate and not past it**: the band comes from `IBenchmarkService`'s existing fixture / market-feed adapter and this tenant's corpus, **labelled representative**; a paid market API stays a §1.2 non-goal (NW-52 DEFERRED). Same two shapes as NW-22; **a fixture figure is never presented as live market truth** (ADR-001 `:57-60`). **"Not yet available" is a placeholder, not an answer** — a missing fact is named together with the way to get it; thin market data says so. *The CA half closes this cell; the UX half is at the end of this row.* **SA — consume `GET /api/contracts/{id}/strategy`** (OQ-w17-005): it **already** returns both `WhenYouMustMove` and `WhereYouCanPush` (`StrategyPack.cs:26-32,51-56`) and is **already in the generated client** (`schema.ts:724`) with **no consumer**; computing a second answer in the view model is the divergence ADR-012 forbids. "When you must move" is additionally answerable from persisted columns today (`Contract360QueryService.cs:94,96` — raw-file correction 2). **Geography is the workspace country, resolved in the host** and passed into `StrategyInputs`, because `Raffa.Insights` is fenced to `[SharedKernel, Benchmark]` (`InsightsEndpointExtensions.cs:34-35`) and cannot read the workspace itself; the supplier-name half is already solved (`ISupplierNameLookup`, registered `ServiceCollectionExtensions.cs:41`). With both resolved, `ToPricedLines`' hardcoded `Benchmark: null` (`:47-48`) becomes a real call — that file names this method as the place (`:50-51`). **No per-contract geography column this wave** (a migration the cap cannot afford). The abstain path stays: no band → "insufficient market data", never a fabricated number. ⚠ **`OpenWeakFacts` is filtered at 0.8** (`CriticalityScoreCalculator.cs:35`, `StrategyPack.cs:23-25`) while Review moves to **0.90** — it is a *criticality* input, **not** a review decision, and **must not be rendered as a review statement this wave** (**OQ-w17-sa-01**). **CA — consume the server's answer; never compose a second one in the view model, and the cost is already paid.** `buildAnswers` (`contract360ViewModel.ts:160-199`) **maps** a server answer; it does not compute one. `contract360/index.tsx:115-119` already runs parallel GETs after the 360 and `:48-50` already documents the degrade rule — an optional source that fails *"degrades its own answer to an honest 'not yet' rather than failing the screen"* — so adding `getContractStrategy` (already generated, `schema.ts:724`, **no consumer**) to that `Promise.all` is a **wiring change, not an architecture change**, and **NW-62 is not a `client.ts` writer**. **The acceptance line**: `SAVINGS_NOT_YET_AVAILABLE` / `LEVER_NOT_YET_AVAILABLE` (`:150-152`) survive **only** as the absent-or-failed state of a source that was really called — **never as the steady state of a source that is never called**, which is precisely the defect today; a task that leaves them reachable by default has not closed the item. If SA instead composes the answer into the 360 payload, the band reads **that** and does not call `/strategy` — either shape is acceptable, **two sources for one question on one screen is not**. ADR-012 w17 clause 35. **UX — three real states per cell, and the gap sentence stops being the steady state.** Each of the two left cells renders exactly one of: **(i)** a figure and its lever; **(ii)** a **representative** band whose provenance rides the `save.lever` detail line (`AnswersBand.tsx:57`) and is **never bare**, reusing the vocabulary `ia-v2.md:110` already established for Ask (`adapter A, n = 214`) rather than inventing a second one for the same idea; **(iii)** **no answer yet**, naming what is missing **and the way to get it** — a placeholder that names neither is the defect, not the absence. **No component and no CSS change**: `answerDisplayClass` (`:33-35`) already drops text over 28 characters to `is-prose`, so a real figure stays display-size and a gap sentence stays readable — the band was **designed** for the gap, which is exactly why the gap reads as normal today and why (iii) must be visibly an edge state. **Cross-item**: "When you must move" is answerable now (raw-file correction 2), so its (iii) state is reached **only** on an extraction miss — which is NW-64's section — and it therefore reads "**Add the end date**" and links to Review, rather than "Not determined". The two items compose instead of each inventing a dead end. **`OpenWeakFacts` renders no review statement and no copy on this screen uses the word "weak"** (OQ-w17-sa-01). ADR-020 w17 §14(f)-(g). | **PO**: amend ADR-001 (w17 clause 4); **SA**: amend **ADR-024** (w17 clauses 7–9), **ADR-002** (w17 clause 3); **CA**: amend **ADR-012** (w17 clause 35 — one answer source per screen); **UX**: amend **ADR-020** (w17 §14(f)-(g)) | `ADR-001-scope-r0-r4.md`, `ADR-024-ask-raffa-v2.md`, `ADR-002-dotnet-solution.md`, `ADR-012-web-stack.md`, `ADR-020-web-screen-inventory.md` |
| NW-26 | software-architect, cloud-architect *(conditional)* | **SA — new ADR-029.** Rasterise **in the Worker at pipeline time, never on-demand in the API** (a synchronous rasterise in `GET .../preview` puts unbounded CPU on the request path). The stage runs **after admission** and is **independent of extraction success** — a failed document must still be viewable, which is what makes NW-73's bulk reprocess reviewable at all. A real renderer registers **ahead of** the placeholder (`ServiceCollectionExtensions.cs:127`), which stays the honest fallback (`DocumentPreviewService.cs:53-54`). `DocumentStoragePath` gains `BuildPreviewPage(tenantId, documentId, page)` under the **same tenant prefix** and the **same `EnsureWithinTenant` guard** (`:28-38`, ADR-009); `BuildPreview` stays page 1 so nothing breaks. The route gains optional 1-based **`?page=n`** bounded by the **already-persisted** `document.page_count` (`documents-contracts.sql:834`, verified) — out of range is **404, never a silent page 1**, because silently serving the wrong page is how a viewer lies about where a clause came from. **`pageCount` joins the document read model** (without it the viewer has exactly one page — the failure that would strand NW-63). The page budget is a **stated cap, never a silent truncation**. The renderer package, its **licence** and its **Linux native-dependency list** are named in the task's DoD; if that list is non-empty the Dockerfile layer lands in the **same task** (cloud-architect's clause), never a later one. **CLOUD — no new Azure resource, no SKU move, no Foundry / model-deployment / capacity change; $0.00.** Nothing on the AI account renders a page: Document Intelligence returns **geometry, not pixels**, so a renderer that calls an Azure API for the raster must **name the operation** and this seat re-prices. Two constraints bind, because each decides whether the SKU moves. **(i) Memory.** Both apps run at **0.25 vCPU / 0.5 GiB** from a **single shared pair** (`containerapps/variables.tf:70-80`); an A4 page at 150 DPI is ≈ **8.4 MB per bitmap**; the Worker runs **`MaxConcurrentCalls = 4`** per replica (`:424-427`). So the task **renders and stores page-by-page, disposing each bitmap, never materialising a document's pages as a set** — multi-page then multiplies the *work*, not the *peak*. Consumption's fixed ladder means **memory cannot be raised alone**, and the pair is shared, so bumping the Worker bumps the API; hence a **pre-authorised contingency with a named ceiling**, needing no new council round: split into `worker_cpu`/`worker_memory` and raise **the Worker only to 0.5 vCPU / 1.0 GiB**. **(ii) The image — the constraint no seat had priced.** The Worker base is **`mcr.microsoft.com/dotnet/runtime:10.0`** (`Raffa.Worker/Dockerfile:29`), chosen over `aspnet` **deliberately** (`:25-28`): slim Debian with **no fontconfig, freetype, libjpeg, libpng or libgdiplus**. A managed-only renderer leaves the image **byte-identical**; a native one needs an `apt-get` layer **above `USER $APP_UID` (`:34`)** or the build fails permission-denied **inside ACR Tasks** (`backend.yml:130-144`), not on a runner where someone would see it, **and above `COPY --from=build` (`:31`)** so it lands in a layer shared by every `:<sha>` tag instead of being re-stored per commit. **ACR stays `Basic`** — tens of MB once, on a shared layer. **Storage**: per-page objects are ≈ **4 GB ≈ $0.07/month** at 1,000 docs × 20 pages — immaterial, on one rule: **reprocessing overwrites by deterministic path**, never accumulates a suffix, or the `documents` container grows unbounded and nothing deletes it today. **Acceptance**: a renderer that *throws* is absorbed (`DocumentPreviewService:56-61` degrades to "no preview"), but one that *OOMs* kills the replica and burns deliveries against `max_delivery_count = 8` — so **NW-26's acceptance includes one 20-file batch on `dev` with zero Worker restarts**, or the memory question is answered by hope. **CLOUD round 2 — the ratification condition is verified, and the finding is *where* the clauses live.** This seat's vote ratified ADR-029 "on condition it carries the four clauses this lane owns"; ADR-029 was then **read in full** rather than trusted, and **one of the four is in it** (the image layer, `ADR-029:121-129`, attributed to this seat by name). The other three are in ADR-005 §19–§20. Nothing is missing on disk — but the two clauses that decide whether the Worker **OOMs** and whether the `documents` container **grows forever** live in the **Azure SKU ADR**, while the engineer who writes the render loop opens **ADR-029**, whose own "Implications for the decomposition" (`:148-162`) names the files and says nothing about the render loop or the overwrite path. Read alone it points the other way: its Consequences accept "**storage grows per page per document**" (`:142-144`) with no overwrite rule attached. **So both clauses are copied into NW-26's Definition of Done as the task's own words, not as a citation** (ADR-005 w17 §23). ADR-029 is **not** edited by this seat — it is software-architect's and the clause is not in dispute, only its reachability. ⚠ **The cross-item risk neither row carried, because it exists only in the composition (ADR-005 w17 §24): w17 ships a page renderer and a bulk re-render trigger in the same wave.** NW-73 reprocesses a whole tenant, a reprocess re-runs the pipeline, and after NW-26 the pipeline rasterises — so the console is the **largest concurrent render this product will ever have run**. **Verified**: `infra/modules/storage` declares **no `management_policy`, no lifecycle rule, no `delete_retention`, no versioning** — zero matches; unlike ACR (§21) there is not even a Premium feature being declined, the policy simply does not exist. A non-deterministic page key therefore turns **one** bulk reprocess into a **permanent doubling** of the container, reclaimable only by hand — the cost of getting it wrong is **operational, not financial**, which is precisely why the cost ADR was the wrong place for it to live alone. **Order constraint**: §19's 20-file measurement runs **before** the first whole-tenant reprocess on `dev`, never after — a bulk run is not a substitute for it, since `MaxConcurrentCalls = 4` with `min_replicas = 0` scales replicas out and multiplies the concurrent bitmaps. **And `demo` now inherits all of it** (ADR-005 w17 §25): PO's clause 9 makes the promotion this wave's business, so the renderer reaches `demo` on the **same shared 0.25 / 0.5 GiB pair** and the same unpruned storage account — the environment the client pilot is run from. If the contingency bump is taken it is wired in **both roots in the same PR**, or `dev` passes and `demo` runs the pilot at the floor with a renderer already measured as needing more. | **SA**: **new ADR-029 — document page rendering and the preview contract**; amend **ADR-027** (w17 clause 1); **CLOUD**: amend **ADR-005** (w17 §19–§21); `none` on **ADR-006/007/008** — no resource, region, layout or Foundry change. This seat **ratifies ADR-029** rather than writing a competing ADR, on condition it carries the four clauses this lane owns (page-by-page render-and-dispose; deterministic overwrite path; the three image constraints; the pair unchanged with the named contingency) | `ADR-029-document-page-rendering.md`, `ADR-027-async-document-processing.md`, `ADR-005-azure-skus.md` |
| NW-63 | client-architect, ux-ui-designer, software-architect | **SA — the split is adopted and the line is named: w17's server half is ADR-029 clauses 1–7 and nothing more.** The viewer anchors on the **existing** `SourcePage` + `SourceSpan` (`ExtractionEvidence.cs:46-47`): **no gateway change, no migration, no new write endpoint**. Warrant, not assertion: `DocumentIntelligencePage` already carries `Spans` as utf16 offsets into the concatenated `content` (`DocumentIntelligenceContracts.cs:15-26,28-33`), so **page-level anchoring is derivable from what is already stored** — **bounding boxes are the only missing piece**. **No phrase-edit endpoint this wave**: there is no `MapPatch`/`MapPut` on documents at all (`DocumentsEndpointExtensions.cs:90-96`), the existing correction path is field-name → scalar, and editing an OCR *phrase* is a different provenance question (does it rewrite the extracted text, the evidence, or both?) that would blur the proposal-vs-override distinction `ExtractionEvidence.cs:14-19` exists to keep. **W18 shape pre-decided so it is not re-litigated**: `prebuilt-layout` + widened wire contract and `AiOcrPage`; geometry columns (ADR-003 w17 clause 2 refuses them now); the phrase-edit write path. Note for the cap: because NW-63 needs **no migration**, `documents-contracts.sql` has a single writer and constraint 5 dissolves. **CA — the split costs no runtime dependency, and the real client risk is an object URL that has never once been revoked.** Because NW-26 rasterises **server-side**, the viewer renders **PNG pages**: no PDF library this wave — **and none for the W18 remainder either**, since a bounding-box overlay is absolutely-positioned DOM over an `<img>`. Verified: `web/package.json:19-25` carries exactly **five** runtime dependencies and must still carry five, so **ADR-012's dependency fence is never breached**. Recorded as a rule rather than an outcome, because an implementer handed a work item called "document viewer" reaches for `react-pdf` reflexively and nothing in the task text would stop them. **The binding client risk**: `getDocumentPreviewUrl` performs an **authenticated** fetch (`X-Tenant-Id` + bearer, `client.ts:2086`) and returns `URL.createObjectURL(blob)` (`:2100`), so a plain `<img src="/api/…">` **cannot work at all**; `:418-426` already states that the caller owns `URL.revokeObjectURL` and that *"this client does not track outstanding object URLs itself"* — and a repository-wide grep finds **zero callers today**, so that contract has never been exercised. Rule: every page fetch revokes in the `useEffect` cleanup that owns it and the viewer holds **at most the current page** alive, or a 40-page document paged end to end leaks **one blob per page**. **Route**: `/documents/:documentId/viewer?page=<n>&clause=<clauseId>` — the current page lives **in the URL, not React state** (reload and deep link land on the same page); `?clause=&page=` is **already** this product's citation-landing convention (`contract360/index.tsx:52,65-67,113`) and is reused, not re-invented; the viewer is a **citation-reached state, not a rail row**, so **`navItems.ts` gains no row and has zero writers** — the same call the V2 IA already made for Review (`navItems.ts:18`). One `<Route>` line above the `*` catch-all (`WorkspaceShellApp.tsx:94`), on a map that has **no `documents/:documentId` segment today**. **The dependency this seat was owed is discharged**: SA's `?page=n` plus `pageCount` on the document read model is exactly it — without both the viewer has exactly one page and the item's own "uploaded pages" is unmet. Adopted with SA's null-`page_count` rule: show page 1 and **say the count is unknown**, never guess a total. **NW-63 owns `client.ts`** (the method gains a parameter) — see **OQ-w17-cl-01**. **UX — the chrome is decided from the locked catalogue, and the binding rule is copy, not engineering.** **No export exists for this surface** (`inputs/design/**` carries none), so ADR-020 w17 §16 decides it from ADR-019: page canvas on `--color-surface` with a **2 px left rule** (`design-system.md:47`, the detail-pane idiom), zero radius, page navigation as a **native control row** (`.btn-ghost` prev/next + "Page N of M") and **never a floating overlay** — `--shadow-*` is dialogs-only (`:25`), so a hovering page control would be the **first shadow inside the app** — and the highlight as a `--color-accent-100/200` **tint**, never a new colour. **The one failure mode is a promise**: w17 ships the page image plus the **text-level** highlight (`ClauseHighlight.tsx:28-36`) while the bounding-box overlay is W18, so the affordance reads "*Page N — the wording is highlighted below*" and **must not promise a box drawn on the page**. The split is invisible to the user *unless the words create an expectation the screen then breaks*. **Four states, not three** (ADR-018 w17 clause 12): **loading** = skeleton at the page's aspect and the same footprint, so nothing jumps; **empty** = "no page image yet", the honest fallback the server already degrades to, never a broken `<img>`; **error** = 2 px accent rule + h4 + the plain service name + Retry; **not found** = the **route's own** state for a bad `documentId` or an out-of-range `?page=` — never the shell's `*` catch-all, which redirects to `/` and would land a broken citation on **Ask with nothing saying anything went wrong**. `pageCount` null → "Page 1 · total unknown", never a guessed total. ⚠ **Correction against this seat's own lane**: the lane ruled the viewer "a state of screens 4/5, **not a route**"; **client-architect owns ADR-018's route half and ruled otherwise, and it is right** — a full-page surface with a shareable, deep-linkable URL is a route. The lane's **IA** half survives intact and client-architect reached it independently: **no rail row** (`navItems.ts:18`). | **SA**: **new ADR-029** (the split is recorded there); amend **ADR-017** (w17 footer — `prebuilt-layout` stays uncalled), **ADR-003** (w17 clause 2 — geometry refused); **CA**: amend **ADR-012** (w17 clauses 32–33) and **ADR-018** (w17 clauses 9–11 — **one route added, the first since w14**); **UX**: amend **ADR-020** (w17 §16, §18) and **ADR-018** (w17 clauses 12–14 — the states half, a separate footer leaving clauses 9–11 byte-identical) | `ADR-029-document-page-rendering.md`, `ADR-017-ocr-in-v1.md`, `ADR-003-data-store.md`, `ADR-012-web-stack.md`, `ADR-018-web-information-architecture.md`, `ADR-020-web-screen-inventory.md` |
| NW-64 | ux-ui-designer, client-architect, product-owner | **PO** — IN. Spec `:341`'s five critical fields (`annualSpend`, `totalContractValue`, `cancellationDeadline`, `endDate`, `renewalTermMonths`) plus the already-required ones are **always shown, recovered or not**, as **empty fillable rows**; hiding a field because extraction returned nothing asserts a completeness the system does not have (w14 clause 3). **Termination and price uplift have no correctable field at all and are not invented this wave** — a bounded, recorded gap and a W18 candidate. **CA — the read-back needs no endpoint, and the test must be rewritten rather than deleted.** `reviewViewModel.ts:244` today **drops** a row whose `currentValue` is null with no proposal; under NW-64 the row survives in a **`missing`** state instead. **No new endpoint**: the fill reuses the existing `PATCH /api/contracts/{id}` (`client.ts:2403-2411` via `useReviewSession.ts:159-162`), so the item stays pure-web as its Area says, and the persisted value reads back through the `load()` the correction already triggers. **The test is the task**: `reviewViewModel.test.ts:347` locks the current skip and must be **rewritten to assert the new row** — deleting it is how a behaviour change becomes unobservable. ⚠ **Single-writer collision not in §5**: `reviewViewModel.ts` has **two** items (NW-71 at `:282`/`:294`, NW-64 at `:244`); constraint 1 *orders* them, but **order is not a phase** and `check_single_writer.py` rejects on the file. **Two phases, or one task** — and the same holds for `ReviewFieldList.tsx` and `reviewViewModel.test.ts`. e2e **N18** in `w17-review.spec.ts`: a field whose OCR missed renders fillable, and the fill **survives reload**. **UX — a section at the end of the field-list column, and only for fields that can actually be corrected.** Heading **"Not found in the document"**; one sentence: *"Raffa could not find these in the file. Type the value if you have it — it is saved as your correction."* Each row is the field label + an empty `.input` + the **same** correction write the rows above it already use. **Placement diverges from the raw file and it is named**: the item says "next to the viewer", which this seat reads as *beside* the evidence column and **not inside it** — `design-system.md:47` sizes the detail pane at 340–400 px and a fill-in form there is cramped, and the pane keeps its single job of showing the page a value came from. **Only fields with a correctable target appear**: PO's clause 5 records that **termination and price uplift have no correctable field at all** this wave, and an input that writes nowhere is **worse than an absent row** — it takes the user's answer and silently drops it. **The section does not render at all** when every canonical field was recovered — no "nothing here" sentence, because a sentence announcing an absence is itself noise. **A missing field is never given a confidence tag**: it has no confidence, and `—` is not a value. Visibility covers every canonical field with **no value and no proposal**, regardless of `required` (`reviewViewModel.ts:82`, `:84` mark `endDate` and `cancellationDeadline` `required: false`, which is exactly why they vanish at `:244` today); the required/optional **wording** stays product-owner's. The field list therefore gains a **fourth row state** (`not found`) while the **screen's** four states (`screens-v2.md:93`) are unchanged. ADR-020 w17 §13(c)-(e). | **PO**: amend ADR-001 (w17 clause 5); **CA**: amend **ADR-012** (w17 clause 38); **UX**: amend **ADR-020** (w17 §13) | `ADR-001-scope-r0-r4.md`, `ADR-012-web-stack.md`, `ADR-020-web-screen-inventory.md` |
| NW-65 | ux-ui-designer, client-architect | **CA — delete the block, move the gate, and repair a sentence that is untrue today.** `DetailsSection.tsx:78` goes; "Review all →" (`:79-81`) stays; the officialized gate moves into the view model so the column is not left sparse. **The sharper half is the copy.** `NO_ATTENTION_MESSAGE` (`contract360ViewModel.ts:586`) reads *"None — every fact is above 95% **or signed off by you**"*, while `computeNeedsAttention` (`:568-583`) reads **no human-decision state at all** — the second clause is simply false on `d3d2d24`. Two honest repairs: drop the clause, **or** — better, and free — read **NW-71's persisted decision**, which makes the sentence true for the first time; NW-71 lands first (constraint 1), so the second option is available and should be taken. ⚠ **The gate itself is a colour check** (**OQ-w17-cl-02**): `:575` tests `tag.variant !== "neutral"`, inferring a decision from **presentation** against `semantics.ts:45-46`'s explicit instruction not to. Today that happens to mean "at or below 95 %"; the moment NW-71 collapses the bands it **silently changes which facts this list shows, with nobody editing the line**. NW-65 rewrites this function anyway, so the fix is free — but unnamed it will be preserved verbatim as working code. ⚠ **`FactTable.tsx` has no assigned writer.** It is listed as a `getConfidenceTag` consumer in constraint 1 but appears in **no item's file set**, while raw NW-65 §1 extends the officialized gate to Products / Obligations / Risks — which render through it (`FactTable.tsx:35`). The decomposer must assign it, or the gate ships on two thirds of screen 5. **UX — delete the block, keep every row, and let the false half die by deletion rather than by rewording.** `DetailsSection.tsx:78`'s heading and the attention rows (`:83-93`) go; **"Review all →"** (`:79-81`) **stays**, re-homed as the column's single trailing line carrying a count — "**N facts still need you — Review all →**" — and **when N = 0 the line does not render at all**. That deletes `NO_ATTENTION_MESSAGE`, whose *"or signed off by you"* clause is **untrue on `d3d2d24`**: when the system does not hold a claim the honest repair is to **stop making it**, not to phrase it more carefully. **The officialized gate keeps every row**: an unofficialized value renders the **em-dash placeholder already in this view model's vocabulary** (`contract360ViewModel.ts:375-400`, `:387`, `:391`, `:392`) — deleting a product line would hide contract content, and the user cannot tell an absent line from an unextracted one. **The sparse-column risk is the filter, not the deletion, and the raw file points at the wrong one**: `contract360.css:391-394` is `repeat(2, minmax(0,1fr))` with a full-width third child (`:400-402`) and the columns are **independent grid items**, so removing the second half of column 2 leaves Key terms | Documents intact. What *would* leave a hole is dropping unofficialized **rows** from Key terms — column 1 empties while column 2 stays full — which the em-dash rule prevents structurally. ⚠ **`FactTable.tsx:35` must be assigned a writer** (client-architect reached this independently): raw §1 extends the gate to Products / Obligations / Risks, which render through it, and unassigned the gate ships **on Key terms alone** while the other three still show unofficialized values — a screen inconsistent with itself, which is worse than either end state. The component docstring `:26-31` quotes the export's "facts still to decide" verbatim and is rewritten **with** the code, or it cites an oracle it no longer follows. **Divergence ratified**: `screens-v2.md:103-104`. Stale prose `web/README.md:630`; test-locked `contract360ViewModel.test.ts:402`, `:452`. ADR-020 w17 §14(a)-(d), §17. | **CA**: amend **ADR-012** (w17 clause 36 carries the gate rule); **UX**: amend **ADR-020** (w17 §14(a)-(d), §17) and ride **ADR-019** w17 clause 11 (no confidence tag renders on Contract 360 at all) | `ADR-012-web-stack.md`, `ADR-020-web-screen-inventory.md`, `ADR-019-web-design-system.md` |
| NW-66 | ux-ui-designer, client-architect, product-owner | **PO** — IN, and a **deliberate divergence** from `screens-v2.md:101`: confidence leaves the Why row. **One rule binds NW-65 and NW-66 — confidence lives in Review; Contract 360 never renders it.** Appendix C `product-spec.md:954` ("never show a consequential extracted fact without source evidence **and confidence metadata**") is therefore superseded **in its rendering half, on 360 only** — every fact there is *officialized* and carries page · section, a stronger signal than a score; it stands **verbatim in Review and in Ask**, and the **storage half (`:127`) is untouched**. Leverage words: **Push to change** (Critical/High) · **Worth raising** (Medium) · **Standard terms** (Low); the raw `ContractRiskLevel` enum never reaches the screen. **CA — the truncation cap already exists, and the deep-link is NW-63's route reused verbatim.** `formatSource` (`contract360ViewModel.ts:361-367`) joins an **uncapped** source column; the Review screen already truncates the same class of field at 60 characters (`ReviewFieldList.tsx:84,92-96`) — **adopt that existing cap**, do not invent a second rule for the same problem on a neighbouring screen. `getClauseRiskTag` (`:255-259`) stops returning the raw enum. **The link is `/documents/:documentId/viewer?page=<sourcePage>&clause=<clauseId>`**, which is **why the route must be decided this wave even though the overlay half is deferred**: three surfaces point at it (Why clauses, Ask citations, the Documents row), and a link target that changes shape between waves breaks all three. **Ordering: NW-66 after NW-63.** ⚠ ux-ui-designer proposes the leverage map live in `contract360ViewModel.ts` rather than `semantics.ts` (which constraint 2 reserves for NW-71) — **this seat agrees**, and adds that it keeps that file at **three** contending items rather than four, because NW-71 need not open it at all (see NW-71's CA half). e2e **N20** in `w17-viewer.spec.ts`: a clause row shows no quote, carries a leverage word, and its link lands on the cited page. **UX — the words are product-owner's, the treatment is this seat's, and the answer is that nothing moves.** PO's three words are adopted and **this seat's lane's four-word set (Leverage / Weak for you / Protect / Watch) is withdrawn** — the lane itself conceded the vocabulary to product-owner. The treatment ruling: **Semantic-mapping row `:106` is unchanged**, because `getClauseRiskTag` (`contract360ViewModel.ts:255-259`) **already** computes `High || Critical → accent`, everything else → `neutral`, with the **label** carrying the meaning and its own docstring (`:254`) already saying *"Text first, colour only as emphasis."* So this is a **label-only** change: the variant expression stays byte-identical, and Medium and Low keep sharing `.tag-neutral`, distinguished by their **words**, exactly as ADR-019 `:124-125` requires — the precedent is already in the file rather than invented here. ⚠ **Scope fence, which dissolves the collision this seat's lane raised**: the relabel touches `getClauseRiskTag` **only**. `semantics.ts:getRiskTag` (`:91-96` — "High risk"/"Medium risk"/"Low risk") is a **different function on Portfolio and Renewals** and is **not** renamed: renaming it would silently change rows no item in this wave touches, **and** would put NW-66 into `semantics.ts`, which constraint 2 reserves for NW-71. The fence keeps that file single-writer **at no cost in phases**. **A null risk level stays null** (`:256`): "Standard terms" is a statement *about the clause*, so synthesising it where risk was never determined invents precisely the kind of fact this wave exists to stop (w14 clause 3). The row is type (`WhyClauses.tsx:51`) + officialized value (`:52`) + leverage tag + one-line why + **Open in document viewer**; the source cell (`:54`) and the confidence tag (`:56`) go, and **the quote moves into the existing `ClauseHighlight` (`:64`)** — the "specchietto" is **not a new component**, so the locked catalogue is untouched. `formatSource` (`:361-367`) stops feeding the row; the short `p.N · §` reference in the highlight header adopts **Review's existing 60-character cap** (`ReviewFieldList.tsx:84`, `:92-96`) rather than a second rule for a neighbouring screen. **Three-term legend** in the hint slot the component already has (`WhyClauses.tsx:29`), because a vocabulary the user must infer means nothing; `:16-19`'s docstring quotes the export and is rewritten with the row. **Divergence ratified**: `screens-v2.md:101-102`. ADR-019 w17 clause 9; ADR-020 w17 §14(e), §17. | **PO**: amend ADR-001 (w17 clause 6); **CA**: amend **ADR-012** (w17 clause 36) and **ADR-018** (w17 clause 10 — three surfaces link to the viewer); **UX**: amend **ADR-019** (w17 clause 9) and **ADR-020** (w17 §14(e), §17) | `ADR-001-scope-r0-r4.md`, `ADR-012-web-stack.md`, `ADR-018-web-information-architecture.md`, `ADR-019-web-design-system.md`, `ADR-020-web-screen-inventory.md` |

`Decision`: one or two sentences, or `task only` when no seat is involved.
`ADR action`: `new ADR-NNN <slug>` | `amend ADR-NNN` | `supersede ADR-NNN by
ADR-NNN` | `none — <why>`. `File`: the ADR path written or amended.

Next free ADR number at the time of this skeleton: **ADR-029**
(`reports/architecture/ADR-001…028` on disk).

### Product ruling on an item that is not in the table above — NW-75 is OUT

Recorded by **product-owner** at the table. This **refines** the note at the top
of this record ("NW-75, NW-23, NW-25, NW-74 … are not decided here"): that note
is right about NW-23, NW-25 and NW-74, which are **queued**, and the intake
itself reserves the NW-75 decision for this council —
`w17-requirements.md:767-768`, "**and if the council rules it OUT, it leaves the
live set entirely and is recorded, not queued**". Scope is this seat's, the raw
file asks for the ruling by name, and leaving it queued costs W18's head.

**NW-75 (designed Savings section for tracked renewal actions) is ruled OUT for
V1**, on four grounds, in full at **ADR-001 w17 clause 7**:

1. **No estimate exists to show.** The oracle's table is Supplier · Action ·
   **Estimate** (`screens-v2.md:135`); a tracked action holds no system-held
   estimate until an opportunity exists, so the row must either invent a number
   — against `E04/F03/US01` AC-3's "never fabricated precision" and w14 clause 3
   — or ship an undefined blank column. This is why w16 retired
   `buildTrackedOpportunityRow`.
2. **Two homes already exist** — Renewals (`screens-v2.md:127-130`) and the 360
   tracker (`:105-108`). A third surface adds no user-visible outcome
   (ADR-001 `:84-85`).
3. **The pilot puts this surface out of the demo** (`percorso-pilota-v1.md:148`).
4. **It is `could`, and it is overflow.** OUT removes it from the live set
   instead of parking it, freeing W18's head for NW-63's deferred half.

**Re-entry condition — this is a ruling, not a refusal**: NW-75 returns as a
**new item** once a tracked action can carry a system-held estimate, i.e. after
NW-62's benchmark join (OQ-w17-004). Not before, and never as a pseudo-row.
**No task, no story, no ADR-020 change, and no work item is cancelled** — NW-75
is an unstarted queue entry, not a backlog item with a `status:` line.

**Consequence for the decomposer and for the W18 intake**: W18's head is
**NW-23, NW-25, NW-74** (in that order), preceded by NW-63's deferred half if
OQ-w17-001's split stands. NW-75 is **not** in that queue.

### Security note on an item that is not in the table above — NW-74's deferral carries no security debt

Recorded by **security-architect**, who the roster lists on NW-74
(`w17-requirements.md:716`). This is **not a ruling and does not reopen the
queuing decision** — it answers the one question the queuing raises for this
seat, so W18 does not rediscover it and no reader mistakes a deferred item for an
unenforced control.

ADR-022 w16 §3 `:227-230` already deferred this work **once**, from w16 to W17, as
**client presentation work**, and recorded why that was safe. W17 queued it again,
to W18. **The same clause makes the second deferral safe, and this seat
re-verified it on the code rather than re-citing itself**: `GetCapabilities()`
takes **no parameters at all** and returns `CapabilityCatalog.All` **unfiltered**
(`CapabilitiesEndpointExtensions.cs:31-33`), so the response is **provably
identical** for no token, a non-Admin and an Admin — the property ADR-022 §2a made
load-bearing when the role gate was dropped. **Every action behind an admin-gated
entry is enforced server-side by membership on its own endpoint.** Hiding a chip
is therefore cosmetic in the precise sense that matters: **deferring it leaves no
control unenforced**, this wave or the next.

**Two conditions travel with the item to W18, unchanged** (ADR-022 w17 clause 4):
the role passed to both renderers is the **server-derived** membership role
(`workspaceRole.ts:37-39`) — never client-asserted, never read from
`localStorage`, never an `X-Role` revival; and `GET /api/capabilities` is **not**
membership-gated, because w16 took that alternative deliberately. **The item must
not be written as a security fix** — §3's own last sentence, restated because the
title alone will invite it a year from now.

### Priority note on the same item — NW-74 must not be deferred a third time

Recorded by **product-owner**, whom the roster does not list on NW-74; this is
**not a ruling on the item** and does not reopen the queuing, which this seat
already ratified. It is a **constraint on W18's order**, which is this seat's,
written here because the W18 intake reads this file.

NW-74 is a **`should`** queued behind **`could`**s. That is defensible exactly
once, and only on the reason `w17-requirements.md` §5 gives: the raw file fixes
the overflow sequence, NW-74 is the cheapest item in the wave, and **queuing is
not demotion**. But it has now been deferred **twice** — w16 → W17 (ADR-022 w16
§3) and W17 → W18 — and with NW-75 ruled OUT above, W18's head is **NW-23,
NW-25, NW-74**, preceded by NW-63's remainder. **A third deferral behind the
same two `could`s would be a demotion in fact**, whatever the status line says,
and it would be the second time this product let an item survive on the grounds
that moving it is cheap.

**Binding on the W18 intake**: if W18's cap binds, NW-74 is **promoted ahead of
NW-23 and NW-25** — the raw file's overflow order governed *this* run's
overflow, not W18's internal priority, where a `should` outranks a `could`. If
it is deferred again anyway, that needs a **written reason naming this clause**,
per raw §0.2. The same rule that costs `demo` a slot in ADR-001 w17 clause 9
applies here: a deferral that never costs anything is how something reaches a
fourth wave.

### Security note (round 2) on two items — the second context with clause 1's trap, and a builder this seat bounded at the wrong place

Recorded by **security-architect**, who is rostered on **NW-20** (via OQ-w17-sa-03)
and **not** on **NW-26**. Neither part reopens a decision: the first **refines this
seat's own ADR-011 clause 22**, the second **corrects this seat's own ADR-009
clause 3**. Both were produced by verifying round-1 rulings against rulings that
landed after this seat spoke, rather than by re-reading the lane.

**1. NW-20 — the permitted projection lands on a second context carrying clause
1's trap (ADR-009 w17 clause 5).** ADR-009 clause 1 named the
optional-third-argument trap on `DocumentsContractsDbContextOptions` and bound it
to NW-73's console. **`AuditDbContextOptions.Configure` has the identical
signature** (`:24-27`), wiring the RLS interceptor **only when the third argument
is supplied** (`:35-38`). This seat permitted the `activity` projection *after*
writing clause 1 and did not notice the permitted read lands on the second
context; software-architect then ruled it **host-composed in `Raffa.Api`**, which
is exactly where the trap bites, and cloud-architect's §26 supplied the confirming
fact — `ConnectionStrings__Audit` is **already bound** on the API
(`containerapps/main.tf:90`), so nothing external gates the wrong wiring. The
store itself is sound: `audit_event` is `tenant_id NOT NULL` + `ENABLE` + `FORCE`
+ `tenant_isolation` on the standard shape (`audit.sql:20`, `:54-58`), so a
mis-composed read fails **closed** — **and on this surface the closed failure is
the unconditional `[]` that product-owner, software-architect and this seat each
refused separately.** The RLS misbinding and the forbidden empty array are one
defect with two names. **Ruling**: the read is a method on `AuditQueryService`,
which opens its own scope per call (`:75`) and documents the ordering a host
composition would have to rediscover — the interceptor reads
`ITenantContext.Current` **only when the connection opens**, lazily on first use
(`:71-74`). This **refines** software-architect's ruling rather than contradicting
it: the host still composes and merges, as `DependencyDirectionTests.cs:63`
requires; it must not **author the query**. ⚠ **The existing reader cannot be
narrowed into this** (ADR-011 w17 clause 25): `GetEventsAsync` returns the **200
most recent events for the whole tenant** with **no contract filter and no action
filter** (`:66`, `:68-80`) — the Admin-only feed clause 22 refuses to put on a 360
tab. NW-20 must add a **new** method, and that method is the single moment
conditions 1 and 2 are implemented or silently lost, which is why they are written
as **query predicates**. The obvious implementation — call the existing reader and
filter its 200 rows in memory — is tenant-wide at the database, drops a contract's
older events, has no allow-list, and **looks exactly like the feature**.

**2. NW-26 — the page bound belongs in the builder, because NW-73 is a caller with
no endpoint (ADR-009 w17 clause 6).** This seat's clause 3 bounded `page` "before
any path is built" and leaned on ADR-029's 404. **Both are endpoint rules**, and
the same composition cloud-architect found from the capacity side (§24) defeats
them from the path side: NW-73 re-runs the pipeline directly, the pipeline
rasterises after NW-26, so `BuildPreviewPage` is reached where no route, no model
binder and no 404 exist. **Rule**: the builder validates `page >= 1` and throws
itself, exactly as `DocumentStoragePath.Build` already does for `versionNumber`
(`:42-46`) — the precedent is four lines from the new method. **`page` is an
`int`, never a string**: `EnsureWithinTenant` is a prefix test with **no
canonicalisation** (`:32`), `Sanitize` is applied to `fileName` in `Build` and to
**nothing** in the preview path, and `BuildPreviewPage` is the first
caller-influenced component a preview path has ever had (`BuildPreview`'s
`page-1.png` is a literal, `:21`). **No ADR-029 or ADR-005 edit is proposed**;
this is ADR-009's own clause, corrected.

**3. Offered to cloud-architect, not asserted over it (ADR-009 w17 clause 7).**
That seat's §23 finding is that the deterministic-overwrite rule lives in the cost
ADR while the render loop is written from the rendering ADR. **There is a closer
home and it is already written**: `BuildPreview`'s docstring states a preview is "a
derived rendering, **replaced in place** whenever the document is reprocessed"
(`DocumentStoragePath.cs:16-18`) — in the file the new method is added to. So a
key derived from three server-held values with no timestamp, run id or suffix
**overwrites by construction**. It is an isolation rule as well as a capacity one:
a suffixed key leaves a prior rendering of a **since-corrected** document readable
at a path nothing reaps, since `infra/modules/storage` has no lifecycle rule.

### Client note (round 2) on an item that is not in the table above — NW-20's two members reach the generated client, and nothing renders them

**This seat is not rostered on NW-20** (seats: software-architect, product-owner,
plus security-architect via OQ-w17-sa-03) and rules nothing about it. But
software-architect's round-2 shapes — `Contract360ActivityEntry` and
`Contract360BenchmarkEntry` — land on `Contract360Result`, which is a payload the
**web consumes**, so the generated client changes and this seat records the client
consequence rather than letting it arrive unnoticed in W18.

**Verified, not inferred**: `activity` appears **nowhere under `web/src`** — no
occurrence in `contract360ViewModel.ts` — so both members are today memberless
records with **no web consumer**. Adding members to a response type is
**additive**: `npm run generate:api && tsc --noEmit` stays green and the member
lands **silently**. That is the same shape as the defect **NW-62 exists to repair
in this very wave** — `getContractStrategy`, generated at `schema.ts:724`, with no
consumer — which is why it is worth one paragraph now rather than a rediscovery
later.

**Recorded decision**: w17 ships `activity` and `benchmark` **on the wire with no
web consumer and no client task**. This is deliberate and it is the cheap option:
it keeps `contract360ViewModel.ts` at the **three** contenders of constraint 3
(NW-62, NW-65, NW-66) instead of adding a fourth to a file the cap already strains,
and it contradicts nothing — product-owner's clause 3 fences the *projection*, not
its rendering, and neither NW-20 nor any other row names a client file for it.

**The rule travels to W18 with the surface** (ADR-012 w17 clause 45): whichever
task builds the tab must distinguish *called-and-empty* from *never called*,
exactly as clause 35 already requires of NW-62's answers band. An empty `activity`
renders "no recorded activity yet" **only** when the call succeeded — never as the
default paint. ⚠ This is the client half of security-architect's round-2 finding:
that seat showed a mis-bound projection **fails closed** and renders an empty tab
indistinguishable from "nothing happened", and software-architect has already
solved the sibling member by construction (the benchmark abstain rides **as an
entry**, `Status = insufficient_data`, never `[]` — ADR-024 w17 clause 11).
**`activity` has no such entry shape**, so for it the distinction must be carried
by the view model. Three seats, three routes, one rule.

### Design note (round 2) on the same item — four oracles still promise the Activity **tab**, and one of them is the file NW-20 edits

**This seat is not rostered on NW-20 either** and decides no screen for it. It
agrees with the client ruling above and adds the half that seat could not see:
client-architect ruled from `web/src` and ruled **correctly** — `activity` is
absent there — but the pull toward *building the tab* lives in **four** places
**outside** `web/src`, three of them this seat's own exports.
`Contract360Result.cs:12` lists "…Activity." among the **Tabs** and `:219` calls
the record "Placeholder row shape for **the Activity tab**"; the **V2** prototype's
`app.jsx:112` still carries `'Activity'` in `moreTabNames`; `ia.md:27` lists it in
the V1 tab set; and `screens.md:29` gives the tab template as
"Term · Value · **Source · Confidence**".

**All four are already falsified.** The screen has had **no tabs since V2** —
`contract360ViewModel.ts:28-30` records the ten-tab layout as **gone**, and
`contract360.css:2` and `contract360ViewModel.ts:18` both cite `screens-v2.md #5`
"Contract 360 — **no tabs**". The template's **Confidence** column is refused by
ADR-019 w17 clause 11 (no confidence tag renders on 360 at all) and its **Source**
column is the cell NW-66 deletes. And software-architect's
`(OccurredAt, Action, ActorLabel)` — **when · action · actor** — matches that
template in no column at all. ⚠ **The V2 export contradicts itself** (`app.jsx:112`
vs `screens-v2.md #5`) and the shipped product already resolved it; named here per
this seat's standing duty rather than left for a later reader.

**Recorded**: **no Activity surface in w17**, and **a wire member is not a screen
commitment** — W18 designs it from the three members and `ia-v2.md`, never from the
tab template. **One free sweep rides NW-20's existing task, no new writer**:
software-architect opens `Contract360Result.cs` to give the record its members, so
`:12` and `:219` are rewritten in the same edit — the **fifth** application of this
wave's rule that a docstring quoting an oracle the code no longer follows is
rewritten with it. Left alone, the tree keeps promising a tab in the very file W18
reads first. ADR-020 w17 §22; a **sixth** export is owed to W18 and blocks nothing
here.

### Client note (round 3) on NW-63 — the reap stops at the tab, and the 404 names the wrong thing

**ADR-029's round-3 clauses 1–2** (a deterministic page key replaced in place; a
shorter re-render reaps `n > pageCount`) are **adopted whole and ADR-029 is not
edited** — it is software-architect's. Their **client half** is this seat's:
**ADR-012 w17 §46–§48** and **ADR-018 w17 clause 15**, both on disk.

⚠ **The reap does not cross into the browser.** The overwrite exists precisely
because a suffixed key would leave "a prior rendering of a **since-corrected**
document readable where nothing reaps it" (`ADR-029:204-206`, security's half of
the reason). It reaps that rendering **in the container**. The viewer never reads
the container: it reads an authenticated blob and materialises it with
`URL.createObjectURL` (`client.ts:2100`) — a copy in the tab, on an opaque handle
with no relation to the storage key and **nothing to revalidate against**. ⚠ And
`cache: "no-store"` (`:2087`) is what **hides** it: the HTTP layer provably never
serves stale bytes, so the paint is assumed fresh when it comes from a blob no
fetch directive governs. w17 is the wave that makes it reachable — **NW-73
re-renders a whole tenant while NW-63 ships the first surface that holds pages
open**.

**The client cannot detect it, by design**: the key carries no discriminator
(clause 1), `pageCount` is unchanged by a same-length re-render, and the document
read model has no other candidate (`documentProgress.ts:65`,
`documentTable.ts:145`; the generated schema's only `versionNumber` is on
contract corrections, `schema.ts:393`). ⚠ **Nor does the poll** —
`useDocumentsList.ts:148` runs only while a row is `Uploaded`/`Processing` and
`:55` stops it after five minutes, so in the steady state where a reviewer opens a
viewer **no poll is running**. **Ruling**: the viewer holds **no page cache across
a page navigation**; every page view is a fetch (already `no-store`), revoked per
ADR-012 clause 33 — the memory rule and the correctness rule are **one rule**. **No
contract member is asked for**, declined deliberately in a capped wave, with the
W18 condition recorded instead (an overlay pinning coordinates to bytes **does**
need a discriminator).

⚠ **`client.ts:2104` answers every 404 with `No document found for id ${id}.`**,
while ADR-029 clause 5 makes 404 the *page*-out-of-range answer and round-3 clause
2 makes "that page was reaped" an ordinary outcome. ux-ui-designer's ADR-018
clause 12 asks the surface to tell those apart and the data layer as written
cannot — a viewer wired to it says *"No document found"* about a document open in
front of the user. Repaired **inside NW-63's own `client.ts` edit**, so ADR-012
clause 39's writer count is unchanged, and discriminated from `pageCount`, which
the viewer already holds (`page > pageCount` needs **no fetch**). ⚠ **No `onError`
fallback to page 1 and no clamp**: ADR-029 clause 5 refuses that at the route, and
the client can recreate the identical lie **above a correct server** in three
lines. ADR-018 clause 15 keeps the URL on the page that was asked for, so the
failure stays reproducible.

**NW-62 needs no change, and that is a ruling rather than silence**: under
ADR-011 clause 26 a document left with no corpus renders the 360's "not yet
available" constants — **correct** under ADR-012 clause 35, because the source was
genuinely called and genuinely has nothing. No special case and no client task.

### Round 3 — design (ux-ui-designer): ADR-020 §24–§26

**The sentence three round-3 rulings are machinery for does not exist.** ADR-029
round-3 clause 2 reaps surplus pages, ADR-012 §47 discriminates the two 404 causes
and ADR-018 clause 15 keeps the URL — and all three land on a **not found** state
whose copy this seat's own ADR-018 clause 12 routed to "ADR-020 w17 §16", which
**does not contain it** (`ADR-020:1410-1439` is chrome, the overlay-promise rule
and the `pageCount`-null rule). ⚠ **A correction against this seat's own footer**:
a clause that cites a section for a string it does not hold is worse than an
omission, because the next reader stops looking. **ADR-020 §24** writes it: **one
state, two causes, two sentences** — *"This document has N pages."* + **Go to page
1** when `page > pageCount` (no fetch), and *"This page is no longer part of this
document — it was re-processed after this link was made."* + **Reload the
document** when the server 404s a page the tab still counts. ⚠ **They are one URL
in sequence** — after the reload the tab holds the shorter `pageCount` and the same
address becomes the first sentence; stated because read cold they look mutually
exclusive and an implementer deletes one. **Not found takes the *empty* treatment,
never *error*'s Retry**: after ADR-029 round-3 clause 2 it is an ordinary outcome,
and a Retry invites the user to press it until they conclude the product is broken.
**ADR-019 `none`** — no token, component or treatment.

⚠ **ADR-012 §48's conclusion stands and its premise does not.** No client task for
NW-62 — agreed. But `contract360ViewModel.ts` has **zero** occurrences of `corpus`
or `chunk`: the 360 reads structured facts and the Why row's evidence trail
(`client.ts:671-674`), while ADR-011 clause 26 destroys **embedding chunks**. They
are **disjoint** — a document whose corpus was destroyed renders a 360 that is
**complete, populated and confident**, the opposite of legible. The constant named
is not a corpus state either: `SAVINGS_NOT_YET_AVAILABLE` (`:150`) is the
**benchmark**'s, and `:152` promises it "lights up with the Benchmark Service" and
asserts the clauses below are **validated** — firing it here would be worse than
silence. Corrected because a premise in a vote row is what a decomposer copies into
a DoD. **What the user meets is Ask**: the off-state gate counts *validated*
contracts and a chunk delete leaves validation intact, so Ask stays **on**, still
claims *"Answers only from N validated contracts"* (`askViewModel.ts:320`) and
**abstains** — which `:344` has taught the user to read as *"that is not in your
contracts"*. ⚠ **The honest sentence exists and is unreachable**: `:285` ("Raffa.ai
could not finish processing your documents") is wired to the off-state gate, not to
a corpus deleted **after** validation — and with no audit row (clause 26) the
product has **no other account of the event at all**. **Ruling: no copy and no
state this wave**, because ADR-022 clause 6b ships NW-73 `dev`-only and no pilot
user stands behind that screen — a clause, not a budget. **One prohibition**: no
task may add a reassuring "re-indexing" banner, which would be a *not ready yet*
claim that can never resolve — **ADR-018 w15 clause 6** (`:474-476`) already forbids
it. **OQ-w17-ux-05** open with the assumption in force: widening NW-73 beyond `dev`
needs a **corpus-health signal** first, so Ask can tell *retrieved nothing* from
*has nothing to retrieve*; a W18 condition on the widening, not a w17 task.

## Open questions owed a ruling at this table

From `w17-requirements.md` §7. Each carries an assumption in force; the table
either ratifies or overrules it. **OQ-w17-001 is the only one that can change the
wave's shape.**

> **Seat prefixes on the OQs raised at this table**, recorded because two seats
> abbreviate to the same two letters: `po` = product-owner, `sa` =
> software-architect, **`ca` = cloud-architect**, `sec` = security-architect,
> **`cl` = client-architect**, `dm` = delivery-manager. A later reader resolving
> `OQ-w17-ca-01` against the wrong seat would send the Service Bus ruling to the
> web lane.

| OQ | Item | Owed by | Ruling |
|---|---|---|---|
| OQ-w17-001 | NW-63 | client-architect, software-architect, ux-ui-designer, product-owner | **PO half — the split is RATIFIED, on one condition.** A reduced `must` is honest; a silently deferred one is not. The condition is already on the record: the deferred half becomes the **head of W18, ahead of this run's four overflow items** (`w17-requirements.md:811-813`). If the council instead **rejects** the split it must cut **two `should` items and name them** — it may not absorb the difference by narrowing a `must`. **SA half — adopted, and the line is named rather than left vague: w17's server half is ADR-029 clauses 1–7 and nothing more** (no gateway change, no migration, no write endpoint), because page-level anchoring is **already derivable** from the spans on disk and only bounding boxes are missing. The W18 remainder is pre-shaped in ADR-029 and ADR-017's w17 footer so it is not re-litigated. If the council instead **rejects** the split, this seat does **not** get to absorb it by narrowing the server half further — two `should` items are cut and named. **CA half — the split is adopted, and the reason no seat had priced: it costs no runtime dependency, this wave or the next.** This OQ, and this seat's own lane, both framed the viewer as *"the first new runtime dependency since ADR-012"*. **It is not one.** Because NW-26 rasterises server-side the viewer renders **PNG pages**, so no PDF library is needed in w17 — **and none in the W18 remainder either**, since a bounding-box overlay is absolutely-positioned DOM over an `<img>`. `web/package.json:19-25` stays at **five** runtime dependencies. The split therefore carries **zero dependency risk on either side of the line**, which strengthens PO's ratification rather than qualifying it, and removes the one cost that would have argued for landing NW-63 whole. **The route is decided this wave regardless of the split** (ADR-018 w17 clauses 9–10): three surfaces link to the viewer, and a link target that changes shape between waves breaks all of them. ADR-012 w17 clause 32. **UX half — the split is adopted, and the only thing it can break is a promise.** Design-side the deferral is **invisible**: the page image, the page-navigation row and the text-level highlight are the whole w17 surface, and a bounding box is an **addition drawn on top of a page that already renders**. So there is no layout that must be built twice, no state that changes shape in W18, and nothing this seat must design now and redesign then. **The one real risk is copy.** If w17's affordance says "see the highlighted box on the page", the wave ships a promise it cannot draw and the user reads a working screen as broken — a failure that no test catches and no engineering decision causes. Hence the binding rule in ADR-020 w17 §16: "*Page N — the wording is highlighted below*", **never a box**. Second reason the split costs this seat nothing: **no export exists for the viewer either way**, so the chrome is decided from ADR-019's locked catalogue in both halves, and export (19) is requested for the **whole** surface rather than once per half — a re-export of half a viewer would itself become a stale oracle. ADR-020 w17 §16, §18. |
| OQ-w17-002 | NW-71 | software-architect, client-architect | **PO corollary, not a ruling on the boundary** (the raw-vs-rounded question is SA's + CA's): whichever way it is ruled, **a percentage displayed beside a decision is floored, never rounded up across the bar**, or omitted — `0.895` must never read "90 % · Review". ADR-001 w17 clause 2. **SA ruling — the server decides on the raw stored double, `confidence >= 0.90`, with no rounding before the compare.** Rounding first is the one variant that must not ship: it makes `0.895` accept in the payload and review in the audit row — the record disagreeing with itself. Presentation rounding is a **separate, later step** on a decision already taken, and it is floored (the PO corollary), so the two rulings compose rather than conflict. ADR-024 w17 clause A1. **CA half — adopted: the web renders, it does not decide. And the client's own rounding must go the same way, which is a code change this OQ does not name.** The server compares the raw stored double; the web learns only the outcome; the threshold number is **never** a client rule and the legend is server-fed. Two client consequences. **(a)** `semantics.ts:33` currently does `Math.round(confidencePct)` before building **every** label. Today that is harmless. The moment NW-71 collapses the bands at 90, a `review_required` field at `0.895` renders **"Review · 90 %"** — the screen printing the bar's own number beside the word that denies it. So PO's floor-never-round corollary is not only a copy rule: it is a **one-line change in `semantics.ts`**, and it belongs to NW-71, which constraint 2 gives that file. ux-ui-designer reached the same defect independently from the token side; two seats, two routes, one line. **(b)** The `stash@{2}` web half is presentation-only under this ruling: take its band collapse, **drop its rounded `>=` comparison** — a client-side comparison against the bar is exactly the recomputation this ruling removes, and keeping it would put a second, rounded decision beside the server's unrounded one. ADR-012 w17 clause 34. |
| OQ-w17-003 | NW-71 | software-architect, product-owner | **PO half** — the review bar and the `needs_review` badge are **one** definition, consumed by both, so the badge cannot desync from the status. `DocumentAdmissionOptions.AdmissionThreshold` is a **different decision** (is the file admitted at all — ADR-027 / ADR-024) and **stays**; `Raffa.Quotes`' own bar is a separate domain (spec §11) and **stays**. *SA owns which sites retire.* **SA half — exactly two sites retire, five stay, and the scope is closed.** **Retire** into one `ExtractionConfidencePolicy`: the duplicated pairs at `StagedExtractionService.cs:78,88` and `DocumentQueryService.cs:33,42`. **Stay, each for its own reason**: `DocumentAdmissionOptions.AdmissionThreshold` (`:33`) — "is the file admitted at all" is a different decision (ADR-024 w15 / ADR-027); `Raffa.Quotes`' bar — separate domain (spec §11); `SavingsProvenanceClassifier.cs:37,46` (0.7 / 0.4) — provenance banding, not a review decision; `CriticalityScoreCalculator.cs:35` (0.8) — criticality input, **but see OQ-w17-sa-01**, because it is the one that collides with NW-62 in the same payload. ⚠ **The sprawl is wider than this OQ mapped** — the last two sites are not in its list. ADR-024 w17 clause A3. |
| OQ-w17-004 | NW-62 | software-architect, product-owner | **PO half** — geography is the **workspace country** (the w14 profile field), used as the contract's market and **labelled representative**. A per-contract geography field is a new capability plus a migration and is **not this wave**; a paid lookup is §1.2 (NW-52 DEFERRED). ADR-001 w17 clause 4. *SA owns the join.* **SA half — the join is resolved in the host, not in the module.** `Raffa.Insights` is fenced to `[SharedKernel, Benchmark]` (`InsightsEndpointExtensions.cs:34-35`), so it **cannot read the workspace itself**; the host resolves the workspace country and passes it into `StrategyInputs` alongside the supplier name, which `ISupplierNameLookup` already solves (registered `ServiceCollectionExtensions.cs:41`). With both halves of the key resolved, `ToPricedLines`' `Benchmark: null` (`:47-48`) becomes a real call. **One resolution per screen** — NW-20's 360 `benchmark` and NW-62's answers use the same key and the same call. ADR-024 w17 clause 7. |
| OQ-w17-005 | NW-62 | software-architect, client-architect | **SA half — the 360 consumes `GET /api/contracts/{id}/strategy`; it does not compute a second answer.** The endpoint **already** returns both `WhenYouMustMove` and `WhereYouCanPush` (`StrategyPack.cs:26-32,51-56`), is **already in the generated client** (`schema.ts:724`), and **has no consumer** — so this is a wiring decision, not a build. A second answer computed in the view model is exactly the client/server divergence ADR-012 forbids, and it would put two numbers for one question on one screen. ADR-024 w17 clause 9. **CA half — adopted, and it costs no new loading architecture at all.** `contract360/index.tsx:115-119` **already** runs parallel GETs after the 360 and `:48-50` **already** documents the rule this wiring needs — an optional source that fails *"degrades its own answer to an honest 'not yet' rather than failing the screen"*. Adding `getContractStrategy` to that `Promise.all` is a **wiring change, not an architecture change**, the method is already generated (`schema.ts:724`), and **NW-62 is therefore not a `client.ts` writer** — which matters for constraint 4, since it leaves that file to NW-63 and (optionally) NW-72. **The rule this seat adds to SA's**: `SAVINGS_NOT_YET_AVAILABLE` / `LEVER_NOT_YET_AVAILABLE` (`contract360ViewModel.ts:150-152`) survive **only** as the absent-or-failed state of a source that was genuinely called — **never as the steady state of a source never called**, which is what they are today. That distinction is the item's acceptance criterion: a screen that still shows them by default has not consumed anything, and no test that only asserts "the constant is gone" would catch it. ADR-012 w17 clause 35. |
| OQ-w17-006 | NW-72 | software-architect, product-owner | **PO half** — "verified" is the **`RealizedSavings`** rows (the audit-tracked record), **grouped by currency**; `RealizedAmount` stays **PATCH-only**; spec `:447`'s "**Verified** negotiated/implemented savings" is the wording on screen. ADR-001 w17 clause 1. *SA owns the reader and the stale-comment sweep.* **SA half — the reader and the shape.** `SavingsKpiQueryService.cs:32-36` projects `RealizedSavings` alongside `SavingsOpportunities` and hands the calculator a **second input sequence**, so the calculator stays pure (Appendix C rule 6). The shape changes because a verified figure is **one number**, not a band: `RealizedSavingsByCurrency(Currency, Amount, Count)` replaces `SavingsRangeByCurrency`, which **breaks the wire on `SavingsKpiSummary.Realized`** — declared, not smuggled. **Grouped by currency, never summed across them** (no conversion service exists). Sweep: `SavingsKpiCalculator.cs:46-54` and `SavingsOpportunityStatus.cs:37-44` both still assert the gap this closes, and a comment saying "no reader exists" after this wave is a false statement in the tree. ADR-028 w17 footer. |
| OQ-w17-007 | NW-23 *(queued to W18 — travels with the item)* | product-owner, software-architect | **PO half, recorded so W18 does not re-litigate it** — Portfolio filters by **supplier category** (spec §8.1 `:358` lists Category among the required filters); `screens-v2.md:119`'s "Type" column stays the **document** type. Two dimensions, **never merged**. ADR-001 w17 clause 7. *Travels to W18 with the item; SA half owed there, not here.* |
| OQ-w17-008 | NW-73 | security-architect, cloud-architect, delivery-manager | **CLOUD third — yes: Send only, topic-scoped, and granting it *narrows* the surface rather than widening it.** This seat's constraint was only ever that the principal be **resolvable without a new secret**, and Terraform resolves exactly two today: `module.identity.workload_principal_id` (which already holds Send **and Receive**) and `data.azuread_service_principal.ci_deploy`. ADR-015 keeps SPs out of band, so a purpose-made publisher SP would cost an out-of-band creation, a federated credential, a GitHub Environment variable and a **third** literal client id in source — and both roots warn that a stale literal **fails the plan outright** (`dev/main.tf:218-222`); w16 already ruled "no new identity, credential, app role or secret". **There is no third option**, so the real choice is "grant the CI SP Send" vs "run as the workload identity", and §16 of the ADR-005 w17 footer rules the first because the second is **wider**. Two independent warrants are on the record and neither is this seat's: security's (the message is a **pointer** — the Worker re-reads authority under RLS and `MessageId` collapses duplicates — so Send crosses no confidentiality boundary while Receive would) and delivery's (Contributor on the RG already makes `az servicebus namespace authorization-rule keys list` reachable, so the **SAS shortcut is mechanically available today** and an explicit topic-scoped role makes the forbidden path the anomalous one). **Blast radius, stated plainly**: whoever can trigger the workflow inherits publish rights on `extraction-events`, bounded by topic scope, Send only, ids-only messages (ADR-009) and the `document.reprocessed` audit row — **and not bounded by tenant**, because a Send right is tenant-blind; tenant scoping is the console's argument and the audit row, never the role. **SEC third — yes, and the warrant is an asymmetry, not a verb count.** ADR-022 §4 `:250-254` parked this question on this seat by name; it is **discharged in ADR-022 w17 clause 1**. The message is a **pointer, not content** — `ExtractionRequested(tenantId, documentId, jobId, schemaVersion)` (`ServiceBusExtractionQueuePublisher.cs:24`) — and the Worker **re-reads all authority from the database under RLS** when it claims the job, with `MessageId` collapsing duplicates (`:50`). So a forged or replayed message **cannot** make the Worker read a document the sender could not already reach, and cannot make it act twice. **Send crosses no confidentiality boundary. Receive would** — a receiver drains the Worker's deliveries (denial of processing) *and* reads tenant and document ids out of the envelope. That is the whole ruling, and it is **not** a general licence: it holds because this topic's messages carry ids only (ADR-009). **This seat also records being wrong in its own lane.** The lane preferred shape (C) because it "buys zero new rights"; cloud-architect's evidence — verified first-hand here, `modules/servicebus/main.tf:71-87` **and** `:89-102`, Sender **and** Receiver both to `var.workload_principal_id` — shows (C) would hand the console the one capability this seat refuses. **This seat's own rule condemns this seat's own preference**; the mistake was measuring *rights added* instead of *what the principal can do*. Adopted: **(A), Send only, topic-scoped.** Blast radius as cloud-architect states it, with one addition: it is **not bounded by tenant** — a Send right is tenant-blind — so tenant scoping is the console's argument and the audit row, **never the role** (ADR-009 w17 clause 1, rule 3). **DM third — yes, and the CI plane adds one thing neither other third could see: the grant and its *reachability* are now two different questions, and only the first is settled this wave.** The right is an Azure **data-plane role on an existing principal** — no federated credential, no GitHub secret, no subject claim, no Graph right — so **ADR-015 is untouched** (ADR-016 w17 clause 36, ADR-014 w17 round-3 preamble). It is granted by Terraform in **both roots** (clause 37) and consumed by **exactly one `workflow_dispatch`-only workflow** that never joins promotion (clause 38). The SAS argument cloud-architect carries is this seat's and stands: `backend.yml:251` records `raffa-sp-<env>` as **Contributor on the resource group**, so `az servicebus namespace authorization-rule keys list` is **mechanically available today** — an explicit, topic-scoped, auditable Send is *narrower* than the capability the principal already carries ambiently, and it makes the forbidden path the anomalous one. ⚠ **Round 3 splits the answer in two.** Clause 6b narrows `target_environment` to **`dev`**, so from PR 1's merge (round-3 clause 43) `demo` **holds a Send grant nothing can target** — kept because the variable is required and `raffa-demo`'s plan must stay green, **not** because `demo` has a consumer, and stated as such in the acceptance doc so no reader infers a working `demo` console from the grant existing. ⚠ **And the security property of OQ-w17-sa-02 now rests on something different from what that row says.** It states the property is "already satisfied by the existing operator-workflow idiom… and **`environment:`** so demo's required reviewers gate it" — but under 6b the `environment:` expression can only resolve to **`dev`** (`verify-tenant-corpus.yml:52` is `${{ inputs.target_environment }}`, and `:15-16` names *demo's* reviewers as its whole purpose). So on this wave the property — *no wider than the set who could do the same as tenant Admins through the API* — holds because **`dev`'s tenants are the team's own test tenants**, not because a reviewer gate holds; the honest boundary is **repository write plus an explicit dispatch** (clause 38, unchanged). **It is the widening that breaks the property**, which is why clause 6a's behavioural approval test is not a nicety deferred for tidiness: it is the mechanism that **restores** the property when `demo` — a running client pilot's tenants — enters the options list (ADR-016 w17 round-3 clause 45(b), (c)). |
| **OQ-w17-po-01** *(new, raised at this table)* | NW-71 | product-owner, delivery-manager *(seeds)*, software-architect | **Raised and ruled by PO: the fixture changes, never the threshold.** At a single bar of 90 % a seeded field at exactly `0.9` flips *Flagged* → **Accepted**, and `percorso-pilota-v1.md:122` requires **two fields below the bar** on the live-uploaded MSA or the pilot's HITL act has nothing to review. **This breaks the demo while every test stays green.** *Assumption in force*: the fixture keeps **at least two** fields below 0.90. **No task may soften the 90 % rule to keep a seed interesting.** *DM owns the seed; SA owns the fixture values.* **SA half — confirmed on the code, and the fix is bounded.** The flip is real: at `>= 0.90` a seeded `GoodConfidence = 0.9` is **accepted**, and the pilot's HITL act (`percorso-pilota-v1.md:122`) then has nothing to review while every suite stays green. *Ruling*: the fixture keeps **at least two** of the pilot's critical fields **strictly below 0.90**, and the chosen value is **not adjacent to the bar** (0.85, not 0.899 — a value that renders "89 %" reads as a rounding artefact and re-opens OQ-w17-002 on screen). **No task may soften the 90 % rule to keep a seed interesting**; the fixture prose at `FixtureContractFactExtractor.cs:57-58` moves with the values. |
| **OQ-w17-sa-01** *(new, raised at this table)* | NW-71 × NW-62 | software-architect, product-owner, ux-ui-designer | **Raised and ruled by SA — a collision no lane owned, because it lives between two items.** `StrategyPack.OpenWeakFacts` is filtered at **0.8** (`CriticalityScoreCalculator.cs:35`, `StrategyPack.cs:23-25`) and feeds the exact payload NW-62 is told to consume, while Review moves to **0.90**. Render it as a review statement and **two screens carry two definitions of "weak" for one contract**. *Ruling*: `OpenWeakFacts` is a **criticality input, not a review decision**; NW-62 renders the strategy sections but **not** `OpenWeakFacts` as a review statement this wave; the bars reconcile in W18. ADR-024 w17 clause A4. **UX half — adopted, and the screen-level rule is sharper than "do not render it".** After NW-66, Contract 360 renders **no confidence at all** (ADR-001 w17 clause 6, ADR-019 w17 clause 11) — so a strategy section derived from a 0.8 filter would be the **only** place on that screen where a hidden threshold decides what the user sees: invisible, unlabelled, and silently contradicting the 90 % bar Review states out loud. That is worse than two visible bars, because the user cannot even see that a second rule exists. Adopted with one addition: the strategy sections render, `OpenWeakFacts` does not appear as a review statement, and **no copy on Contract 360 uses the word "weak" this wave**, so nothing on the screen implies a second bar that the wave has not reconciled. ADR-020 w17 §14(g). |
| **OQ-w17-sa-02** *(new, raised at this table)* | NW-73 | security-architect *(rules)*, software-architect | **Raised by SA; security rules.** `DocumentReprocessService` is **role-blind by design** — `:30-31` says it "does not know about roles: the endpoint enforces Admin before calling" — so a console **bypasses** `DocumentsEndpointExtensions.cs:506-509` entirely. *Assumption in force*: an operator tool authorized by its principal's Azure RBAC plus the Postgres credential, taking tenant id as an **explicit input**, writing actor `system:bulk-reprocess`. RLS still binds the reprocess itself (`ReprocessAsync` opens its own tenant scope, `:67`). **Where this assumption and security-architect's S17-3 conflict, S17-3 wins**: the *worklist* query lives in the console, outside that scope, and must use the three-argument `Configure` or it silently returns zero rows. **SEC RULES — permitted: the Admin gate is *relocated*, not bypassed, and the relocation is written down.** Left as SA states it, `:30-31` describes an Admin-only capability with **no Admin check**. That is acceptable only because the authorization is replaced in full by a named chain (ADR-022 w17 clause 3): **trigger** (who may dispatch the workflow), **Azure** (the deploy principal's federated credential + the topic-scoped Send), **database** (the `postgres-connection` secret via the principal's existing `Key Vault Secrets User`), **tenant** (RLS, bound explicitly — the gate that does not depend on the other three being right). **The security property, stated so it can be checked rather than felt: the set of humans who can trigger a bulk reprocess must be no wider than the set who could do the same thing as tenant Admins through the API.** The mechanism is delivery-manager's; the property is this seat's — and it is **already satisfied by the existing operator-workflow idiom**, which NW-73 inherits rather than reinvents (`workflow_dispatch` only, required `tenant_id`, `permissions: contents: read`, and **`environment:`** so demo's required reviewers gate it — `verify-tenant-corpus.yml:15-16`, `:17-35`, `:52`). ⚠ **New finding that changes the risk class**: the predecessor's own comment reads *"This job reports; it does not mutate"* (`:8`), so **NW-73 is the product's first mutating operator workflow** — its controls must be **at least** those of the read-only one it copies, never fewer on the grounds that it is "the same kind of job"; add a concurrency group keyed by environment+tenant (`:40-42`'s idiom). **Ceiling**: the console must never acquire a capability the Admin endpoint lacks — no cross-tenant mode, no direct SQL mutation, no path that skips the audit row. It **replaces** an Admin's reach; it does not exceed it. |
| **OQ-w17-sa-03** *(new, raised at this table)* | NW-20 | security-architect, product-owner, software-architect | **Raised by SA.** `activity` has no domain concept and this seat will not invent one. The only honest existing source is the **audit trail**, projected read-only through the host with an **action whitelist** and never an extracted value — but a 360 tab is a **wider audience than ADR-011's w16 reader ruling contemplated**, so it is a security question before it is an architecture one. *Assumption in force*: permitted under a whitelist; **if security refuses, the member is removed and the record type deleted** (ADR-001 w17 clause 3), never an unconditional `[]`. **SEC RULES — permitted as a contract-scoped provenance projection; refused as an audit reader.** SA framed this correctly as a security question first, and the concern is **verified rather than inferred**: `GET /api/audit` is **live-`Admin`-only** (ADR-011 w16 clause 14's ladder), while `GET /api/contracts/{id}` is gated by `ICallerContext.ResolveTenantAsync` — identity, tenant header as an authorized selector, then **membership** — with **no Admin check anywhere** (`ContractsEndpointExtensions.cs:77`, `:101-105`). So a 360 tab is readable by **any live member of any role**, and a naive projection moves an Admin-only read onto an any-member surface. **This is the reversal ADR-011 clause 14c said must not land as a routine addition, and clause 22 of the w17 footer is the re-review it reserved.** Five conditions, and they are what make it a *different read* rather than the same read with a filter: (1) **contract-scoped, never tenant-wide** — no `?tenantId=`, no "all activity" mode, no pagination over the trail; a tenant-wide feed **is** the audit reader and stays Admin-only; (2) a **closed allow-list of action constants, default-deny** — an audit action added later is **invisible until explicitly added**; a blocklist is refused because with one, every future action leaks by default, in a task that never mentions security; (3) **names, never values** — the same rule as clause 21a, so the two items cannot drift apart; (4) **no actor identifier beyond what the member list already shows** (clause 14c's own test): `system:<component>` verbatim, a human event as the display name the member list already discloses, **never a raw subject GUID**, and an unresolvable actor as a removed member; (5) `/api/audit` keeps its ladder **unchanged** and this projection does not become a second route into it. **The pinning test — a non-Admin member sees the contract's activity and still receives 403 from `GET /api/audit`** — states as a fact what this row states as a rule; plus another tenant's events never appear for the same contract id, and a non-whitelisted action never appears. **Not granted**: an actual audit *screen* stays Admin-only and still owes its own ADR-020 row and a fresh re-review. **If it does not fit the wave, delete the member** — this seat prefers a deleted member to a rushed one, and refuses the unconditional `[]` for the same reason PO does: an empty array is a claim that nothing happened. **SA closes it (round 2) — adopted whole, and the payload is named so a task cannot guess it**: `Contract360ActivityEntry(OccurredAt, Action, ActorLabel)` and `Contract360BenchmarkEntry(Metric, Status, Position?, AdapterName?, SampleSize?, AsOf?)`, the abstain as an entry rather than `[]` (ADR-024 w17 clause 11). ⚠ **One of the five conditions needed a mechanism, not just a rule.** Condition 3 (*names, never values*) is defeated by the **obvious** implementation: the trail's only human-readable column is `AuditEvent.Detail`, which is `text` with **deliberately no `HasMaxLength`** (*"unbounded free-form context"*, `AuditEventConfiguration.cs:24`) and **already** carries contract-derived dates (`RenewalAlertService.cs:254-255`, `RenewalThresholdScheduler.cs:148-149`). So the clause forbids projecting `Detail` **at all** — including a per-action exception for `document.validated`, whose detail happens to hold only field names (`DocumentValidationService.cs:124-135`), because whitelisting a free-text column breaks the first time a writer adds context, in a task that never mentions security. A structured "what changed" is W18's, with its own column or typed contract. |
| **OQ-w17-sa-04** *(new, raised at this table)* | NW-26, NW-63 | software-architect, cloud-architect | **Raised by SA, answering cloud-architect's OQ-w17-ca-03 as far as this seat can honestly go.** This council **does not name the renderer package**: naming one without verifying its licence and its Linux native-asset set would be a claim it cannot back. *Binding rule instead*: the NW-26 task names the package, its **licence** and its **native-dependency list** in its Definition of Done; if that list is **non-empty** the Dockerfile layer lands in the **same task**, above `USER $APP_UID` (`Raffa.Worker/Dockerfile:34`) and above `COPY --from=build` (`:31`); if it is **empty**, no Dockerfile change is owed and NW-26's image cost is zero. Second half: where `document.page_count` is null, the viewer shows page 1 and **says the count is unknown** rather than guessing. ADR-029. **CLOUD half — the rule is accepted verbatim, and this seat supplies the three constraints that make it checkable.** Not naming the package is right; what the DoD must produce is a **licence** and a **Linux native-dependency list**, and if that list is non-empty the layer lands in the same task **above `USER $APP_UID` (`Raffa.Worker/Dockerfile:34`)** — or the build fails permission-denied **inside ACR Tasks**, where no one is watching a local terminal — **and above `COPY --from=build` (`:31`)**, so it is stored once in a shared layer instead of per `:<sha>` tag. **ACR stays Basic either way.** If the list is **empty the image is byte-identical and NW-26's image cost is zero**, which is the outcome this seat expects and cannot assert. ADR-005 w17 §20. |
| **OQ-w17-ca-01** *(new, raised at this table)* | NW-73 | cloud-architect *(rules)*, delivery-manager, security-architect | **Raised and RULED by CLOUD: (A), the GitHub Actions runner.** Three shapes were priced — (A) a runner as `raffa-sp-<env>`, (B) an operator laptop, (C) a Container Apps Job under the workload identity. Security-architect's lane prefers **(C)** because it needs **no new role assignment at all** and so "the wave buys zero new rights" — **true, and not the whole measure**. `modules/servicebus` grants the workload identity **both Sender (`:71-87`) and Receiver (`:89-102`)**, so under (C) the console could **receive from `document-processing`**, the single subscription the Worker depends on, where a stray receive takes a message the Worker needed. **The question is not how many rights the wave adds but what the console can do**: (C) adds zero and grants the **wider** capability, (A) adds one and grants the **narrower**. Delivery-manager's independent reason (D3) is adopted alongside: (C) also costs a Terraform resource, a third image in ACR Basic, a third deploy path and a **permanently triggerable mutation surface in Azure**. **(B) refused** — the human principal holds no Send right, and granting one means an out-of-band assignment invisible to Terraform or a person's object id in source. **This closes the conditional in this seat's lane**: the ADR action is a real amendment, not `none`. If a later council takes (C), ADR-005 w17 §16 is the clause to amend and §15 drops entirely. |
| **OQ-w17-ca-02** *(new, raised at this table)* | NW-26 | cloud-architect, software-architect | **Raised by CLOUD; ruled with a pre-authorised contingency so it cannot stall a wave.** Does the renderer need the Worker container bumped? **Ruling: no — ship at 0.25 vCPU / 0.5 GiB**, page-by-page render-and-dispose. The bump is **pre-authorised with a named ceiling** — split `modules/containerapps` into `worker_cpu`/`worker_memory` and raise **the Worker only to 0.5 / 1.0 GiB**, recorded then as an ADR-005 amendment plus an operator HCP apply, **no new council round**. It is **proved or refuted by measurement, not by argument**: one 20-file batch on `dev` with zero Worker restarts. Cost is **$0.00 either way** — `min_replicas = 0` on both apps means an idle environment bills nothing and a bump changes only the rate while a replica is alive. Retail lookups (Container Apps Consumption vCPU-second / GiB-second, North Europe) are owed **only if the bump is taken**. |
| **OQ-w17-ca-03** *(new, raised at this table — now CLOSED)* | NW-26 | cloud-architect, software-architect | **Closed by OQ-w17-sa-04.** The question was whether the chosen renderer needs native libraries, because the Worker base image (`mcr.microsoft.com/dotnet/runtime:10.0`) carries none. Software-architect declined to name a package without verifying its licence and native-asset set — **the right refusal** — and bound the task instead. Recorded as closed rather than deleted, so a later reader sees that the image question was **asked and answered by a task rule**, not overlooked. |
| **OQ-w17-ca-04** *(new, raised at this table — recorded, no task)* | none *(pre-existing)* | delivery-manager, cloud-architect | **Raised by CLOUD and deliberately NOT pulled into this wave.** **Nothing prunes ACR.** `modules/acr` declares no `retention_policy` (untagged-manifest retention is a **Premium** feature; the registry is **Basic**, `main.tf:24`) and **no workflow purges** — zero `acr purge` / `acr repository delete` across `.github/workflows` — while `backend.yml:134,142` pushes `raffa-api:<sha>` **and** `raffa-worker:<sha>` on every commit against Basic's included **10 GB**. Growth is slow because tags share base layers, but it is **unbounded and nothing reclaims it**. *Ruling*: **pre-existing, not NW-26's to absorb** — recorded here so it is not rediscovered as a surprise, **no task minted**, revisited when the registry approaches its included storage. |
| **OQ-w17-sec-01** *(new, raised at this table)* | NW-73 | security-architect *(rules)*, software-architect | **Raised and RULED by SEC: human attribution is required, and it must not live in `Actor`.** `system:bulk-reprocess` satisfies ADR-011 clause 16 and **loses who asked** — for a privileged bulk mutation that is a real gap. *Ruling*: the console writes **one run-scoped row before the loop**, actor `system:bulk-reprocess`, detail `requestedBy=<triggering actor>; run=<run-id>; tenant=<id>; count=<n>`. The distinction is the whole rule: **`Detail` is free text and asserts no identity; `Actor` asserts one**, and the append-only trigger makes a wrong `Actor` permanent and uncorrectable. **No CI-controlled string is ever interpolated into `Actor`** — not `github.actor`, not a run id, not a workflow input. The action constant follows the existing `document.*` vocabulary and its placement is the task's; the *requirement* is this seat's and is not optional. If a later council refuses the extra row, the fallback is the CI run record alone — recorded as the **weaker** option, because it lives outside the tenant's own trail. ADR-011 w17 clause 20b. |
| **OQ-w17-sec-02** *(new, raised at this table — now CLOSED)* | NW-73 | security-architect, cloud-architect, delivery-manager | **Closed by OQ-w17-ca-01, against this seat's own stated preference.** The lane asked whether a Container Apps Job under the workload identity would be strictly better on the identity plane, since that identity **already holds** the topic-scoped Send grant and the wave would then buy **zero new rights**. Cloud-architect ruled (A) and produced the disqualifying fact, **verified first-hand here**: the same identity also holds **`Azure Service Bus Data Receiver`** (`modules/servicebus/main.tf:89-102`). Recorded as closed rather than deleted, so a later reader sees that the cheaper-looking shape was **asked, priced and refused on this seat's own asymmetry rule** — and does not re-propose it as an optimisation. |
| **OQ-w17-sec-03** *(new, raised at this table — now CLOSED)* | NW-71 | security-architect, software-architect | **Closed by ADR-003 w17 clause 1 — a column, not a table.** The lane's assumption was a column on `extraction_evidence` (zero new isolation surface) with four binding requirements if a table were chosen instead. SA ruled the column. The four requirements are therefore **not live this wave**, and are recorded as a **standing rule** in ADR-009 w17 clause 2a — together with the silent-escape path they defend against, which is new: the RLS guard discovers tables **from the EF model**, and its own guard assert catches an **empty** list but **never a missing member**. |
| **OQ-w17-sec-04** *(new, raised at this table — ruled, no task)* | NW-73 | security-architect, software-architect | **Raised by SEC on a reliability edge with an honesty consequence.** The console is another process against `max_connections = 50` with `MaxPoolSize = 8` per process (`DocumentsContractsDbContextOptions.cs:19-40`), so a mid-run connection failure leaves a tenant **half-reprocessed**. *Ruling*: this needs **no new control**, because re-runs are safe by construction — `ReprocessAsync` requeues idempotently per document and `MessageId` collapses duplicate publishes — but it does need **honest reporting**: the console exits **non-zero** if it did not process every document in its worklist, and prints processed/total. Same family as ADR-009 w17 clause 1 rule 4: **a partial run must never read as a completed one.** No task minted beyond that exit rule, which travels inside NW-73's own task. |
| **OQ-w17-cl-01** *(new, raised at this table — ruled)* | NW-63 × NW-72 | client-architect *(rules)*, software-architect | **Raised and RULED by CA. `web/src/api/client.ts` has two contending items, and the record's constraint 4 hedges it as "if needed".** It is needed, and the two edits are of different kinds: NW-63 adds a **page parameter** to `getDocumentPreviewUrl` (today `(tenantId, id)`, `client.ts:2082`) plus its result type, while NW-72 wants at most a **one-line derived alias** beside `:986-987` for the new realized shape. `check_single_writer.py` rejects on the **file**, not on the size of the diff. *Ruling*: ADR-012 w16 clause 25 applies unchanged — **different phases, or one task**. **The free escape, with its cost named**: NW-72's alias is *optional*, because `savingsViewModel.ts` can index the generated body inline (`SavingsKpiSummaryBody["realized"][number]`) exactly as `:986-987` do; **drop the alias and `client.ts` has exactly one writer**. That trades a naming convention for a phase, and the **decomposer** decides which it needs — this seat records both prices rather than forcing the contortion. ADR-012 w17 clause 39. |
| **OQ-w17-cl-02** *(new, raised at this table — ruled)* | NW-65 × NW-71 | client-architect *(rules)*, ux-ui-designer | **Raised and RULED by CA — a defect that changes behaviour with no diff, which is why no lane found it.** `computeNeedsAttention` (`contract360ViewModel.ts:568-583`) decides which facts appear in "Facts you still need to decide" with `if (tag.variant !== "neutral")` (`:575`) — it infers a **decision** from the tag's **presentation variant**, which is precisely what `semantics.ts:45-46` instructs screens not to do (*"Screens gate CTAs on this function, not on inspecting the tag's colour/variant"*). Today the expression means "at or below 95 %". When NW-71 collapses three bands to two, **the expression's meaning changes and the line is never edited**: the list silently changes size, and no diff in the pull request explains why. *Ruling*: NW-65 rewrites this function anyway, so the gate **must be replaced with the server's persisted decision in the same task** — never with a re-banded colour test, and never left as-is on the grounds that it still compiles. A vitest case asserts membership from a decision, not from a variant. This is the same class as OQ-w17-002's rounding: **presentation must not be load-bearing.** ADR-012 w17 clauses 34 and 36. **UX half — agreed, and this finding is the reason ADR-019 w17 clause 7 refuses a third variant.** `:575`'s `tag.variant !== "neutral"` is the exact failure `semantics.ts:45-46` and ADR-019 `:124-125` legislate against, and it is why this seat separates `auto_accepted` from `human_accepted` **by label and not by a new variant**: minting one would hand the next `computeNeedsAttention` a fresh colour to read, and the defect would return in a file nobody was looking at. **The token-side consequence the code fix must respect**: after NW-71 the variants **no longer partition confidence at all** — `.tag-neutral` means *accepted* on Review and *leverage* on Contract 360 (ADR-019 w17 clause 11) — so any surviving variant test is not merely stale, it is reading a value whose meaning now **depends on which screen it is rendered in**. Replacing the gate with the server's persisted decision is therefore the only fix that is still correct after the vocabulary change, not just the tidier one. ADR-019 w17 clauses 7, 11. |
| **OQ-w17-cl-03** *(new, raised at this table — a check owed, not a claim)* | NW-63, NW-66 | client-architect, ux-ui-designer | **Raised by CA against its own route decision.** A citation deep-link (`/documents/:documentId/viewer?page=3&clause=…`) is the **first** route in this product whose **query string carries meaning a user can arrive on cold** — every earlier deep link either had none or was reached from inside an authenticated session. This seat has **not** verified that the post-login return URL preserves a query string, and will not assert it either way: ADR-012's w14 footer (`:189-285`) owns "the screen the redirect actually lands on". *Owed*: NW-63's task **runs the check** — deep-link the viewer while signed out, complete the OIDC round trip, and land on **page 3 with the clause still selected**. If the return URL drops the query, the fix is in the auth landing, not in the viewer, and it is ADR-012's w14 clause that is amended. Recorded as an open question rather than a rule because a rule stated without the check would be exactly the unverified claim this council forbids. **UX half — this seat cannot rule on the return URL either, but it can rule what the user sees if the query is dropped.** If the deep link lands on the viewer **without** `page`/`clause`, the screen must **not** silently render page 1 as though that were the citation: it shows page 1 in the ordinary page state **and says the citation could not be restored**, with the document still open and navigable. Silently landing on page 1 is the same class of defect as the `*` catch-all in ADR-018 w17 clause 12 — **a wrong destination with no error** — and it is the more dangerous of the two, because the user does not conclude "the link is broken", they conclude "**the clause is not in this document**" and stop looking. That copy is owed whether or not the check passes, since a query string can also be lost to a truncated paste. ADR-020 w17 §16. |
| **OQ-w17-ux-01** *(new — raised in this seat's lane, CLOSED before this seat spoke)* | NW-66 | ux-ui-designer, product-owner | **Closed by product-owner's NW-66 ruling.** The lane asked which words carry leverage and proposed **four** (Leverage / Weak for you / Protect / Watch). PO ruled **three**, keyed on the existing `ContractRiskLevel`: Critical/High → **Push to change**, Medium → **Worth raising**, Low → **Standard terms**. **The lane's set is withdrawn** — the lane itself recorded that the words are product-owner's and only the treatment is this seat's, so this is the protocol working, not a concession. It is also the better answer: three words map onto the **two variants `getClauseRiskTag` already computes** (`:257`), where four would have forced a third treatment onto a screen that is losing one. Recorded as closed rather than deleted, so ADR-019 w17 clause 9 does not read as a free-standing vocabulary decision by this seat. |
| **OQ-w17-ux-02** *(new — raised in this seat's lane, CLOSED before this seat spoke)* | NW-71 | ux-ui-designer, software-architect, client-architect | **Closed by software-architect's wire ruling.** The lane warned that a **boolean** decision on the wire would collapse `auto_accepted` and `human_accepted` into one label and leave the durability defect invisible on screen. SA ruled **three** values, and client-architect withdrew its own two-value proposal for the same reason reached from the client side. The assumption in force became the decision: **ADR-019 w17 clause 7's three-row table is implementable exactly as written**, and ADR-001 w17 clause 8's "three distinguishable states" has a wire that can carry it. Recorded as closed so no later reader mistakes the three labels for a design preference the contract does not support — which is precisely how a screen ends up painting two states alike. |
| **OQ-w17-ux-03** *(new — raised in this seat's lane, CLOSED before this seat spoke)* | NW-71 | ux-ui-designer, client-architect, product-owner | **Closed by the PO corollary and client-architect's OQ-w17-002 half.** The lane asked that the web never recompute the decision and only floor the label. Both landed: PO ruled that a percentage beside a decision is **floored, never rounded up across the bar**, and CA ruled that the client renders rather than decides — **and found the same `Math.round` at `semantics.ts:33` from the call site that this seat found from the semantic-mapping table.** So it is a **one-line code change in NW-71's file**, not a copy note (ADR-019 w17 clause 8). Recorded as closed, and worth noticing as a property of the lane mechanism: the same defect surfaced twice from two independent directions in one round, which is what makes it safe to bind in an ADR rather than to leave as a task's judgement. |
| **OQ-w17-ux-04** *(new, raised at this table — **RULED** by product-owner, ADR-001 w17 clause 10)* | NW-72 | product-owner *(rules)*, ux-ui-designer | **Raised by UX against product-owner's clause 1, which it otherwise adopts in full.** PO rules that the realized-money cell "is labelled **Verified**" (spec `:447`). This seat preserves the required word and proposes the label **"Savings verified"**: the KPI band's other three labels are noun phrases — "Contracts analyzed", "Upcoming renewals", "Savings identified" (`savingsViewModel.ts:85-87`) — and in a row of **equal** cells a bare "Verified" would be the only one naming a **status** rather than a **thing counted**, so it reads as a filter or a state rather than as money. *Assumption in force*: **"Savings verified"**, carrying spec `:447`'s word unchanged. **Nothing blocks on this**: the slot, the cell shape, the zero state and the provenance meta are decided either way (ADR-020 w17 §15), and if product-owner holds to the bare word only the label string changes. Raised rather than absorbed because the **wording is product-owner's to rule** and this seat is refining it, not overruling it. **RULING (product-owner, ADR-001 w17 clause 10): "Savings verified" — adopted whole.** The refinement is right and its structural reason is adopted verbatim. It also surfaced an error in **this seat's own clause 1**, now corrected on the record: `product-spec.md:447` is a **two-column** row whose **label** column reads **"Savings Realized"** and whose **meaning** column reads "**Verified** negotiated/implemented savings" — clause 1 cited the meaning as if it were the label, so what it actually carries is the **evidence standard**, not the bare string. One product reason is added that UX did not have to make: **"Realized" is the word attached to the defect** — the bucket summing estimates today is called `Realized` (`SavingsKpiCalculator.cs:86`, `:103-104`), and labelling the repaired cell with the defective bucket's own word leaves the screen reading as it did before the fix, while "verified" names the standard the number now meets. **Fence**: the screen word and the domain word may differ and nothing is renamed to align them — `RealizedSavings`, `realizedAmount` and the domain bucket keep their names, and SA's **declared** break on `SavingsKpiSummary.Realized` stays the only rename this wave. **Divergence from the spec's label column is ratified as one**, joining the design divergence already recorded for this cell (ADR-020 w17 §17). **A17-S1 gains one check**: the cell is labelled "Savings verified" and the estimate's word never labels it. Nothing else in clause 1 moves. **SA note (round 2) — one word of the fence, not the ruling, and the wire is this seat's.** The fence is adopted whole. The precision: the declared break on `SavingsKpiSummary.Realized` is a **type** change, **not a rename** — the member keeps the identifier `Realized`, the wire keeps the property key `realized`, and only the type behind that key changes (`SavingsRangeByCurrency` → `RealizedSavingsByCurrency`). Read as a rename it invites the **opposite** of what the fence intends: an implementer renaming the wire key to `verified` to match the new screen label would ship a **second, undeclared** break on top of the declared one, and would break `client.ts:985-987`, which derives `SavingsKpiSummaryBody` from the generated response **by that key**. **Nothing is renamed this wave** — `RealizedSavings`, `RealizedAmount`, `realizedAmount`, `Realized`, `realized` all keep their names — and "Savings verified" stops at the view model: never a DTO member, a wire property, a column or a domain type. ADR-028 w17 clause 6. |
| **OQ-w17-ca-05** *(new, raised at this table in round 3 — open, an operator read)* | NW-73 | cloud-architect *(raises)*, delivery-manager, security-architect | **Raised by CLOUD against its own framing, after delivery-manager's round-2 finding (i) proved it wrong.** ADR-005 w17 §25 `:1189-1190` and ADR-007 `:223-224`, `:282` all say `demo` applies "at its promotion". **False, and the verification corrects delivery-manager's version too.** `infra.yml`'s `apply` job is named *"terraform apply skipped"* and its only step writes to `$GITHUB_STEP_SUMMARY` (`:114-136`) — no Terraform, deliberately (`:104-113`: a CLI apply against a VCS-connected workspace is rejected) — and `demo-promote.yml:123-127` says so itself. The trigger is **not the tag**: `hcp_vcs_wiring.py:104-106` wires **both** workspaces to `EXPECTED_BRANCH = "main"` with `EXPECTED_TRIGGER_PREFIX = "infra/"`, `:101-103` calling it "the one mainline branch **every** workspace must track". **Corrected rule (ADR-007 w17 §9): one merge to `main` under `infra/` queues a VCS run on `raffa-dev` *and* `raffa-demo` at the same instant; `demo` moves on the merge, never on the promotion tag.** This **strengthens** §5/§8/§17 — a dev-only wiring does not break later, it queues a run against a demo root missing the input **in the same merge**, in a workspace nobody watches. ⚠ **Finding no seat carried**: `infra.yml:121` puts `environment:` on the **apply** job, i.e. on the echo, so ADR-016's required-reviewer gate on `demo` **pauses nothing on the Terraform plane**. Recorded so no task is written believing a human approves `demo`'s infra; **no proposal to move the gate this wave**. *The open half, and why it is not asserted*: `raffa-demo`'s **auto-apply** setting is **live HCP state, unreadable from this checkout** — the script asserts VCS wiring and execution-mode, **not** auto-apply (`:33-46`). The branches differ materially: **on** = `demo` infra changes in the same merge as `dev`, under a running client pilot, with no approval in front of it; **off** = the run sits queued and `demo` silently lacks NW-73's role assignment while every workflow is green. ***Assumption in force: off***, on `infra.yml:109`'s own instruction to "Confirm the VCS-triggered run in the HCP UI (or enable workspace auto-apply)". **Owed as an operator read, not a task** — confirm both workspaces' setting before the `demo` promotion and record it. Independently reaches delivery-manager's **two applies before any acceptance walk** (OQ-w17-dm-03) from the workspace wiring rather than from PR #118. **SEC half — the assumption is adopted, and the branch this seat cannot read is the one that *bites*, for a reason neither raising seat carried.** In the **off** branch `demo` is green with **no topic-scoped Send grant**, so the console's first publish there returns **403** — and that failure is **destructive, not inert**. `RemoveChunksAsync` opens its own tenant scope and commits **its own** `SaveChangesAsync` (`EmbeddingRetrievalService.cs:169`, `:181-182`) **before** `PublishAsync` (`DocumentReprocessService.cs:110-113`), while the requeue, the status write (`:115`) and the **audit row** (`:117-126`) all sit *after* it. So each 403 **strips a document's chunks, requeues nothing, and writes no trail** — and the source comment that would reassure the reader, *"a publish failure fails the request with nothing changed"* (`:107-109`), is **true of the DbContext and false of the embeddings** (ADR-011 w17 clause 26). It is also the moment the **SAS shortcut clause 1 refuses by name** stops looking like a violation and starts looking like unblocking a deployment. *Consequence — and it removes this wave's dependence on the unread setting*: NW-73 ships `target_environment` **`dev` only** (ADR-022 w17 clause 6b); the inherited workflow already offers `options: [dev, demo]` (`verify-tenant-corpus.yml:26`), so this is a **deliberate narrowing of a copied list**, which a task will otherwise reproduce verbatim. The operator read stays owed; **the wave no longer waits on it, and A17-S2 runs on `dev` regardless.** |
| **OQ-w17-ux-05** *(new, raised at this table in round 3 — open, a W18 condition with its assumption in force)* | NW-62, NW-73 | ux-ui-designer *(raises)*, client-architect, security-architect | **Raised by UX while correcting the premise of ADR-012 §48** (whose *conclusion* — no client task for NW-62 — this seat adopts). ADR-011 clause 26 destroys **embedding chunks**; `contract360ViewModel.ts` has **zero** occurrences of `corpus` or `chunk` and reads structured facts plus the evidence trail (`client.ts:671-674`). ⚠ **Disjoint** — so the 360 renders **complete, populated and confident** for a document whose corpus is gone, and `SAVINGS_NOT_YET_AVAILABLE` (`:150`) is the **benchmark**'s constant, whose companion sentence `:152` promises the estimate "lights up with the Benchmark Service" *and* asserts the clauses below are **validated**. **The surface that actually degrades is Ask**, and it degrades into a sentence the product has trained the user to misread: the off-state gate counts **validated** contracts (untouched by a chunk delete), so Ask stays **on**, still claims *"Answers only from N validated contracts … cites or abstains"* (`askViewModel.ts:320`), and abstains — which `:344` glosses as *"every answer cites its page or says it cannot answer"*. ⚠ **The honest sentence exists and is unreachable**: `:285` (*"Raffa.ai could not finish processing your documents"*) is wired to the off-state gate, not to a corpus deleted **after** validation; with no audit row (clause 26) the product has **no account of the event anywhere**. *The open question*: the surface cannot distinguish **retrieved nothing** from **has nothing to retrieve**, and no read-model member carries the difference — so a corpus-health signal is owed **before** NW-73 is widened. ***Assumption in force: `dev`-only***, on ADR-022 w17 clause 6b, which is why **w17 ships no signal, no copy and no state** and why the deferral is a clause rather than a budget. **Owed as a W18 condition on widening NW-73 beyond `dev`, not as a w17 task.** ⚠ **One prohibition binds now**: no task may add a reassuring **"re-indexing"/"catching up"** banner to Ask or the 360 — nothing requeues after clause 26, making it a *not ready yet* claim that can never resolve, which **ADR-018 w15 clause 6** (`:474-476`) already forbids. ADR-020 w17 §25. |
| **OQ-w17-dm-04** *(new, raised at this table in round 3 — open, an operator read with its assumption in force)* | NW-73 | delivery-manager *(raises)*, security-architect | **Raised by DM because two of security-architect's own round-3 clauses compose badly, and the composition is on this seat's plane.** Clause 6a requires the NW-73 workflow's approval gate to be proved **behaviourally** — *approval pending ⇒ zero messages on `extraction-events` and zero chunks deleted* — precisely because a test asserting the key merely **appears** in the YAML passes on the `infra.yml` shape. Clause 6b then ships `target_environment` **`dev` only**. ⚠ **There is no "approval pending" state to enter on `dev`** unless the `dev` GitHub Environment carries a required-reviewer protection rule — and **environment protection rules are GitHub repository settings, not files in this tree**. This checkout cannot read them and this seat asserts nothing about them; the inherited workflow's own comment points the other way, naming **demo's** reviewers as the reason the key is there (`verify-tenant-corpus.yml:15-16`), and ADR-016 w17 clause 38 already recorded the `dev` gate honestly as *repository write plus an explicit dispatch*. ***Assumption in force: `dev` carries no required reviewers***, therefore **no DoD line is written for clause 6a's behavioural test this wave** and it travels as a **W18 condition on the widening** (ADR-016 w17 round-3 clause 45(b)–(c)). **Stated as a rule because the alternative is predictable**: a task handed an unrunnable assertion does not delete it, it weakens it into the YAML-shape check 6a exists to forbid — the clause would then manufacture the defect it was written against. **The `environment:` key still ships** on the job holding `id-token: write` (6a's placement, unchanged): it binds the Key Vault secret read and it is the seam the widening needs. ⚠ **What may not ship is the *description***: no task, DoD, runbook or acceptance line may call it a reviewer gate on `dev` — the same defect security-architect filed against `infra.yml:121` (a recorded approval attesting to nothing), one file over, with the false reason **written in the comment NW-73 copies**, so the comment is edited rather than inherited. **Owed as an operator read, not a task** — confirm `dev`'s and `demo`'s environment protection rules in GitHub settings and record them, alongside OQ-w17-ca-05's HCP auto-apply read, which is the same class of unreadable live state. ADR-016 w17 round-3 clause 45. |

## Raw-file corrections carried into this table

The intake re-audited every "Today" paragraph against `d3d2d24`. Five of the raw
file's own claims are wrong or incomplete, and each one changes a decision. Seats
must read `w17-requirements.md` §2 rather than the raw file for these.

| # | Raw-file claim | What the code says | Bears on |
|---|---|---|---|
| 1 | NW-72: `Domain.RealizedSavings` is "the missing verified-value record" | It **exists and is written** (`Raffa.Savings/Domain/RealizedSavings.cs:26`, written `SavingsOpportunityService.cs:325`) but is **write-only** — no reader anywhere. Status is **PARTIAL**, and two code comments are stale. | NW-72 scope; OQ-w17-006 |
| 2 | NW-62: notice deadline / term end are "null even when the clause text has them" | They are **real persisted columns fed by extraction** (`Contract.cs:28`, set at `StagedExtractionService.cs:608-612`, `:622-626`). Null only on an extraction miss. **"When you must move" is answerable today** → item is **PARTIAL**. | NW-62 scope and acceptance |
| 3 | NW-62: "`Why` is an extraction dump (`WhyClauses.tsx`)" | `WhyClauses.tsx:21-67` is a **curated clause list** with a real empty state. The actual dump is `DetailsSection.tsx:35-41`/`:115-117` — **NW-65's file**. | Do not merge NW-62 and NW-65 on a false premise |
| 4 | NW-71: `DocumentQueryService`'s 0.6/0.8 bars "decide `needs_review`" | They are **display-only** and never touch `DocumentProcessingStatus`. `needs_review` is decided by a **separate duplicated pair** in `StagedExtractionService.cs:863-884` and `DocumentProcessingPipeline.cs:195,:378`. Changing one **desyncs the badge from the status**. | NW-71 design; OQ-w17-003 |
| 5 | NW-73: the console "writes the `extraction_job` row" | `ReprocessAsync` usually **requeues and resets** an existing job (`DocumentReprocessService.cs:138-176`), preserving `AttemptCount` and nulling `ClaimedAt` — which is what makes the Worker re-deliver. A console must **call** it, never replicate it. | NW-73 shape |

**Two capabilities already exist and are unwired** — neither is in the raw file,
and both shrink a `must`:

- `GET /api/contracts/{id}/strategy` already returns **`WhenYouMustMove`** and
  **`WhereYouCanPush`** (`Raffa.Insights/Contracts/StrategyPack.cs:26-31`,
  `:51-54`), is already in the generated client (`schema.ts:724`), and **no
  component consumes it** (OQ-w17-005).
- `IBenchmarkService` is real and registered with `MarketFeedBenchmarkAdapter`
  as the **default active** adapter (`Raffa.Market/Benchmark/MarketFeedBenchmarkAdapter.cs:61`,
  `:108`). The blocker for 360 is a **join key**, not a missing service:
  `BenchmarkQuery` needs supplier **name** + **geography**
  (`InsightsEndpointExtensions.cs:40-53`); the name half is now solved
  (`ISupplierNameLookup`, registered `Raffa.Suppliers.Products/Infrastructure/ServiceCollectionExtensions.cs:41`),
  **geography is the remaining gap** (OQ-w17-004).

## Single-writer constraints the decomposer must resolve

Verified on disk by the intake. **Constraint 3 is not in the raw file.**

| # | File | Items that want it | Resolution owed |
|---|---|---|---|
| 1 | `web/src/styles/semantics.ts` | NW-71 (bands), consumed by NW-65, NW-66 | **NW-71 owns it.** `getConfidenceTag` has five consumers (`WhyClauses.tsx:40`, `FactTable.tsx:35`, `contract360ViewModel.ts:574`, `reviewViewModel.ts:282`, `:294`) ⇒ **NW-71 before NW-64/65/66** |
| 2 | `web/openapi/raffa-api.v1.json` | NW-71, NW-20, NW-22, NW-62, NW-26, NW-63 (+ NW-72 if the KPI shape moves) | Five+ edits cannot each own a phase inside 5. **Two contract tasks**, as w16 did: epic-21 (NW-20/22/62) and epic-22 (NW-71 + NW-26/63), different phases |
| 3 | `web/src/routes/contracts/contract360/contract360ViewModel.ts` | NW-62 (`buildAnswers` `:160-199`), NW-65 (`computeNeedsAttention` `:571-580`, `NO_ATTENTION_MESSAGE` `:586`), NW-66 (`formatSource` `:361-367`, `getClauseRiskTag` `:255-259`) | **One task, or three phases.** Not in the raw file's constraint list |
| 4 | `web/src/api/client.ts` | NW-63 (viewer/preview wrappers), NW-72 (realized read, if needed) | ADR-012 w16 clause 25 — hand-written glue. Same phase ⇒ one task |
| 5 | `documents-contracts.sql` | NW-71 (per-field decision column), NW-63 (geometry columns) | Two independent migrations regenerating one byte-compared script. One writer per phase, or one task owns both. **No `backend.yml` array moves** (ADR-021 `:277`, `:309` already list the module) — a `backend.yml` diff is a defect |
| 6 | `.github/workflows/**` | NW-73 only | ADR-014 w16 clause 4 — no other task may open CI YAML |

Other ordering: **NW-26 before NW-63** (the viewer overlays a real page);
**NW-20 / NW-22 before NW-62** (the answers consume those wires).

## Design refs carried into decisions

| Item | Oracle | Anchor |
|---|---|---|
| NW-71 | `inputs/design/prototypes/raffa-v2/screens-v2.md` | `:87-89` — the Review confidence tip quotes the **old** bands verbatim (">95% accept, 80–95% flag, <80% you decide"), and `:81` the "N facts below 80%" title. **Both become stale copy the moment NW-71 lands** — ux-ui-designer owns the disposition |
| NW-72 | `screens-v2.md` | `:132-137` — the KPI triple is *Contracts analyzed · Upcoming renewals · Savings identified* and the opportunities column is **Estimate**. **The oracle carries no realized-money KPI at all**, so NW-72's slot must be designed, not assumed (same anchor that justified w16's money fence) |
| NW-62 | `screens-v2.md` | `:97-101` — the Answers band is *Where you can save* (estimate + lever), *When you must move* (notice deadline, days left, auto-renews), *What to do* (action + rationale + primary button) |
| NW-63, NW-66 | `screens-v2.md` | `:101-104` — **Why**: 2–3 clauses (type · normalized · page § · risk · confidence), click → original wording highlighted (`hl`), citation landing from Ask. Note the oracle itself lists *confidence* on the row, which **NW-66 removes** — a deliberate divergence the designer must record |
| NW-64, NW-65 | `screens-v2.md` | `:104-107` — **Details ▾** "All terms, documents and open facts": key terms table, documents in family, **facts still to decide**. NW-65 **deletes** the third — a deliberate divergence from the oracle, not an oversight |
| NW-23 *(queued)* | `screens-v2.md` | `:114-121` — the Portfolio table is Supplier · contract · **Type** · Annual spend · Ends · Notice by · Risk. "Type" is the product word if Category is not a domain concept (it is not — `Contract.cs:21` is `ContractDocumentType`) |
| NW-74 *(queued)* | `ia-v2.md` | `:92-115` — Ask intents and the chips the prototype offers per screen |

**Three of these anchors record a deliberate divergence from the design oracle**
(NW-66's confidence removal, NW-65's deletion of "facts still to decide",
NW-71's superseded confidence tip). ADR-019/ADR-020 are the place they are
ratified; a task must not "restore" the export.

### Disposition (ux-ui-designer, at the table)

**All of them are ratified, and there are five, not three.** The intake's closing
paragraph counts three; two more of its own rows are divergences rather than mere
refs, and naming them matters because an unratified divergence is what a later
task "fixes" back to the export. The full set, with the clause that ratifies each,
is **ADR-020 w17 §17**:

| Export anchor | What the export specifies | What w17 ships | Ratified by |
|---|---|---|---|
| `screens-v2.md:87`, `:89-91` | field list with **three** confidence tags; the tip quotes ">95% accept, 80–95% flag, <80% you decide" | two treatments over three server decisions; one bar at 90 % | ADR-019 w17 clause 7 |
| `screens-v2.md:83` *(counted as a ref, not a divergence)* | title "N facts below 80% — you decide" | "**N facts need you — you decide**" — no threshold in a title, ever | ADR-020 w17 §13(b) |
| `screens-v2.md:101-102` | Why row = type · normalized · page § · risk · **confidence** | confidence **and** the source cell removed; a leverage word replaces the raw enum | ADR-019 w17 clause 9; ADR-020 w17 §14(e) |
| `screens-v2.md:103-104` | Details ▾ = key terms · documents in family · **facts still to decide** | the third block deleted; one count line replaces it | ADR-020 w17 §14(a) |
| `screens-v2.md:134-135` *(counted as a ref, not a divergence)* | a KPI **triple**, with **no realized-money slot at all** | a **fourth equal cell**, "Savings verified" | ADR-020 w17 §15 |

**A sixth case is not a divergence but an absence**: **no export anywhere carries
document-viewer chrome**. It is decided from ADR-019's locked catalogue
(ADR-020 w17 §16) rather than deferred, so **NW-63 does not stall on an export**.

**Five exports are owed** and are listed as (15)–(19) in ADR-020 w17 §18 with a
single `HITL_CLAUDE_DESIGN` line for the operator's Claude Design round-trip.
**None of them blocks this wave** — every one is assembled from the locked
catalogue and each screen's own idiom, which is why they are a round-trip and not
a dependency. **The exports under `inputs/design/**` are not edited**: they are the
operator's, this council records the divergence and requests the re-export.

**One standing instruction to the decomposer**: no task may "restore" the export.
Concretely — it must not re-add the confidence tag to the Why row, must not
re-add the "facts still to decide" block to Details, and must not re-introduce the
three-band confidence tip, on the grounds that the prototype still shows them. The
prototype is stale from the moment these tasks land, and it stays stale until the
operator re-exports.

## Votes (final table round)

| Seat | Vote |
|---|---|
| product-owner | **APPROVE** *(round 2 — unchanged, extended)* — ADR-001 w17 footer (clauses 0–7, plus **clauses 8–9 appended at the table**: the three acceptance states must stay distinguishable, raised by ux-ui-designer; and `demo` is not dormant, answering delivery-manager; plus **clause 10, appended in round 2**: the realized-money KPI is labelled **"Savings verified"**, ruling **OQ-w17-ux-04**, the table's last open question) on disk and read back; seven item rows, five OQ halves, one new OQ, one OQ ruled back to this seat, and the NW-75 ruling recorded above. **Clause 10 corrects a citation inside this seat's own clause 1** — spec `:447`'s label column reads "Savings Realized" and "Verified" is its meaning column — and amends no decision above. **One `must` is reduced this wave and none is hidden**: NW-63's split stands only because its remainder is the **head of W18**, ahead of this run's overflow. |
| software-architect | **APPROVE** — **one new ADR-029** (document page rendering and the preview contract) and **seven w17 amendment footers** (ADR-002, ADR-003, ADR-017, ADR-021, ADR-024, ADR-027, ADR-028) on disk and read back; **eight** item rows (NW-72, NW-73, NW-71, NW-20, NW-22, NW-62, NW-26, NW-63 — NW-23 is queued, its SA half owed in W18 with OQ-w17-007), **seven** OQ halves (001–006 + po-01) and **four** new OQs (sa-01…sa-04) recorded above. No ADR body rewritten, none superseded. Three rulings depend on other seats and are recorded as such, not as decisions taken: **OQ-w17-sa-02** and **OQ-w17-sa-03** (security), **OQ-w17-sa-04** (cloud). **Round 2 — APPROVE unchanged, extended by two clauses on footers already on disk, both written because another seat's ruling landed after this seat spoke.** **ADR-024 w17 clause 11**: security ruled OQ-w17-sa-03 *permitted*, so clause 10's "if security refuses" conditional **resolves** — both 360 members land and their shapes are named (a permitted member with no shape is still a guess), and ⚠ **`AuditEvent.Detail` is never projected**, because the trail's only human-readable column is deliberately unbounded `text` (`AuditEventConfiguration.cs:24`) that **already** carries contract-derived dates (`RenewalAlertService.cs:254-255`) — so the obvious implementation breaks security's *names, never values* on day one, permanently, on an append-only table. **ADR-028 w17 clause 6**: product-owner's clause 10 fence is adopted whole with one precision the wire owes it — the declared break on `SavingsKpiSummary.Realized` is a **type** change, **not a rename**; nothing is renamed this wave and "Savings verified" stops at the view model. **Round 3 — APPROVE unchanged, extended by one new footer and one clause, both written because another seat's round-2 finding landed on a file only this seat may edit.** **ADR-029 w17 round-3 footer (clauses 1–2)**: the per-page key is **deterministic and replaced in place** — no suffix, no timestamp, no render id, no attempt counter — which ratifies a rule **already written in the file the method is added to** (`DocumentStoragePath.cs:16-18`: a preview is "a derived rendering, replaced in place whenever the document is reprocessed"), so it is a ratification, not an invention. **Cloud-architect (ADR-005 §24) and security-architect (ADR-009 clause 7) reached that same key rule independently and neither could write it** — one from cost (w17 ships a page renderer **and** a bulk whole-tenant re-render in the same wave, and `infra/modules/storage` has no `management_policy`, lifecycle rule, `delete_retention` or versioning, so a suffixed key makes one bulk reprocess a **permanent doubling**), one from correctness (a suffixed key leaves a prior rendering of a **since-corrected** document readable where nothing reaps it). Cost and correctness landing on one rule is why it belongs in the key scheme and not a runbook; both are adopted whole and neither ADR is edited. This **corrects ADR-029's own `:142`**: storage grows per page per document **once**, not per render. ⚠ **Clause 2 is a gap no seat carried**: determinism overwrites pages 1…N and **reaps nothing beyond N**, and the case is reachable — `BuildPreview`/`BuildPreviewPage` carry **no version segment** while `Build` takes an explicit `versionNumber` (`DocumentStoragePath.cs:40`), so a re-uploaded v2 with fewer pages leaves v1's surplus pages at exactly the keys this route serves, hidden from the API by the `page_count` 404 but still in the container; the render stage deletes `n > pageCount` in the same stage that writes 1…N, the shape `DocumentReprocessService.cs:99-101` already uses for chunks. **ADR-028 w17 clause 7 — a correction against this seat's own footer**: clause 6 fenced "the wire keeps the property key `realized`" and **no such property exists** — `raffa-api.v1.json:4960-4962`, `:4991`, `:5051` and `client.ts:987` show the key is **`savingsRealized`** (the payload prefixes the whole family), and `:985-987` does not index the realized key at all, so clause 6's citation was wrong too. Raised by client-architect (ADR-012 §42) and **offered rather than asserted**; verified first-hand against the contract before adopting. Not cosmetic: a fence naming a key that is not in the contract licenses the **undeclared second break it exists to prevent** — an implementer either renames `savingsRealized` → `realized` *to comply*, or finds no `realized` key and renames freely toward the screen label. The break surfaces at `savingsViewModel.ts:110`, already in NW-72's DoD via ADR-012 §43, so this clause does not restate it. No ADR body rewritten, none superseded, no new ADR beyond ADR-029. |
| cloud-architect | **APPROVE** — **two w17 amendment footers** on disk and read back: **ADR-005** (clauses **15–22**, continuing from w15 round 3's §14) and **ADR-007** (clauses **5–8**). **ADR-006 `none`** (region unchanged) and **ADR-008 `none`** (no Foundry account, project, deployment or capacity change) — recorded as decisions, not silence. Two item rows (NW-73, NW-26), the **cloud third of OQ-w17-008**, the **cloud half of OQ-w17-sa-04**, and **four new OQs** (ca-01 ruled, ca-02 ruled with a pre-authorised contingency, ca-03 closed by sa-04, ca-04 recorded with no task). No ADR body rewritten, none superseded, no new ADR from this seat — **ADR-029 is ratified, not competed with**. The wave's entire cloud delta is **one RBAC row at $0.00/month on both environments**. **Round 2 — APPROVE unchanged, extended by four clauses (ADR-005 §23–§26) on a footer already on disk, written because this seat verified its own ratification instead of repeating it.** **§23**: ADR-029 was read in full; **one** of the four conditions this seat attached to its vote is in it (the image layer, `:121-129`), the other three are in §19–§20 — the condition **holds**, and the finding is that the two clauses stopping an **OOM** and an **unbounded container** live in the *cost* ADR while the render loop is written by someone reading the *rendering* ADR, which accepts "storage grows per page per document" (`ADR-029:142-144`) with no overwrite rule attached. Both are copied into **NW-26's DoD as the task's own words**; ADR-029 is not edited — it is software-architect's. **§24**: ⚠ the composition no row carried — **w17 ships a page renderer and a bulk whole-tenant re-render trigger in the same wave**, and `infra/modules/storage` has **no `management_policy`, no lifecycle rule, no `delete_retention`, no versioning** (verified, zero matches), so a non-deterministic page key makes one bulk reprocess a **permanent doubling** of the container; the 20-file measurement runs **before** the first bulk run, never after. **§25**: PO's clause 9 makes the `demo` promotion this wave's business, so the renderer and the per-page blobs reach `demo` on the **same shared 0.25 / 0.5 GiB pair** — the environment the client pilot runs on — and any contingency bump is wired in **both roots in the same PR**, §17's rule applied to `worker_cpu`/`worker_memory`. **§26**: §18's "zero new environment keys" **re-verified against an item that landed after it was written** — security permitted NW-20's audit projection, and `ConnectionStrings__Audit` is **already bound** on the API (`containerapps/main.tf:90`) and the Worker (`:354`), so the claim now holds against every item at this table, not just the two this seat was rostered on. Still **$0.00/month on both environments**; no resource, SKU, region, Foundry or key change. **Round 3 — APPROVE unchanged, extended by two clauses (ADR-007 §9, ADR-005 §27), both written because another seat's round-2 finding landed on this seat's own framing and proved it wrong.** **ADR-007 §9 — a correction against this seat's own words.** §25 `:1189-1190` and ADR-007 `:223-224`, `:282` all say `demo` applies "at its promotion". **False.** Delivery-manager raised it; verified first-hand, and the verification **corrects delivery-manager's version too**. `infra.yml`'s `apply` job is named *"terraform apply skipped"* with a single `$GITHUB_STEP_SUMMARY` step (`:114-136`), invoking no Terraform by design (`:104-113`: a CLI apply against a VCS-connected workspace is rejected), and `demo-promote.yml:123-127` says so itself. The trigger is **not the tag**: `hcp_vcs_wiring.py:104-106` wires **both** workspaces to `EXPECTED_BRANCH = "main"` with `EXPECTED_TRIGGER_PREFIX = "infra/"` (`:101-103` — "ADR-014 is the one mainline branch **every** workspace must track"). Corrected rule: **one merge to `main` under `infra/` queues a VCS run on `raffa-dev` *and* `raffa-demo` at the same instant; `demo` moves on the merge, never on the promotion tag.** This **strengthens** §5/§8/§17 rather than weakening them — a dev-only wiring does not "break at the next promotion", it queues a run against a demo root missing the input **in the same merge**, in a workspace nobody watches: **sooner and quieter**. ⚠ **A finding no seat carried**: `infra.yml:121` puts `environment:` on the **apply** job — the echo — so ADR-016's required-reviewer gate on `demo` **pauses nothing on the Terraform plane**, HCP's run being triggered outside GitHub Actions entirely. Recorded so no task is written believing a human approves `demo`'s infra; **no proposal to move the gate this wave**. **OQ-w17-ca-05 open with an assumption in force**: `raffa-demo`'s auto-apply setting is **live HCP state, unreadable from this checkout** (the script asserts VCS wiring and execution-mode, **not** auto-apply, `:33-46`) and the two branches differ materially — **on** = `demo` infra changes in the same merge as `dev` under a running client pilot with no approval in front of it; **off** = the run sits queued and `demo` silently lacks the role assignment while every workflow is green. Assumed **off** (`infra.yml:109`); owed as an **operator read, not a task**. Independently reaches delivery-manager's **two applies before any acceptance walk** (OQ-w17-dm-03) from the workspace wiring rather than from PR #118. **ADR-005 §27**: the cost line is **unchanged — $0.00, one free RBAC row** — and ⚠ the one thing that does change is §25's contingency: the renderer reaches `demo` in the **promotion** (an image push), but a `worker_cpu`/`worker_memory` bump is **Terraform** and lands **at the merge**, so the contingency's two halves arrive on `demo` **in the reverse order from `dev`** — raised SKU first, renderer second. Harmless in money (`min_replicas = 0`), but it must not be read during the pilot as a failed apply. **ADR-029 ratified, not edited**: software-architect adopted §24 whole (round-3 footer clauses 1–2) and correctly records that §19 `:1043-1047` already carried the overwrite rule; its clause 2 reap is **sound on this seat's plane** — `modules/storage` has no `delete_retention_policy` and no versioning (re-verified: zero matches across all of `infra/`), so a delete **actually reclaims** rather than leaving billed soft-deleted blobs, which is what makes clause 2 a cost control and not just a hygiene rule. **ADR-006 `none`** and **ADR-008 `none`** re-verified against every round-2 and round-3 ruling: no region, Foundry account, project, deployment or capacity change. |
| security-architect | **APPROVE** — **three w17 amendment footers** on disk and read back: **ADR-009** (clauses 1–4), **ADR-011** (clauses **20–24**, continuing w16's 19) and **ADR-022** (clauses 1–5, **discharging the "Owed to W17" left at w16 §4 `:250-254`**). **ADR-010 `none`** (no token, claim, app registration or federated credential change) and **ADR-025 `none`** (no membership, role or invitation change; the SP gets no `workspace_membership` row) — recorded as decisions, not silence. Halves on **NW-73**, **NW-71** and **NW-20** (via sa-03); the **security third of OQ-w17-008**; **rulings on OQ-w17-sa-02 and OQ-w17-sa-03**, both routed here by software-architect; **four new OQs** (sec-01 ruled, sec-02 and sec-03 closed, sec-04 ruled with no task); and a note on **NW-74**, which is queued, not decided. No ADR body rewritten, none superseded, no new ADR from this seat. The wave's entire identity delta is **one topic-scoped Send assignment** and its entire RLS delta is a **binding rule for a new kind of host** — no new tenant table, no policy edit, no new secret. **One correction against this seat's own lane is on the record** (OQ-w17-sec-02 / ADR-022 clause 2): the lane preferred the Container Apps Job on "zero new rights", and this seat's own Send/Receive asymmetry disqualifies it. **Round 2 — APPROVE unchanged, extended by four clauses (ADR-009 §5–§7, ADR-011 §25) on footers already on disk, all four written against this seat's *own* round-1 rulings rather than against another seat's.** **ADR-009 clause 5**: clause 1's optional-third-argument trap is **not one context's** — `AuditDbContextOptions.Configure` carries the identical signature (`:24-27`, interceptor wired only when supplied `:35-38`) — and this seat **permitted NW-20's projection after writing clause 1 without noticing it lands on the second context**. The store is sound (`audit_event`: `tenant_id NOT NULL`, `ENABLE`+`FORCE`+`tenant_isolation`, `audit.sql:20`, `:54-58`), so it fails **closed** — ⚠ and on a provenance tab the closed failure **is** the unconditional `[]` that PO, SA and this seat each refused separately: **one defect, two names**. Ruled onto `AuditQueryService`, which opens its own scope (`:75`) and documents the ordering a host composition would rediscover (`:71-74`); **refines** SA's host-composition ruling, does not contradict it. **ADR-011 clause 25**: the existing reader **cannot be narrowed into this** — `GetEventsAsync` is tenant-wide, 200 rows, no contract or action filter (`:66`, `:68-80`) — so NW-20 must add a **new** method, and that method is the one moment clause 22's conditions 1–2 are implemented or lost; written as **query predicates**, because the obvious in-memory filter of the existing feed satisfies none of them and looks exactly like the feature. **ADR-009 clause 6 — a correction against this seat's own clause 3**: `page` was bounded "before any path is built" and on ADR-029's 404, **both endpoint rules**, while NW-73 reaches `BuildPreviewPage` with **no endpoint at all**; the builder now validates and throws itself as `Build` already does (`:42-46`), and `page` is an **`int`** because `EnsureWithinTenant` is a prefix test with **no canonicalisation** (`:32`) and nothing sanitises a preview path. **ADR-009 clause 7**: offered to cloud-architect's §23 — the deterministic-overwrite rule has a **closer home already written**, `BuildPreview`'s own "replaced in place" docstring (`DocumentStoragePath.cs:16-18`), in the file the new method is added to; a suffixed key leaves a prior rendering of a **since-corrected** document readable where nothing reaps it. **No ADR-029 or ADR-005 edit proposed** — both are other seats'. No ADR body rewritten, none superseded, no new ADR. **ADR-010 `none`** and **ADR-025 `none`** re-verified against every round-2 ruling: the audit projection adds no token, claim, app registration, federated credential, role or membership row. **Round 3 — APPROVE unchanged, extended by three clauses (ADR-009 clause 8, ADR-011 clause 26, ADR-022 clause 6) on footers already on disk, each written because another seat's round-3 ruling landed on a plane this seat owns.** **First, a correction this seat checked for and does *not* owe.** Cloud-architect's ADR-007 §9 corrects "`demo` applies at its promotion" wherever it appears; this seat's footers **never carried that framing** — verified rather than assumed: ADR-011 has **zero** occurrences of `promotion|promote|demo-v`, and ADR-022's only matches (`:6`, `:8`, `:30`, `:41`) are original Day-1 body text, not w17 clauses. The corrected rule is nonetheless **adopted as a premise** below, because it moves *when* the `demo` grant exists. **ADR-009 clause 8** — software-architect's ADR-029 round-3 clause 2 (reap `n > pageCount`) is **adopted whole and ADR-029 is not edited**; what it needs from this seat is the **boundary**, because the guard an engineer will reach for cannot give it one. `EnsureWithinTenant` advertises itself as the "fail-closed guard for the read/**delete** side" (`DocumentStoragePath.cs:24-26`) but is a **tenant** prefix test only (`:32`): a reap with the wrong document id, or one that enumerates the *tenant* prefix instead of the *document's*, is **legal under ADR-009's own guard** — same tenant, wrong document, guard silent. ⚠ Stated precisely because it is **not** a cross-tenant defect and must not be written up as one. Bound two ways: keys under `{TenantPrefix}documents/{documentId}/preview/` only, and `pageCount` a **confirmed positive render result** — null/zero/defaulted deletes **nothing**, since "reap everything above 0" is the arithmetic that reaps the document. The precedent SA cites already obeys this (`RemoveChunksAsync(tenantId, DocumentSourceType, documentId)`, `:99-101`), so this ratifies rather than imposes. **ADR-011 clause 26 — the wave's sharpest finding, and it exists only as a *composition* of two other seats' rulings with code neither read.** `RemoveChunksAsync` opens its own scope and commits **its own** `SaveChangesAsync` (`EmbeddingRetrievalService.cs:169`, `:181-182`) **before** `PublishAsync` (`DocumentReprocessService.cs:110-113`) — so the source comment *"a publish failure fails the request with nothing changed"* (`:107-109`) is **true of the DbContext and false of the embeddings**. On a failed publish the chunks are **gone and committed**, `SaveChangesAsync` (`:115`) never runs so nothing is requeued, and `auditWriter.WriteAsync` (`:117-126`) is after that commit so **no `document.reprocessed` row is written at all**: a destructive act, no recovery, **and no trail** — which is this ADR's own subject (clause 15 rules that a row which cannot name its actor is not written; here there is no row). **Reachable in exactly the window this table created**: ADR-007 §9 lands `demo`'s grant at the *merge*, OQ-w17-ca-05 leaves auto-apply unread with `off` assumed, so `demo` can be green with **no Send grant** and every publish 403s. Three rules: the console **stops at the first publish failure** (never "log and continue" — past the first failure every iteration destroys and repairs nothing, and a whole-tenant loop makes that the tenant's entire corpus); it **exits non-zero with processed/total** (sec-04 unchanged); and ⚠ **the delete-before-publish order is not reversed** — deleting after a successful re-index would make the loop safer and leave **superseded text citable by Ask**, which is what clause 3 exists to prevent. The failure is made **rarer and louder, never reordered**. **A second correction against this seat's own ruling**: OQ-w17-sec-04's "re-runs are safe by construction" holds against **partial completion** — the question it was asked — but **not** against a missing Send grant, where the re-run never reaches the publish and each attempt destroys one more document. The ruling stands **with its scope named**, which it lacked. **No defect is filed against the single-document endpoint** (blast radius: one document an Admin is watching); what binds is that **NW-73 may not inherit that comment as if it were true**. **ADR-022 clause 6 — the Trigger plane of clause 3, which rests on a *placement*, not a presence.** **6a**: `infra.yml:121` puts `environment:` on a job whose only step writes `$GITHUB_STEP_SUMMARY` (`:122-135`), **deliberately** — the file says so at `:110-112`. Cloud-architect recorded that this pauses nothing; ⚠ **the consequence this seat adds is the record it leaves**: GitHub stores the click as a deployment approval that a later auditor reads as *a human approved this infra change*, and it **attests to nothing**. No task, runbook or acceptance doc may cite it as the control on `demo`'s infra; **the gate is not moved this wave**. The sound idiom is in the workflow NW-73 inherits — `verify-tenant-corpus.yml:52` gates the **`verify` job**, the one holding `id-token: write` (`:53-55`), for the reason stated at `:15-16`. Rule: NW-73's `environment:` sits on the job that logs into Azure and publishes, and **the test is behavioural** — approval pending ⇒ **zero messages on `extraction-events` and zero chunks deleted** — because a test asserting the key merely *appears* in the YAML **passes on the `infra.yml` shape**. **6b**: `target_environment` ships **`dev` only**, narrowing the inherited `options: [dev, demo]` (`:26`), because the 403 window is both destructive (clause 26) and the moment the **SAS key clause 1 refuses by name** looks like unblocking a pilot — delivery-manager's own third of OQ-w17-008 records that `az servicebus … keys list` is already mechanically available. Costs the wave nothing: A17-S2 runs on `dev`. **The wave's identity delta is still one topic-scoped Send assignment** — 6a and 6b add no right, no secret and no identity. **NW-74 unchanged and still queued, not decided** (ADR-022 S16-11 stands: presentation, never a control; `GET /api/capabilities` stays un-gated). **ADR-010 `none`** and **ADR-025 `none`** re-verified a third time against every round-3 ruling: no token, claim, app registration, federated credential, role, membership or invitation change. No ADR body rewritten, none superseded, no new ADR from this seat, no application code, no CI YAML and no Terraform. |
| client-architect | **APPROVE** — **two w17 amendment footers** on disk and read back: **ADR-012** (clauses **32–41**, continuing the w16 footer's 21–31) and **ADR-018** (clauses **9–11**, adding **one route** — the first since w14 — and touching nothing of ux-ui-designer's IA/states half, clauses 1–8). **ADR-013 `none`** (the mobile scaffold is untouched and stays non-gating) — recorded as a decision, not silence. **Seven** item rows (NW-72, NW-71, NW-62, NW-63, NW-64, NW-65, NW-66), **three** OQ halves (001, 002, 005) and **three** new OQs (**cl-01** and **cl-02** ruled, **cl-03** recorded as a check owed rather than a rule this seat cannot back). No ADR body rewritten, none superseded, no new ADR from this seat — **ADR-029 is consumed, not competed with**. The wave's entire client dependency delta is **zero**: `web/package.json` stays at five runtime dependencies. **One correction against this seat's own lane is on the record** (NW-71 / ADR-012 clause 34): the lane proposed a two-value decision enum, which would have forced the very session store it was retiring to survive in order to hold a human acceptance; software-architect's three states are adopted. **Round 2 — APPROVE unchanged, extended by four clauses (ADR-012 w17 §42–§45) on a footer already on disk; ADR-018 clauses 9–11 unchanged and ADR-013 still `none`. Three of the four are corrections against this seat's own round-1 rulings.** **§42**: clause 39's "free escape" offered `SavingsKpiSummaryBody["realized"][number]`, and **the generated body has no `realized` key** — it is **`savingsRealized`** (`savingsViewModel.ts:110`), which **this seat's own w16 clause 29 already quotes verbatim**, so the w17 footer contradicted an earlier clause of the same ADR; the idiom's *shape* is right and is the file's own (`client.ts:986-987` both index `[number]`). A wrong key is not cosmetic here: it makes the alias-free path fail to compile, and the cheapest-looking repair restores the alias — spending the exact phase clause 39 gave it up to save. *Offered to software-architect, not asserted over it*: ADR-028 w17 clause 6 fences "the wire keeps the property key `realized`"; the key the client indexes is `savingsRealized`, and read as the JSON key that fence would license the **undeclared second break it exists to prevent**. **§43**: this seat's NW-72 row claimed regeneration + `tsc --noEmit` makes a declared break "fail the build rather than reach a screen" — **false at `savingsViewModel.ts:110`**, because `countOf` (`:72`) is typed `ReadonlyArray<{ count: number }>`, **structural not nominal**, so `RealizedSavingsByCurrency` keeping `Count` compiles unchanged while the population behind the number changes. **A declared break names its call sites**; NW-72's DoD names `:110`, at nil cost since ADR-020 §15 moves that fragment anyway. **§44**: `KpiCellView.lines`'s own invariant (`:63`, "empty **only** when `kpis` is `null`") is **falsified** by ADR-020 §15's zero state, so `—` would mean two things in one row of equal cells; the discriminator already exists — `meta === null` is *not loaded*, `meta !== null` with empty `lines` is *loaded and empty* — so the distinction rides `meta`, never `lines`, and the docstring is rewritten with the code. **§45**: ⚠ **NW-20's two members reach the generated client and nothing renders them** — `activity` occurs **nowhere under `web/src`**, NW-20 rosters no client or UX seat, and adding members is **additive**, so it lands green and silent: the same shape as the defect NW-62 repairs this wave. Ruled: **no web consumer and no client task this wave** — which keeps `contract360ViewModel.ts` at constraint 3's three contenders — with the *absent ≠ empty* rule travelling to W18. **Three surfaces, one rule** (clause 35 answers, clause 44 KPI cell, clause 45 activity), reached independently by this seat, software-architect (the benchmark abstain as an **entry**) and security-architect (a projection that fails **closed** paints an empty tab). No ADR body rewritten, none superseded, no new ADR, no application code. **Round 3 — APPROVE unchanged, extended by three clauses (ADR-012 w17 §46–§48) and one new ADR-018 footer (clause 15), all written because software-architect's ADR-029 round-3 footer landed on the one surface this seat owns.** **ADR-029 is adopted whole and not edited** — both its round-3 clauses are right; what they leave is a client half no other seat can bind. **§46 — the reap stops at the tab.** The deterministic key exists so that a re-render **overwrites** the "prior rendering of a **since-corrected** document" (`ADR-029:204-206`, security's half); that reap is real **in the container** and does not cross into the browser, because `getDocumentPreviewUrl` materialises the page with `URL.createObjectURL` (`client.ts:2100`) — a copy in the tab, on an opaque handle with no relation to the storage key and **nothing to revalidate against**. ⚠ **`cache: "no-store"` (`:2087`) is what hides it**: the HTTP layer provably never serves stale bytes, so the *paint* is assumed fresh when it comes from a blob no fetch directive governs. Reachable **only in this wave's own combination** — NW-73 re-renders a whole tenant while NW-63 ships the first surface that holds pages open (and `client.ts` still has **zero callers** of this method, re-verified). **The client cannot detect it by design**: no discriminator in the key (clause 1), `pageCount` unchanged by a same-length re-render, and no other candidate on the document read model (`documentProgress.ts:65`, `documentTable.ts:145`; the schema's only `versionNumber` is on contract corrections, `schema.ts:393`). ⚠ **Nor does the poll** — `useDocumentsList.ts:148` runs only while a row is `Uploaded`/`Processing` and `:55` stops it after five minutes, so in the steady state where a reviewer opens a viewer **none is running**; named so "the poll will tell us" is not proposed and then found false in implementation. Rule: **no page cache across a page navigation**, every page view a fetch, revoked per clause 33 — the memory rule and the correctness rule are **one rule**, which is the strongest form this seat can record. **A contract member is named and declined**, not asked for: the rule costs nothing and the wave's cap binds; the **W18 condition is recorded instead** (the deferred bbox overlay pins coordinates to bytes and *does* need a discriminator). **§47 — the 404 branch names the wrong thing.** `client.ts:2103-2104` returns a hard-coded `No document found for id ${id}.` for every 404, while ADR-029 clause 5 makes 404 the **page**-out-of-range answer and round-3 clause 2 makes "that page was reaped" ordinary; ux-ui-designer's **ADR-018 clause 12** requires the surface to tell those apart and the data layer as written cannot — a viewer wired to it says *"No document found"* about a document open in front of the user. Repaired **inside NW-63's existing `client.ts` edit** (so clause 39's writer count is unchanged) and discriminated from `pageCount`, which the viewer already holds — `page > pageCount` needs **no fetch**, so there is **no ask and no contract change**. ⚠ **No `onError` swap to page 1 and no clamp**: ADR-029 clause 5 refuses that at the route, and the client can recreate the identical lie **above a correct server** in three lines, as the reflexive repair for a broken image. **ADR-018 clause 15** (own footer; clauses 9–11 and ux-ui-designer's 12–14 byte-identical, **no route added, moved or removed**) keeps the URL on the page that was asked for, so a citation stays reproducible and clause 12's state is reachable. **§48 — NW-62 needs no change, ruled rather than left silent**: under security-architect's ADR-011 clause 26 a document left with no corpus renders the 360's "not yet available" constants, which is **correct** under clause 35 — the source was genuinely called and genuinely has nothing; no special case, no client task, and the screen's honesty is what makes a silent server-side failure legible. Two e2e checks added to `w17-viewer.spec.ts` (a reprocessed document's viewer stops painting the pre-reprocess page; a deep link beyond `pageCount` renders not-found **with the URL still reading `?page=N`**). **ADR-013 `none`** re-verified a third time. No ADR body rewritten, none superseded, no new ADR from this seat, no application code, no CI YAML and no Terraform. |
| ux-ui-designer | **APPROVE** — **three w17 amendment footers** on disk and read back: **ADR-019** (clauses **7–12**, continuing the w15 re-entry round's 4–6), **ADR-020** (sections **13–19**, continuing w15 round 3's 10–12) and **ADR-018** (clauses **12–14**, written as a **separate footer** so client-architect's route clauses 9–11 stay byte-identical). **Seven** item rows (NW-72, NW-71, NW-62, NW-63, NW-64, NW-65, NW-66), **four** OQ halves (001, sa-01, cl-02, cl-03) and **four** new OQs (**ux-01, ux-02 and ux-03 raised in this seat's lane and closed by other seats before it spoke**; **ux-04** open and owed back to product-owner). No ADR body rewritten, none superseded, **no new ADR** from this seat. ADR-019 clause 7 is the **first change to that ADR's confidence rows since it was accepted** — the w14 footer §6 deferred them to this wave by name. The wave's entire design-system delta is **no new token and no new component**: the clause "specchietto" is the existing `ClauseHighlight`, the fourth KPI cell is the existing cell shape, and the viewer is composed from `.btn-ghost`, `.table` and surface tokens. **Two corrections are on the record, one against an earlier footer and one against this seat's own lane** — ADR-019 w17 clause 10 (the w14 §6 evidence claim is not on `d3d2d24`) and ADR-018 w17 clause 13 (the lane ruled the viewer "not a route"; client-architect owns that half and is right). **Five divergences from the design oracle are ratified** in ADR-020 w17 §17, and **five exports are requested** without any of them blocking the wave. **Round 2 — APPROVE unchanged, extended by four sections (ADR-020 w17 §20–§23) on a footer already on disk; ADR-019 clauses 7–12 and ADR-018 clauses 12–14 unchanged, sections 13–19 byte-identical. Two of the three substantive sections correct §15, this seat's own round-1 ruling.** **§20**: `design-system.md:45`'s "row of **equal** cells" makes the band's **arity a design property**, and **three** literals fix it — `KpiRow.tsx:20` (`Array.from({ length: 3 })`, the skeleton), `savingsViewModel.ts:83-88` (the `kpis === null` branch) and `:93-112` (the ready branch) — of which **NW-72's item text names only the third**. `buildKpiCells` returns `readonly KpiCellView[]`, so widening the `key` union binds **no** branch to carry the new literal and the divergence is **invisible to `tsc --noEmit`**. Two failures follow: the load **reflows** 3 skeleton cells into 4 real ones (the rule this seat already bound the viewer to, ADR-018 w17 clause 12), and — because `kpis === null` is **not** loading but a **failed fetch** (`KpiRow.tsx:16-25` returns the skeleton first; only `reduceKpiFetch:56-57` reaches it) — "Savings verified" renders **absent rather than empty**, the **label** going missing where no dash can rescue it. Rule: **four equal cells in every state**, accepted by `buildKpiCells(null)` and `buildKpiCells(summary)` returning the same `key` sequence. **§21 — a correction against this seat's own citation**: §15 warranted its zero state with `savingsViewModel.ts:63`, the docstring that reads `lines` is "empty **only** when `kpis` is `null`" — **the exact invariant the ruling falsifies**; client-architect found it (ADR-012 §44) and the miscitation was this seat's. The ruling stands with the right reason, and the **screen is stronger than the type**: `meta` renders only when non-null (`KpiRow.tsx:51`) and the null branch nulls it on every cell, *plus* that state always paints the `.error-state` block (`:32-41`) and a per-cell **"Stale"** tag (`:55`) — so the dash was never ambiguous to the **user**, only to the next reader of the code, which is why the repair is a docstring. Corrected invariant: **`lines: []` means "no figure"; the reason rides `meta` and the stale tag, never the dash.** Stale-**and**-empty is reachable (a failed refresh with a cached summary) and needs no "as of" clause — `KpiRow.tsx:35` already qualifies the row — but that sentence is written for a **three**-cell band and was re-read against four here rather than assumed. **§22**: the NW-20 note above — four oracles still promise the **tab**, including `Contract360Result.cs:12`/`:219`, the file NW-20 itself edits. **Sixth export owed to W18, blocking nothing.** No ADR body rewritten, none superseded, no new ADR, no application code and no CSS. **Round 3 — APPROVE unchanged, extended by three sections (ADR-020 w17 §24–§26) on a footer already on disk; ADR-019 clauses 7–12 and ADR-018 clauses 12–14 unchanged and byte-identical, sections 13–23 byte-identical. Both substantive sections were written because another seat's round-3 ruling landed on copy this seat owns, and the first is a correction against this seat's own footer.** **§24 — the sentence three round-3 rulings are machinery for does not exist.** ADR-029 round-3 clause 2 (reap), ADR-012 §47 (discriminate the two 404 causes) and ADR-018 clause 15 (keep the URL) all land on the **not found** state whose copy **this seat's own ADR-018 clause 12 routed to "ADR-020 w17 §16"** — and ⚠ **§16 does not contain it** (`ADR-020:1410-1439` is chrome, the overlay-promise rule and the `pageCount`-null rule, and no not-found sentence at all). A clause citing a section for a string it does not hold is **worse than an omission**: the next reader stops looking, having been told where it is. Survivable in round 2, when a 404 meant one thing; not now. Written as **one state, two causes, two sentences** — *"This document has N pages."* + **Go to page 1** for `page > pageCount` (§47 answers it with **no fetch**), and *"This page is no longer part of this document — it was re-processed after this link was made."* + **Reload the document** when the server 404s a page the tab still counts. ⚠ **The two are one URL in sequence**: after the reload the tab holds the shorter `pageCount` and the same address becomes the first sentence — correct, but read cold they look mutually exclusive and the repair an implementer reaches for is to delete one. **Three prohibitions, each against a repair that looks like a fix**: neither sentence may say the **document** was not found (what `client.ts:2103-2104` says today, on a screen where the document is open); **not found takes the *empty* treatment and never *error*'s Retry**, because after ADR-029 round-3 clause 2 it is an **ordinary outcome** and a Retry invites the user to believe it is transient and press it until they conclude the product is broken; and the action is **a button the user presses**, never an automatic clamp — refused at the route (clause 15) and in the client (§47), repeated here because the **third** place a clamp can be reintroduced is the **copy**, where *"showing page 1 instead"* licenses it by describing it. **ADR-019 `none`** — both states reuse the locked catalogue (`design-system.md:64`, `:66`); no token, component or treatment. **§25 — a correction against another seat's premise, not its conclusion.** ADR-012 §48's **conclusion stands** (no client task for NW-62, agreed); its premise — "a document left with no corpus renders the 360's *not yet available* constants" — ⚠ **does not survive the file**: `contract360ViewModel.ts` has **zero** occurrences of `corpus` or `chunk`. The 360 reads structured facts and the Why row's evidence trail (`client.ts:671-674`); ADR-011 clause 26 destroys **embedding chunks**. **Disjoint** — so a document whose corpus was destroyed renders a 360 that is **complete, populated and confident**, the opposite of legible. Nor is the named constant a corpus state: `SAVINGS_NOT_YET_AVAILABLE` (`:150`) is the **benchmark**'s, and `:152` promises it "lights up with the Benchmark Service" *and* asserts the clauses below are **validated** — firing it here would be **worse than silence**. Corrected because a premise in a vote row is what a decomposer copies into a DoD as an expected state. **What the user actually meets is Ask**: its off-state gate counts **validated** contracts and a chunk delete leaves validation untouched, so Ask stays **on**, still claims *"Answers only from N validated contracts … cites or abstains"* (`askViewModel.ts:320`) and **abstains on every clause question** — which `:344` has taught the user to read as *"that is not in your contracts"*. ⚠ **The honest sentence exists and cannot be reached**: `:285` (*"Raffa.ai could not finish processing your documents"*) is wired to the **off-state** gate — a document that failed processing — not to a corpus deleted **after** validation; and with **no audit row** (clause 26) the product has no other account of the event anywhere. **Ruling: no copy and no state ships this wave, and the reason is a clause rather than a budget** — ADR-022 clause 6b ships NW-73 `target_environment` **`dev` only**, so no pilot user stands behind that screen; stated so the next wave does not read it as an accepted permanent gap. **One prohibition, and it is the cheap repair**: ⚠ no task may add a reassuring **"re-indexing" / "catching up"** banner to Ask or the 360 — nothing requeues after clause 26, so it would be a **not ready yet** claim that can never resolve, which **ADR-018 w15 clause 6** (`:474-476`) already forbids in this product's own words (*"a surface that cannot offer the resume must not claim it is waiting"*). **OQ-w17-ux-05 open with its assumption in force**: widening NW-73 beyond `dev` needs a **corpus-health signal** first — the surface cannot distinguish *retrieved nothing* from *has nothing to retrieve* and no read-model member carries it — owed as a **W18 condition on the widening**, not a w17 task. **§24 needs no new export**: both sentences are composed from the locked catalogue, so the five exports of §18 and the sixth of §22 are unchanged and **NW-63 still does not stall on one**. No ADR body rewritten, none superseded, no new ADR from this seat, no route added/moved/removed, no application code and no CSS. |
| delivery-manager | **APPROVE** — **two w17 amendment footers** on disk and read back: **ADR-016** (clauses **36–42**, continuing w16's 35) and **ADR-014** (clauses **1–7**, numbering **restarted** — the two ADRs use opposite conventions and both are restated in the footers, because this seat got them the wrong way round in w16 and the correction is not re-earned per wave). **ADR-015 `none`** (no federated credential, GitHub secret, subject claim or Graph right — the new right is an Azure **data-plane role** on an existing principal) and **ADR-005/ADR-007 `none` from this seat** (the module body is cloud-architect's and is not contested) — recorded as decisions, not silence. **One** item row (NW-73), the **delivery third of OQ-w17-008**, and the wave's **order** with a corrected skeleton. No ADR body rewritten, none superseded, no new ADR, no CI YAML and no application code. **The wave's entire CI delta is one added workflow file**; its entire `infra/` delta is the four files of ADR-016 clause 37. **Three rulings landed on this seat after its lane was written and all three are adopted, two of them as corrections against this seat's own draft**: cloud-architect's **`lifecycle { ignore_changes }`** on the new role assignment (verified first-hand at `modules/servicebus/main.tf:80-86`, `:95-101` — ARM rejects in-place role-assignment updates, so omitting it plans clean today and fails a *later, unrelated* apply); software-architect's ruling that the console **joins `AllRaffaProjects`**, against this seat's draft claim that it is "not scanned and adds no test burden" — **the draft was wrong**, and the reason is sharper than convention: that set is the **membership filter on the violation check** (`:103`; `ArchitectureRuleEnforcementTests.cs:33`, `:83`), so a project absent from it is **invisible to the theory** and a later domain module referencing the console would be **silently legal with the suite green**, which makes it a **DoD line rather than an implementer's option**; and software-architect's **ADR-029** ruling that NW-63 needs **no migration**, which **dissolves this seat's own ordering constraint 4**. **Product-owner's clause 9 overrules this seat's draft assumption on `demo`** (which proposed recording a four-wave deferral) and routes the mechanism back here; accepted without reservation, and the mechanism is ruled in ADR-016 clause 40. ⚠ **Two findings no other seat carried.** **(i) The promotion job does not apply** — `demo-promote.yml:128-136` calls `infra.yml`, whose apply job is a **step-summary echo** named *"terraform apply skipped (demo)"* (`:114-136`), while `promote-backend`/`promote-web` merely `needs` a green infra **plan** (`:140`, `:151`, comment `:123-127`); **a `demo-v*` tag can therefore go green end to end while `demo` lacks NW-73's role assignment**, so clearing the backlog is **two acts in order** — the HCP VCS apply confirmed in the UI, *then* the tag. This **corrects cloud-architect's framing** that `demo` "applies at its next promotion". **(ii) The flags are w15's, not w17's, and they are Terraform variables already defaulting `false`** (`demo/variables.tf:45-49`, `:51-55`) — **three** of them, not two (`:65-67`), the third `false` because the Graph grant is written out of band after `Authorization_RequestDenied` **failed an entire `dev` run** (`:57-64`); w17 flips none, so **clearing the promotion backlog does not clear the invitation walk**, which the acceptance doc must state rather than let a walker discover. **Also raised, and owed to the decomposer rather than to an ADR**: `reviewViewModel.ts` is a **two-item file** (client-architect) needing two phases or one task, **NW-66 must follow NW-63** because e2e N20 asserts the link *lands*, and ⚠ **`FactTable.tsx` still has no assigned writer** — reached independently by this seat, client-architect and ux-ui-designer, and the only finding of this table that **no ADR closes**. **Three open questions stay open and are asked, not asserted**: **OQ-w17-dm-01** (ruled — the engine's `wave-close.md` is a fan-out report and the wave record is `docs/waves/<w>-acceptance.md`, because a council rule cannot rename an engine artefact, which is exactly why this seat's own w16 clause 3 bound nobody and did not execute), **OQ-w17-dm-02** (closed by product-owner's clause 9), and **OQ-w17-dm-03** (PR #118's apply — still unreadable from this checkout, **unclosed one wave later**, so the w17 gate confirms **two** applies before any acceptance walk). **Round 3 — APPROVE unchanged, extended by five clauses on footers already on disk (ADR-016 round-3 clauses 43–45, ADR-014 round-3 clauses 8–9); clauses 36–42 and 1–7 byte-identical. Three of the five are corrections against this seat's own clauses, and the wave's CI delta is still one added workflow file and its `infra/` delta still the four files of clause 37.** **ADR-016 clause 43 — `demo`'s infra moves at the MERGE, not at the promotion, and this seat's model was wrong too.** Cloud-architect's ADR-007 §9 corrected the framing after this seat's round-2 finding (i); verified first-hand rather than taken on report — `scripts/hcp_vcs_wiring.py:104-106` wires **both** `raffa-dev` and `raffa-demo` to `EXPECTED_BRANCH = "main"` with `EXPECTED_TRIGGER_PREFIX = "infra/"`, `:101-103` naming ADR-014 as the reason. Clause 40.1's **conclusion survives** (a `demo-v*` tag can go green while `demo` lacks the grant) and is true across a **longer** window; what was wrong is that this seat still reasoned as though the `demo` apply were an act *near* the promotion. ⚠ **PR 1's merge queues the `raffa-demo` run at the same instant as `raffa-dev`**, so the green-but-ungranted window opens **days before any promotion decision** and is silent, OQ-w17-ca-05 leaving auto-apply unread with `off` assumed. Clause 40.2's two acts become **three, two of them at PR 1**: merge → both runs confirmed in the HCP UI (`raffa-dev` before any dispatch, `raffa-demo` before the tag) → the tag, its number read. ⚠ **Clause 37's stated reason is corrected while its ruling is strengthened**: a dev-only wiring does not "break the promotion path one tag later, in the job nobody runs weekly" — it queues a **failing plan on `raffa-demo` in the same merge**, sooner and quieter than this seat argued. Required + both roots is **more** necessary, not less. **ADR-016 clause 44 — ⚠ a premature dispatch is destructive, not merely failed, and this falsifies this seat's own ADR-014 w17 clause 2.** Under security-architect's ADR-011 clause 26, `RemoveChunksAsync` commits its **own** `SaveChangesAsync` (`EmbeddingRetrievalService.cs:181-182`) **before** `PublishAsync`, so clause 2's *"the console will authenticate and then fail to send… or the first run reads as a broken console"* describes the wrong event: the first run against a missing Send grant **destroys a document's corpus, commits, and leaves no trail**, and a loop that logs-and-continues destroys the tenant's. Clause 26 bounds it to one document by ruling stop-at-first-failure — ⚠ **a property of an implementation that does not exist yet**, so the sequencing note may not lean on it; on the day of the first dispatch the guard and the grant are both unproven. **Rule: NW-73 is not dispatched even once until the `raffa-dev` apply is confirmed landed in the HCP UI** — it leaves the acceptance doc and becomes **gate content**, a data-destruction guard rather than operator etiquette, and the acceptance walk's first act is the confirm, not the console. **ADR-016 clause 45 — clause 6b adopted whole, with three consequences it does not carry.** ⚠ **(a)** the inherited `environment:` line's stated purpose is **void on day one**: `verify-tenant-corpus.yml:52` is `environment: ${{ inputs.target_environment }}`, an **expression**, and `:15-16` says it exists "so demo's required reviewers still gate a job that reads a Key Vault secret" — with `options: [dev]` it can only resolve to `dev`. The line **stays** (it binds the secret read and is the seam the widening needs) but ⚠ **may not be described as a reviewer gate** — the same defect security filed against `infra.yml:121`, one file over, with the false reason **in the comment NW-73 copies**. **(b)** clause 6a's behavioural test has **no reachable state** under 6b → **OQ-w17-dm-04**, assumption in force, **no DoD line this wave**, because a task handed an unrunnable assertion weakens it into the YAML-shape check 6a exists to forbid. **(c)** the widening has an artefact and it is **one line of this seat's** — the workflow's `options:` list — gated on three things w17 does not deliver (the `raffa-demo` apply, ux-05's corpus-health signal, 6a's test made runnable); and ⚠ from PR 1's merge **`demo` holds a Send grant nothing can target**, kept because the variable is required and `raffa-demo`'s plan must stay green, not because `demo` has a consumer. **ADR-014 clause 8 — W17-A1 gains (h)** (the `raffa-dev` apply confirmed **before** the first dispatch) and ⚠ **clause 4(g) is corrected in count and kind**: not "**two** applies" but **three runs across two workspaces**, and **not interchangeable** — `raffa-dev` is a destruction guard, `raffa-demo` a promotion precondition, PR #118's an inherited unknown. A single tick against "the applies" satisfies none of the three. **ADR-014 clause 9 — the phase graph is UNCHANGED by round 3**, stated rather than assumed because five seats appended footers to it, and checked against clause 7's skeleton one by one: ADR-029 clause 2's reap is a DoD line on **NW-26** (P1); ADR-012 §47's 404 repair is explicitly inside **NW-63's existing `client.ts` edit** (one writer, P3); ADR-018 clause 15 adds **no route** and ADR-020 §24 **no export and no component**; ADR-011 clause 26 and ADR-022 clause 6 are DoD lines on the **single** NW-73 task; ADR-012 §48 rules **no client task** for NW-62, so constraint 3's contender set stays three. ⚠ **One new-file question the round opened is already answered by phase**: client-architect's §48 puts two e2e checks in **`w17-viewer.spec.ts`** — verified **not present on this base** (`web/e2e/` holds `day1.spec.ts`, `invite.spec.ts`, `v2.spec.ts`) — while NW-66's **N20** asserts its link lands; clause 7.5 already separates NW-63 (P3) from NW-66 (P4), so `check_single_writer.py` is satisfied **by phase**. Recorded because the tempting repair is to **merge the two tasks so they can share the spec file**, which would re-collide `contract360ViewModel.ts` against clause 7.6 and undo the separation clause 7.5 created. **`FactTable.tsx` is still the one finding of this table that no ADR closes** (clause 7.7) — unchanged by round 3, and no seat claimed it. **ADR-015 `none` re-verified a third time**: a queued VCS run, a narrowed `workflow_dispatch` input and a reap of surplus blobs touch no federated credential, GitHub secret, subject claim or Graph right. **ADR-005/ADR-007/ADR-011/ADR-022 are cited as premises and not edited** — they are their seats'. No ADR body rewritten, none superseded, no new ADR, **no CI YAML, no Terraform and no application code.** |

## Gate verdict

**APPROVED** — 2026-09-15T17:33:55Z — 11 decisions, 19 ADRs amended, 1 new (ADR-029), 6 no-change.

`COUNCIL_APPROVED: w17 — 11 decisions, 19 ADRs amended, 1 new, 6 no-change`
