# Wave w18 — architecture decision record

Generated by `next-intake` (skeleton); completed by the table (w18).
ADR actions are owned by the seat named; files cited exist on disk.

## Items

| Item | Seats | Decision | ADR action | File |
|---|---|---|---|---|
| NW-63r — bounding-box overlay + phrase-edit | sw, cloud, client, ux, po | **product-owner:** in; box over cited phrase + edit persists (second browser), proposal-vs-override never blurred. **software-architect:** `prebuilt-layout` called, wire widens `words`/`polygon`, `AiOcrPage` grows geometry; geometry columns land in the shape ADR-029 pre-decided; phrase-edit is a new override row (`source=phrase-edit`), never mutates the proposal (OQ-w18-002). **client-architect:** overlay is DOM over the `<img>` (no new dep); phrase-edit reuses existing Review `PATCH`, viewer re-reads the server-persisted override. **ux-ui-designer:** overlay affordance + phrase-edit Review-only, never draw→edit on the viewer. **cloud-architect:** account already holds the role — no new SKU. | **product-owner:** amend ADR-029 (provenance + screen promise). **software-architect:** ADR-017 + ADR-003 amended (on disk, w18); ADR-027 `none` — w17 cl 2 binds re-derivation-never-overrides (a phrase-edit is a human correction), and ADR-029's w18 footer carries the override-beside-proposal provenance. **cloud-architect:** `none` — account role (ADR-008/017). | ADR-029-document-page-rendering.md, ADR-017-ocr-in-v1.md, ADR-003-data-store.md |
| NW-23 — Portfolio category filter | sw, po, client, ux | **product-owner:** in, reduced to supplier-category filter hosted in the join; §6 "Type" stays the doc type. **software-architect:** host join by `supplierName` (the sole legal supplier join, ADR-002 DependencyDirectionTests). **client-architect:** filter as query param, never a client store. | `none` — ADR-002 (host composition) + ADR-020 (control) already cover it. | — |
| NW-25 — Savings list filters | client, ux, po | **product-owner:** filter (supplier/status/currency); no invented category. | `none` — ADR-020 (control). | — |
| NW-74 — Hide admin-gated Ask chips | client, ux, sec | **client-architect:** thread server-derived `role` (already at `AppShell.tsx:40`) into both chip selectors; drop chips whose catalog entry carries `roleGate !== "any"` when role ≠ Admin; presentation-only. **ux-ui-designer:** one predicate. **security-architect:** confirm — catalog un-gated, S16-11 stands. | `none` — ADR-022 S16-11 + ADR-012 w16 cl 28 (presentation). | — |
| NW-30 — Hand-authored OpenAPI gaps | sw, client | **software-architect:** both halves closed-on-main (audit route + conversations requestBody); residual is a prose sweep. **client-architect:** re-verify hand-written bodies in `client.ts`, no new wrapper. | `none` — sweep; ADR-012 w16 cl 27 / ADR-026 cover it. | — |
| NW-40 — HCP CA env + Foundry RBAC | cloud, dm | **delivery-manager:** operator walk confirms `ConnectionStrings__Storage` / `AiGateway__*` / `AzureAd__*` in `dev`+`demo`, Foundry RBAC on both. **cloud-architect:** confirm-only walk, no Terraform change. | `none` — ADR-005/016 already bind. | — |
| NW-41 — Deployed API serves market_record | dm, sw, cloud | **delivery-manager:** dev walk proving a seeded `market_record` row is served via `GET /api/market/records/{id}`. | `none` — walk (ADR-024/005). | — |
| NW-50 — day1.spec.ts red vs V2 shell | dm, client | **delivery-manager:** reconcile the pre-V2 spec to the V2 shell; run under vitest, never CI. | `none` — reconciliation (ADR-016/012). | — |
| NW-55 — Ask citation cards | client, ux, sw | **client-architect:** tenant citation → deep-link CTA to the viewer (`/documents/:id/viewer?page&clause`); raffa/product citation → CTA card; never a fabricated `previewUrl`. **ux-ui-designer:** two-corpus treatment. **software-architect:** `PackItem` divides by `corpus`; real `previewUrl` only for tenant pages; `corpus=raffa` never carries a page-preview slot. | none from client — ADR-018 w17 cl 9 + ADR-012 w14 cl 1 bind; **software-architect `none`** — ADR-024's catalog row (`corpus: raffa` citation cards) + ADR-012 w14 cl 1 + ADR-018 w17 cl 9 already bind; **ux-ui-designer:** amend ADR-020 (citation corpus treatments). | ADR-020-web-screen-inventory.md |
| NW-56 — 360 "Ask about it" briefs that contract | po, client, ux, sw | **product-owner:** scoped entry must brief the contract, never land on a generic gate. **client-architect:** consume `?scope=`, bind the briefed contract to the scoped turn. **ux-ui-designer:** heading copy (supplier kicker + scope line). **software-architect:** gate resolves the scope id before the R-ASK-10 check; one engine, scoped entry. | `none` — ADR-024 (one engine, scoped entry) + ADR-020 (heading copy) already own it. | — |
| NW-57 — Quote check: market benchmark | po, client, ux, sw | **product-owner:** job-to-be-done is a market benchmark, not a worksheet; history + benchmark-first UX. | `none` — ADR-024 (quote flow) + ADR-028 (history) + ADR-001 (fixture fence) already cover it. | — |
| NW-59 — Ask never dead-ends (abstain recovery) | client, ux, po | **product-owner:** abstain must carry a recovery action. **ux-ui-designer:** recovery actions are secondary, never primary. **client-architect:** render the server-selected recovery action as secondary. | `none` — ADR-024 (reply kinds) + ADR-020 (copy/actions). | — |
| NW-60 — Hide global Ask bar on Ask screens | client, ux | **client-architect:** suppress `GlobalAskBar` on `/ask` + `/ask/:id` via `useLocation`; keep elsewhere; ⌘K focuses composer. **ux-ui-designer:** route-scoped suppression. | `none` — render rule; ADR-018/020 own IA/copy. | — |

## Votes (final table round)

| Seat | Vote |
|---|---|
| product-owner | APPROVE |
| software-architect | APPROVE |
| cloud-architect | APPROVE |
| security-architect | PASS |
| client-architect | APPROVE |
| ux-ui-designer | APPROVE |
| delivery-manager | APPROVE |
| council-gate | — |
