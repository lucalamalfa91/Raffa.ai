# Wave w14 — council decisions

Skeleton written by `next-intake`; rows completed by the involved seats at
`next-council-close`; verified by `next-council-gate`. Source:
`reports/context/waves/w14-requirements.md`.

Theme: **Workspace is real** — membership, list, members, count, role,
profile, and the invitation lifecycle. Baseline `1650213`
(`helix/next-wave-process`); `origin/main` is `25b10da` and post-rebrand —
read every `Raffa.*` path as `Raffa.*` on that tree (W14-01, OQ-w14-003).

| Item | Seat(s) | Decision | ADR action | File |
|---|---|---|---|---|
| W14-01 | delivery-manager | **DM**: the wave bases on `origin/main` **merged** with the Helix process branch — not branched afresh from either, because **neither side is a subset of the other**: `.helix/` lives in the repo and is not its own git toplevel (`ADR-014:97-98`), so the artefact branch carries `raffa-next-process.yaml` that `main` lacks while `origin/main` carries the `Raffa.*` → `Raffa.*` rename that the artefact branch lacks. The merge, the path translation (`Raffa.X` → `Raffa.X`; `inputs/design/prototypes/raffa-v2/` → `raffa-v2/`, which every design-ref row in this record depends on) and the green-base proof are **operator acts at HITL, before `register_wave.py` and before fan-out**. **W14-01 therefore produces no task and no `depends_on` edge points at it**; its entire enforcement is that `reports/plan/gates/w14.hitl-ok` is not created until the proof passes — if it fails, the wave does not start. **The proof (W14-A1) is five points and the last two are the ones that matter.** (a) `rg -n "Raffa\." backend/src web/src` returns nothing; (b) `dotnet build` and `npm test` green; (c) `rg -ni "raffa" .github/ infra/ scripts/ backend/scripts/` reviewed **line by line** against live Azure / HCP / **Entra** — the rename crosses three planes and CI hardcodes name literals that are **not** .NET namespaces: `rg-raffa-<env>` (`backend.yml:114`, `web.yml:149,210`, `seed-demo-fixture.yml:73`), `ca-raffa-<env>-{api,worker}`, `swa-raffa-<env>`, `id-raffa-<env>-workload`, images `raffa-{api,worker}`, `Raffa.slnx` and the two Dockerfile paths, HCP workspace `raffa-<env>` in org `raffa-platform`, database `raffa_<env>`, SP `raffa-sp-<env>`, KV `kv-raffa-<env>-<6>`, and the seed's `'Raffa Demo'` display name (`demo-fixture-seed.sql:63`); (d) **one throwaway `dev` deploy from the rebased base is green**, reaching `backend.yml`'s "Verify schema applied (ADR-021)" step, which is the only thing that proves the workflow ↔ cloud contract survived; (e) **one interactive sign-in on deployed `dev` returns a token carrying the expected scope**. **Three literals are sharper than a resource name and are why (d) and (e) exist.** (1) The **Entra scopes** `api://raffa-<env>-api/Raffa.{Read,Write}` (`web.yml:204-205`, live in the runbook's own payload at `:180`) are **identity-plane**: a wrong resource name fails a deploy loudly at `az … show`, but a renamed scope that does not match the app registration fails at **token acquisition, in the browser, after CI is green** — and *every* w14 item is about the signed-in identity, so this is the single most dangerous surviving literal in the wave (adopted by security-architect into ADR-010's w14 footer). (2) The ADR-021 script array is **duplicated** — `backend.yml:277-285` (apply) and `:309-317` (verify), kept "in lockstep" by `:274-275` — so the rename had to rewrite **eighteen** module-path literals consistently, not nine; rewriting one array and not the other yields an asymmetric failure where apply succeeds and verify fails. (3) `scripts/check_demo_swa_config.py` **asserts on the brand substring** `raffa-<env>-api` (runbook `:179-180`), so a missed rename there fails the **standing** promotion check on every future promotion, w14's included. **This seat has no `Bash` in this harness and has not read `origin/main`; it asserts neither that this is broken nor that it is fine.** It is a precondition, verified at the gate, which is exactly what W14-A1 is for | **DM**: `amend ADR-014` — footer written, five clauses ("Wave base and integration branch"). It **adds** what the body never covered — the merge-not-fork base, `integration` (a word that does not occur in the ADR body, though `wave-close-e13.md:7,20` shows the two-level structure was already delivered), the green-base proof, and the no-unplanned-CI-YAML rule — and relaxes no protection. `Status: accepted` and the Decision outcome are untouched | `reports/architecture/ADR-014-git-flow.md` |
| NW-02 | software-architect, security-architect | **SA**: `CreateWorkspaceAsync(name, callerIdentity, profile, ct)` writes `workspace` + the five `workspace_role` rows + one `workspace_user` + one `workspace_membership`(Admin) in **one** `SaveChangesAsync` inside the **existing single** `BeginScope(workspace.TenantId)` (`WorkspaceProvisioningService.cs:42`), reusing `WorkspaceMembershipFactory.CreateInvitedUser`/`CreateMembership` rather than new construction logic. The 201 gains `role: "Admin"` so the SPA enters without a second call. **No migration — no schema change.** Identity comes from the one `ICallerIdentity` seam, never `HttpRequest.Headers`. *Yielded to security: absent identity is **401**, not the 400 this seat drafted (ADR-025 §B — absence of identity is an authentication failure).* Backfill of existing `dev` workspaces is an operator script, never a "claim workspace" endpoint (OQ-w14-006) — **SEC**: **401 confirmed for absent identity** (an authentication failure, not a malformed body; matches `AuditEndpointExtensions.cs:33-34`, which already maps `Unauthenticated` → 401), **400 only for malformed**. This is a deliberate change of posture from today's anonymous signup step (`WorkspaceEndpointExtensions.cs:17-18`, "nobody has a tenant claim yet, by definition") because the endpoint now writes an **identity-keyed grant**. **The creator is Admin by virtue of creating the tenant** — a `role` field in the body is *ignored*, never honoured; there is no self-assignable role anywhere in this wave. Creating a tenant grants **no** read of any existing tenant. RLS: the four writes land in **one** `BeginScope(workspace.TenantId)` and **one** `SaveChangesAsync` under the new tenant's own claim — the workspace row *is* the first row of its own tenant (`WorkspaceProvisioningService.cs:17-21`; `UNIQUE INDEX ix_workspace_tenant_id`, `identity-workspace.sql:69`; `WITH CHECK` `:145-147`) — and a partial bootstrap must not be reachable **by a failure path either**. **This path stays strictly one-scope-per-request**; the discovery exception is not available to it. **No new table and no policy is relaxed to make the bootstrap fit.** Backfill: a "claim this workspace" endpoint is a **tenant-takeover primitive** (whoever calls first becomes Admin of a tenant holding another user's uploads) and is **refused**; the backfill is an operator job over `(tenant id, email)` pairs supplied at HITL, audited as `workspace.membership.backfilled` with the operator as actor — an unclaimed stale `dev` tenant is a smaller problem than a takeover path | **SA**: `new ADR-026` §D5 — contract and composition. The RLS/bootstrap footer on ADR-009 is **security-architect's**, not this seat's. **SEC**: `amend ADR-009` — footer written, clause 6 (bootstrap) + clause 7 (verify-then-scope); `new ADR-025` §D.2 | `reports/architecture/ADR-026-workspace-api-and-invitation-schema.md`, `reports/architecture/ADR-009-tenancy.md`, `reports/architecture/ADR-025-workspace-membership-and-invitations.md` |
| NW-01 | software-architect, security-architect, client-architect | **SA**: `GET /api/workspaces` is **two-phase** — (1) discovery with `app.identity_subject` set and **no** tenant scope, returning candidate tenant ids only; (2) per-candidate `BeginScope(tenantId)` projection for name, role, count and NW-24's columns. **The endpoint takes no `X-Tenant-Id`, ever**, so acceptance N5 holds by construction rather than by a guard that can be forgotten. **Inclusion is gated on a live `workspace_membership` row, never on the `workspace_user` row** — removal therefore takes effect on the next request with no cache and no token to invalidate. Empty list is **200 + `[]`**, never 404; order `createdAt` ascending. New `WorkspaceDirectoryService` in `Raffa.Identity.Workspace/Infrastructure/`; row = `{ id, name, createdAt, role, contractCount, country?, currency? }` — **no `roleLabel`** (client-architect F1: the picker derives its tag from `role` via the existing `WORKSPACE_ROLE_LABEL` map), client method `listWorkspaces()` (`client.ts:22-24` already reserves the name) — **SEC**: **NW-01 is an RLS design decision, not an endpoint task.** With no tenant claim the interceptor leaves `app.tenant_id` unset (`TenantRlsConnectionInterceptor.cs:53-57,66-70`, fail closed) so *every* policy in `identity-workspace.sql:143-182` denies every row — and the caller's tenant is precisely the unknown being discovered. Chosen after rejecting three alternatives: a cross-tenant directory table without RLS (a second source of truth RLS does not protect, drifting toward "still listed after removal"), a `BYPASSRLS` role (**ADR-009 `:57` forbids it verbatim**), and identity-keyed policies on all four workspace tables (a policy on `workspace_membership` that must subquery `workspace_membership` — clever RLS is how holes happen). **Exactly one new policy**: `identity_self` on `workspace_user`, **`FOR SELECT` only** (no `WITH CHECK`, so it can never authorize a write), keyed on a new GUC `app.identity_subject`, and **fail-closed** via `nullif(current_setting(...),'') IS NOT NULL` so an absent claim widens nothing. The other three tables keep exactly one policy each, unchanged — the widening is auditable by reading six lines. **Membership, not user existence, is the grant** (Rule F.1d): a removed member keeps their `workspace_user` row by design, so a user-driven list would show them the tenant they were removed from. Discovery returns **candidate tenant ids only**; every tenant *fact* is read in a per-tenant scoped read under that tenant's real claim, capped at **50** candidates (an unbounded loop keyed on caller-controlled identity is a DoS shape) with `workspace.list.truncated` audited. **The GUC is a SQL-injection sink and must be parameter-bound**: `SELECT set_config('app.identity_subject', @identity, false)`, never a copy of `BuildSetCommandText` — its own comment states both that it inlines *because* `TenantId` wraps a Guid **and** that "`SET`/custom GUCs do not accept bind parameters" (`:103-107`), so a task reaching for `SET` will find it cannot bind and will interpolate; **`set_config` is the fix, interpolation is the vulnerability**, with a mandatory negative test on `'`, `;`, `--`. The multi-scope loop is a **named exception** to `ITenantContext.cs:23-24` ("ADR-009 expects exactly one scope per request"), bounded by four rules — sequential never nested, own connection each, one named method, and **any other endpoint doing it is a defect, not a precedent** — **CLIENT**: the picker reads `apiClient.listWorkspaces()` on mount; `loadKnownWorkspaces`, `rememberWorkspace` and the `localStorage` key prefix are **deleted** (`workspaceStore.ts:32,88-106`) and the create 201 is no longer a cache seed (`WorkspacePickerScreen.tsx:36,78-85`). `WorkspaceSummary`'s two invented fields die with it: `contractCount` ("Always 0 from this screen", `:39-45` → NW-09) and `roleLabel` ("Client-side inference, not a server-issued claim", `:56-64` → NW-14). **The picker has no `useEffect` today** — `:36-41` are all `useState` initialisers — so NW-01 introduces this screen's first network read, and with it two states it has never had: a loading skeleton and error+Retry (ADR-018 `:115,117`). ADR-026 §D1 adopted without dispute, including **200 + `[]`, never 404**: "you have none, create one" and "the request failed" are two different screens, and the deleted store is precisely why they were one. Two consequences recorded because they are invisible in the contract: the **50-candidate cap** means the picker renders what arrives with **no pagination**, and if truncation is ever reachable it must be visible rather than silent; and **`role` on this row is the only thing between an unmodelled wire value and Admin affordances**, which is why the parse defaults to least privilege (NW-14 row). e2e **N2**: sign in in a **fresh browser context** (no `storageState`) and assert `.workspace-row` with no create step — the one assertion a `localStorage` list could never pass. `day1.spec.ts:33-41` asserts the **opposite** verbatim ("must create its own workspace — it has no way to discover the ADR-022 fixture-seeded tenant"), so that premise is false after NW-01 and is rewritten by the same task | **SA**: `new ADR-026` §D1 — contract, read model and module. The `identity_self` policy, its fail-closed guard, the parameter-binding rule and the multi-scope exception are **security-architect's** ADR-009 w14 footer. **SEC**: `amend ADR-009` — footer written, clauses 1–5; `new ADR-025` §F.1/§F.2/§F.3. **CLIENT**: `amend ADR-012` — footer written, clauses 1–4 (a client store never stands in for a missing GET; the four generator constraints binding on this contract entry; what the SPA sends) | `reports/architecture/ADR-026-workspace-api-and-invitation-schema.md`, `reports/architecture/ADR-009-tenancy.md`, `reports/architecture/ADR-025-workspace-membership-and-invitations.md`, `reports/architecture/ADR-012-web-stack.md` |
| NW-14 | security-architect, client-architect | **SEC**: **a client-declared role header is never an authorization source.** Authorization for an Admin-only action resolves from role claims on an authenticated principal (`WorkspaceRoleResolver.cs:50-55`), then from the `workspace_membership` row (`:85-119`), and **nothing else**; `X-Role`/`X-Workspace-Role` (`:37-38`, read `:70-83`) are **demoted now** to non-authoritative UI shaping on `/api/capabilities` only. Where header and membership disagree **membership wins in both directions** — a header claiming `Admin` never grants, one claiming `Procurement` never revokes a real Admin. **This costs nothing and breaks no client**: `Grep` over `web/src` for `X-Role\|X-Workspace-Role` returns **zero matches** (the SPA sends `X-Tenant-Id` + `X-User-Id` only, `client.ts:49-54`), and `CapabilitiesEndpointExtensions.cs:44-48` already documents this exact line ("never a hard 401/403 the way `AuditEndpointExtensions` is, since the catalog itself is not sensitive, tenant data") — so this **restates a convention the codebase holds**, and letting the header drift into an authz decision would be the regression. Implementation is a **deletion**: `ResolveAsync`'s middle branch (`:57-60`) is removed so the order becomes claims → membership, leaving `TryResolveHeaderRole` one caller. **N9 needs no new authorization code** — once NW-02 writes the creator's membership row the existing join (`:104-114`) returns `Admin` and the gate passes, so NW-14 is a **proof** task (create → delete a document 204, reprocess 200, Procurement still 403), not a fix task. Executes NW-14's own instruction not to "fix" this with a spoofable `X-Role: Admin` — **CLIENT**: `resolveWorkspaceRole()` is **deleted as the product path**, both halves — the `?role=` query override (`workspaceRole.ts:40-44`) and the `sessionStorage` mirror that **defaults to `"admin"`** (`:46`) for whoever is looking; `App.tsx:113` stops calling it and passes NW-01's `role` field into `WorkspaceShellApp`. **The SPA sends no role header, ever** — concurs with the security seat's demotion, and verified independently: `Grep` for `X-Role|X-Workspace-Role` over `web/src` returns **zero**, so the commitment costs nothing today and is purely a refusal to regress. **The parse rule is the item, not a detail.** The wire vocabulary is **wider than the nav's** — `memberViewModel.ts:9-11` records that the backend also accepts **Legal / Finance / ReadOnly** while `navItems.ts:29` models two roles and NW-54 is deferred — and **neither ADR-025 nor ADR-026 mentions those three** (`Grep` → no matches), so the `role` enum is unspecified and the client must not assume two values. `parseWorkspaceRole(wire)` maps any unmodelled role to the **least-privileged** modelled role and **never to `admin`**: carrying today's default forward would re-introduce this exact bug for Legal/Finance/ReadOnly members while fixing it for Procurement. It lives in `workspaceRole.ts` — the file NW-14 already rewrites — **not** in `navItems.ts`, which keeps NW-09 as its single writer; `WORKSPACE_ROLE_LABEL` (`navItems.ts:32-35`) already maps wire→label and is untouched. `RequireRole` (`WorkspaceShellApp.tsx:67-74`) and `canManageMembers` keep their exact shape; only provenance changes. The role decides which affordances render and **never what is permitted** — the 403 is the authority, the button state is a courtesy. e2e **N9**: (1) the creator uploads then deletes a document → the row disappears (204, not the `.hint` alert); (2) a Procurement member of the same workspace sees no Delete affordance and a direct API call still 403s. **Ordering correction to this seat's own draft**: half 2 needs a Procurement *membership row*, which `InviteAsync` writes today for free — but **NW-58 moves membership to accept time**, so once that lands half 2 requires the full accept flow. The decomposer must therefore either run NW-14's proof task **before** NW-58's invite change or write half 2 against N3b's accepted member; the chain **NW-02 → NW-01 → NW-14** holds only while `InviteAsync` still grants on invite | **SEC**: `amend ADR-022` — footer written, five clauses + the retirement table. ADR-022's Decision outcome and `Status: accepted` are untouched; the footer **narrows** the interim, granting nothing new. **CLIENT**: `amend ADR-012` — footer written, clause 5 (the client's role is presentation-only, server-derived, and defaults to least privilege). The authorization half is ADR-022's and this seat concurs without writing it | `reports/architecture/ADR-022-day1-demo-auth-and-fixture.md`, `reports/architecture/ADR-025-workspace-membership-and-invitations.md`, `reports/architecture/ADR-012-web-stack.md` |
| NW-09 | software-architect, client-architect | **SA**: **no new definition is invented — the canonical one already exists on the server.** `Contract.Status` is free text bootstrapped to the literal `"processing"` (`Contract.cs:22`, `StagedExtractionService.cs:157`) and can never define "validated"; the real rule is *a contract with ≥1 linked document in `ProcessingStatus.Completed`* (`PortfolioQueryService.cs:191`), already served as `contractsAnalyzedCount` on `GET /api/savings/kpis` (`SavingsKpiEndpointExtensions.cs:93`), which no UI consumes. That number becomes **a field on NW-01's row**, computed inside each tenant's own scope — not a second round-trip. **Composed in the host**: `Raffa.Identity.Workspace` may reference only `SharedKernel` (`DependencyDirectionTests.cs:62` — enforced, not preferred), so `Raffa.Api` joins the directory service and the Documents.Contracts query service, precedent `SavingsKpiEndpointExtensions.cs:73-88`. **A SharedKernel port is explicitly rejected.** The count must be a real SQL `CountAsync` over distinct contract ids — there is none over `Contracts` today (`PortfolioQueryService.cs:153,183-200` materialise) and this task must not copy that pattern — **CLIENT**: `WorkspacePickerScreen.tsx:82` stops writing `contractCount: 0` — it stops writing a `WorkspaceSummary` at all (NW-01) — and `:173` renders the server field. **This is shell-wide, not picker-local, and that corrects this seat's own revision-1 draft**: `useValidatedContractCount.ts` lives in `components/shell/`, not `routes/documents/`, and has **four** consumers — `AppShell.tsx:29` feeding the rail badge (`:39`), the `GlobalAskBar` (`:43`) and the `Outlet` context (`:48`), plus `routes/ask/index.tsx:93` independently. It also admits its own defect: `getPortfolio(workspace.id, { pageSize: 100 })` counted client-side (`:60`, `:41-43`) and capped at 100 by its own comment (`:35-39`, "a tenant with more validated contracts than this undercounts here"). So w14 **replaces the number and keeps the verdict**: the hook reads NW-01's field — which honours `AppShell.tsx:45-47`'s own promise that "a screen never has to re-fetch the portfolio for a second opinion", so picker and rail cannot disagree (N8's "rail matches") — while `kbReady` (`:67`, `count > 0`) stays computed where it is, because `ask/index.tsx`'s `!kbReady` early return is **NW-56's bug, queued to W18**, and reaching into a queued item's file buys no acceptance here. **Scope fence**: only the *secondary tier* count (`navItems.ts:128-139`, `buildSecondaryNavItems`); the **Documents** badge (`getDocumentsBadge`, `:99-107`) is **NW-10, W16** — `navItems.ts:85-88` already names NW-10 in its own doc comment. Both badges live in one file and only one is in scope. **ADR-026 §D2 assigns this seat the removal of the client-side heuristic, and the answer is bounded**: `isValidatedContractStatus` (`routes/contracts/contractStatus.ts:17`) has three consumers, and `portfolioViewModel.ts:38` still needs it to filter a screen no w14 item touches. So in w14 it **stops being a count definition** (`useValidatedContractCount.ts:42` stops filtering) and survives only as a row-level display/filter helper; the file is deleted by the item that gives the portfolio a server-side filter. **The two must never both produce a number** — one server definition, or the picker, the rail and `/documents` will disagree again by a different route. e2e **N8**: after the existing upload step, return to `/signin` and assert `.workspace-row-meta` does **not** match `/^0 contracts/`, then assert the rail's secondary badge shows the same number — two assertions, one number | **SA**: `none` — no new decision; the definition is already in code and the field is recorded in `ADR-026` §D2. **CLIENT**: `amend ADR-012` — footer written, clause 7 (a count has one definition and it is the server's; the client predicate stops being one). Screen-1 copy ("N validated contracts · CHF · eu-west") is ADR-020's and the UX seat's | `reports/architecture/ADR-026-workspace-api-and-invitation-schema.md`, `reports/architecture/ADR-012-web-stack.md` |
| NW-04 | software-architect, security-architect, client-architect | **SA**: `GET /api/workspaces/{tenantId}/members` → `{ members: [ { id, email, name?, role, status } ] }`. **The roster is live memberships ∪ live invitations — never a scan of `workspace_user`.** *This corrects this seat's own lane draft*, which derived `status` from `ExternalSubjectId is null` and would have rendered a **removed** member as `Active` (their user row is kept for audit and their subject stays bound). `status` is derived, never stored: membership → `Active`; no membership + live invitation → `Invited`. `name` maps to the existing nullable `WorkspaceUser.DisplayName` (`:28`) and is **never derived from the email** (`MembersTable.tsx:13-16`). A person with two memberships appears **once** at the highest role, reusing `WorkspaceRoleResolver.cs:101-103,116-118` precedence. `invitedAt` dropped — no consumer. Reuses the existing join at `WorkspaceRoleResolver.cs:104-114`. No migration — **SEC** (answers **OQ-w14-005**): **any live member may read the roster**; only an **Admin** may invite, revoke or remove — read ≠ write. The design shows the roster to Procurement (`screens-v2.md` §10) and a team of five cannot operate if only Admins can see who is in the workspace. A **non-member gets 404** — never 403 (a tenant-existence oracle) and **never an empty 200**, which is also an oracle ("that tenant exists and is empty"). **Verify, then scope, then read**: the route tenant is entered as a scope *after* the caller's membership in it is verified — "scope and see what comes back" converts an authorization question into an empty-result question and yields 200-with-nothing where the answer must be 404. Privacy: email addresses are personal data returned **only** to members of that tenant; no endpoint in this wave exposes an email to a non-member, and the pre-accept read exposes none at all. `status` is **derived, never stored**, so the roster cannot disagree with the grant it describes. No new RLS: the join is over three already-policied tables (`identity-workspace.sql:154-158`, `:165-169`, `:176-180`). The `identity_self` policy cannot leak here — it is `FOR SELECT` on `workspace_user` matching only the **caller's own** rows, so at worst a caller sees their own address in a tenant they are not scoped to, which the 404 prevents them from reaching — **CLIENT**: `memberStore.ts` is **deleted in full** (`loadWorkspaceMembers`, `rememberInvitedMember`, `MEMBERS_KEY_PREFIX`, `WorkspaceMemberRow`); `MembersRoute` (`index.tsx:38`) fetches the roster instead of seeding from `sessionStorage`, and the optimistic append after a successful invite (`:67-75`) becomes a **re-read**. `loadWorkspaceMembers` **writes on read** (`:70`) — a "load" that mutates storage — and deleting it removes that surprise with it. Two invented facts go too, and neither is back-filled client-side: `lastActiveAt: new Date().toISOString()` (`:62-68`, "last active = now", always, for whoever is looking — if the server has no such column, `MembersTable` **drops the column**) and the hardcoded self-row `role: "Admin"` (`:65`), the same class of client-side inference as `roleLabel`. `MemberStatus` moves into the generated schema; `memberViewModel`'s `getMemberStatusTag` (`:91-93`) and `memberRoleLabel` (`:84-88`) are pure and survive untouched, which is why this swap is small. **One correction to ADR-026 §D3, in this seat's lane**: its header line reads `X-Tenant-Id` + `X-User-Id`, but the route already carries `{tenantId}`, and the **sibling invite operation on the same route prefix takes the tenant from the route, not the header** — `client.ts:918` states it while quoting that operation's own OpenAPI description, and the ADR-022 w14 footer raises it to a rule ("`X-Tenant-Id` is not an input to a membership route"). Headers are attached **per method** in `client.ts`, never by a global wrapper, and methods already omit it deliberately (`:1139`, `:1147`), so **the client sends `X-User-Id` only** on this route. Sending both would let one caller present two candidate tenants for one operation — the ambiguity the header was demoted for — and would have the members screen sending the tenant two different ways to two endpoints on one prefix. Per the INDEX's own rule (where ADR-025 and ADR-026 touch one mechanism, ADR-025 governs) the ADR-022 footer settles it; **one word in §D3, no other clause moves**. e2e **N3**: `day1.spec.ts:134-147` already invites and asserts the email (`:146`) and "Invited" (`:147`) — both pass on `sessionStorage` alone today, which is the defect. The new assertion is **one line longer and completely different in meaning**: `await page.reload()` between the click (`:145`) and the assertions. A reload-surviving roster is the whole of the acceptance | **SA**: `none` — the read model is `ADR-026` §D3; authorization and the **404-not-403** for a non-member are security-architect's (ADR-025 §B), adopted here. **SEC**: `amend ADR-009` — footer written, clause 7 (verify-then-scope, 404 for a non-member); `new ADR-025` §B/§D.4. **CLIENT**: `none` for the route map and the guard — `/workspace/members` keeps its `RequireRole` wrapper and its two-column body; the persistence rule and the header rule are the ADR-012 footer's clauses 1 and 4, already written for NW-01. Not every involved seat needs an ADR row: once the rule is written down once, this item is a task | `reports/architecture/ADR-026-workspace-api-and-invitation-schema.md`, `reports/architecture/ADR-009-tenancy.md`, `reports/architecture/ADR-025-workspace-membership-and-invitations.md`, `reports/architecture/ADR-012-web-stack.md` |
| NW-58 | product-owner, software-architect, security-architect, cloud-architect, client-architect, ux-ui-designer, delivery-manager | **PO**: the lifecycle is IN and stays `must` — token, accept, sign in, join *that* workspace, Admin removal, re-invite; a **mail transport is deferred** (P1 per spec §13.4/§16.1, excluded from the pilot by `percorso-pilota-v1.md` §2/§5/§7) and is **not** a §1.2 non-goal, so w14 delivers a copyable single-use accept link behind `IInvitationMailer`; the UI must not say "Invitation sent."; an unaccepted invitation grants nothing; the last Admin cannot be removed; the Admin gate is server-side from the membership row. Acceptance = N3b-1…N3b-8. NW-58 is **not cuttable** — it is the only item closing a §20 Day-1 sentence. *(mechanism, storage, routes, copy and CI: other seats append below)* — **SA**: **membership moves to accept time.** `InviteAsync` today writes a live `workspace_membership` at invite time (`WorkspaceMembershipService.cs:98-105`), so once NW-01 keys discovery on identity an invitee would hold full access **without ever clicking the link**, and any address an Admin typos becomes a member. Invite now writes `workspace_user` (**no** membership) + the `workspace_invitation` row; membership is written **only at accept**. *This seat adopts the security seat's F5 shape over its own lane draft*, which said invite writes the invitation row alone — that would have made `WorkspaceSignIn.ResolveSignedInUser` fail on **every** accept (`WorkspaceSignIn.cs:36-41`, "sign-in cannot provision a new workspace user") **and** contradicted this seat's own NW-04 row, which needs the user row to render `Invited`. Token is **`{tenantId:N}.{secret}`**: accept splits, scopes, then matches `token_hash` **inside** the scope — so accept performs **no unscoped read at all**, removing the one ADR-025 §5.5 calls unavoidable. Token travels in an `X-Invitation-Token` **header**, never a path or query string, so the two `/api/invites` routes are not token-parameterised. New ordinary tenant-scoped table `workspace_invitation` with its RLS policy **in the same migration** (`TenantRlsDeployableScriptCheckTests` fails the build otherwise). Endpoints: `POST …/invites` (changed), `GET /api/invites`, `POST /api/invites/accept`, `DELETE …/invites/{id}`, `DELETE …/members/{membershipId}`. Last-Admin guard is a **pure domain function** (`WorkspaceMembershipRemoval.CanRemove`) provable without a database; removal deletes the **membership**, never the user (the FK is `ON DELETE CASCADE` from user to membership, `WorkspaceMembershipConfiguration.cs:30-33`). `IInvitationMailer.TrySendAsync` + **`mailDelivered` on the 201** make the UI's "must not say sent" a **server fact**, and make the wave land whole whether or not a transport ships — **CA**: **w14 ships zero new Azure resources, zero Terraform change, zero new runtime config keys and zero new Key Vault secrets, in both environments**; the mail transport is **decided and deferred** (ACS Email + Azure Managed Domain, recorded in the ADR-005 w14 footer so the transport wave *applies* a design instead of re-opening one). *This seat concurs with the PO's deferral and withdraws its own lane proposal (§1.1/§1.9) to land ACS inside w14*: ADR-026 §D6's `NullInvitationMailer` + `mailDelivered: false` makes "must not say sent" a **server fact in code**, which is exactly what this seat's `invitation_mail_enabled` flag would have done **in config** — at the price of an operator-confirmed HCP apply mid-wave, for behaviour observationally identical on `dev` at acceptance. Two contract properties are what make the zero delta true, and both are **normative, not preferences**: **(1) `acceptUrl` on the invite 201 is site-relative** (`/invite/accept#<token>`), resolved by the SPA against `window.location.origin`. An absolute URL forces a new `Invitations__AcceptUrlBase` env var onto the API app, and `backend.yml` deploys with `az containerapp update --image` **only** (`:198-201`, `:205-208`), never `--set-env-vars` — so that key can arrive **only** through Terraform plus an HCP apply a human confirms in the UI (`infra.yml:104-135`). That is precisely the mid-wave human-gated blocker the PO objects to, arriving through the *contract* rather than through the transport. The API has no notion of the web origin: `var.spa_host_name` reaches the module but feeds **only** the ingress CORS block (`containerapps/main.tf:189`), and `backend/src` greps clean for `SpaHostName\|WebBaseUrl\|BaseUrl\|AcceptUrl`. **The absolute accept URL is a property of the mailer** (which has no browser to resolve a relative path against), **not of the invitation**; `new URL(acceptUrl, window.location.origin)` accepts both shapes, so the client needs **no change** when the transport wave makes it absolute. **(2) No server-held invitation key in w14** — ADR-026 §D4's CSPRNG-and-hashed token needs none, because verification is a hash lookup against a row that must exist anyway (single-use, expiry, revocation). *This seat withdraws its own lane proposal of `Invitations__TokenSigningKey`*: an HMAC would buy stateless verification this design cannot use, while costing a Key Vault secret, a `random_bytes` resource, an env var (hence Terraform + apply), a per-env rotation story, and the property that **rotation invalidates every outstanding invitation**. Cost line: **$0.00 delta; no new fixed monthly line in either environment** — **SEC**: **the invite endpoint has no authorization at all today and this wave is what makes that exploitable.** `WorkspaceEndpointExtensions.cs:59-97` takes a route `tenantId`, a body, the service and a `CancellationToken` — no `HttpContext`, no resolver, no claim, no membership check; `MapPost` `:32` attaches no authorization metadata and the host wires no auth middleware (`WorkspacePrincipalAuthorization.cs:14-17`). The role comes from the **request body** (`:75`) with `Admin` accepted (`:78`), and `InviteAsync` writes a live membership (`:98-105`). So **anyone who can reach the API and knows or guesses a tenant GUID can grant themselves `Admin` over another customer's contracts.** It has been survivable only because discovery is a `localStorage` array in one browser (`workspaceStore.ts:32,88-106`), making the row inert — **NW-01/NW-02 are exactly what make it live**, so the exposure is created *by* this wave's success and **cannot be deferred to "the security wave"**. `POST …/invites` now requires identity (**401**) → live membership in the **route** tenant (**404**) → `Admin` (**403**). *If the decomposer cannot fit NW-58 whole, this guard still ships as its own task.* An Admin may invite another Admin; what is forbidden is a non-Admin reaching the endpoint and any **self**-assignment. **Token — this seat ratifies ADR-026 §D4's `{tenantId:N}.{secret}` and corrects its own lane draft**, which called a hash→tenant lookup "the one read that must happen before a tenant scope exists": under the policy this seat specified in the same section that read is not merely avoidable, it is **impossible** — `workspace_invitation` carries `FORCE ROW LEVEL SECURITY` and one `tenant_isolation` policy, so an unscoped `SELECT … WHERE token_hash = …` returns **zero rows**, and serving it would have needed a third policy widening the invitation table or `BYPASSRLS` (forbidden). The prefixed token removes the read entirely. **Ratified under two normative bounds that are the exception's justification, not decoration**: (C4) the prefix is `Guid.TryParseExact(…, "N")`-parsed **before** it reaches `BeginScope` and a failure is **404**, never a scope — the type system already enforces it (`ITenantContext.BeginScope(TenantId)`, `ITenantContext.cs:26`; `readonly record struct TenantId(Guid Value)`, `TenantId.cs:7`) and **no string-taking overload may be added**, because the prefix is the first caller-controlled value ever to reach `BuildSetCommandText`, which interpolates and justifies that *solely* by the Guid invariant; and (C5) inside that caller-named scope the **token-hash match is the first statement** — nothing is read until it succeeds, or pre-accept becomes a workspace-name oracle for any guessed tenant id, handing back through the front door what the token shape just closed. Token is **256-bit CSPRNG, stored only as a SHA-256 hash** (a database read cannot mint an acceptance), 7-day absolute expiry, single use, **no constant-time compare** (index seek, no secret-to-secret comparison — recorded so nobody "hardens" it into a table scan), and **no server-held key, in w14 or after** — which is the condition cloud-architect's zero-delta rests on, and it holds. **Accept**: identity required (401); the signed-in email must match the invited address case-insensitively (**403**, reason never echoing the address) or a forwarded link is an open door into a customer tenant; on success one transaction binds the subject via the already-written `LinkSignInAsync:118-145` (**unchanged**), inserts the membership and stamps `accepted_at`. Concurrency is **already schema-enforced** — `ix_workspace_membership_workspace_user_id_workspace_role_id` (`identity-workspace.sql:90`) means two concurrent accepts produce one row and one unique violation, so **the real requirement is translating it to 409, not 500**. Pre-accept returns **workspace name, role, expiry — and nothing else**, explicitly **not the invited email** (echoing it turns a leaked link into an address-discovery tool). **Removal**: identity → membership → Admin → **last-Admin guard (409)**, an invariant of "at least one live Admin per tenant, always", as a **pure domain function** provable without a database; it **revokes that email's live invitations in the same transaction**, without which a still-valid link re-admits them and NW-58's own "re-adding needs a new invite" is false; and it is **immediate because nothing caches authorization** — role and membership are read from the database on every request, so there is no session and no token to revoke. **Ask closes with it** (ADR-011): the tenant used for retrieval becomes a *verified membership* fact rather than a client-asserted `X-Tenant-Id`, and "removed but Ask still answers about its contracts" gets its own test. **Nine audit actions, no schema change**; the token, its hash and raw identity headers are **never** written to an audit row or a log — **CLIENT**: **one new public route, `/invite/accept`** — the only route-map change in this wave, verified absent from ADR-018 `:89-102` (headed "Route map (**locked**)") and from `ia-v2.md:44-56`. It is reachable **signed out and with no workspace** and renders **outside `AppShell`** (an invitee is not yet a member of anything the rail could list). That state is exactly the one in which **no router exists today**: every route lives inside `WorkspaceShellApp` and `BrowserRouter` mounts only once `account && workspace` are both true (`App.tsx:91,108`; `WorkspaceShellApp.tsx:81-87`), so the accept route cannot be added to `ShellRoutes` — **`BrowserRouter` hoists into `App.tsx`** with a public branch and `*` falling through to the existing gate (`ShellRoutes` is already exported at `:41` as a testing seam, so this is supported, not a rewrite). It shares its twenty lines with NW-03's resolving state → **one task or strictly sequenced**, and it falsifies `WorkspacePickerScreen.tsx:117-123`'s comment ("no router is mounted anywhere above this component"), which justifies the hard `<a href="/">` at `:124`; the hoisting task fixes that comment. **The token is never persisted client-side — and this corrects this seat's own lane draft**, which proposed one `sessionStorage` key and argued it was transient flow state. ADR-025 Rule C10 forbids any client store, and the mechanism would not have worked anyway: MSAL here is **redirect-only** with a single configured `redirectUri` (`msalConfig.ts:19-33`, `cacheLocation: sessionStorage`; `loginRedirect` at `routes/signin/index.tsx:34`), so a signed-out invitee does **not** come back to the accept route holding anything. Realization (Rules C9/C10 are the security seat's; the route parsing is this seat's, as they recorded): read the **fragment** on mount, hold it in **memory for one mount**, clear the address bar, send it only in `X-Invitation-Token`. **MSAL's `state` parameter is explicitly rejected** — it round-trips through the identity provider, so it would place the token in an Entra authorize URL's query string and logs: strictly worse than the store it replaces, and the obvious shortcut a task would otherwise reach for. Therefore the **guaranteed w14 flow is: sign in, then open the invitation link again**, and `loginPopup` is the one sanctioned way to make it single-click, because the page is never unloaded and memory survives; the popup-blocked and reload cases share one state, so that state ships either way. **A reload or a sign-out loses a live token by design** (the address bar was cleared), so the copy must be "**open your invitation link again**", never "this invitation is invalid" — the latter sends a user back to their Admin for a replacement they do not need. **`mailDelivered` adopted over this seat's own `delivery: "sent" | "link-only"` discriminator**: one boolean, ADR-026 §D6 owns the name, and the client **never infers "sent" from a 201** — today `InvitePane.tsx:101-105` renders `INVITATION_SENT_MESSAGE` whenever `sent && error === null` and `index.tsx:78` sets `sent` unconditionally on a 201, which is the defect NW-58 must #1 names. The pane's prop becomes the server's boolean; UX owns the two strings, this seat owns only the fact that there are two. **The client composes the copyable link** with `new URL(acceptUrl, window.location.origin)`, which accepts both shapes — so cloud-architect's site-relative `acceptUrl` needs **no client change** when the transport wave makes it absolute. Accept response corrected to §D5's `{ workspaceId, workspaceName, role }` (this seat's draft assumed `{ tenantId, name }`) and treated as a **hint**: after accept the client re-resolves the list and enters, so membership is proven by NW-01 rather than trusted from the accept body. **Two destructive affordances, not one** — this seat's draft had only remove: revoke an `Invited` row (`DELETE …/invites/{id}`) and remove an `Active` one (`DELETE …/members/{membershipId}`), each re-reading the roster, with **no optimistic local removal** because the last-Admin 409 is a server rule. Generated client: `getInvitation(token)` and `acceptInvitation(token)` are **hand-written wrappers** — headers and request bodies are not generated (constraint 4) — and never `{token}` in a path. e2e **N3b**: a two-account spec — Admin invites → a second browser context opens the link → signs in → lands in **that** workspace, not a create form → Admin removes them → the removed account's next load loses access (NW-03's revalidation, no new endpoint). It needs a **second Entra account on the same email domain**, because `validateInviteEmail` (`memberViewModel.ts:69-82`) rejects a cross-domain invite **client-side before any request**, deriving the domain from the signed-in Admin's own address (`:63-67`) — the reason `day1.spec.ts:139-142` already builds the invite address that way. Until that account exists the spec is **authored and skipped with a named reason** (`v2.spec.ts:107-112`'s own pattern), never a silent gap — **UX**: the invite result renders the server's `mailDelivered` fact in **two** strings, and this seat **adopts the boolean over its own lane draft's three-outcome discriminator**: `true` → "Invitation sent to {email}." (the export's sentence plus the address, so a typo is catchable at the moment it is made); `false` → "Invitation ready for {email}." with the copyable single-use link, its expiry and a Copy link button, and the word "sent" **nowhere on the screen**. **The `false` copy is deliberately written to be true for both of its causes**: one boolean cannot separate "no transport configured" from "transport errored", so the copy states what is certainly true (the invitation exists, here is the link) and never diagnoses the mailer — in w14 the question is moot because `mailDelivered` is `false` by construction, but the string must not become a lie the day a transport ships. The delivery-**failure** copy is therefore **pre-decided here for the transport wave** so it is not re-opened ("Invitation created, but the email could not be sent." + a **"Try sending again"** secondary, and **never** "invite again" — `WorkspaceMembershipService.cs:93-96` rejects a repeat invite at the same role, which is the state everyone forgets); that wave needs a **third value, not a second boolean**. The sweep must catch **`index.tsx:63` as well** — the *failure* path also claims a mail ("The invitation could not be sent."), not only `INVITATION_SENT_MESSAGE` (`memberViewModel.ts:39`, rendered at `InvitePane.tsx:101-105` off `index.tsx:78`'s unconditional `setSent(true)` on a 201). **The workspace-domain rule is demoted from a block to a warning, and this reverses this seat's own lane draft.** That draft said "keep the rule — it is a real safeguard"; **ADR-001's w14 footer defers the restriction to the wave that lands ADR-010** (`INDEX.md:118-119`), and the draft's own justification does not survive the security seat's framing: `validateInviteEmail` (`memberViewModel.ts:69-82`) runs **client-side, before any request**, and the server has no domain rule at all — so it is a **typo guard, never a safeguard**, and a direct API call bypasses it entirely. Its "workspace domain" is also a proxy read off whoever happens to be signed in (`index.tsx:45`; the code names the gap itself at `:64-67`) — far too imprecise to **gate** an invitation, and perfectly adequate to **inform** one. So a mismatch renders a non-blocking warning under the field — "{email} is outside {domain}. They will get full {role} access to this workspace." — with submit enabled; format errors (`:71-72`) stay **blocking**, because a malformed address is not an address. Two consequences: this seat **withdraws its lane draft's ask for a `domain` column** on NW-24 (a warning on a proxy is fine, and NW-24 is the first item to cut), and NW-58's own acceptance stops depending on it — a hard block makes **N3b unverifiable** whenever the second Entra account sits on another domain, which is exactly the product-owner's standing objection. Status set is **`Active` · `Invited` · `Expired`**, treatments in the ADR-019 footer; `Expired` is the only new mapping and it is *derived* from this system's existing "needs your decision" treatment, never a new colour. **Two destructive affordances, not one — correcting this seat's lane draft, which had only Remove** (client-architect's ADR-018 footer clause 5 is adopted): an `Invited` row is **revoked** and an `Active` row is **removed**, and they take **different consequence copy** because they are different facts — revoke says "Their link stops working. **They never had access to this workspace.**" and must **not** say "they lose access", since an invitation was never a grant (ADR-025) and telling an Admin otherwise misrepresents what they just did. Both are Admin-only in a fourth `Actions` column the export does not have (`markup.html:388` is three columns), gated on the **server** role, confirmed **inline in the row** rather than in a dialog (`--shadow-*` is "dialogs only", ADR-019 `:78`, and the locked catalogue has no dialog), with the self-removal variant reusing the "You" marker the table already renders (`MembersTable.tsx:35,40`). The **last Admin's Remove is a visibly disabled control with a `.hint`** ("This is the last Workspace Admin. Invite another Workspace Admin first." — "invite", not "promote", because no role-change affordance exists in w14), never a hidden control and never a click that 409s. **Two traps that live in this record's own design-ref row are corrected** (`:56`, rewritten below): there is **no member name** — `MembersTable.tsx:39` renders the **email** as the primary line and `:13-16` documents that a name is deliberately **never derived from the address**, so a task "aligning to `name · email · role · status`" would fabricate one; and the Admin role summary is **not** `markup.html:395`'s "Also uploads, deletes, manages members" but `memberViewModel.ts:30-33`'s D8 / R-WEB-07 wording, so copying the export **regresses an accepted decision**. Procurement sees the roster **read-only**, which resolves a contradiction *inside the design oracle* rather than overriding it (ADR-018 `:107-108` and `ia-v2.md:13-15` say read-only; `markup.html:403` hides the table outright), and `Request access` becomes a real `mailto:` to the workspace's now-knowable Admin addresses — **a dead button is the one option not available**. The roster is a list surface, so ADR-018 `:112-119` binds it: skeleton, and error + Retry that **never** renders the last known roster. **Screen 11 (`/invite/accept`) is specified in ten states**, of which three are decisions rather than copy: "**open your invitation link again**" is a **normal outcome, not an error** (the token lives in memory for one mount and the address bar is cleared, so any redirect sign-in or reload reaches it — wording it "invalid" sends a user back to their Admin for a replacement they do not need, and invitations are single-use so that "fix" costs a real one); the wrong-account state **cannot echo the invited address** — ADR-025 makes the 403's reason non-echoing and the pre-accept payload does not carry it, **correcting this seat's lane draft**, which wrote "This invitation was sent to {invited email}"; and the pre-accept block shows the workspace name and offered role **and nothing else**, because adding "3 validated contracts" for warmth would leak tenant data to anyone holding a URL. Expired and revoked share a first sentence so the screen never tells a prober which case they hit, and a 503 on the token lookup gets the ADR-018 error treatment that terminal informational states do not satisfy. Six Claude Design exports are owed and recorded; **none is blocking** — every state is decided from ADR-019's locked catalogue and the export's own idiom — **DM**: **zero Terraform delta confirmed reciprocally with cloud-architect (C4), and Option A is now realized as `/invite/accept#<token>` — site-relative (C2) *and* fragment (Rule C9) — which keeps the delta at zero rather than merely near it.** Verified from this seat's own side that the new public route costs **no deploy-config change**: `web/public/staticwebapp.config.json:2-5` rewrites every non-asset path to `/index.html` and excludes only `/config.json`, `/staticwebapp.config.json` and `/assets/*`, so `/invite/accept` is served with no `routes` entry. The two shortcuts are named as **drift** in the ADR-016 footer so no task reaches for them: `--set-env-vars` in `backend.yml` (which deploys `--image` only, `:196-208`) and folding a value into the one `dynamic "env"` block (`containerapps/main.tf:165-174`, bound by ADR-004's amendment to `var.ai_gateway_model_env`) are both reverted by the next HCP apply. **The promotion blocker this seat owns, and nobody else would catch.** Once NW-01 lists by membership, the ADR-022 fixture tenant on `demo` is seeded by **SQL** (`demo-fixture-seed.sql:60-67`, run by `seed-demo-fixture.yml:132-135`), not by `POST /api/workspaces`, so it can **never** receive a membership row from NW-02's new code path. Proven twice: the seed inserts `workspace` + `contract` + three `savings_opportunity` rows and nothing else, and a repo-wide grep for `workspace_membership|workspace_user` across every `*.sql`/`*.yml`/`*.yaml` returns **exactly one file** — `identity-workspace.sql`, the migration that *creates* the tables. **Nothing anywhere seeds a membership row.** Promote w14 as-is and the demo user signs in, `GET /api/workspaces` returns `[]`, and the stakeholder-facing environment offers "create a workspace". The same holds for every workspace already created on `dev`. This is **mandatory however the table rules on anything else**, because the fixture tenant has no interactive creator to claim it. **Two deliverables, both this seat's, both zero infra delta (C4b).** (1) **Seed** — extend `demo-fixture-seed.sql` with an idempotent Admin `workspace_user` + `workspace_membership` for the fixture tenant, following the file's own conventions, which are stricter than "append an INSERT": its header (`:33-43`) is a **registry of fixed well-known ids** (`…0001` tenant, `…0002` contract, `…0011/12/13` opportunities), so the two new rows take documented ids (`…0003`, `…0004` are next free) and **never** `gen_random_uuid()`; `ON CONFLICT (id) DO NOTHING` inside the existing `BEGIN;`/`COMMIT;` (`:55`, `:148`); RLS stays on, the script setting `app.tenant_id` at `:53` and never touching a role, GRANT or policy (its own AC-4 note, `:48-52`). (2) **Backfill** — one new operator workflow `backfill-workspace-membership.yml` modelled line-for-line on `seed-demo-fixture.yml`: `workflow_dispatch` with `target_environment` as a **typed `choice`** (`:15-22`) plus `workflow_call`; `environment: ${{ inputs.target_environment }}` so `demo` inherits its required reviewers (`:49-53`); OIDC via `./.github/actions/azure-login` (`:65-70`); Key Vault `postgres-connection` (`:86-118`); a **precondition guard** in the `:120-130` style; an idempotent `psql` insert; and a closing verification that **fails if zero** rather than merely printing (`:141-149`) — a backfill that silently inserts nothing is the failure mode worth catching. **No new identity and no new federated credential**: the per-env deploy SP already holds Key Vault Secrets User (`seed-demo-fixture.yml:60-64`, verbatim). *Inherited caveat, not a new one*: that role is granted **out of band, not by Terraform** (`backend.yml:251`, `infra/README.md` "Known gaps"), so this job fails exactly where `backend.yml` and `seed-demo-fixture.yml` already would. **`seed-demo-fixture.yml` needs a one-line change after all** — its guard (`:123-130`) exists precisely to give "an honest failure instead of a confusing 'relation does not exist'" (`:120-122`) and must gain `workspace_membership`, or seeding a `demo` whose schema predates w14 fails obscurely mid-script. **So w14 touches exactly two workflow files, one new and one one-line**, and the final-integration task asserts that `git diff --stat origin/main -- .github/workflows` shows those two and nothing else. **The ordering correction this seat found at the table, which changes the phase plan.** ADR-025's OQ-sec-001 requires the invite-endpoint guard to **merge before NW-01**, because NW-01/NW-02 are what convert a dormant row into a working cross-tenant join path. This seat's lane plan put the whole NW-58 backend in phase 4, *after* NW-01 in phase 2 — **a direct violation**. It cannot be fixed by simply moving the guard earlier on its own, because a second constraint runs the other way: the guard resolves `Admin` **from the membership row**, so shipping it before NW-02 writes the creator's membership would leave **no caller able to invite at all**. Both constraints are satisfied by exactly one placement, at zero extra task cost: **the guard folds into the phase-1 task that already owns `WorkspaceEndpointExtensions.cs`** (NW-02's creator-membership task). One file, one phase, one owner, two small edits that are both about who may do what — which is also the single-writer-correct answer for a file four items want to write | **PO**: `amend ADR-001` — footer written, clauses 1–4 and 7. **SA**: `new ADR-026` §D4/§D5/§D6 — schema, endpoint contract and mailer seam; authorization, token strength and audit are security-architect's ADR-025. **CA**: `amend ADR-005` — footer written, "mail transport: decided, not applied"; w14 Azure delta zero, ACS rows + rejected alternatives + module/secret shape + config keys recorded for the transport wave. `amend ADR-006` — footer written, "global-only resource types are not a second region" (closes the undocumented Static Web Apps deviation, pre-decides ACS). **No new ADR from this seat**. **SEC**: **`new ADR-025 workspace-membership-and-invitations`** — written, `Status: accepted`, the authorization / token-strength / RLS-policy / audit half of the wave; plus `amend ADR-009` (clause 4, the accept path's caller-supplied scope as the second and last named exception; clause 8, the new tenant table), `amend ADR-011` (footer written — no Key Vault entry for the token, authz-before-retrieval strengthened, nine audit actions), `amend ADR-010` (footer written — the token carries identity only, forward-binding NW-05/NW-08). **CLIENT**: **`amend ADR-018`** — footer written: the locked route map gains `/invite/accept` (public, outside `AppShell`, signed-out reachable, fragment-carrying), the `BrowserRouter` hoist, the four accept-route failure states, and revoke-vs-remove on `/workspace/members`. It **adds to the epic-13 V2 footer and does not supersede it**. Plus `amend ADR-012` clauses 4 and 6 (headers; the token is never persisted client-side). **No new ADR from this seat** — it co-signs ADR-025/026 and writes neither. **UX**: `amend ADR-020` — footer written (screen 10's invite outcomes, domain warning, status set, two destructive affordances, last-Admin control, email-primary row, D8 summaries, Procurement variant; **new screen 11** = the accept flow in ten states); `amend ADR-019` — footer written (the member/invitation status rows, the disabled-CTA rule generalised, inline-not-dialog confirmation, one role vocabulary across two maps); `amend ADR-018` — **a second w14 footer beneath client-architect's, which it adds to and supersedes nothing**: theirs owns the route, the router hoist and the guard, this one owns the IA-level *states* that route creates plus the Procurement read-only reading. **DM**: `amend ADR-016` — footer written, seven clauses: w14 adds no per-environment key; the site-relative + fragment `acceptUrl` is *what keeps that true*, with the two shortcuts named as drift; a future mail transport's promotion ordering (apply `CURRENT` → `promote-backend` → `promote-web`) as a **HITL gate, never a `depends_on`**, because an infra change's effect is not observable by the wave that wrote it; **seeds and backfills are data-plane acts and are never promoted** (the body's isolation rule, `:79-80`, applied to this wave's new deliverable); exactly two workflow files change and the new route needs no deploy-config change; the standing `check_demo_swa_config.py` gate with its repo-root-`scripts/` path warning; and the w14 sequence (acceptance on deployed `dev` **before** `demo-v4`, four separate approvals, `seed-demo-fixture.yml` on `demo` only after `promote-backend` applies the schema its guard requires). Tag-triggered promotion is **not** re-opened. `amend ADR-014` is recorded in the W14-01 row. **`ADR-015`: `none` — already covers it**: the seed and backfill reuse `raffa-sp-<env>` and its existing Key Vault Secrets User role; no new OIDC subject claim, no new federated credential, no new stored secret. **No new ADR from this seat** — the delivery half of this wave is a wave-base rule and a promotion ordering, not a new mechanism | `reports/architecture/ADR-001-scope-r0-r4.md`, `reports/architecture/ADR-026-workspace-api-and-invitation-schema.md`, `reports/architecture/ADR-005-azure-skus.md`, `reports/architecture/ADR-006-region.md`, `reports/architecture/ADR-025-workspace-membership-and-invitations.md`, `reports/architecture/ADR-009-tenancy.md`, `reports/architecture/ADR-011-secrets-and-rag.md`, `reports/architecture/ADR-010-entra-oidc.md`, `reports/architecture/ADR-018-web-information-architecture.md`, `reports/architecture/ADR-012-web-stack.md`, `reports/architecture/ADR-019-web-design-system.md`, `reports/architecture/ADR-020-web-screen-inventory.md`, `reports/architecture/ADR-016-promotion-dev-demo.md` |
| NW-03 | client-architect | **CLIENT**: `raffa.signin.currentWorkspace` **survives as a session hint, demoted from source of truth** — `loadCurrentWorkspace`/`selectCurrentWorkspace`/`clearCurrentWorkspace` keep their signatures, and nothing trusts the value until it is checked against the server list. Resolution order on every mount once `GET /api/workspaces` resolves: empty → create form; **exactly one row → enter it, no picker** (`percorso-pilota-v1.md` §2 step 1, "Se ne ha uno, entra"); ≥2 with hint ∈ list → enter the hint; ≥2 with hint ∉ list or absent → picker. **`hint ∉ list` ⇒ discard the hint**: that is the client half of NW-58's removal requirement and it needs no endpoint, no polling and no cache invalidation — revalidation *is* the mechanism (ADR-025 Rule F.1d is the server half of the same rule). **The real change is the app's top-level gate, not the store.** `App.tsx:49` calls `loadCurrentWorkspace()` **synchronously during render** and `:91` branches on it immediately, so today `App` renders either `SignInRoute` or `WorkspaceShellApp` with nothing in between; making the workspace a server fact inserts an inherently async step, so `App` needs a third state — and **"resolving" must not flash the sign-in screen**, which would read as a logout on every reload. Two further consumers must read the one resolved value instead of calling `loadCurrentWorkspace()` themselves: `useValidatedContractCount.ts:54` (its effect keys on `workspace?.id`, `:76`) and `MembersRoute` (`index.tsx:36`). **No new endpoint** — NW-03 consumes NW-01's, so **NW-01 lands one phase earlier**; a web task consuming an endpoint that lands in its own phase is a broken chain. **Blocking e2e fact, not a risk**: `v2.spec.ts:68-79` pins the fixture tenant by writing this very key and calls it "the documented seam, not a mock" (`:78`); the seam **survives** NW-03 but only for a tenant the caller is a **member** of, and a repo-wide `Grep` finds `workspace_membership`/`workspace_user` in exactly one SQL/YAML file — the migration that creates them. `seed-demo-fixture.yml` greps clean for `invite|membership`. Nothing has ever made the e2e account a member, so **the V2 suite goes red at NW-03 unless a membership seed lands with it** (delivery-manager; OQ-w14-client-02 is a verified fact, not an assumption). e2e **N4**: reload with `sessionStorage` cleared and the MSAL account intact → the shell mounts on `/ask`, `.shell-rail-workspace-name` unchanged (`day1.spec.ts:132`'s own selector), no picker | **CLIENT**: `amend ADR-012` — footer written, clause 2 (the hint and its resolution order) with clause 1 (the persistence rule). **No new ADR** — the route map is untouched by this item | `reports/architecture/ADR-012-web-stack.md` |
| NW-24 | product-owner, software-architect, ux-ui-designer | **PO**: **reduced**. The create form asks `name` + `industry` + `country` and nothing else, industry and country as **closed lists** (`markup.html:55-56`); **currency and business region are derived from country and stored, never typed** (the prototype has no currency control; `CHF · eu-west` is pick-row *output*, `markup.html:63`); a workspace currency is a display default that **never** overrides a contract's own extracted currency. Priority `should`, lowest in the wave and **the first item to cut** if the 20-task cap binds. Acceptance = W14-A2-1…W14-A2-4. *(columns and migration: software-architect; form and pick-row copy: ux-ui-designer)* — **SA**: three columns on `workspace`, **all nullable**: `industry varchar(120)`, `country varchar(2)` (ISO 3166-1 alpha-2), `currency varchar(3)` (ISO 4217, **derived from country, never typed**). Nullable is deliberate: existing rows predate the columns and a NOT NULL column on a populated table needs a default that would be a fabricated business fact — precedent `quotes.sql:163,170,195,202`, trap documented at `QuoteLineConfiguration.cs:45-49` (EF backfills a NOT NULL enum-as-string with `""` the converter cannot parse). A missing value renders as **absent**, never as an invented `"CHF"`. The PO's guard is enforced as a **data rule**, not only a UI rule: a workspace currency never overrides a contract's own extracted currency. Migration `AddWorkspaceProfileColumns`; validation (2/3-letter uppercase) returns 400 through the existing `Result<T>` failure path (`WorkspaceProvisioningService.cs:35-38`), never a raw Postgres length error — **UX**: the create form adopts `markup.html:52-57` whole and in order — `Create your workspace` / "A workspace is your tenant. Contracts uploaded here never leave it." / **Company · Industry · Country** / `Create workspace` — replacing today's single field labelled "Workspace name" (`WorkspacePickerScreen.tsx:189-212`, label `:192`). **The label is "Company"; the field is `name`** — a task must not rename the field to match the label. The tenancy sentence at `:53` is kept deliberately: it is the only place in the product that tells a user their contracts stay in their tenant, and it is the copy that earns the first upload. Both lists ship **verbatim**: Industry is exactly five options ending in `Other` and **`Other` is the "not specified" case** — this **corrects this seat's own lane draft**, which specified a leading "Not specified" option that `markup.html:55` does not contain and which would have been an unnamed divergence, the one thing this seat may not do. Country is exactly Switzerland · Italy · Germany · Austria (`:56`), which makes the currency derivation **total over the export's own list** — so w14 needs no fallback branch and no "unknown country" state, and that is why no currency control ships. The derived value is echoed under the Country select as a `.micro-meta` line, **"Amounts are shown in CHF."**, so a wrong guess is catchable *before* the workspace exists. The pick-row meta line is a **segment list that degrades**: up to three segments joined by ` · `, and a null segment is **dropped** — never an empty gap, a dash or a placeholder — with the count segment carrying zero and singular forms (`No validated contracts yet` / `1 validated contract`). That is not a missing prototype pattern but an inconsistency *inside* the export: `app.jsx:160` already implements singular/plural for `askScope` while `app.jsx:138` hardcodes `seededCount: 3` and therefore never exercises 0 or 1 — and **every pre-existing `dev` workspace renders the zero form on day one**. Today the row renders **two** segments, not three (`WorkspacePickerScreen.tsx:173-174` appends `currencyRegion` as one pre-joined string), so splitting it into two real fields is part of this item, not a refactor a task may skip. **Divergence, named**: the third segment is the **business country name** (`Switzerland`), never the export's `eu-west` cloud-region slug — printing a region slug directly beneath "Contracts uploaded here never leave it" reads as a data-residency promise the product has not made, and contradicts ADR-006's `northeurope` besides. **The pick row also carries a role tag, and it currently lies — this is new, and it corrects `:57` of the design-ref table below.** `markup.html:62` is a two-cell grid whose second cell (`:64`) renders `{{ roleLabel }}`, already implemented at `WorkspacePickerScreen.tsx:177`; it is a **third frozen field** beside `contractCount: 0` — `:83` hardcodes `roleLabel: "Workspace Admin"` and `workspaceStore.ts:56-64` types it as that **string literal**, so the type system itself asserts "every workspace you can see is one where you are Admin". The moment NW-58 lands, an invited Procurement member signs in and screen 1 tells them they are a Workspace Admin: a false claim on the product's first screen after sign-in, produced by this wave's own feature. The tag renders the **server role from NW-01's row**, reusing the existing vocabulary with **pass-through for unmodelled roles** (ADR-019 w14 clause 5) so no new string is invented — and **if NW-24 is cut for the cap, this half must not be cut with it**: it is NW-01/NW-14 work that happens to land on a screen-1 surface. Screen 1's state inventory also grows beyond `screens-v2.md:21`'s idle · signing · create · pick, because w14 makes this the product's **first network read**: skeleton while loading, error + Retry that **never** falls back to a cached list, the **create form as the empty state** (there is no separate "you have no workspaces" screen), and a resolving state that **must not flash the sign-in screen** — that reads as a logout on every reload. One unrecorded divergence is named rather than left in the code: `WorkspacePickerScreen.tsx:100-137`'s "You're in {name}" interstitial exists nowhere in the export (`app.jsx:139` goes straight to Ask); whether it survives for a single-membership user is **NW-03's routing rule**, and ADR-020 records the real state set either way. This seat **withdraws its lane draft's request for a fourth `domain` column** here — see the NW-58 row: with the domain rule demoted to a warning, the existing proxy is adequate, and NW-24 is the wave's first item to cut | **PO**: `amend ADR-001` — same footer, clauses 5–6; `none` for ADR-006 (`eu-west` is a business region string, `northeurope` untouched). **SA**: `amend ADR-003` — footer written, clauses 1–5; this seat owns ADR-003 (`:5`). **UX**: `amend ADR-020` — the same w14 footer as NW-58, screen 1 section (create form and its two closed lists, the derived-currency line, the pick-row segment contract with its degraded / singular / zero forms, the server-role tag, and the full state inventory including the previously unrecorded interstitial); `none` for **ADR-019** — `.field > label`, `.input`, `.tag` and `.micro-meta` already cover every control on this screen and **no token or component is added**; `none` for **ADR-018** — `/signin` already carries create and pick, so no route changes for this item | `reports/architecture/ADR-001-scope-r0-r4.md`, `reports/architecture/ADR-003-data-store.md`, `reports/architecture/ADR-020-web-screen-inventory.md` |

`Decision`: one or two sentences, or `task only` when no seat is involved.
`ADR action`: `new ADR-NNN <slug>` | `amend ADR-NNN` | `supersede ADR-NNN by
ADR-NNN` | `none — <why>`. `File`: the ADR path written or amended.

## Open questions the table must answer

| OQ | Question | Seat(s) that own it | Assumption in force if unanswered |
|---|---|---|---|
| OQ-w14-001 | Which identity keys `workspace_membership` while ADR-010's JWT is queued for W15? | security-architect, software-architect | **ANSWERED (SEC, w14 table; software-architect concurred).** The interim `X-User-Id` (the MSAL account username the web already sends on every call, `client.ts:49-54`), resolved in **exactly one place** so NW-05 (W15) swaps it for the token subject by editing one file. The seam already half-exists: `WorkspaceRoleResolver.ResolveMembershipRoleAsync` (`:85-119`) matches `workspace_user.Email` **or** `ExternalSubjectId` (`:111`) — exactly the shape that survives the swap, since ADR-010's `sub`/`oid` lands in `ExternalSubjectId` via `LinkSignInAsync:118-145`. **Rule A2**: the interim identity is trusted for one thing only — *which membership rows to look up*. It confers no role, no tenant and no scope; any `X-User-Id` with no membership row for the target tenant is a non-member (404). **Rule A3**: retirement is NW-05, and the test is written now and activated then — with a validated token present the header must be **ignored entirely, not merely overridden** (token `A` + `X-User-Id: B` acts as `A`, never as `B`); precedence is not enough. After the wave, `Grep "X-User-Id"` in `backend/src` must find it in **one** file besides the document/conversation readers NW-05/NW-32 retire. `X-Role` is never the product answer — demoted this wave (NW-14 row) |
| OQ-w14-002 | Is a real email transport in scope for w14? (`percorso-pilota-v1.md` §2 excludes email from onboarding; spec §20 requires "invite Procurement users") | product-owner, cloud-architect | **ANSWERED (PO, w14 table).** The question conflates *transport* with *lifecycle*. The **lifecycle is in w14** (spec §16/§20); the **transport is deferred** — P1 in §13.4/§16.1, excluded from the pilot by §2/§5/§7 of `percorso-pilota-v1.md`, and explicitly **not** a §1.2 non-goal, so a later wave can pick it up without re-litigating. w14 = copyable single-use accept link behind `IInvitationMailer`, and the UI never claims a mail was sent. Recorded in ADR-001 clauses 2–3. **Not yet reconciled with cloud-architect** (they have not spoken at this table): their lane draft §1.1 lands an ACS Email transport *as infrastructure* inside w14, behind `invitation_mail_enabled` defaulting to `false` (§1.4), with a mandatory Terraform apply **before** the NW-58 backend task may ship (§1.9 step 2). This seat's counter, for their turn: by their own §1.9 step 4 the UI may not claim a mail was sent until a post-wave probe and flag flip, so on `dev` at acceptance time the two options are **observationally identical** — the transport buys no acceptance in w14 while spending cap on a P1 and inserting a human-gated HCP apply mid-wave (`infra.yml:104-135` never applies; a human confirms in the HCP UI). Ruling stands unless the table overrules it; if the table lands ACS in w14, NW-24 and NW-03 are cut to pay for it and this seat moves to `PROPOSE`. — **RECONCILED (CA, w14 table): the PO's ruling is accepted in full and this seat withdraws its lane draft's §1.1/§1.9.** The observational-identity argument is correct, and this seat supplies the mechanism that makes the deferral *free* rather than merely cheaper: ADR-026 §D6 turns "must not say sent" into a **server fact** (`mailDelivered`), which is what `invitation_mail_enabled` would have done in config at the price of an operator-confirmed HCP apply. **No cut is required — NW-24 and NW-03 are not paid for, and the PO's `PROPOSE` condition does not trigger.** The lane work is not discarded: the ADR-005 w14 footer records the chosen transport (ACS Email + Azure Managed Domain), its SKU/meter rows, the rejected alternatives with reasons, the module/wiring/secret shape, the `Invitations__*` keys and the per-environment flag — so the transport wave applies a design instead of re-litigating one. **w14 Terraform delta: zero**, conditional on the two normative contract properties recorded in the NW-58 row (site-relative `acceptUrl`; no server-held invitation key) |
| OQ-w14-003 | Which commit does the wave branch from, given the `Raffa` → `Raffa` rename on `origin/main`? | delivery-manager | **ANSWERED (DM, w14 table) — the assumption is confirmed and made precise, with one word changed: it is a *merge*, not a rebase.** The base is `origin/main` **merged** with the Helix process branch, because neither is a subset of the other (`.helix/` is in the repo, `ADR-014:97-98`: the artefact branch has `raffa-next-process.yaml`, `origin/main` has the rename). "Rebase onto `origin/main`" read literally would replay the artefact commits and is the wrong instruction for a two-way divergence. **A specific commit id is deliberately not named here**: this seat has no `Bash` in this harness, so it has not read `origin/main`, and naming a SHA from a council document is exactly the failure ADR-014's new clause 3 exists to prevent. The base is whatever `origin/main` is at HITL time **provided it contains the rebrand and passes W14-A1's five points** — including one green `dev` deploy and one interactive sign-in. Path translation (`Raffa.X` → `Raffa.X`, `raffa-v2/` → `raffa-v2/`) runs over `w14-requirements.md` and, once written, `reports/plan/slices/w14.yaml`; **every design-ref anchor in this record depends on that second translation**, so it is not optional bookkeeping. Recorded in ADR-014's w14 footer, clauses 1 and 3 |
| OQ-w14-004 | Which workspace profile fields — currency/region, or also industry/country? | product-owner, ux-ui-designer | **ANSWERED (PO, w14 table) — the intake's assumption is corrected.** The fields are **`name` + `industry` + `country`**, industry and country both **closed lists** (`markup.html:55-56`), and **currency is never asked**: it and the business region are **derived from country** and stored (`markup.html:63` is output, not input). "`industry` optional free text" and "`currency` first-class" are both wrong. Business region ≠ ADR-006 Azure region stands. Recorded in ADR-001 clauses 5–6 |
| OQ-w14-005 | Who may list and remove members? | security-architect | **ANSWERED (SEC, w14 table) — the assumption is confirmed and made precise.** **Any live member** reads the roster (`name`, `email`, `role`, `status`); only an **Admin** invites, revokes or removes. Read ≠ write. A **non-member is 404**, never 403 and never an empty 200 — both are tenant-existence oracles. Removing the **last Admin** (including self) is **409**: at least one live Admin membership per tenant, always, because a tenant with zero Admins is unrecoverable without operator intervention; the guard is a pure domain function provable without a database. Revocation is on the **next request** and that is the *strongest available* form, not a compromise — the API resolves membership and role from the database on every request (`WorkspaceRoleResolver.cs:85-119`), so there is no session and no token to revoke, and removal additionally revokes that email's live invitations in the same transaction. Recorded in ADR-025 §B/§D.4/§D.5 and the ADR-009 w14 footer (clause 7) |
| OQ-w14-po-01 | Does an unaccepted invitation grant access? | product-owner (observable rule), security-architect + software-architect (enforcement) | **Product answer: no** — `Invited` is not membership (N3b-3), which *changes shipped behaviour* and is why E01/F05/US01/T02 is superseded. The storage shape (pending-invitation row vs membership with a pending flag, and what RLS does with it) is the architects' call, not this seat's — **ANSWERED (SEC, w14 table; software-architect concurring): a pending-invitation row, never a membership with a pending flag, and RLS treats it as an ordinary tenant row.** Three reasons, in order of weight. (1) **A flag is a grant you have to remember to check.** Every membership read — the roster, the Admin gate, discovery, and `WorkspaceRoleResolver`'s join at `:104-114` — would need the same `WHERE accepted IS TRUE` predicate, and the first one that forgets it silently grants access to someone who never accepted. With a separate table there is **nothing to forget**: a membership row *is* the grant, so "an unaccepted invitation grants nothing" is true by the shape of the data rather than by the diligence of every query. (2) It keeps `WorkspaceSignIn`'s "sign-in never provisions a user" invariant (`WorkspaceSignIn.cs:23-25,36-41`) intact — invite writes the `workspace_user` row with **no** membership, so accept has a row to bind and `LinkSignInAsync` is unchanged. (3) It gives NW-04 its two states for free with **no third persisted state**: no membership + live invitation → `Invited`; membership → `Active`. **What RLS does with it**: `workspace_invitation` is an **ordinary tenant table** — `tenant_id` not null and indexed, `ENABLE` + `FORCE ROW LEVEL SECURITY`, a `tenant_isolation` policy with both `USING` and `WITH CHECK`, shipped in the same migration as the table, and **no `identity_self` policy** (that widening stays confined to `workspace_user`). So an invitation is visible only inside its own tenant, and the pre-accept lookup reaches it by entering that tenant's scope from the token's own prefix, not by any policy exception. Recorded in ADR-025 §D/§F.4 and the ADR-009 w14 footer clause 8 |
| OQ-w14-po-02 | NW-58's own stated dependency on NW-05 ("who is allowed to invite/remove", `inputs/next/next-waves-todo.md:119-120`) is unmet — NW-05 is queued to W15 | product-owner, security-architect | Ship NW-58 on the OQ-w14-001 interim identity. **N3b-8 is what makes that honest**: the Admin check resolves from the membership row, never from a client-supplied role header. The residual (a caller can still assert another `X-User-Id` until ADR-010 lands) is a **recorded limitation of w14, not a defect of NW-58**, must be written into the epic story, and is closed by NW-05 in W15. A reviewer must not reject NW-58 for it — **CONCURRED (SEC, w14 table), with the enforcement half.** The product-owner's ruling is exactly the security position: ship NW-58 on the interim identity, because **the Admin gate resolves from the membership row and never from a client-supplied role** (NW-14 row; ADR-022 w14 footer clause 1). The residual is precisely bounded and is *not* open-ended: a caller may assert another `X-User-Id`, but that grants only what that identity's **membership rows** already grant — no role, no tenant, no scope follow from the header (Rule A2). So the wave's own guarantees (a non-Admin cannot invite; a non-member cannot read a roster; the last Admin cannot be removed) hold against every caller *except* one who impersonates a specific known identity, which is the single defect NW-05 closes. That is a strictly smaller surface than today's, where `POST …/invites` needs **no identity at all**. Recorded in ADR-025 §A and the ADR-022 footer as a **limitation of w14, to be written into the epic story**, and it is why this seat treats NW-05 as W15's highest-priority item rather than a cleanup |
| OQ-w14-cl-01 | Does the accept link carry the token in a **query string** or a **fragment**? | security-architect (logging / rejection contract), client-architect (route parsing); raised by cloud-architect | **Raised, not decided — this seat does not own it**, but the hosting surface is this seat's so the fact belongs on the record. `web/public/staticwebapp.config.json:2-5` rewrites every non-asset path to `/index.html`, so the platform logs the accept **path**; a query string is logged with it and travels in the `Referer` header to any third-party asset the accept page loads, whereas a fragment (`/invite/accept#token=…`) is never sent to any server. ADR-025's own Rule 5.2f/5.2g reasoning (query strings land in access logs, `Referer` and browser history) applies to the **link**, not only to the API call that ADR-026 §D5 already routes through the `X-Invitation-Token` header. *Assumption in force*: query string. The fragment is a **no-cost hardening** for the security seat to accept or refuse. Either choice is **zero infra delta** and neither changes the Terraform ruling. — **DECIDED (SEC, w14 table): the fragment. The hardening is accepted.** The accept link is **`/invite/accept#<token>`** — site-relative per the ADR-005 footer, and a **fragment, which is never transmitted to any server**. Cloud-architect's reasoning is adopted verbatim and is exactly right: `staticwebapp.config.json:2-5` rewrites every non-asset path to `/index.html`, so the platform logs the accept **path**; a query string would be logged with it, would ride the `Referer` header to every third-party asset the accept page loads, and would persist in browser history. This seat's own Rule C9 already required "never a URL path or query string server-side", so the fragment is not a new constraint but the correct realization of one — and it costs nothing, is compatible with `new URL(acceptUrl, window.location.origin)`, and leaves ADR-026 §D5's `X-Invitation-Token` header untouched (the header governs the **API call**; the fragment governs the **link**). The SPA reads the fragment, **clears it from the address bar**, and sends the token in the header — never as a query parameter, and **never persisted** in `localStorage`/`sessionStorage` (Rule C10: a token in `localStorage` is a token in every XSS payload's reach). Recorded in ADR-025 §C. Route parsing remains client-architect's. — **REALIZED (CLIENT, w14 table): accepted, and it forces the flow, not just the parsing.** `/invite/accept#<token>` is read from `window.location.hash` on mount, held **in memory for one mount**, and the fragment is cleared with `history.replaceState`; the token then travels only in `X-Invitation-Token`. Rule C10's "never a client store" is compatible with this wave **only because the flow changes with it**, which is the part neither the raising seat nor the deciding seat could see from their side: MSAL here is **redirect-only** with one configured `redirectUri` (`msalConfig.ts:19-33`; `loginRedirect` at `routes/signin/index.tsx:34`), so a signed-out invitee who signs in **does not return holding the token** — there is nowhere legal to keep it across the redirect. Hence the guaranteed w14 flow is *sign in, then open the link again*, with `loginPopup` as the one sanctioned single-click variant (the page is never unloaded, so memory survives). **MSAL's `state` parameter is rejected**: it round-trips through Entra, putting the token in an authorize-URL query string and the identity provider's logs — the same defect the fragment ruling closes, one layer further out, and the shortcut an implementer would otherwise take. Consequence recorded so it is not read as a bug: **a reload or a sign-out loses a live token by design**, so the copy is "open your invitation link again", never "invalid invitation". Realized in the ADR-012 w14 footer clause 6 and the ADR-018 w14 footer clause 4 |
| OQ-w14-cl-02 | Deliverability of an Azure Managed Domain on stakeholder-facing `demo` | cloud-architect, delivery-manager | **Deferred with the transport — no longer a w14 question.** The managed sender is `DoNotReply@<managed-domain>.azurecomm.net`, which corporate mail filters treat harshly, and `demo` is stakeholder-facing. A custom domain needs DNS TXT/SPF/DKIM records on a domain Terraform does not own plus a manual verification wait, so it cannot be automated inside a single wave. *Assumption*: managed domain first; the custom domain is a separate DNS-gated task; the copyable link stays the guaranteed path until it lands. Recorded in the ADR-005 w14 footer |
| OQ-w14-cl-03 | Does the API fail-fast on a missing `Invitations__*` key? | software-architect | **CLOSED — answered by precedent, and adopted.** `ConnectionStrings:Market` is deliberately optional in the same file (`Program.cs:122-128`, no `?? throw`; intent stated at `containerapps/main.tf:127-135`), unlike the nine fail-fast guards. ADR-026 §D6 adopts this verbatim. In w14 the point is **moot** — no `Invitations__*` key exists — but it becomes load-bearing in the transport wave, so it is recorded rather than dropped |
| OQ-w14-cl-04 | Resource-name prefix after the `Raffa` → `Raffa` rebrand | delivery-manager (W14-01), cloud-architect | **Open, and unaffected by the zero-delta ruling.** This seat has no `Bash` in this harness and has **not** read `origin/main`'s `infra/`; every identifier in its lane draft comes from the wave base `1650213`. *Assumption in force*: any infra task reads the prefix and tag values from `infra/environments/<env>/main.tf` on the wave base and **never** copies an identifier out of a council document — a renamed `azurerm_resource_group.name` is a destroy-and-recreate of an entire environment. w14 writes no Terraform, so this cannot bite **this** wave; it binds the transport wave — **DM half (w14 table): concur, and the CI plane is verified even though the Terraform plane is not.** The same rule applies one layer out: **no workflow, job or task may copy an identifier out of a council document.** W14-A1 point (c) resolves every literal against live Azure / HCP / **Entra** rather than against this record, and the table of literals in the W14-01 row is a *checklist for that review*, not a source of truth — the values in it were read on the wave base `1650213` and are pre-rebrand by construction. Two additions from this seat: the rename surface is wider than `infra/` (it includes `.github/workflows/**`, `scripts/check_demo_swa_config.py` and `backend/scripts/demo-fixture-seed.sql`), and the **identity plane** is the part no resource-name review would catch (ADR-010 w14 footer) |

**Note on OQ numbering.** Three lanes independently proposed a different
`OQ-w14-006`. To stop two different questions merging under one id, this seat
uses the seat-scoped form `OQ-w14-po-NN`; the other seats' new questions keep
their own scoped ids. Whoever appends to `reports/open-questions.md` should
carry the scoped ids, not renumber them.

## Design refs carried into decisions

Filled by **ux-ui-designer** at the w14 table. Design paths are relative to
`inputs/design/prototypes/raffa-v2/` (the newest export, the one the
requirements name; read as `raffa-v2/` on the post-rebrand tree); code paths to
`../web/src/`. **Every `layer: web` task in this wave cites a row of this
table**; a web task with no anchor is under-specified and the checker should say
so. Where the export and the requirements differ, the requirements win and the
difference is named — per `ia-v2.md:125-136`.

| Item | Oracle | Anchor (design) | Anchor (code) |
|---|---|---|---|
| NW-01, NW-03 | `ia-v2.md` route map `:44-56` | `/signin` "Entra sign-in → workspace create/pick → lands on Ask" (`:46`); `app.jsx:138-139` `signCreate` / `signPick` / `enterWs` | `WorkspacePickerScreen.tsx:100-137` — the "You're in {name}" interstitial is **in the code and in no export**; `App.tsx:49,91` |
| NW-09, NW-24 | `screens-v2.md` §1 (`:13-21`); `markup.html:52-57,62-64` | create form **Company · Industry · Country** with both option lists verbatim (`:55-56`, five industries ending in `Other`, four countries); pick row `"{{ seededCount }} validated contracts · CHF · eu-west"` (`:63`) — "**validated**", not "all documents", and **`eu-west` does not ship** (the business country name does); role tag `:64` / `app.jsx:140` | `WorkspacePickerScreen.tsx:189-212` (form; label `:192`), `:172-175` (meta line, two segments today), `:173` (renders the literal `contracts`) |
| NW-09 — sibling counts | `ia-v2.md:30-31`; `screens-v2.md:118-120`; `app.jsx:160` | Portfolio **and** Renewals rail badges are both "number of validated contracts" — **one server field, five surfaces**. **The Documents badge (`ia-v2.md:25`, `N to review` / `N docs`) counts documents and is legitimately a different number**; no task may force it to agree (that is NW-10, W16) | `navItems.ts:128-139`; `useValidatedContractCount.ts`; `app.jsx:160` is also the export's own singular/plural idiom |
| NW-14 | `markup.html:64`; `app.jsx:140` | **Corrected — this row previously read "none — no visual change". There is a visual surface**: the pick row renders a role tag, hardcoded to "Workspace Admin" for whoever is looking, so the wrong-role bug is visible on the first screen after sign-in | `WorkspacePickerScreen.tsx:177` (render), `:83` (hardcoded on create), `workspaceStore.ts:56-64` (typed as the **string literal** `"Workspace Admin"`) |
| NW-04, NW-58 — members table | `screens-v2.md` §10 (`:147-154`); `markup.html:388,394-395` | table is **Member · Role · Status** — three columns, **no remove affordance**; invite roles with Procurement first (`:394-395`). **Two anchors in the previous version of this row are superseded and must not be copied into a task**: the `name · email · role · status` shape (the backend stores **no member name**) and the Admin summary "Also uploads, deletes, manages members" (superseded by **D8 / R-WEB-07**) | `MembersTable.tsx:39` (the **email** is the primary line) + `:13-16` (why a name is never derived); `memberViewModel.ts:30-33` (the **D8** summaries that ship) + `:23-29` (why); `:16` (role order); `:91-93` (status tag — a **binary ternary**); `memberStore.ts:14` (binary union) |
| NW-58 — invite result | `markup.html:399`; `app.jsx:170` | "Invitation sent." unconditional — **the defect, not the target**; the export sets it from a regex with no request at all | `memberViewModel.ts:39` (the string), `InvitePane.tsx:101-105` (render site), `:17-18` (the `boolean` prop that becomes the server's `mailDelivered`), `index.tsx:78` (set on any 201), **`index.tsx:63`** (the *failure* path also claims a mail) |
| NW-58 — domain rule | `screens-v2.md:150`; `markup.html:398` | "email must match the workspace domain" — in force as a **non-blocking warning, not a block** (ADR-001's w14 footer defers the restriction to the ADR-010 wave) | `memberViewModel.ts:69-82` (`validateInviteEmail`), `:58-61` (`formatDomainError`), `:64-67` (the proxy, named as a gap in the code itself), `index.tsx:45` |
| NW-58 — non-Admin state | `markup.html:403`, gated at `:384`; `ia-v2.md:13-15`; ADR-018 `:107-108` | **The design oracle contradicts itself**: the ADR and `ia-v2.md` say "read-only with request access", the markup hides the whole grid (`:386-401`). ADR-018's w14 design footer rules for **read-only** | `RequireRole.tsx:20-26` (the copy was generalised **pending exactly the endpoint NW-04 ships**), `:41` (the inert `Request access` button), guard `WorkspaceShellApp.tsx:70`, `index.tsx:28-29` |
| NW-58 — accept screen | **none — no accept flow exists anywhere in the V2 export** | Entra CTA copy `markup.html:44` **verbatim**; "Signed in as {email}" kicker `markup.html:51,60`; states contract ADR-018 `:112-119`. The ten states are ADR-020's w14 footer, **screen 11** | new route; the kicker idiom already ships at `WorkspacePickerScreen.tsx:148` |
| NW-58 — the tip | `markup.html:385` (`kbOff`) | `MEMBERS_TIP` verbatim and its gating are **unchanged**; no task should "improve" it into an always-on banner | `memberViewModel.ts:36`; gate `index.tsx:101-105` (`shell?.kbReady === false`) |

## Work items this wave supersedes (instruction for next-decomposer)

The intake's §6 records "none". One row is wrong. This seat owns the honest
cancellation of a backlog item a new requirement replaces, so both rulings are
written here rather than left to inference. **This seat does not write work-item
files** — `next-decomposer` applies the banners, append-only, body byte-identical.

| Work item | Ruling | Why |
|---|---|---|
| `epic-01-platform/feature-05-identity-workspace/us-01-workspace-roles/tasks/task-02-membership-invite.md` (`status: live`) | **supersede** — set `status: superseded`, append `## Superseded (2026-09-10, wave w14)` naming NW-58 and the w14 story that replaces it | It delivered "invite ⇒ membership row **immediately**" (`WorkspaceMembershipService.InviteAsync`). NW-58 must #2/#4 makes membership happen **on accept**. Same word, two different product rules — a replacement, not an extension. Leaving it `live` tells a future intake the old rule is still wanted. The banner must also say the never-delivered "with OIDC claims" half moves to **W15 / NW-05**, so nothing is silently dropped |
| `epic-06-web-foundation/feature-04-workspace-members-ui/us-01-workspace-members-invite/tasks/task-01-workspace-members-invite.md` | **not superseded** — no banner | Its three ACs (members table, invite pane + roles, non-Admin state) are still exactly what the product wants. NW-04 changes the table's **data source** and NW-58 changes the pane's **copy and payload**: amended behaviour inside a still-wanted story. The false "Invitation sent." string is a defect NW-58 fixes, not evidence the story was wrong |

## Constraint this wave places on NW-09 (no product-owner seat on that item)

NW-09 names no product-owner, so this is an **acceptance constraint for its
seats**, not a decision taken on their behalf. If NW-24 is cut for budget,
NW-09 **must not** print a literal `CHF · eu-west` to match the prototype
pixel — the row degrades to `"{n} validated contracts"`. A hardcoded currency
and region is the same class of defect as "Invitation sent." with no mail:
the screen asserting a fact the system does not hold (ADR-001 w14 clause 3).
Second, the label is "**validated** contracts", not "documents" — the number
counts contracts whose facts are canonical (`percorso-pilota-v1.md` §3
`completed`), and the picker, the rail and `/documents` must all show the same
number. If the three cannot agree in this wave, **change the label, not the
number**.

## Software-architect ↔ security-architect reconciliation (w14 table)

The two seats drafted concurrently and both landed on the same shape for the
hardest decision of the wave. Where they differed, this is what was settled
and who yielded. **Both drafts are on disk and disagree in six places; this
section is what the decomposer follows.**

| # | Point | Resolution |
|---|---|---|
| R1 | **ADR number.** Both lanes claimed ADR-025 | **Security keeps ADR-025** (`ADR-025-workspace-membership-and-invitations.md`, authorization + token + RLS + audit). This seat takes **ADR-026** (contract + data model + composition). Their own numbering note refuses to spend a round on it; this seat's draft said it would renumber rather than collide. Neither ADR restates the other; **where they touch one mechanism, ADR-025 governs** |
| R2 | **GUC name.** `app.identity_key` (SA draft) vs `app.identity_subject` (SEC draft) | **`app.identity_subject` adopted.** The policy lives in security's ADR-009 footer, so the name is theirs. Two names for one GUC would have shipped as two implementations |
| R3 | **The GUC is a SQL-injection sink** | **Converged independently** — SA §11 and SEC Rule 3.2b found the same sink with no contact. `BuildSetCommandText` interpolates (`TenantRlsConnectionInterceptor.cs:103-107`) and its comment justifies that *solely* because `TenantId` wraps a Guid; an identity is caller-controlled text from `X-User-Id`. Copying the pattern would let `X-User-Id: x'; SET app.tenant_id = '<victim>'; --` repoint the RLS claim. **Normative in both ADRs**: `set_config('app.identity_subject', @identity, false)` with a real bind parameter, `RESET` on close, and a negative test with `'`, `;`, `--` |
| R4 | **Accept path.** SA draft: invite writes the invitation row only | **Security's F5 shape adopted.** Invite writes `workspace_user` (no membership) + invitation row. The SA draft would have failed at `WorkspaceSignIn.cs:36-41` on every accept **and** contradicted its own NW-04 row, which needs the user row to render `Invited`. The SA draft was internally inconsistent; this is the fix |
| R5 | **Status codes.** SA draft: 400 for absent identity, 403 for non-member | **Security's §B adopted** — **401** for absent identity (an authentication failure, not a malformed body) and **404** for a non-member (a 403 on a tenant id you do not belong to is a tenant-existence oracle). The rejection contract is their lane |
| R6 | **Token shape.** SEC §5.5 accepts one unscoped hash→tenant read as "unavoidable" | **SA proposes `{tenantId:N}.{secret}`** so accept scopes first and reads nothing outside a scope — strictly narrower on ADR-009's own criterion, with hash-only storage unchanged. The tenant half is a routing prefix, not a credential (tenant ids are already in route paths). **Security ratifies; if they prefer their lookup, only ADR-026 §D4's first paragraph changes** and no table, endpoint or client moves |

Client-architect reconciliations adopted without dispute: **no `roleLabel`** on
the workspace row (derived from `role` via the existing `WORKSPACE_ROLE_LABEL`
map), the client method name **`listWorkspaces()`** (`client.ts:22-24` already
reserves it), and the members row `{ id, email, name?, role, status }` with
`invitedAt` dropped for want of a consumer.

## Open questions this seat opened or closed

| OQ | Question | Status |
|---|---|---|
| OQ-w14-001 | Which identity keys `workspace_membership` before ADR-010's JWT? | **Concurred with security-architect's §A.** One `ICallerIdentity` seam: claims first, then `X-User-Id`. Every new endpoint in this wave consumes the seam and **none reads `HttpRequest.Headers` directly**, so NW-05 (W15) retires the header in one file. The existing membership predicate already matches on email **or** external subject (`WorkspaceRoleResolver.cs:111`), which is exactly the shape that survives the swap |
| OQ-w14-sa-01 | How do existing `dev` workspaces get an Admin? *(this seat's lane called it OQ-w14-006; renamed to the scoped form per the PO's note above)* | **Open — a data question, not an architectural one.** They cannot be attributed retroactively: `CreateWorkspaceAsync` never recorded a creator, so no column, log or audit row names one, and there is no EF data-migration precedent in this repo. Security refuses a "claim this workspace" endpoint as a tenant-takeover primitive and this seat agrees. **Assumption in force**: the operator either recreates the `dev` workspaces or runs a one-off idempotent script under `backend/scripts/` taking `(workspace id, admin email)` pairs supplied at HITL, modelled on `demo-fixture-seed.sql:8-10,60-67` with its own workflow. Needs a product-owner / delivery-manager ruling — **ANSWERED (DM, w14 table) — the delivery half, and the mechanism already exists in this wave.** The assumed "one-off idempotent script with its own workflow" **is** `backfill-workspace-membership.yml` (NW-58 row), so this costs **no additional task**: `workflow_dispatch` with a typed `choice` environment, `environment:` so `demo` inherits its reviewers, existing OIDC and Key Vault path, a precondition guard, an idempotent insert and a **fail-if-zero** verification. Three delivery rulings on top of the mechanism. (1) **Which `dev` tenants**: the operator supplies `(tenant id, admin email)` pairs at HITL — this seat does not enumerate them in a council document, and a tenant nobody claims is simply left unclaimed, which is the smaller problem security-architect's refusal of a "claim this workspace" endpoint already prices in. (2) **When**: the backfill is **not** a post-wave cleanup. Client-architect's prerequisite 3 is right — the moment the picker reads the server list, every pre-existing `dev` workspace disappears for its creator — so the *workflow file* is authored in-wave and the *run* happens on deployed `dev` **after** the merge that applies the schema and **before** acceptance (promotion path step 3). A fan-out task cannot run it: it needs a deployed environment and a Key Vault the task has no path to. (3) **`demo` needs no backfill at all** — its only pre-existing tenant is the ADR-022 fixture, which the **seed** covers (NW-58 row). Not this seat's to rule: whether any given `dev` tenant is worth claiming rather than recreating is the product-owner's call |
| OQ-w14-sa-02 | Does security accept a second, `SELECT`-only RLS axis on `workspace_user`? *(lane id OQ-w14-007)* | **CLOSED — yes, reached independently.** Security's §3.1 evaluated four options and chose the same one, with a fail-closed `IS NOT NULL` guard this seat had not specified. No fallback needed; option 3 (directory table) stays recorded in ADR-026 as the named fallback that would change **no endpoint contract** |

## Cloud-architect reconciliations (w14 table)

**Headline for the decomposer: wave w14 has no infra task.** No Azure resource,
no SKU, no Terraform module, no environment key, no Key Vault secret, no deploy-path
change, in either environment. Any task proposing one is out of scope for this wave.

| With | Point | Resolution |
|---|---|---|
| C1 | product-owner — ACS in w14 vs W15 | **This seat yields, and says why in its own terms.** The transport buys no w14 acceptance because ADR-026 §D6 already makes "must not say sent" a server fact; landing it anyway would insert an operator-confirmed HCP apply between W14-01 and the wave's only §20 item. The decision is preserved in the ADR-005 footer, so nothing is lost by waiting. **No item is cut to pay for this** |
| C2 | software-architect — ADR-026 §D5 `acceptUrl` | **One word changes: `acceptUrl` is site-relative in w14, not absolute.** This is the single most consequential infra point of the wave and it lives in *their* contract, not in this seat's Terraform. An absolute URL would silently reintroduce the mid-wave HCP apply the PO just ruled out (`backend.yml` deploys `--image` only; a new env var can arrive only via Terraform). Requested as an amendment to §D5's payload line; **no other clause of ADR-026 moves**, and the client is identical either way via `new URL(acceptUrl, origin)` |
| C3 | security-architect — token strength must not require a server-held key | ADR-026 §D4's random-and-hashed token needs none, and this seat has **withdrawn** its own `Invitations__TokenSigningKey` proposal. Narrow constraint on ADR-025 (not yet on disk at this seat's turn): if the specified token requires a server-held signing key, **w14 reacquires a Key Vault secret, an env var, a Terraform PR and an operator-confirmed apply**, and this seat's zero-delta confirmation lapses. Everything else in ADR-011 is untouched: the Key Vault → Container App secret → env var path is unchanged, no new role assignment, no new grant, no cross-environment reach. ADR-011 `:95-97` already anticipates "signing config" in the vault, so **if** a key is ever needed no new principle is required — only an infra wave |
| C4 | delivery-manager — "cloud-architect confirms w14 has zero Terraform delta under Option A" | **Confirmed**, conditional on C2 and C3. Two additions. (a) Their `Web__BaseUrl` finding and this seat's C2 are **the same finding reached independently in two lanes** — the API cannot learn the SPA origin without Terraform, because `backend.yml` never sets env vars. (b) The `demo` membership seed and the operator backfill script are **also zero infra delta**: existing workflow, existing Postgres, existing identity. **No infra task may be invented for them** |
| C5 | ux-ui-designer — screen 10 | In w14 only the **link** state is reachable. The "Invitation sent." state may be designed but has no path to the screen, because `mailDelivered` is `false` by construction until a transport ships. One boolean drives both states, so the transport wave needs no new design |
| C6 | Scope of the ADR-006 footer, named by this seat rather than left for the gate | The Static Web Apps deviation predates w14 and strictly serves no w14 **item**. It is written anyway because ADR-006 `:55` currently contradicts the deployed tree, the rule it states is the one that governs the deferred ACS decision, and it is this seat's ADR to correct (`ADR-006:5`). **It generates zero tasks.** If the gate reads that as out of scope, the footer stands as documentation and no decomposition changes |

## Security-architect reconciliations (w14 table)

**Headline for the decomposer: where ADR-025 and ADR-026 touch the same
mechanism, ADR-025 governs.** That is not a claim of seniority — it is how the
two were written, and the INDEX says so. ADR-026 is contract, data model and
composition; ADR-025 is authorization, token strength, RLS policy and audit.

The software-architect recorded six points (R1–R6). This seat accepts their
resolution of all six, and one of them is a **correction of this seat's own lane
draft** rather than a concession.

| # | Point | This seat's position |
|---|---|---|
| R1 | ADR number | **Accepted — this seat takes ADR-025**, written and on disk. Their draft said it would renumber rather than collide; this seat's own numbering note refused to spend a round on it. Both held |
| R2 | GUC name `app.identity_subject` | **Confirmed.** Two names for one GUC would have shipped as two implementations |
| R3 | The GUC is a SQL-injection sink | **Converged independently, and now sharpened.** Both lanes found `BuildSetCommandText` interpolating with a justification that holds *only* for a Guid. The added precision: that same comment states **"Postgres `SET`/custom GUCs do not accept bind parameters"** — which is true, so "bind the parameter" is not achievable in the `SET` form. A task that reaches for `SET`, finds it cannot bind, and interpolates has reproduced the vulnerability while following the rule. **The remedy is a different statement form — `SELECT set_config(…, @p, false)`** — and both ADRs already say so. Recorded as normative with a mandatory negative test |
| R4 | Accept path | **Their adoption of this seat's F5 shape is correct**, and this seat notes their draft was internally inconsistent (its NW-04 row needed the `workspace_user` row its §5.1 had stopped writing). No residual disagreement |
| R5 | Status codes (401 / 404) | **Confirmed as the rejection contract** (ADR-025 §B), with Rule B2 recording *why* `GET /api/audit` keeps its 403: its tenant comes from the caller's own claim, never from a route or header, so there is nothing to enumerate. Audit is not changed |
| R6 | **Token shape `{tenantId:N}.{secret}`** | **RATIFIED — and this is a correction of this seat's lane draft, not a concession.** Their argument is right on ADR-009's own criterion, but the stronger reason is one neither lane stated: this seat's §5.5 called a hash→tenant lookup "the one read that must happen before a tenant scope exists" **while specifying, in the same section, the policy that makes that read return zero rows**. `workspace_invitation` carries `FORCE ROW LEVEL SECURITY` and one `tenant_isolation` policy; an unscoped read is not narrow, it is **impossible**, and serving it would have required a third policy widening the invitation table or `BYPASSRLS`. Their shape removes the read entirely. **Ratified under two bounds that are its justification, not decoration**: `Guid.TryParseExact` before scope entry (Rule C4) and hash-match-first inside it (Rule C5). Drop either and the shape becomes a tenant oracle |

**Ratification detail the decomposition depends on (Rule C4).** The token prefix
is the **first caller-controlled value in the product to reach a tenant scope**.
It is safe today because the type system makes it so — `BeginScope` takes
`TenantId` (`ITenantContext.cs:26`) and `TenantId` is
`readonly record struct TenantId(Guid Value)` (`TenantId.cs:7`), constructible
only from a parsed `Guid`. That is precisely the invariant `BuildSetCommandText`
cites to justify interpolation. **No string-taking overload or implicit string
conversion may be added to `TenantId`**, in this wave or any other; doing so
would silently convert an interpolation that is provably safe into one that is
not.

| With | Point | Resolution |
|---|---|---|
| cloud-architect C3 | "the token must not require a server-held key, or the zero-delta confirmation lapses" | **Confirmed — it does not, and this is now on disk in two places** (ADR-025 Rule C8, ADR-011 w14 footer clause 1). The random-and-hashed token needs no key because verification is a hash lookup against a row that must exist anyway for single use, expiry and revocation. This seat's lane had already rejected the intake's assumed "signed token, secret in Key Vault" for the same reason and **withdraws nothing** — the two seats reached the identical conclusion from opposite directions (cost, and cryptographic necessity). **w14 adds no Key Vault secret and no environment variable.** ADR-011's `:95-97` "signing config" slot is left unused: a narrowing, not a contradiction |
| cloud-architect C2 | `acceptUrl` is site-relative in w14 | **No security objection — and it composes with the fragment ruling.** `/invite/accept#<token>` is both site-relative and fragment-carrying; the two constraints are orthogonal and both hold. This seat additionally notes the security *reason* their infra reason lands on the same answer: a relative URL keeps the token out of any server-side redirect the API would otherwise have to construct |
| cloud-architect OQ-w14-cl-01 | query string vs fragment | **Decided in the OQ row: fragment.** Raised by a seat that does not own it, correctly, and adopted |
| product-owner | "no design may hardcode a fact the system does not hold" | **Same rule, applied to security**: `mailDelivered` as a server fact is the mechanism this seat also needs, because "Invitation sent." when nothing was sent leaves an Admin believing access was granted when it was not. Recorded as a security statement, not only a copy one |
| delivery-manager | the Entra scope literals | **Adopted into ADR-010's w14 footer.** Their `web.yml:204-205` finding is the *identity-plane* half of the rebrand risk and belongs in the ADR that owns app registrations — whose Assumptions still call those scopes "placeholders". It fails differently from a resource name: at token acquisition, in the browser, after CI is green |
| client-architect | NW-03 revalidation | **Rule F.1d is the server half of their client rule**: a cached current workspace absent from the server list must be discarded. Same requirement, both ends |

## Open questions this seat opened or closed

| OQ | Question | Status |
|---|---|---|
| OQ-sec-001 | Is the unauthenticated invite endpoint patched **in w14**, or is the exposure accepted until W15? | **CLOSED — patched in w14, and it merges before NW-01.** One resolver call plus a guard. This wave converts a dormant row into a working cross-tenant join path, so the exposure is created by this wave's success. If NW-58 cannot be fitted whole, **the guard still ships as its own task** |
| OQ-sec-002 | Must the accepting identity's email match the invited address? | **Open — assumption in force: yes, case-insensitively; mismatch is 403.** Cost: an Entra account whose username differs from the invited mailbox (an alias) cannot accept, and the Admin re-invites the right address — which this wave makes work. Loosening it makes the link forwardable into a customer tenant, and in the pilot there is no other gate. If the pilot hits the alias case the fix is "invite by Entra UPN", not dropping the check. **product-owner may overrule**; the accept event would then have to record both addresses |
| OQ-sec-003 | Invitation expiry | **Open — assumption in force: 7 days, server-side absolute.** The product-owner may change the number; the mechanism does not change |
| OQ-sec-004 | Tenant-creation abuse — `POST /api/workspaces` stays effectively open | **Open, not a w14 blocker.** Acceptable for `dev`/`demo`: it is the signup step, and a new tenant grants nothing over an existing one. Rate limiting is a later cloud/delivery item |
| OQ-sec-005 | ADR-025's number | **CLOSED** — taken by this seat, ADR-026 by software-architect, agreed in one line as both drafts intended |
| OQ-sec-006 | Does ADR-010's amendment need NW-08 re-scoped? | **Open — assumption in force: no w14 task edits `WorkspacePrincipalAuthorization.cs`.** w14 records the rule and writes the retirement test as a forward test; the code change lands with NW-05/NW-08 in W15, since it touches `GET /api/audit`. If the table wants it earlier it is a **W15 ordering note for delivery-manager**, not a new w14 task |
| OQ-w14-sa-01 | How do existing `dev` workspaces get an Admin? | **Concurred with software-architect, and the security half is a refusal**: a "claim this workspace" endpoint is a tenant-takeover primitive and is rejected. Operator job over HITL-supplied `(tenant id, email)` pairs, audited with the operator as actor. Needs a product-owner / delivery-manager ruling on which `dev` tenants are worth claiming |

## What this seat objects to (standing, for the seats still to speak)

Stated in advance so no later turn has to be reversed. This seat will move to
`OBJECT` on any decision that:

1. answers `GET /api/workspaces` by **relaxing or removing a `tenant_isolation`
   policy**, by a `BYPASSRLS` role, or by a second un-RLS'd membership table;
2. lets **`X-Role` grant an Admin action** — including "the SPA sends back the
   role the server gave it"; the server must not believe the client about a fact
   it can look up;
3. leaves any invite / accept / revoke / remove path **reachable without an
   authorization check**, or returns **403 to a non-member** (it is an oracle);
4. stores a token **in plaintext**, puts it **in a URL path or query string**,
   writes it to an **audit row or log**, or keeps it in
   **`localStorage`/`sessionStorage`**;
5. treats a **role or tenant claim in the ADR-010 token as authoritative** — the
   stale-authorization window that would silently reopen in W15;
6. drives the workspace list from **`workspace_user` existence rather than live
   membership**;
7. ships a UI that says **"Invitation sent."** when nothing was sent;
8. adds a **third** multi-scope or caller-supplied-scope path (the two exceptions
   are named and bounded, or they are not exceptions);
9. makes **sign-in provision a `workspace_user` row**, converting
   `WorkspaceSignIn`'s explicit "self-service join is out of scope" invariant
   into self-service join;
10. adds a **string-taking overload to `TenantId`** (Rule C4).

## Client-architect decisions and reconciliations (w14 table)

**Headline for the decomposer: a client cache never stands in for a missing
GET.** Four stores in this SPA exist only because an endpoint did not, and each
says so in its own header comment. Three are deleted and one is demoted to a
hint. A failed read renders ADR-018 `:117`'s error state — **there is no "keep
the old list as a fallback" task**, in any item, and a task that proposes one
has reproduced the defect the wave exists to fix.

**Endpoint before consumer, one phase later.** NW-01's endpoint ships in phase
*n*, the picker rewrite in *n+1*; the same for NW-04 and each NW-58 operation.
A web task that consumes an endpoint landing in its own phase is a broken
chain.

### Single-writer risks (`check_single_writer.py --slice w14`)

| File | Would-be writers | Resolution |
|---|---|---|
| `web/openapi/raffa-api.v1.json` + `web/src/api/generated/schema.ts` | NW-01, NW-04, NW-58 | **One task owns the contract file per phase.** `schema.ts` is regenerated wholesale on every build (`generate-api-client.mjs:21-23`), so two parallel edits produce a conflicting regenerated artefact, not a mergeable diff. Preferred: the software seat lands all w14 operations in one contract task; web tasks only consume |
| `web/src/api/client.ts` | NW-01, NW-04, NW-58 | Append-only method additions in a fixed order, one task per endpoint group, sequenced behind the contract task |
| `web/src/App.tsx` | NW-03 (async gate + resolving state), NW-58 (hoisted `BrowserRouter`) | **Same task or strictly sequenced** — the two edits touch the same twenty lines (`:49`, `:91`, `:108-113`). NW-03 first, the router hoist second |
| `web/src/components/shell/navItems.ts` | NW-09 only | Single writer **by design**: NW-14's parse goes to `workspaceRole.ts`, and NW-10 (Documents badge, same file, `:99-107`) is queued to W16 |
| `web/src/routes/signin/workspaceStore.ts` | NW-01 (delete the list), NW-03 (demote the hint) | One task — two halves of one file's rewrite |
| `web/src/components/shell/useValidatedContractCount.ts` + `AppShell.tsx` | NW-09 | Four consumers; `kbReady` deliberately left alone for NW-56/W18 |

`WorkspaceShellApp.tsx` is touched only by NW-58 (dropping its
`BrowserRouter`); `RequireRole` and `canManageMembers` are untouched by NW-14
by design.

### Prerequisites that block proof rather than code

1. **A membership seed for the e2e account**, or the V2 suite goes red at
   NW-03 — verified, not hypothetical (`v2.spec.ts:68-79` pins the tenant
   through the very key NW-03 revalidates; no membership row exists anywhere).
2. **A second Entra account on the same email domain as the first**, or
   NW-58's and NW-14's two-account specs cannot run:
   `validateInviteEmail` rejects a cross-domain invite client-side, before any
   request.
3. **Membership backfill for existing `dev` workspaces** is a prerequisite of
   NW-01's *web* task, not a follow-up: the moment the picker reads the server
   list, every pre-existing `dev` workspace disappears for its creator. The
   client must not soften that with a cache (the headline rule), so the
   operator path lands with or before the web task.
4. **Two e2e header comments assert the opposite of this wave** and are
   rewritten by the tasks that falsify them: `day1.spec.ts:33-41` and
   `v2.spec.ts:68-79`. A spec whose documented premise is false is a trap for
   the next reader even while it passes.

### Reconciliations

| With | Point | Resolution |
|---|---|---|
| software-architect | ADR-026 §D1/§D3 row shapes, `listWorkspaces()`, no `roleLabel` | **Adopted in full**, and this seat's three inputs were taken without dispute (the method name `client.ts:22-24` already reserved, the dropped `roleLabel`, `invitedAt` dropped for want of a consumer) |
| software-architect | **§D3's header line** | **One word, and it is this seat's lane.** `GET …/{tenantId}/members` should take **no `X-Tenant-Id`**: the tenant is already in the route, the sibling invite operation on the same prefix already works that way (`client.ts:918`), and the ADR-022 w14 footer makes it a rule. Two tenant inputs for one operation is the ambiguity the header was demoted for. **No other clause of ADR-026 moves** |
| software-architect | the `role` enum | **Open and needed in the contract**: neither ADR-025 nor ADR-026 mentions Legal / Finance / ReadOnly, which `memberViewModel.ts:9-11` says the backend accepts. Whatever the enum lists, the client parse maps unmodelled values to least privilege (OQ-w14-client-05) |
| security-architect | Rules C9/C10 — fragment, and never a client store | **Adopted, and this seat's own lane draft is withdrawn** (it proposed a `sessionStorage` stash). Their rule is right and the stash would not even have worked: MSAL is redirect-only with one `redirectUri`, so nothing survives the redirect. The realization — memory for one mount, cleared address bar, `state` rejected, "open your link again" as a normal outcome — is recorded in OQ-w14-cl-01 and both footers |
| security-architect | no role header from the SPA | **Concurred and verified independently**: zero `X-Role`/`X-Workspace-Role` call sites in `web/src`. It is a commitment not to regress, and it is now an ADR-012 clause so it survives this wave |
| cloud-architect | site-relative `acceptUrl` | **No client cost, confirmed from this side.** `new URL(acceptUrl, window.location.origin)` accepts both shapes, so the transport wave's absolute URL needs **no client change** — their C2 holds without a follow-up web task |
| ux-ui-designer | states and copy | Five new loading/error states need copy; the invite pane needs **two** strings (`mailDelivered`), not one; `/workspace/members` needs **two** destructive confirmations (revoke vs remove); and the accept route's "open your link again" is a *normal outcome*, not an error. The picker's role tag derives from `role` via the existing `WORKSPACE_ROLE_LABEL`, so it needs no new string. **Concur with their flag** that the design-ref row `:57` ("NW-14 — none, no visual change") is wrong: `WorkspacePickerScreen.tsx:177` renders a role tag hardcoded to `"Workspace Admin"` at `:83`. That row is theirs to correct |
| delivery-manager | the two prerequisites above | Both block **proof**, not code. Neither needs infrastructure (cloud-architect's C4 already confirms the seed is zero infra delta) |
| product-owner | "no screen asserts a fact the system does not hold" | **Same rule, three more instances on the client**: `contractCount: 0`, `lastActiveAt = now`, and role defaulting to `admin`. All three are deleted by this wave rather than corrected in place |

### Open questions this seat opened or closed

Renamed to the seat-scoped form per the PO's numbering note (the lane draft
used `OQ-w14-c1…c5`).

| OQ | Question | Status |
|---|---|---|
| OQ-w14-client-01 | Is `contractCount` *validated* contracts, server-computed and uncapped? | **CLOSED by ADR-026 §D2** — yes, and the definition already existed in code (`PortfolioQueryService.cs:191`). The client-side predicate stops being a definition; picker, rail and `/documents` must show one number, and if they cannot agree in this wave, **change the label, not the number** |
| OQ-w14-client-02 | Is the e2e account a member of the fixture tenant? | **CLOSED to a fact: no, and no seed makes it one.** A membership seed is a wave prerequisite, not an open question |
| OQ-w14-client-03 | Is a second Entra test account available, on the same email domain? | **Open. Assumption in force: no** — the two-account specs are authored and skip with a named reason until one exists |
| OQ-w14-client-04 | Does the invite 201 carry a delivery discriminator? | **CLOSED by ADR-026 §D6** — `mailDelivered`, a boolean, adopted over this seat's own two-value string. The client is correct whether or not a transport ever lands, and does not change again when one does |
| OQ-w14-client-05 | What does the client do with a `Legal` / `Finance` / `ReadOnly` role, given NW-54 is deferred? | **Open, and load-bearing.** Assumption in force: `parseWorkspaceRole` maps them to the least-privileged modelled role, so no unmodelled role can ever receive Admin affordances. Needs the software seat to fix the enum in the contract; if the PO wants those roles visible in V1 that is NW-54, a different wave |

## UX/UI-designer decisions and reconciliations (w14 table)

**Headline for the decomposer: no screen states a fact the system does not
hold.** That is the product-owner's rule and it is the through-line of every
design decision in this wave, because screen 1 and screen 10 currently break it
three times — `contractCount: 0` for every workspace, `roleLabel: "Workspace
Admin"` for whoever is looking, and "Invitation sent." when nothing was sent.
All three are **removed rather than corrected in place**, and each has a task
that owns it. A fourth is removed by the same rule: `eu-west` under "Contracts
uploaded here never leave it" is a residency claim the product has not made.

**Second rule, for the same reason: a copy string is an anchor, not a
suggestion.** Nine of the ten rows in the design-ref table above now carry a
code anchor beside the design anchor, because in four places the **export is
not the oracle** — it contradicts an accepted decision (the D8 role summaries),
contradicts its own IA doc (the non-Admin state), contradicts itself (a pick
row printing `CHF` from a form that never asks for it), or is simply absent
(the accept screen). A task that "aligns to the prototype" in those four places
ships a regression.

### Reconciliations

| With | Point | Resolution |
|---|---|---|
| product-owner | **the workspace-domain invite rule** — their standing objection, and their ADR-001 w14 footer defers the restriction to the ADR-010 wave | **This seat's lane draft is reversed, and the mechanism is better than either "keep" or "delete".** The rule is **demoted from a block to a non-blocking warning**: the design rule's intent survives (`screens-v2.md:150` — tell the Admin loudly they are inviting an outsider), the deferral is honoured (a warning restricts nothing), and NW-58's own acceptance stops depending on the operator finding a second Entra account on a matching domain. The draft's justification was the error: a check that runs **client-side before any request**, against a server that has no domain rule at all, is a **typo guard, not a safeguard** — and its "workspace domain" is a proxy read off whoever is signed in, which is too imprecise to gate an invitation and entirely adequate to inform one. **This seat therefore also withdraws its lane ask for a `domain` column on NW-24** |
| product-owner | "no design may hardcode a fact the system does not hold" (their NW-09 constraint, `:76-83`) | **Adopted as this seat's headline**, and extended with the instance they could not see: `eu-west` is not merely a hardcoded string, it is a **cloud-region slug rendered as a data-residency promise**, one line under the tenancy sentence. The third segment ships the **business country name** |
| client-architect | `/workspace/members` needs **two** destructive confirmations, not one | **Adopted, and it corrects this seat's lane draft**, which specified only Remove. The two take **different consequence copy** because they are different facts: revoke ends a link, remove ends access. Revoke must **not** say "they lose access immediately" — an invitation was never a grant, and saying so misrepresents what the Admin just did |
| client-architect | the picker's role tag derives from `role` via the existing `WORKSPACE_ROLE_LABEL`, so it needs **no new string** | **Confirmed, with one precision that is now an ADR-019 clause.** There are **two** label maps in different key spaces — `WORKSPACE_ROLE_LABEL` (`navItems.ts:32-35`, lowercase wire role) and `INVITE_ROLE_LABEL` (`memberViewModel.ts:18-21`, backend enum) — and they emit **identical strings**, so this is one vocabulary and no string is invented. They must not be allowed to diverge. **And the least-privilege parse must not reach the label**: mapping an unmodelled `Legal` to `procurement` for *affordances* is right, but rendering it as "Procurement" tells that member they are something they are not. `memberRoleLabel:87` already passes unknown roles through; the tag does the same. Permissions degrade, **labels do not** |
| client-architect | "open your invitation link again" is a **normal outcome**, not an error | **Adopted as screen 11 state 5, and it is the state that most needed naming.** It follows from ADR-025 Rule C10 plus redirect-only MSAL, so it is reached by any redirect sign-in **and by any reload** — it is the *common* path, not an edge case. Wording it "this invitation is invalid" would send a user to their Admin for a replacement they do not need, and since invitations are single-use that "fix" costs a real one |
| client-architect | five new loading/error states need copy | **Supplied**, and the two that are decisions rather than copy are recorded: on screen 1 the **create form is the empty state** (no separate "you have no workspaces" screen), and every error state **renders Retry, never a cached list** — the client half of their own headline rule |
| security-architect | the accept path must leak nothing pre-acceptance; the 403's reason **never echoes the invited address** | **Adopted, and it corrects this seat's lane draft**, whose wrong-account copy read "This invitation was sent to {invited email}." The pre-accept payload does not carry that address (ADR-026 §D5), so the screen literally cannot render it — the state is reached **after** an accept attempt, not before one, and it names only the address the visitor already knows: their own. The pre-accept block shows workspace name and offered role and **nothing else**; expired and revoked share a first sentence so the screen never tells a prober which case they hit |
| cloud-architect (C5) | in w14 only the `mailDelivered: false` state is reachable; one boolean drives both | **Adopted over this seat's own three-outcome discriminator.** The consequence they did not state is the one that matters for copy: **one boolean cannot separate "not configured" from "errored"**, so the `false` string is written to be true for **both** causes and never diagnoses the mailer. The delivery-failure copy is decided here anyway, so the transport wave applies a string instead of re-opening one — and it needs a **third value, not a second boolean** |
| software-architect | the roster is one row per person at the highest role (ADR-026 §D3) | **Confirmed, and it was this seat's design constraint**: `WorkspaceMembershipService.cs:52-55` adds a *second* membership at a different role, which would render two rows for one person with two Remove buttons and an ambiguous last-Admin count — making the removal confirm ("They lose access immediately") false. Their precedence rule satisfies it; the payload decision was theirs and this seat does not re-open it |
| software-architect | `name?` on the members row | **Correct as specified — nullable and server-supplied.** The row renders it **only when the server actually has one** (`WorkspaceUser.DisplayName`); the primary line stays the **email**, and no name is ever derived from the local part (`MembersTable.tsx:13-16`) |

### Open questions this seat opened or closed

| OQ | Question | Status |
|---|---|---|
| OQ-w14-004 | Which workspace profile fields? | **Concurred with the product-owner's ruling in full**, and this seat's lane assumption is corrected by it: currency is never asked. Adds the copy consequence — the label on the `name` field is **"Company"** (`markup.html:54`), the field is not renamed, and `Other` is the Industry list's own "not specified" case, so **no empty sentinel option is added** |
| OQ-w14-005 | Who may list and remove members? | **Concurred with security-architect.** The design half: any member reads the roster, only an Admin sees the invite pane and the `Actions` column, and the last Admin's Remove is a **visibly disabled control with a reason** rather than a hidden one or a 409 on click |
| OQ-w14-ux-01 | Does screen 1's "You're in {name}" interstitial survive for a single-membership user? | **Open — routing is client-architect's (ADR-018 `:125-126`), and this seat does not decide it.** *Position for the record*: for a user with exactly one membership it should not — it is a click between the user and the product that the export does not have (`app.jsx:139`). **Assumption in force**: it stays until NW-03's task rules otherwise; ADR-020 records the real state set either way, so neither answer leaves an undocumented state |
| OQ-w14-ux-02 | Does `Request access` ship as a `mailto:` to the workspace Admins, or is the button dropped? | **Open — either is acceptable; a third option is not.** It is inert today (`RequireRole.tsx:41`), and NW-04 makes the real Admin addresses knowable for the first time. **Assumption in force**: `mailto:` with a prefilled subject. **A dead button must not ship** |
| OQ-w14-ux-03 | Does the invite response carry the invitation's **expiry date** for the `mailDelivered: false` copy? | **Open, small, and load-bearing for one string.** "Send them this link. It expires {date} and can be used once." needs the date; ADR-025 sets a 7-day absolute expiry server-side. **Assumption in force**: the 201 carries it. If it does not, the sentence degrades to "It can be used once." — **the client must not compute the date itself**, which would be this wave's own defect in miniature |

### Claude Design exports still owed (`HITL_CLAUDE_DESIGN`)

**None is blocking** — every state is decided from ADR-019's locked catalogue
and the export's own idiom, so no task waits on these. They are recorded because
ADR-020 `:121-124` makes a missing screen "a **defect to raise**, not a silent
gap to fill with a divergent UI", and because the Design round-trip is lost
until they exist. Also recorded in the ADR-020 w14 footer.

`HITL_CLAUDE_DESIGN: screen 11 — the /invite/accept screen in its ten states; no accept flow exists anywhere in the V2 export.`

`HITL_CLAUDE_DESIGN: screen 10 members table with the fourth Actions column, the two inline confirms (revoke vs remove), and the last-Admin disabled state with its visible reason; markup.html:388 is three columns with no affordance.`

`HITL_CLAUDE_DESIGN: screen 10 invite pane in its mailDelivered:false form — readonly accept-link field, Copy link, expiry micro-meta — and the domain mismatch as a non-blocking warning; markup.html:399 exports only the unconditional "Invitation sent." and :398 only a blocking error.`

`HITL_CLAUDE_DESIGN: screen 10 in the Procurement read-only variant — roster without Actions or invite pane, explanation block and real Admin addresses beneath; markup.html:403 hides the table entirely and names a fixture admin.`

`HITL_CLAUDE_DESIGN: screen 1 create form with the derived-currency micro-meta under Country, and the pick row in its zero / singular / null-profile forms including the role tag for a non-Admin member; app.jsx:138 hardcodes seededCount 3 and roleLabel is always "Workspace Admin".`

`HITL_CLAUDE_DESIGN: screen 10 member row in the Expired status with its "Send a new invitation" row action; app.jsx:133-134 exports Active and Invited only.`

### Constraint this wave places on NW-09 (this seat is not a listed seat on that item)

The copy half of NW-09 lands on a screen this seat owns, so — as with the
product-owner's constraint at `:71-83` — this is an acceptance constraint for
NW-09's seats, not a decision taken on their behalf. **`WorkspacePickerScreen.tsx:173`
renders the literal `contracts`**, dropping "validated"; the string is fixed in
the same task that makes the number real, or the picker claims a portfolio the
user does not have. **No separate design task is opened for it.** And the
"same number" set is **five** surfaces, not three — the pick row, screen 2's
`askScope`, screen 6's `pfSummary`, and the rail's **Portfolio** and
**Renewals** badges (`ia-v2.md:30-31`) — while the rail's **Documents** badge
counts documents and must be left alone (NW-10, W16). Acceptance N8's "rail
matches" means the Portfolio/Renewals badges, never the Documents one.

### Corrections to this seat's own lane draft, on the record

Four, and three of them were caught by other seats' work rather than by this
one: the domain rule is a **warning, not a block** (the product-owner's
deferral); `/workspace/members` needs **two** destructive affordances, not one
(client-architect); the accept screen's wrong-account copy **may not echo the
invited address** (security-architect's non-echoing 403 plus ADR-026 §D5's
payload); and the invite result ships **two** strings under one boolean, not
three under a discriminator (cloud-architect's C5 and software-architect's §D6).
The lane draft also carried a fifth error of its own: it specified a leading
"Not specified" option on the Industry select that `markup.html:55` does not
contain — an **unnamed divergence**, which is the one thing this seat may not
produce.

## Delivery-manager decisions and reconciliations (w14 table)

**Headline for the decomposer: w14 changes exactly two workflow files** — one
new (`backfill-workspace-membership.yml`) and one one-line (the guard in
`seed-demo-fixture.yml`). Any task whose diff touches `.github/workflows/**`
outside those two is a defect, and the final-integration task fails on it
(ADR-014 w14 footer, clause 4). Cloud-architect's headline is its counterpart:
**no infra task exists in this wave**, and none may be invented for the seed or
the backfill (C4b).

### Ordering constraints (`depends_on` hints for the decomposition)

Three mechanical hazards drive this section, all of which cost e13 real work.
`check_single_writer.py` **refuses** a slice whose same-phase tasks claim one
file, creation included (`wave-close-e13.md:82`) — it runs as a start hook, so a
violation kills the run rather than warning. The e13 union-merge defect that
shipped a broken build was two tasks writing one C# file; union is now limited
to prose and code collisions go to `conflict-fixer`, then abort (`:63-66`). And
**`depends_on` is now load-bearing, not advisory**: the process sets
`require_delivery: true` and `task_failure_markers: ["HALTED:"]` (`:73-74`), so
a halted task is recorded as failed and **blocks its dependents**, where in e13
it was silently "finished". A wrong edge now stops the wave instead of producing
a lie.

| Cluster | Claimed by | Constraint |
|---|---|---|
| `…/Raffa.Api/WorkspaceEndpointExtensions.cs` | NW-02, the ADR-025 invite guard, NW-01, NW-04, NW-58 | **Serialize across phases, one owner per phase.** The guard is **not** a fifth claim — it folds into NW-02's phase-1 task (see below) |
| `…/Raffa.Identity.Workspace/Migrations/Scripts/identity-workspace.sql` | NW-58 (`workspace_invitation` + its policy), NW-24 (three columns) | **One migration task owns this file for the whole wave, in phase 1.** Both land in it, and the RLS policy ships in the same migration or `TenantRlsDeployableScriptCheckTests` fails the build |
| `web/openapi/raffa-api.v1.json` + `web/src/api/generated/schema.ts` + `client.ts` | NW-01, NW-04, NW-58 | Client-architect's rule adopted: `schema.ts` is regenerated wholesale, so parallel edits produce a conflicting artefact, not a mergeable diff. One contract owner per phase; each backend endpoint task carries its own regeneration; **never** a separate "regenerate the client" task |
| `web/src/App.tsx` | NW-03 (async gate), NW-58 (`BrowserRouter` hoist) | Same task or strictly sequenced, NW-03 first — the two edits touch the same twenty lines |
| `web/src/routes/signin/*` (`workspaceStore.ts`, `WorkspacePickerScreen.tsx`) | NW-01-web, NW-03, NW-09-web, NW-24-web | **Fold into one web task.** They are one coherent change: the picker stops being a `localStorage` cache |

**The correction this seat brings to its own lane plan, and it is the reason
this section is not a restatement.** ADR-025's OQ-sec-001 requires the invite
guard to **merge before NW-01**; this seat's lane put the whole NW-58 backend in
phase 4, *after* NW-01 in phase 2. Moving the guard earlier on its own does not
work either, because the guard resolves `Admin` **from the membership row**, so
shipping it before NW-02 writes the creator's membership leaves **nobody able to
invite**. One placement satisfies both, at zero extra task cost: **the guard
folds into the phase-1 task that already owns `WorkspaceEndpointExtensions.cs`.**

```
prereq (HITL, no task) : W14-01                  -> reports/plan/gates/w14.hitl-ok

P1  t01 schema           identity-workspace.sql: workspace_invitation + policy
                         + workspace.industry/country/currency        deps: -
    t02 NW-02 + guard    creator membership on POST /workspaces AND the
                         Admin gate on POST …/invites — sole owner of
                         WorkspaceEndpointExtensions.cs this phase     deps: -
    t03 seed + backfill  demo-fixture-seed.sql membership rows
                         + seed-demo-fixture.yml guard line
                         + backfill-workspace-membership.yml          deps: -

P2  t04 NW-01 + NW-09    GET /api/workspaces (+role, +count) + contract
                         + client                                     deps: t01, t02

P3  t05 NW-04            GET …/members + client                       deps: t04
    t06 NW-14            header demotion + the two-account proof      deps: t04

P4  t07 NW-58 backend    invites / accept / DELETE ×2 + last-Admin guard
                         + IInvitationMailer + contract + client      deps: t01, t05

P5  t08 web sign-in      NW-01-web + NW-03 + NW-09-web + NW-24-web
                         (owns App.tsx, workspaceStore, picker)       deps: t04, t07
    t09 NW-58 web        /invite/accept + members from server
                         + copyable link + honest copy                deps: t07, t08
    t10 final integration                                             deps: all
```

**`t03` is `deps: -`, which is a deliberate correction to this seat's lane
plan** (it had the seed at phase 3 depending on the schema task). It has **no
schema dependency at all**: `workspace_user` and `workspace_membership` already
exist — `identity-workspace.sql` is the migration that *creates* them, and
`InviteAsync` writes a membership row today — so the seed rows and the guard
line need nothing from t01. Putting the operator path in phase 1 is the
de-risking move: it is the wave's only deliverable that a fan-out task cannot
verify, so it should exist as early as possible. **Phase 1 is the wave's single
point of failure** — under `require_delivery`, a halt in t01 or t02 correctly
fails t04 and therefore everything — which is why all three phase-1 tasks are
small and `deps: -`. `t05`/`t06` are same-phase but disjoint (endpoint file vs
test + `components/shell/*`); confirm before scheduling. Per-task wall clock is
bounded by the engine's own 900 s inactivity / 10800 s absolute deadlines
(`wave-close-e13.md:87-88`).

### The domain-rule prerequisite is relaxed by this table, and the record now disagrees with itself

Client-architect's prerequisite 2 (`:258`) requires "a second Entra account **on
the same email domain**", because `validateInviteEmail` rejects a cross-domain
invite client-side. Ux-ui-designer then **demoted that rule from a block to a
warning** with submit enabled (NW-58 row; design-ref row `:68`). Both are on the
record and they no longer agree. This seat owns the resolution because it
decides whether N3b is runnable on deployed `dev`, which is the gate on
promotion:

- **After t09 lands, the domain constraint is gone** — the warning does not
  block submit, so any second Entra account works. The demotion ships in the
  same task as the accept flow, so by acceptance time the constraint is not
  there.
- **It never binds an API-level invite at all**: the check is client-side, before
  any request, and the server has no domain rule (security-architect confirmed
  none is added in w14). A `curl` invite bypasses it entirely.
- **It binds exactly one case**: a *UI-driven* proof run executed before t09 —
  which is NW-14's two-account chain (t06). For that window, either use an
  address on the Admin's domain or drive the invite through the API.

So the operator prerequisite for w14 is **a second Entra account**, with no
domain condition, and client-architect's prerequisite 2 is superseded on that
point. Recorded here rather than left as two rows that contradict each other.

### What the final-integration task must run

Modelled on e13's F11/T01, the precedent for this seat's wave-close duties
(`wave-close-e13.md:29`).

1. **Build/test on the renamed base**: `dotnet restore/build/test`, and
   `npm ci && npm run build && npm test` in `web/`.
2. **Rename assertion**: `rg -n "Raffa\." backend/src web/src` returns nothing.
3. **Acceptance doc** — `docs/workspace-identity-acceptance.md`, numbered
   operator-runnable steps against **deployed `dev`** for N1, N2, N3, N3b, N4,
   N5, N8, N9, W14-A1 and W14-A2, each naming the URL, the header posture and
   the expected status code; same shape as `docs/ask-v2-acceptance.md`. It must
   be runnable by a human with a browser and `curl`, **not** by CI — no workflow
   runs Playwright (`web.yml:71-81`; NW-50 is queued W18), and N9 (Delete → 204
   for the creator, 403 for Procurement, from a second browser) cannot be proven
   any other way. *Why this earns its cost*: `containerapps/main.tf:117-121`
   records that a missing `ConnectionStrings__Suppliers`, which crashed the live
   API on boot, was found by E13/F11/US01/T01 **while writing the V2 acceptance
   runbook**, and `:89-95` records two more env vars missing in production since
   the tasks that needed them landed. Writing the acceptance doc is how that
   class of defect gets caught.
4. **README sweep**: `backend/README.md`'s "interim header posture" must now
   state that the **membership row** is the role source of truth and that
   `X-Role`/`X-Workspace-Role` are never the product answer (NW-14);
   `web/README.md` for the server-backed count and the `/invite/accept` route.
   `infra/README.md` only if infra changed — it does not.
5. **No-CI-drift assertion**: `git diff --stat origin/main -- .github/workflows`
   shows **exactly two** files. Any other workflow diff fails the task.
6. **PR `integration → main`** with CI green on the required checks
   (`backend / build + test`, `web / build`; `infra / terraform fmt + validate`
   only if `infra/**` was touched, which it is not).
7. **Do not cut a `demo-v*` tag.** Promotion is a separate operator act after
   the gate below — "then `docs/ask-v2-acceptance.md` A1–A14 on `dev`, **then**
   `demo-v*`" (`wave-close-e13.md:105`). A wave that tags itself has skipped the
   gate.

### Promotion path for w14

1. Tasks merge to `integration`; PR `integration → main`; CI green.
2. Merge to `main` → `dev` deploys automatically, and `backend.yml` applies the
   new schema to `raffa_dev`.
3. **Operator runs `backfill-workspace-membership.yml` against `dev`** for the
   HITL-supplied `(tenant id, admin email)` pairs — **before** acceptance, not
   after, or N2/N8/N9 fail for exactly the workspaces that matter.
4. **Operator runs `docs/workspace-identity-acceptance.md` on deployed `dev`.**
   A browser and `curl` against the real environment, not a green CI suite.
5. Only then `demo-v4` (`demo-v3` = `2db5734` is live; `demo-v2` exists but was
   cancelled), approving **four** pending deployments separately:
   `demo-promotion`, `promote-infra`'s apply, `promote-backend`'s deploy,
   `promote-web`'s deploy (runbook `:90-97`).
6. **Run the standing promotion check** (runbook `:8-11`, `:99-113`):
   `python scripts/check_demo_swa_config.py --host <swa-host> --environment demo`,
   or dispatch `demo-config-check.yml`. Expect `[PASS]`. **Repo-root
   `scripts/`, not `.helix/scripts/`.**
7. **After** the promotion, run `seed-demo-fixture.yml` against `demo` so the
   fixture tenant gets its membership rows, then re-check the picker on `demo`.
   Order matters: the seed's guard requires the new schema, which
   `promote-backend` applies.
8. Rollback is unchanged (re-tag a known-good `main` SHA as `demo-v5`; there is
   no "undo"). A code rollback **does not un-apply schema** — ADR-021 scripts
   are forward-only and idempotent — which is safe here because w14's schema
   changes are purely additive. Promotion never touches data either way.

### Reconciliations

| With | Point | Resolution |
|---|---|---|
| D1 | cloud-architect C4 — zero Terraform delta | **Confirmed reciprocally, and their C2 is the stronger form of this seat's own finding.** Both lanes independently concluded the API cannot learn the SPA origin without Terraform; they located it in ADR-026 §D5's payload where this seat had located it in `Web__BaseUrl`. Their framing is better: it is a **contract** property, so it is enforced where it can actually be violated. C4b adopted — the seed and backfill are zero infra delta and **no infra task may be invented for them** |
| D2 | security-architect — OQ-sec-001, "the guard merges before NW-01" | **Adopted, and it changed the phase plan** (above). Their requirement plus the guard's own dependency on NW-02's membership row admit exactly one placement. This is the wave's tightest ordering constraint and neither seat could see it alone: they own *that* it must precede NW-01, this seat owns *where* it can physically go |
| D3 | security-architect — the Entra scope literals | Adopted into ADR-010's w14 footer, which is the right home: it is the identity-plane half of the rebrand and that ADR's own Assumptions still call those scopes "placeholders". W14-A1 point (e) is the check that catches it |
| D4 | software-architect — `WorkspaceEndpointExtensions.cs` | The four-way serialization is granted by the phase plan, with the guard folded into t02 rather than added as a fifth claim. If they prefer to split the file in phase 1 instead, that is compatible; the constraint is one writer per phase, not one shape |
| D5 | client-architect — prerequisites 1–3 | **All three adopted, one corrected.** The membership seed and the backfill are this seat's t03, in phase 1. Their prerequisite 3 ("the operator path lands with or before the web task") is satisfied twice over — in the wave by t03's phase, and on the environment by promotion step 3. Their prerequisite 2 is **superseded on the domain point** by the ux ruling (above) |
| D6 | ux-ui-designer — the domain warning | Its delivery consequence is the one that matters here: it removes an operator prerequisite that would otherwise have made **N3b unverifiable** whenever the second Entra account sat on another domain, and an unverifiable acceptance step is an unpromotable wave |
| D7 | product-owner — OQ-w14-002 | Concur with the deferral, from scheduling grounds that are additive to theirs: the transport's Terraform apply is human-confirmed in the HCP UI and is **not observable by any task in the wave that wrote it**, so it is a HITL barrier mid-wave, not a `depends_on`. Had the table ruled the other way, this seat would have asked for the transport as its **own wave** rather than compressing this one |

### Open questions this seat opened or closed

| OQ | Question | Status |
|---|---|---|
| OQ-w14-003 | Which commit does the wave branch from? | **ANSWERED** in the OQ table — a *merge*, not a rebase; no SHA is named from a council document; W14-A1's five points are the gate |
| OQ-w14-sa-01 | How do existing `dev` workspaces get an Admin? | **ANSWERED, delivery half** — the mechanism is this wave's `backfill-workspace-membership.yml`, run on deployed `dev` before acceptance; `demo` needs none (the seed covers the fixture tenant); *which* tenants is an operator/product call |
| OQ-w14-cl-02 | Managed-domain deliverability on `demo` | **Concur — deferred with the transport.** Naming it here because this seat is a listed owner: it is a DNS-gated task that cannot complete inside one wave, so it could never have been a w14 item regardless of the transport ruling |
| OQ-w14-cl-04 | Resource-name prefix after the rebrand | **Open, delivery half added** — no identifier is ever copied from a council document; the rename surface includes `.github/`, `scripts/` and `backend/scripts/`, and the identity plane is the part a resource-name review misses |
| OQ-w14-dm-01 | Does w14 add a per-environment web-origin setting to the API? | **CLOSED — no.** The accept link is composed client-side from a site-relative `acceptUrl` + fragment. The setting becomes mandatory the moment a real mail transport is scheduled, and **it cannot be added by CI** (`backend.yml` rolls `--image` only) |
| OQ-w14-dm-02 | Is the CI principal's Key Vault Secrets User role granted by Terraform? | **Open, pre-existing, not w14's to fix.** `backend.yml:251` documents that `modules/keyvault` grants it to the workload identity only, not the CI principal (`infra/README.md` "Known gaps"). The backfill inherits that gap exactly as `seed-demo-fixture.yml` and `backend.yml` already do — it does **not** create it. Cloud-architect owns the module |

**W15 ordering note, recorded because security-architect addressed it to this
seat** (OQ-sec-006): if the table ever wants `WorkspacePrincipalAuthorization.cs`
changed earlier than NW-05/NW-08, that is a **W15 ordering decision, not a new
w14 task** — it touches `GET /api/audit`, which no w14 item owns.

### Corrections to this seat's own lane draft, on the record

1. **"No workflow change" for the seed was wrong.** `seed-demo-fixture.yml`'s
   guard (`:123-130`) exists precisely to convert a missing relation into an
   honest error and must gain `workspace_membership`. Consequently the
   final-integration assertion moved from "exactly **one** workflow file" to
   **two** — revision 1 contained an assertion that would have **failed its own
   integration task**.
2. **The phase plan violated ADR-025's OQ-sec-001** (guard before NW-01), and
   the fix is not "move the guard earlier" but "fold it into t02" (above).
3. **The seed task had a schema dependency it does not have.** `workspace_user`
   and `workspace_membership` pre-date this wave, so t03 is `deps: -` and
   belongs in phase 1.
4. **"There is no generic extra-env variable" was too absolute** — one
   `dynamic "env"` block exists (`containerapps/main.tf:165-174`), bound by
   ADR-004's amendment to per-role model ids. Recorded precisely, because it is
   the shortcut someone will propose.
5. **A workflow comment was cited as ADR-016.** "Keep it that way" is
   `demo-promote.yml:43-50`; ADR-016's own wording is `:79-80`. Same conclusion,
   wrong source — both are now cited.
6. **§7 omitted the runbook's standing promotion check entirely**, and §4
   dismissed `demo-config-check.yml` as "untouched" without noting it must be
   **run**.
7. **This seat's own lane skill is stale.** `skills/delivery-lane.md` still
   lists as locked "GitHub org Raffa, four private repos, not a monorepo".
   Accepted **ADR-014 `:7`** says "**One public** repository `raffa` … Not
   four remotes", corroborated by `ADR-015:78` and the runbook's live
   `gh api repos/lucalamalfa91/raffa/…` (`:46`). The lane draft assumed the
   monorepo throughout and was right; the staleness is recorded so **no seat
   re-opens a settled decision from a skill file**.

### Calendar

Anchored on the run date (`2026-09-10`) in **elapsed days**, not weekday names —
this seat has no calendar oracle and will not invent one.

| Window | Content | Gate at the end |
|---|---|---|
| D+0 | HITL: merge base, path translation, **W14-A1 incl. one green `dev` deploy and one interactive sign-in** | `reports/plan/gates/w14.hitl-ok` |
| D+0 → D+2 | P1–P3 (schema, membership + guard, seed/backfill, `GET /workspaces`, members, NW-14) | `dev` deploy green |
| D+2 → D+4 | P4–P5 (invitation lifecycle, web sign-in cluster, `/invite/accept`) | final-integration task |
| D+4 | PR `integration → main`, CI green, merge, `dev` auto-deploy, **backfill run** | acceptance doc on `dev` |
| D+5 | `demo-v4` + four approvals + config check + `seed-demo-fixture.yml` on `demo` | wave closed |

**Assumptions in force** (for `reports/open-questions.md`): a single operator
drives the HITL gates — corroborated by the runbook `:43-56`, where `demo`'s
only required reviewer is `lucalamalfa91`; **no second wave runs concurrently**
(e13's D13 hazard, `wave-close-e13.md:84-86`: "Never touch `integration` or the
clone's HEAD while a wave runs"); and a second Entra account exists for N3b,
with **no domain condition** (above).

## Votes (final table round)

| Seat | Vote |
|---|---|
| product-owner | APPROVE — ADR-001 w14 footer written, INDEX section appended, NW-58 + NW-24 rows filled, OQ-w14-002 and OQ-w14-004 answered |
| software-architect | APPROVE — ADR-026 written (new), ADR-003 w14 footer appended, INDEX rows added, NW-01/NW-02/NW-04/NW-09/NW-58/NW-24 rows filled, OQ-w14-001 concurred, OQ-w14-006/007 resolved; six reconciliations with security-architect recorded below |
| cloud-architect | APPROVE — ADR-005 + ADR-006 w14 footers written (bodies untouched, `Status: accepted` unchanged), INDEX section appended, NW-58 row filled, OQ-w14-002 reconciled with the product-owner, OQ-w14-cl-01…04 recorded; **w14 Azure and Terraform delta confirmed zero in both environments**, conditional on the two normative contract properties in the NW-58 row |
| security-architect | APPROVE — **ADR-025 written (new, `Status: accepted`)**; w14 amendment footers appended to **ADR-009, ADR-010, ADR-011 and ADR-022** (all four bodies untouched, every `Status: accepted` unchanged, every footer a **narrowing**); INDEX section appended with the ADR-025 row and the four footers; NW-14 row filled and this seat's half appended to NW-01, NW-02, NW-04 and NW-58; **OQ-w14-005 answered**, OQ-w14-001 answered with software-architect, **OQ-w14-cl-01 decided (fragment)**, OQ-w14-po-02 concurred with the enforcement half; OQ-sec-001…006 recorded, six reconciliations with software-architect and five with the other seats. **ADR-026 §D4's token shape is ratified** under Rules C4/C5, and cloud-architect's zero-delta condition is **confirmed: no server-held key, no Key Vault secret, no environment variable from this seat** |
| client-architect | APPROVE — **ADR-012 and ADR-018 w14 amendment footers written** (both bodies untouched, both `Status: accepted` unchanged; ADR-012's is its first footer, ADR-018's **adds to** the epic-13 V2 footer and does not supersede it), INDEX section appended, **NW-03 row filled** and this seat's half appended to NW-01, NW-04, NW-09, NW-14 and NW-58; **OQ-w14-cl-01 realized** (fragment → memory for one mount → header; MSAL `state` rejected), OQ-w14-client-01…05 recorded, eight reconciliations. **No new ADR from this seat.** Two corrections of this seat's own lane draft are on the record: the `sessionStorage` token stash is **withdrawn** (ADR-025 Rule C10, and it would not have survived the redirect anyway) and NW-14's second-account chain holds only while `InviteAsync` still grants on invite. One open request to software-architect: **no `X-Tenant-Id` on a route that already carries `{tenantId}`** (ADR-026 §D3's header line) |
| ux-ui-designer | APPROVE — **ADR-018, ADR-019 and ADR-020 w14 amendment footers written** (all three bodies untouched, all three `Status: accepted` unchanged; ADR-018 now carries **two** w14 footers — client-architect's route footer at `:166` and this seat's design footer at `:236`, which **adds to it and supersedes nothing**), INDEX section appended, **NW-58 and NW-24 rows filled**, and the **"Design refs carried into decisions" table rewritten** — the row that told the decomposer to use `name · email · role · status` and "Also uploads, deletes, manages members" is corrected, because following it literally would have regressed decision **D8** and fabricated member names the backend does not store, and the NW-14 row that read "none — no visual change" is corrected, because the pick row's role tag is hardcoded to "Workspace Admin" for whoever is looking. OQ-w14-004 and OQ-w14-005 concurred; OQ-w14-ux-01…03 recorded; nine reconciliations. **No new ADR from this seat** — w14 adds screens and states to an existing system, not a new design surface. Four corrections of this seat's own lane draft are on the record, three of them found by other seats: the **workspace-domain rule is a warning, not a block** (the product-owner's ADR-001 deferral — a client-side check against a server with no domain rule is a typo guard, not a safeguard), **two** destructive affordances rather than one, the accept screen's wrong-account copy **may not echo the invited address**, and the invite result ships **two** strings under one boolean rather than three. This seat also **withdraws its lane ask for a `domain` column** on NW-24. Six Claude Design exports are recorded as owed and **none is blocking** |
| delivery-manager | APPROVE — **ADR-014 and ADR-016 w14 amendment footers written** (both bodies untouched, both `Status: accepted` unchanged; each is the **first** amendment its file has ever carried — ADR-014 `## Decision outcome` `:49` / `**Chosen: Option 1**` `:51` with the footer at `:110`, ADR-016 `:39`/`:41` with the footer at `:93`), INDEX section appended at `:319`, **W14-01 row filled** and this seat's half appended to NW-58; **OQ-w14-003 answered** (a *merge*, not a rebase, and no SHA named from a council document), **OQ-w14-sa-01 answered on its delivery half** after two seats referred it here, OQ-w14-cl-04 given its delivery half, OQ-w14-cl-02 concurred, OQ-w14-dm-01/02 recorded; seven reconciliations. **`ADR-015`: `none` — no new identity, federated credential or stored secret.** **No new ADR from this seat.** Cloud-architect's zero-delta confirmation is **reciprocated**: w14 changes **exactly two workflow files** and no Terraform. Three corrections of this seat's own lane draft are on the record, one of which is load-bearing: the lane phase plan **violated ADR-025's OQ-sec-001** by landing NW-01 before the invite guard, and the fix is not "move the guard earlier" — the guard resolves `Admin` from the membership row, so it must land *with* NW-02 or nobody can invite — it is **fold the guard into the phase-1 task that already owns `WorkspaceEndpointExtensions.cs`**, which satisfies the security requirement and the single-writer rule at zero task cost. Also corrected: the seed needs a **second** workflow file, so revision 1's "exactly one file" integration assertion would have **failed its own task**; and the seed task has no schema dependency, so it moves to phase 1. One resolution this seat owns because it gates promotion: client-architect's "second Entra account **on the same domain**" prerequisite is **superseded on the domain point** by ux-ui-designer's demotion of that rule to a warning — the two rows contradicted each other on the record, and an unverifiable N3b is an unpromotable wave |

## Gate verdict

**approved** — run `f3018639` (Helix Studio, 2026-09-10, gate's third pass over
the disk state, 21:18 local): nine decisions; two new ADRs (ADR-025, ADR-026);
fourteen ADRs amended by footer (001, 003, 005, 006, 009, 010, 011, 012, 014,
016, 018, 019, 020, 022) with every body and every `Status: accepted` intact
and no w14 supersede; seven `none — no change` actions; `INDEX.md` complete
at 26 rows, none removed. All seven seats involved; every latest vote `APPROVE`.

Applied from the gate's final turn (the gate cannot edit this file). The run
was recorded as `closed_without_required_artifact` only because Studio had
launched it with the working directory `.helix/.git.nest.bak`, so the engine
evaluated `close_requires_glob` outside the artifact folder (NEXT-PROCESS.md
D-N11); the council's files were complete on disk.

Open threads the gate handed to HITL and to the decomposer:

- Re-issue of a lapsed invitation has **no legal path yet**: ADR-020's second
  footer (`:504-532`) needs it, ADR-026's partial unique index
  (`:209-210`, `WHERE accepted_at IS NULL AND revoked_at IS NULL`) blocks it;
  software-architect + security-architect own the mechanism, and delivery-
  manager confirmed neither alternative moves the phase plan.
- Three ADR **bodies** still carry text a later footer supersedes —
  `ADR-005:126` (`?token=` accept link), `ADR-026:146` (`X-Tenant-Id +
  X-User-Id` header line), ADR-020 row 10.3 ("Send a new invitation"). Tasks
  must read the footers, never copy a body line verbatim.
- ADR-026's "ADR-025 Rule 5.2f/5.2g" resolves only through ADR-025's footer
  (`:743`); T15 and the browser tests have no CI runner and live in the manual
  acceptance doc (ADR-016 `:177`); `w14-requirements.md:730-731`'s infra-
  ordering bullet is dead — `waves/w14.md:567` governs.
