# Wave w16 — council decisions

Skeleton written by `next-intake`; rows completed by the involved seats at
`next-council-close`; verified by `next-council-gate`. Source:
`reports/context/waves/w16-requirements.md`.

Wave theme: **nothing the product knows lives only in a browser tab.** Baseline
`f0b3436` (`helix/w16`); the wave base **is** `origin/main` @ `f0b3436` —
`0 0` ahead/behind, so unlike w14 and w15 there is no base row and no
"merge main first" open question.

Rows below are the **nine in-wave items** of `w16-requirements.md` §5, in
selection order: the four carry-over head items (already decomposed under
epic-18 F02/F03, promoted `queued → live`), then the four session-store items,
then one intake-originated cleanup.

**NW-10 is not at this table** — it is CLOSED-ON-MAIN (`9c4975e`, ancestor of
`HEAD`), confirmed with evidence per raw §0.5 and kept out of the wave.
**NW-71 is not at this table** — it is parked on `helix/w17-input` and raw §0.3
forbids ingesting it.

> **Re-entry note (intake, third pass — baseline refresh).** The raw file's
> `source_sha256` is unchanged (`d3518a6…`), so `w16-requirements.md` was
> **kept, not rewritten**, per the verify-or-write rule. The **baseline moved**
> `ff66ee6` → **`f0b3436`** (PR #119 documents queue order, PR #118 Worker
> throughput). That delta is **four files**, none of them evidence for any item
> here, so every row's audit still holds — it was re-verified on `f0b3436`
> (see `w16-requirements.md` §1 "Wave base"). §3's roster was re-checked
> against §2's per-item seats and **matches exactly**.
> A previous w16 run reached the table and left one lane draft on disk
> (`reports/architecture/draft/next/delivery-manager/w16.md`, NW-31 + OQ-w16-008)
> but never closed: every row here is still `pending`. Seats re-entering should
> read that draft before re-deriving NW-31.

| Item | Seat(s) | Decision | ADR action | File |
|---|---|---|---|---|
| NW-07 — conversation `user_id` is the token subject | product-owner, software-architect, security-architect | **PO**: scope IN, **reduced** — the keying seam closed in w15, so what remains is a *data disposition*: pre-w15 rows are **recorded as retired, never remapped, never deleted** (OQ-w16-001 ratified; a blind email→`oid` remap could hand one user's threads to another — worse than the loss). **A16-1 reworded** (the UPN-casing check is vacuous: the key is `oid`), and the promoted task's DoD `:69` **must not be implemented** — it would re-introduce the normalization ADR-010 rejected. No surface may count a conversation it cannot open. **SA**: the keying seam **is already closed on this tree** — `TryResolveUserId` exists nowhere, all five handlers resolve through `ICallerContext`, absent identity is 401 — so `OQ-askv2-005` **retires with this item** and the task writes **no migration**. What it must do: delete the stale paragraph `ConversationsEndpointExtensions.cs:23-37` (it still asserts an `X-User-Id` fallback and a 400 the code beneath it does not have — the doc-comment class that makes a grep audit return a false verdict). The `ExternalSubjectId` comparison rule is **ADR-010's** (security S16-1/S16-2), cited not decided here. **SEC** (ADR-010 owner, and the rule is broader than the intake's finding): **the same predicate — "is this identity this `workspace_user`?" — exists four times and they disagree on two axes.** **S16-1**: `CallerContext.cs:143` (membership → 404) and `WorkspaceRoleResolver.cs:80` (role → 403) both match `Email == identity || ExternalSubjectId == identity`, so **membership and role can be granted by a match on `workspace_user.Email`** — a column an Admin writes at invite time. **Not exploitable on this tree** (the identity is always a GUID-shaped `oid`; an email never equals one) — a latent divergence, not a live bypass — but it is **one `workspace_user` row carrying a GUID-shaped `Email` away from being a grant**, creatable through the ordinary invite path. The email leg is deleted from both comparators; email keeps only the two jobs w15 §2.1 gave it. **S16-2**: the four sites also disagree on normalization — `WorkspaceDirectoryService.cs:100,104,144` lower-cases the identity and compares the **`oid`** leg against it, RLS `identity_self` is `lower(email)=lower(guc)` OR ordinal subject, the two authorization comparators are raw. Benign while every subject is a lowercase GUID; a **user-visible split** the moment one is not — *listed workspace, 404 on every route inside it* — and nothing enforces GUID-ness (`varchar(200)`). **Fixed at the source, not per site**: the identity stops being lower-cased and each leg normalizes itself, as the shipped policy already does; **the policy text is not rewritten** and ADR-009 w15 5b's bound parameter stays. **S16-3** (security half of OQ-w16-001): PO's disposition is ratified and **one mechanism is forbidden regardless of it** — a backfill re-keying `conversation.user_id` by an `Email` match would re-point ownership **through the very leg S16-1 deletes**, a cross-user data move by the weakest key in the system; if history is ever wanted the only acceptable form is an explicit `(old key, oid)` pair at HITL, one tenant at a time, audited. **A16-1's UPN-casing check is vacuous *because* `oid` is deliberately not lower-cased (`CallerIdentity.cs:60-68`) — a task reading "normalization mismatch" must not reach for `ToLowerInvariant()`.** Test **S-T24**. | `amend ADR-001` (w16 clauses 0–1) · `amend ADR-024` (w16 clause 1) · `amend ADR-010` (w16 clauses 1–4) · `amend ADR-009` (w16 clause 1 — the w15 §6 NW-07 bullet's evidence line is corrected) | `reports/architecture/ADR-001-scope-r0-r4.md` · `reports/architecture/ADR-024-ask-raffa-v2.md` · `reports/architecture/ADR-010-entra-oidc.md` · `reports/architecture/ADR-009-tenancy.md` |
| NW-08 — `GET /api/audit` works for a real Admin | security-architect, software-architect, client-architect | **SA**: the route adopts the shape every other tenant-scoped route already uses — `ICallerContext.ResolveTenantAsync` then `WorkspaceRoleResolver.IsAdminAsync` (`DocumentsEndpointExtensions.cs:492-509`) — and the claim-reading guard is dropped. **OQ-w16-002 answered: this is a contract change on a live route**, not a drop-in — the tenant moves from a `tenant_id` claim to an `X-Tenant-Id` **verified against membership**, so the contract must declare the header or the client discovers it at runtime; the "no `?tenantId=`" property survives in the stronger form *selector, never an authorization input*. Ladder is security's S16-4 verbatim; **`WorkspacePrincipalAuthorization` is deleted whole** (S16-5 adopted — leaving the type leaves a working claims authorizer for the next endpoint to pick up). **SEC** (ADR-011 / ADR-025 owner): **S16-4 — who may read a tenant's audit trail: a live `Admin` membership in the selected tenant, and nobody else.** Ladder verbatim, no bespoke version: no validated token → **401**; missing/non-GUID `X-Tenant-Id` → **400**; well-formed tenant with no live membership → **404**, never 403 (Rule B1 — a 403 is a tenant-existence oracle); member without the role → **403**; Admin → **200**, that tenant's rows only. **The guard fails closed today, which is correct — the danger is the repair, not the defect**: mapping `tid` → `tenant_id` is refused by ADR-010 w15 §2.2 (one directory across both environments ⇒ `tid` is identical for every workspace and can never select a tenant) and minting a role claim by §3. **S16-5 adopted as SA states it**: `WorkspacePrincipalAuthorization` is deleted **whole** — the minimal repair leaves a working, fail-closed, helpfully-named claims authorizer in the domain assembly for the next endpoint to pick up, which is exactly how this defect arrived. **OQ-w16-002 answered — the swap strengthens the property it appears to relax**: "no `?tenantId=`" defended against a cross-tenant read; under `ICallerContext` the tenant is still never trusted but is a **candidate verified against the token subject's membership before the scope opens** (`CallerContext.cs:135-152`), so enforcement moves from *the absence of a parameter* to *a membership fact*. Recorded so no reviewer reads "claim → header" as "authorization → client input". **RLS is the backstop, membership is the gate**: `audit_event` is already `ENABLE`+`FORCE`+`tenant_isolation` (`audit.sql:54-56`) and the read runs **inside** the verified scope (ADR-009 w16 clause 3a) — that is what makes a mistake in the gate survivable, never a reason to soften it. **Deletion proof required beyond the ladder (S-T25)**: a token carrying `tenant_id` **and** `roles: Admin` for a tenant the caller has no membership in → **404**, not 200 — an unreachable claims branch passes every other test until something reaches it. Plus **S-T26**, *another tenant never appears*: Admin of T1 with `X-Tenant-Id: T2` → 404 and zero T2 rows in any form. **OQ-w16-003: concur, no web surface — and the reversal is bounded**, not free: the body is a whole tenant's trail, the most sensitive read in the product, so a screen would need its own ADR-020 row and a re-review here (pagination bounds; no actor identifier beyond what the member list already shows). **CLIENT** (ADR-012 owner; w16 footer clauses 24 + 27): the contract gains the path and **`client.ts` gains no method** — `/api/audit` has no caller, so it joins the documented-but-unwrapped set the contract file already names (`raffa-api.v1.json:6`: `/api/insights/criticality` and `GET /api/contracts/{id}/strategy`, "for completeness, no `client.ts` wrapper"). **C1 — the contract edit has a prose half, and without it the file contradicts itself**: `info.description` at `:6` names **`GET /api/audit`** by hand among the routes "that this task deliberately did NOT add here", so adding the path and leaving that sentence ships a contract that documents a route and denies documenting it — **the exact stale-record class NW-31 spends this wave deleting**. Both halves are one edit in one task. **PO's "A16-2 closes through `GET /api/audit` plus a typed client" is satisfied by the generated type, not by a wrapper**: `schema.ts` is regenerated wholesale, so the type arrives with the path at zero cost — recorded so the gate does not read a missing `ApiClient` method as an unmet ruling, and so no task adds a method with no call site. ADR-026 w16 clause 2's declared `X-Tenant-Id` is **documentation `tsc` cannot check** — the generator parses only `responses` (`generate-api-client.mjs:132-146`), so a declared *parameter* never reaches `schema.ts`. **No SPA surface**: no route, no rail entry, no fetch, no state — `navItems.ts` and `App.tsx:222-236` untouched, **ADR-018 / ADR-020 `none`**; A16-2's ladder is proven by backend tests and `curl`, **no e2e and no vitest from this seat**. If OQ-w16-003 is ever reversed the client cost is a route, a rail entry, a wrapper, its surface-test row, a loading and an error state — a wave item, never a task addendum. | `amend ADR-026` (w16 clauses 1–2, 4) · `amend ADR-002` (w16 clause 5) · `amend ADR-011` (w16 clause 14) · `amend ADR-025` (w16 §K.1–§K.3) · `amend ADR-009` (w16 clause 3a) · `amend ADR-012` (w16 clauses 24, 27) | `reports/architecture/ADR-012-web-stack.md` · `reports/architecture/ADR-026-workspace-api-and-invitation-schema.md` · `reports/architecture/ADR-002-dotnet-solution.md` · `reports/architecture/ADR-011-secrets-and-rag.md` · `reports/architecture/ADR-025-workspace-membership-and-invitations.md` · `reports/architecture/ADR-009-tenancy.md` |
| NW-31 — dual role headers retired (+ the reprocess workflow) | software-architect, security-architect, delivery-manager, client-architect, cloud-architect *(conditional — OQ-w16-004)* | **SA**, two halves. **Capabilities**: `X-Role` goes **and the server-side role filter goes with it** — the endpoint takes no tenant and no caller (`CapabilitiesEndpointExtensions.cs:62`), the catalog is static and tenant-free, Ask's own routing already reads it in-process with no gate (`AskCopilotService.cs:181-206`), and **nothing in `web/src` reads `roleGate`**. This takes security's S16-6 *named-acceptable alternative*, not its primary form: membership-gating needs "200 without a token" that `ICallerContext`'s ladder cannot express, and the client sends no `X-Tenant-Id` here, so it would 400 on every call from a shell component. `roleGate` stays on the wire as presentation. **Contract**: the `X-Role` parameter (`:5239`/`:5236`) and the stale `X-Workspace-Role` prose (`:1227`, inherited `:1310`) go; **`X-Tenant-Id` stays** (S16-7 / D7); the gate is a **grep, not a green build** (C4). **ADR-027 correction**: `:198` is a *handler* property and licenses **no** credential-free re-enqueue — OQ-w16-004's zero-cost premise is withdrawn (converging with D1 and security); the file's disposition stays delivery-manager's. **PO (round 2, acceptance wording only — ADR-001 w16 clause 8; no claim on the file, the steps or the identity plane)**: both published branches of A16-3 were declined, so the acceptance is **re-worded, not quietly narrowed** — *no `X-Role`/`X-Workspace-Role`/`X-User-Id` survives in `.github/`, in the contract or in capabilities; Admin catalog rows come from membership; **an Admin resubmits a document on deployed `dev` through the product's own path** and the Worker drives it to a terminal state; the surviving verification job runs green.* Nothing the product *does* is lost — the deleted CI path is 401 today — and the in-product path is a **stronger** close (real Admin, real token). **Recorded, not dropped**: w16 does **not** restore the **bulk whole-tenant** reprocess; it is deferred to **W17** with its shape designed (delivery-manager D5) and **must appear in this wave's known-gaps record** so a `demo` walker does not read its absence as a regression (w15 clause 7: a loss is acceptable only when recorded and bounded). No `must` sits behind a `could`. **Never narrowed**: the grep half — and a replacement still *named* `reprocess-*` that never reprocesses would re-create the stale record this item deletes, so delivery-manager's primary is ratified and their own fallback refused. **CA (round 2 — conditional seat resolved to *not involved*; no ADR action)**: OQ-w16-004's withdrawal is confirmed from **this seat's own files**, independently of ADR-027's text — both Service Bus roles are granted to `var.workload_principal_id` (the container-apps managed identity), topic-scoped (`infra/modules/servicebus/main.tf:71-102`), the module takes **no CI input at all**, and the CI deploy SP holds exactly **one** Terraform-granted role in this product, `Key Vault Secrets User` (`infra/modules/keyvault/main.tf:66-75`). So no runner-side publish exists without a new `azurerm_role_assignment`, and the SAS-key shortcut is refused by the module's own rule (`servicebus/main.tf:64-70`). With the API steps deleted, **w16 changes no `infra/` file, no SKU, no container-app env key and no apply path: €0.00/month, zero Terraform, no HCP run owed by this wave.** **This seat's own draft recommended a third `Data Sender` grant (Path B) — withdrawn**: it was conditional on a runner-side publisher the table did not take, and **no infra task may be minted from it**. **A16-3's re-worded in-product path needs nothing from this lane** — every key `POST /api/documents/{id}/reprocess` → Worker → terminal state consumes is already deployed on `dev` (`ServiceBus__FullyQualifiedNamespace`/`TopicName`/`SubscriptionName` `containerapps/main.tf:397-410`, `MaxAutoLockRenewalMinutes=30` `:412-415`, `MaxConcurrentCalls=4` `:424-427`, the fail-fast `ConnectionStrings__Storage` `:387-390`, `AiGateway__*`+`AZURE_CLIENT_ID` `:432-450`) and **none moves**. **W16-01 verified infra-free**: the worker container declares no `Queue__*` env var anywhere in its block (`:348-461`), so deleting the R0 trio leaves no dead key. **D12 / OQ-w16-dm-03 answered as far as this tree permits** — HCP state is not readable from here, so it is asked, not asserted: the pending apply is `worker_max_replicas 3→5` (`dev/main.tf:164`) + `MaxConcurrentCalls=4`, **already authorised by ADR-005's w15 footer §2**, an **operator** action (gate item (g)) and never a wave task, it **cannot roll the running image back** (`ignore_changes = [template[0].container[0].image]`, worker `:489-491`, verified at the table), and **it does not gate A16-3** — one document reaches a terminal state with or without it; the apply changes how fast twenty do. **Promotion arithmetic recorded so `demo` is not surprised**: `demo` keeps `worker_max_replicas = 3` (`demo/main.tf:182`) but inherits the module's `MaxConcurrentCalls = 4` ⇒ **12 documents in flight** against `gpt-5.4` @ 200k TPM (`demo/main.tf:222`, with `extract` **and** `answer` both on it `:229-230`) = **16.7k TPM per in-flight document**, versus `dev`'s 20 in flight on `gpt-5.4-nano` @ 300k = 15k — **the promotion does not under-provision `demo` and needs no capacity or SKU change**. Residual named, not hidden: on `demo` extraction shares one deployment with interactive Ask, so a large batch during a live pilot walk can throttle **answers**; a 429 on extraction is absorbed (`ExtractionTransientException` → abandon → redelivery, `max_delivery_count = 8`, TTL `P1D`, durable DLQ — `servicebus/main.tf:53-62`), so the failure mode is **delay, never loss**. If it ever bites, the remedy is a `demo`-root `gpt-5.4` capacity bump (200→300) at **€0.00 fixed cost** — `DataZoneStandard` capacity is a **rate limit, not a reservation**; ADR-005 rejects *provisioned* SKUs, which is a different thing. Constraint 4 re-affirmed as this seat's own keys: `Invitations__Mail__Enabled` and `guest_provisioning_enabled` stay `false` on `demo`. **SEC (ADR-022 owner — this footer is mine and it fires the schedule's last two rows)**: after w16 the interim **identity** posture is fully retired (`X-Role`/`X-Workspace-Role` by NW-31, `"unattributed"` by NW-32); what remains under ADR-022 is the fixture seed, which is **data, not identity**. **S16-6 — the table took this seat's *named-acceptable alternative*, and it is accepted on verified grounds, not deferred to**: `GetCapabilities(HttpRequest)` (`CapabilitiesEndpointExtensions.cs:62`) takes no tenant and no caller, `CapabilityCatalog.All` is static and tenant-free, and `ICallerContext`'s ladder cannot express "200 without a token" — so membership-gating would 400 every call from a shell component. **Not acceptable, then or now: keeping any client-asserted role.** **S16-6a — the rule the alternative needs, now load-bearing**: the catalog is henceforth served **whole to an unauthenticated caller**, which is acceptable *only* because it discloses **which admin features exist and nothing else** — so `CapabilityCatalog` carries no tenant data, no per-tenant entry, no count, no workspace name, nothing caller-derived, and the task proves it (**identical response for no token, a non-Admin token and an Admin token**). That property used to be protected by accident, because the gate hid half the list; from w16 it is protected by the rule. **S16-11 — `roleGate` on the wire is presentation, never authorization**: client-side hiding is not a control, every action behind an admin-gated entry is enforced server-side by membership, and this is precisely why OQ-w16-sa-02's deferral to W17 is safe — and why it must never be re-described as a security fix. **OQ-w16-004 — refused on this lane's own ground, independently of the infrastructure evidence**: an app role + app-only token is the token ADR-010 w15 §1.1 *rejects by design*, carries **no `oid`** ⇒ no membership ⇒ no tenant, and so would need a bypass of `CallerContext` as well — a **second authorization source built for a CI job**; the alternative, a `workspace_membership` row making a service principal a workspace Admin, is worse (ADR-025's last-Admin guard and the member list both count it). **→ no new identity, credential, app role or secret in w16**; ADR-010/ADR-015 unchanged. **S16-7 — `X-Tenant-Id` does NOT retire** (w15 clause 2's membership-verified selector): the sweep's grep is **paired** — zero `X-Role`/`X-Workspace-Role` across `backend/src`, `web/`, `web/openapi/`, `.github/`, **and `X-Tenant-Id` present and unchanged, asserted positively so the sweep cannot overrun** (concurs with delivery D7 and the client's contract row, reached independently). **The stale prose goes in the same task**, because it is the thing that fakes the verdict in both directions — grep `X-Role`, hit a comment, re-open a closed item; grep `unattributed`, hit three doc comments, report a defect that no longer exists. Test **S-T27**. **CLIENT** (ADR-012 owner; w16 footer clause 28): **the client half is contract-only** — `web/src` sends neither retired header (`client.ts:76` attaches `Authorization` and nothing else), re-verified on `f0b3436`. Three contract sites: the `X-Role` **parameter** `:5239`, its description `:5236`, and the stale `X-Workspace-Role` sentence on `deleteDocument`'s description `:1227`, which `reprocessDocument:1310` inherits by reference. **C4 — this edit is invisible to every check the build runs, so its gate is a grep, not a green build**: the generator parses **only `responses`** (`generate-api-client.mjs:132-146`), so a `parameters` entry **never reaches `schema.ts`** — deleting `X-Role` changes **no generated byte**, fails no `tsc --noEmit` and fails no build. That is §10's eighth-site hazard reached from the other end, and §10's ruling applies unchanged. **Concurring with security S16-7 and delivery D7, reached independently from the generator's behaviour: the grep is paired** — zero `X-Role`/`X-Workspace-Role` across `backend/src`, `web/`, `web/openapi/` and `.github/`, **and `X-Tenant-Id` present and unchanged, asserted positively so the sweep cannot overrun**. What the paired assertion protects is `client.ts:1943-1950` — only `X-Tenant-Id` + bearer — which is the exact path A16-3's re-worded in-product acceptance runs on, so **NW-31's header deletion cannot break clause 8's acceptance** (converging with SA's OQ-w16-po-02 check, from the client side). **Regression cover needing no edit**: the Admin catalog now comes from membership and is observable at `/ask`; the existing e2e case **`v2.spec.ts:648`** (A8, "cosa sai fare?" answered from the capability catalog with working links) is the one that would catch a regression — named so the acceptance walk runs it rather than a new case being written. **OQ-w16-sa-02 accepted as this seat's, for W17**: per S16-11 `roleGate` is **presentation, never authorization**, so hiding an admin-gated chip from a non-Admin is client work, deferred — and it **must never be re-described as a security fix**. **DM (ADR-014 / ADR-015 / ADR-016 owner; the file's disposition, the wave's CI-YAML set and the wave's order are this seat's)**: **OQ-w16-004 — neither option, because the premise was false.** The assumption in force held that ADR-027's re-enqueue "needs no new identity, no secret and no Terraform". **Refuted from the Terraform**: both Service Bus roles are granted to the *container-apps workload identity only*, topic-scoped (`modules/servicebus/main.tf:71-102`), while CI's `raffa-sp-<env>` holds exactly one Terraform-granted role, `Key Vault Secrets User` (`modules/keyvault/main.tf:66-75`). **There is no zero-cost repair** — so **w16 buys CI no new credential**, and the wave's cloud delta is zero **because the API steps are deleted, not because re-enqueue was free**. Recorded that way deliberately: the withdrawn premise is what W17 would otherwise re-derive. **The file is broken on three axes and a credential repairs one**: (1) **auth** — 401 at the first call since NW-05, making its own 403 diagnostic (`:274`) unreachable dead code; (2) **contract** — the POST loop accepts only `200` and reads `.pagesParsed`/`.chunksIndexed` (`:260-270`) while NW-27 now returns **202** (`DocumentsEndpointExtensions.cs:523-535`), so every document falls to `*)` → `FAILED` → `exit 1`; (3) **timing** — the `%PDF` assertion (`:345-366`) races work that is now asynchronous, giving a false red or a false green. **A token fixes (1) and leaves a job that fails differently or passes wrongly: the repair is a rewrite, and the rewrite is what does not fit this wave.** **Disposition — `reprocess-tenant-documents.yml` deleted, `verify-tenant-corpus.yml` added** (one file-set change, one writer, one task): **keep** the credential-free half (OIDC login `:124-129`, RG resolution, Key Vault `:302-334`, `psql` with the `app.tenant_id` GUC, the `%PDF` assertion (R-DOC-07 AC-1), the supplier report (R-SUP-03), the summary, and **`environment:` `:104`** so `demo`'s required reviewers still gate it); **delete** the three API steps (`:178-290`), all four `X-*` headers (`:204-206`, `:255-257` — **verified present on `f0b3436` at the table**), `OPERATOR_USER_ID` `:95` (an audit actor for rows it no longer writes, which also keeps NW-32's sweep clean), the dead `workflow_call` `:68` (**zero callers**) and the stale "Role posture" header `:38-48`; **add** a psql enumeration of the tenant's documents and `processing_status` plus a named worklist. **The job reports; it does not mutate.** **OQ-w16-dm-01 ruled: the rename — and this seat's own fallback refused**, converging with product-owner: a file named `reprocess-*` that never reprocesses is a **new instance of the stale-record defect NW-31 exists to delete**, created in the wave that sweeps it. **Three shortcuts named so they are refused rather than discovered**: a Service Bus SAS key in Key Vault (forbidden by the module's own rule, `servicebus/variables.tf:28`), re-adding any `X-*` header (nothing reads them after NW-05 — it only turns a 401 into a different 401), and — the dangerous one, because rows change and it *looks* like it worked — inserting `extraction_job` rows with `status='Queued'` via psql: **nothing sweeps them** (ADR-027 §D3 has no sweeper by design and the Worker is message-driven), so a row with no message is a permanently-`Queued` job the UI renders as "processing" **forever**. **The bulk whole-tenant job is designed here and deferred to W17** (an operator console calling `DocumentReprocessService.ReprocessAsync`, so the `extraction_job` row, the `document.reprocessed` audit row and §D5's replace step stay the **product's own code, never re-implemented in bash**; published under `DefaultAzureCredential` with one topic-scoped `Data Sender` rather than an app-only token, because that never touches `azuread_application.api` and never makes a service principal a workspace Admin) — and per product-owner it **must appear in the wave's known-gaps record**. **A16-3 still closes**, because the product path is live today: `useDocumentsList.ts:240` → `POST /api/documents/{id}/reprocess` → `ResolveTenantAsync` → `IsAdminAsync` → **202**, a real Admin with a real token — a **stronger** close than the CI path it replaces, which is 401. **Process, and it is where this wave's worst traps live.** **CI-YAML set = exactly two files**; verified on `f0b3436` that `reprocess-tenant-documents.yml` is the **only** file under `.github/workflows` carrying any retired header. **`X-Tenant-Id` does not retire** (concurring with security S16-7 and client C4, reached independently from the workflow side) — so the final-integration grep is **paired**: the two role headers gone **and `X-Tenant-Id` present and unchanged**, asserted positively so the sweep cannot overrun. **ADR-016 w15 clause 26's inherited question is asked and answered as a negative result**: w16 changes no `infra/` file, so nothing `dist/config.json` is built from moves — **no forced SPA redeploy is owed and no `workflow_dispatch` is added to `web.yml`**. **The close record becomes wave-scoped**: `wave-close.md`'s **generic filename** is the defect, not its stale content — it is permanently the newest file with that name, it was stamped **before** the merge that delivered w15, it lists **9 of 11 delivered tasks as undelivered**, and it cost this wave a seven-line banner to neutralise. Rule: **`reports/execution/wave-close-<w>.md`, written after the `integration → main` PR merges**, stating the promotion outcome even when there is none. **OQ-w16-008 has the same root cause and is ruled**: the operator stamps the missing `w15.hitl-ok` at the w16 HITL gate (`check_slice_prereqs.py --record-hitl w15`) — accurate, not a rubber stamp — **no task**. **`demo` is three promotions deep** (`demo-v3` since 2026-09-04; w15 clause 18's `demo-v4` cut never executed) and **the next promotion is no longer apply-free**: `demo` keeps `worker_max_replicas = 3` but consumes the same module, so the baseline's `MaxConcurrentCalls = 4` reaches it — **the first banked promotion that is not flag-only**. The number is **read at the gate (`git tag -l "demo-v*"`), never assumed.** **The baseline carries an infra delta this wave did not write** (PR #118): an **operator apply**, **not** a wave task and **not** ADR-014 w15 clause 5 firing; it **cannot roll the running image back** (`ignore_changes` on both container apps, verified with cloud-architect); it **does not gate A16-3**; and the wave's negative assertion must be a **two-dot diff** (`git diff --stat origin/main..HEAD -- infra` empty), **never "no infra commits in the range"** — the *log* over that span is not empty, and a reviewer reading the log fails a clean slice. **One PR this wave** (zero `infra/` delta ⇒ w15 clause 5's two-merge shape does **not** apply and must not be re-derived); **no `demo-v*` tag cut**. **W16-A1** gains item **(g)**: the baseline's apply confirmed landed on `dev` and the running image tag checked against `main`, before any acceptance walk. **Order — and this corrects this seat's own draft.** The published skeleton put `NW-11 · NW-12 · NW-13` in one phase; it **fails `check_single_writer.py` twice**, on `client.ts` (NW-12's `getQuote` vs NW-13's two wrappers) and on `contract360/index.tsx` (both retire a store, both touch `handleUndo`). **Neither collision is in the intake's §5 list**; both came from client-architect, and both sit inside the one artefact this seat owns. **Corrected**: P1 theme-A backends + the workflow task alone · P2 NW-32 + contract-A · P3 NW-12 full-stack (sole writer of the contract file *and* `client.ts`) + NW-11/NW-13/NW-21 backends + W16-01 · P4 **one combined** NW-11+NW-13 web task · P5 final integration alone. ≈13 tasks, 5 phases. | `amend ADR-016` (w16 clauses 27–35) · `amend ADR-014` (w16 clauses 1–5) · **ADR-015: `none`** — no federated credential, GitHub secret, subject claim or Graph right changes this wave · `amend ADR-024` (w16 clause 2) · `amend ADR-026` (w16 clauses 3–4, 6) · `amend ADR-027` (w16 clause 1) · `amend ADR-001` (w16 clause 8 — acceptance wording) · `amend ADR-022` (w16 clauses 1–6) · `amend ADR-012` (w16 clause 28) · **cloud-architect: `none`** — ADR-005 / ADR-006 / ADR-007 / ADR-008 untouched; no `infra/` file, SKU, env key or apply path changes this wave | `reports/architecture/ADR-016-promotion-dev-demo.md` · `reports/architecture/ADR-014-git-flow.md` · `reports/architecture/ADR-024-ask-raffa-v2.md` · `reports/architecture/ADR-026-workspace-api-and-invitation-schema.md` · `reports/architecture/ADR-027-async-document-processing.md` · `reports/architecture/ADR-001-scope-r0-r4.md` · `reports/architecture/ADR-022-day1-demo-auth-and-fixture.md` · `reports/architecture/ADR-012-web-stack.md` |
| NW-32 — every write names its actor | software-architect, security-architect | **SA** (signatures; ADR-011/ADR-022 are security's): a **required `string` actor, positionally after the entity id**, on nine service types across five modules — the shape the three already-correct document paths use (`DocumentsEndpointExtensions.cs:147,512,576`). **No new type and no ambient accessor**: an ambient actor lets the two caller-less sites compile and silently write nothing. **OQ-w16-007**: the caller-less sites take a reserved **`system:<component>`** principal, the convention already live at `NegotiationOutcomePropagationService.cs:97` — a reserved actor is a fact, `"unattributed"` is a lie — and `RagAnswerServiceTests.cs:70` is **rewritten, never deleted**. Cross-module signature edit ⇒ **theme A strictly before theme B** (same five services NW-11/12/21 modify). **SEC (ADR-011 owner — audit integrity is this seat's, and the reframing is confirmed independently)**: every endpoint reaching the nine services 401s first since PR #117, so `"unattributed"` is **not** an identity-absent branch — it is an **unconditional hardcode on every call, signed or not**. **The defect is a falsified audit trail, not an authentication bypass**; a live ADR-011 violation, and this seat concurs with keeping it `should` rather than promoting it. **This corrects my own w15 rule S15-41**, which assumed the placeholder survived on an identity-absent branch. **S16-8 — an audit row that cannot name its actor is not written**: required parameter, **no default**, on all nine methods, so the placeholder cannot return by omission; `AuditEvent.Actor` stays `required string` (`AuditEvent.cs:27`) — **the column was never the problem, the placeholder exists precisely because null is impossible**. **S16-9 — the reserved principal, with the two properties that make it a fact**: `system:<component>`, the convention **already live** at `NegotiationOutcomePropagationService.cs:97`, (a) carries a character no token subject can produce (`:`) so it can **never collide with an `oid`**, and the reserved prefix is **never accepted as a resolved token subject**; (b) is greppable, which is what makes A16-4's "grep returns nothing" reachable without inventing a vocabulary. **A reserved actor is a fact; `"unattributed"` is a lie** — it means "we did not know", written by a system that did. **S16-9a — one stale record this creates, closed inside NW-32's own file (no new task, no new writer)**: the doc comment at `NegotiationOutcomePropagationService.cs:94-96` calls that string an "interim actor placeholder (**ADR-010 is not wired in yet**)" — ADR-010 **was** wired in w15 and the value is now permanent and correct. Left standing it is the same trap NW-31 spends this wave deleting, one file away. **S16-10**: `RagAnswerServiceTests.cs:70` is rewritten, never deleted — it is the only evidence the behaviour changed. **The append-only consequence, recorded so nobody proposes a cleanup**: `audit.sql:76-88` rejects UPDATE and DELETE on `audit_event`, so the `"unattributed"` rows already written are **permanent and uncorrectable**. That is correct for an audit trail. **The trigger is never dropped — not for a backfill, not inside a migration, not temporarily.** This item stops the bleeding; it does not clean history, and a trail that can be rewritten to look correct is worth less than one with an honest, bounded gap. Tests **S-T28**, **S-T29**. | `amend ADR-002` (w16 clause 4) · `amend ADR-011` (w16 clauses 15–18) · `amend ADR-022` (w16 clause 1) | `reports/architecture/ADR-002-dotnet-solution.md` · `reports/architecture/ADR-011-secrets-and-rag.md` · `reports/architecture/ADR-022-day1-demo-auth-and-fixture.md` |
| NW-11 — renewal actions have an HTTP read-back | software-architect, client-architect | **SA**: `GET /api/renewals/{id}/action` (200/404) mirroring the POST's own id semantics, **plus** the persisted row embedded in every `GET /api/renewals` row — Renewals and Savings are *list* surfaces and a per-row GET is an N+1. **Binding: the embedded field is `savedAction`, never `action`** — that name already carries the computed `RecommendedAction` (`RenewalsEndpointExtensions.cs:239`), and reusing it would overwrite a deterministic calculator's output with user state on a shipped screen (client C9 adopted). **No migration** (`GetActionAsync:158-171` exists with zero callers; the table is unique + RLS-protected), **no widening** with `supplierId`/`annualSpend`, and **no DELETE route** — absence of a row **is** the status `NotStarted` (client C8). **CLIENT** (ADR-012 w16 footer clauses 21–22, 24, 30) — **and this row carries a correction to my own lane draft, because a task implementing the draft as written would have added work that is not needed.** The draft asked for the embedded field *plus* "a per-contract read for Contract 360, **which does not fetch that list**". **The parenthetical is false**: `contract360/index.tsx:115` already calls `apiClient.getRenewals(workspace.id)` — in the same `Promise.all` as `getRenewalPriority` at `:116` — and derives its row at `:227`. So §D1's embedded **`savedAction`** reaches **all three** surfaces through the **existing** `getRenewals` wrapper, and **NW-11 adds no `ApiClient` method at all**. `GET /api/renewals/{id}/action` is published in the contract and left **unwrapped** (clause 24: a wrapper is added in the same task as its first caller), stated so the task does not add one out of a sense of completeness. `renewalActionStore.ts` (`raffa.renewals.actions`, `sessionStorage`) is **deleted outright**, its row joining ADR-012 §1's disposition table. **Blast radius is seven src files and six test files, not the three surfaces the item names**: `renewals/index.tsx:8`, `renewalPipelineViewModel.ts:4`, `InsightCard.tsx:10`, `contract360/index.tsx:6`, `AnswersBand.tsx:2`, `savings/index.tsx:5`, `savingsViewModel.ts:2` — four import only the *type* `TrackedRenewalAction`, replaced by the generated row type re-exported from `client.ts`, **no hand-written DTO** (`ADR-012:53`). **C8 upheld and binding**: `handleUndo` (`contract360/index.tsx:261-269`) posts `NotStarted` **and** calls `forgetRenewalAction`; the server row is an upsert on `(tenant_id, contract_id)` and **survives** the undo, so today only the local forget hides it — after the read-back it returns as `NotStarted` and **every surface must render that as "no action taken"**, or Undo will look broken. `forgetRenewalAction` dies with the store; no DELETE is requested. **OQ-w16-ca-01 is ruled here because no other seat can rule it** — product-owner is seated on NW-12/NW-13/NW-21 but **not on NW-11**, so leaving it open means a task decides it by accident. `buildOpportunityRows` (`savingsViewModel.ts:236-245`) **prepends every tracked action** to the opportunities table as a pseudo-opportunity row with `estimate: NOT_YET_AVAILABLE` (`:223`), the code conceding its own premise at `:210-214`. Today that set is **this session's** actions — a handful; read back from the server it becomes **every renewal action in the tenant, permanently**, filling the table with rows that are not opportunities and carry no estimate. **A naive store→GET swap ships that flood**, as a side effect of a read-back nobody asked to change Savings — so **`buildTrackedOpportunityRow` is retired and Savings renders only real `SavingsOpportunity` rows** (NW-21 supplies the real link the pseudo-row stood in for). The **product** half is deferred, not answered: whether Savings should show tracked renewal actions at all is product-owner's, and if the answer is yes it is a **designed section** of that screen — not an estimate-less pseudo-row — which seats ux-ui-designer in **W17**. | `new ADR-028` §D1 · `amend ADR-026` (w16 clause 5) · `amend ADR-012` (w16 clauses 21–22, 24, 30) | `reports/architecture/ADR-028-server-side-state.md` · `reports/architecture/ADR-026-workspace-api-and-invitation-schema.md` · `reports/architecture/ADR-012-web-stack.md` |
| NW-12 — quote GET + negotiation-outcome list | software-architect, client-architect, product-owner | **PO**: scope IN, **reduced to the read-back** — W16 renders the recorded outcome of the quote in front of you; the cross-quote history / levers / Ask-citable list stays **NW-57 (W18)** (`screens-v2.md:143-144`). No new screen. **`E08/F03/US01` AC-4 is re-pointed** to NW-12 + NW-21 as its honest server-side implementation — **not cancelled**: story stays `active`, no status banner, no `superseded:` line. Its surface is **Savings** (not "Home") and what moves is the realized **count**. **SA**: `GET /api/quotes` (list) and `GET /api/quotes/{id}` **with its outcomes embedded**, newest first — the outcome is a property of the quote (`NegotiationOutcome` is keyed by `QuoteId` and is append-only). **The assessment endpoint is not overloaded** (declining client C10's shape, with the reason): `POST /{id}/assessment/recalculate` returns that same shape, so a recalculation would appear to re-report a negotiation it never touched — one extra GET on mount is the cheaper cost. **No `GET /api/negotiations/outcomes`** — a tenant-wide feed has no caller until NW-57 (W18) and publishing it now pre-empts that item. New `QuoteQueryService`; **no migration** (both tables already RLS-enabled). **CLIENT** (ADR-012 w16 footer clauses 21, 23–25) — **my shape preference (C10) is declined, and the premise under it was false; that makes SA's decline correct on stronger grounds than the ones it was given.** The draft asked the recorded outcome to ride the **existing** `GET /api/quotes/{id}/assessment`, "which the screen already calls on mount". **The screen does not call it — on mount or ever.** The mount effect (`quotes/index.tsx:118-132`) calls `load()` → `apiClient.recalculateQuoteAssessment` (`:91`), and `client.ts:805-813` records exactly why: the recalculate response is a **superset** of the assessment GET's shape, so `src/routes/quotes/` "never calls `getQuoteAssessment` directly". So the shape the outcome would have ridden is **the one this screen never reads**, and the call it *does* make on mount is precisely the recalculate POST §D2 identifies as the hazard ("a recalculation would appear to re-report a negotiation record it did not touch"). **A task implementing C10 would have shipped a read-back the screen cannot see** — recorded as a correction rather than quietly re-worded. **Adopted instead**: **one `GET /api/quotes/{id}` call on mount** alongside the existing recalculate (§D2's "cheaper of the two costs", accepted), with a **new `getQuote` wrapper** — it has a caller in the same task, which is exactly what clause 24 requires. **`GET /api/quotes` (the list) gets no wrapper** — no list screen exists and none is built until NW-57 (W18). **ADR-018 stays `none`, and that is what makes this item cheap rather than a wave item**: `/quotes/:quoteId` **already exists** (`WorkspaceShellApp.tsx:90`), the screen already reads it (`useParams`, `:68`), and upload already navigates to it (`:151`) — so **the quote id survives a reload today**. Had it not, NW-12 would have needed a new route and a designer. `quoteOutcomeStore.ts` (`raffa.quotes.negotiationOutcomes`) is **deleted**: its read half is already dead in production — `loadNegotiationOutcomes` and `sumRealizedSavings` have **zero `src` callers**, only `rememberNegotiationOutcome` (`:290`) is live — so the deletion removes one call site and one test file, and **what is genuinely new is that no surface renders a recorded outcome at all today**, which is why product-owner is seated. Check: vitest that the quote screen renders a **server-supplied** outcome with **no session write**. | `amend ADR-001` (w16 clause 2) · `new ADR-028` §D2 · `amend ADR-026` (w16 clause 5) · `amend ADR-012` (w16 clauses 21, 23–25) | `reports/architecture/ADR-012-web-stack.md` · `reports/architecture/ADR-001-scope-r0-r4.md` · `reports/architecture/ADR-028-server-side-state.md` · `reports/architecture/ADR-026-workspace-api-and-invitation-schema.md` |
| NW-13 — Contract 360 step ticks are server state | software-architect, client-architect, product-owner | **PO**: scope IN as scoped. Three product rulings (OQ-w16-006's product half): the **four named steps** of `screens-v2.md:106-108` are canonical (four, no more — a fifth is a product change); rows are keyed by **stable step key, never array index**, and the rendered label stays client-side because two of four labels are parameterized (**w14 clause 3** — a row must not freeze a fact that later changes); ticks are **per contract**, matching `renewal_action`'s unique `(tenant_id, contract_id)` — **not** per renewal cycle (tick history would be a new capability, out for V1). **SA** (OQ-w16-006's module half): the ticks are a **contract child resource owned by `Raffa.Documents.Contracts`** — `Raffa.Renewals` is structurally impossible (allow-list `[SharedKernel, Benchmark]`, `RenewalActionService.cs:23`). `GET`/`PUT /api/contracts/{id}/negotiation-steps`, the `GET /api/contracts/{id}/corrections` shape verbatim. **One new tenant table `contract_negotiation_step`**, unique `(tenant_id, contract_id, step)`, step stored **by name** as a closed enum, **the row's presence is the tick** (so the whole-set PUT is idempotent with no third state), RLS policy **in the same migration as the table**. **OQ-w16-ca-02 answered: Undo is two idempotent writes — ticks first, then the action — not one composed call**: folding the tick-clear into the renewals POST would make a write named "set the renewal action" silently erase another module's rows for any future non-UI caller; the client invents no partial-failure state because both surfaces re-read. **SEC (not seated on this item; ADR-009 is this seat's ADR and this wave's only new tenant table lands here — the RLS consequence is written, not left to a task)**: SA's "RLS policy in the same migration as the table" is **ratified and made checkable**. `contract_negotiation_step` is an ordinary tenant table under ADR-009 w14 clause 8 / w15 clause 4a — `tenant_id` not null and indexed, `ENABLE` **+ `FORCE`** + a `tenant_isolation` policy with **both `USING` and `WITH CHECK`**, in the table's own migration, **no identity-keyed policy** (the §F.1 widening stays confined to `workspace_user`). The template is three lines away in the script this migration regenerates — `documents-contracts.sql:421-423` — and all eleven tenant tables there carry the full three; **a table shipping `ENABLE` without `FORCE` would be the only one, and `FORCE` is the clause that binds the table owner.** **The part that is not automatic, and the reason this clause exists**: ADR-009 w15 clause 4c makes the CI guard conditional — `TenantRlsMigrationCheckTests` discovers its table list dynamically from every `TenantScopedEntity` subclass **of `DocumentsContractsDbContext` alone**. SA's module ruling puts the table in exactly that context, **so the free branch is available and w14's `workspace_invitation` escape hatch is not needed** — but only if the entity actually subclasses `TenantScopedEntity` there. **Take it deliberately**: the proof of coverage is that the check's table count goes up. If any task instead places it elsewhere, clause 4c's second branch fires and it **owes a hand-written per-table RLS test** including clause 4d's trap (**the test creates its own unprivileged Postgres role — Testcontainers hands you a superuser, and RLS constrains neither a superuser nor a table owner, so the obvious test is green and worthless**). Either way the task carries the negative: **a second tenant's ticks never appear** for the same contract id. **CLIENT** (ADR-012 w16 footer clauses 21, 25–26): **C11 adopted** — the wire is keyed by the four **named** steps, never by array index. The store is a positional, unnamed `boolean[4]` (`negotiationStepsStore.ts:11,18,24,31`) whose labels live in `contract360ViewModel.ts:211-218` with **two of the four parameterized**, so an index key is brittle by construction. Client rules: an **unknown step key from the server is ignored**, never rendered as a fifth tick; a **missing key is unticked**; `NEGOTIATION_STEP_COUNT = 4` stops being a client constant that shapes the wire; the rendered label stays client-side. **C12, binding — the optimistic tick reverts.** `handleToggleStep` (`contract360/index.tsx:271-276`) is a pure local toggle today; it becomes write + read-back, and on failure the tick **reverts to the last server-known set** — trivial under §D3's whole-set `PUT`, since the client already holds all four — with ADR-018 `:117`'s error state. **A tick the server rejected must never survive on screen**: that is w15 clause 4's fabricated-fact class, not a cosmetic concern. **§D4 accepted and my own ask withdrawn**: I asked for one composed call so the client would have no partial-failure state to invent; §D4's reason is better than the convenience it refuses, and **I ratify its ordering from the client side** — **ticks `PUT` first, then the action `POST`**, because an action still in progress with no ticks is an honest state a user can reach, while `NotStarted` with four ticks is a contradiction no flow produces. The order is load-bearing, not arbitrary. The client invents nothing because **both writes are idempotent and both facts are already re-read on mount** (`contract360/index.tsx:93,115`): recovery from a partial failure is a **re-read, never a compensating write**. `negotiationStepsStore.ts` is **deleted** — one importer (`index.tsx:20`) and one presentation consumer (`AnswersBand.tsx:10-11,104-112`), the cheapest of the three retirements. **One single-writer collision the intake's list does not carry**: NW-11 and NW-13 both retire a store inside `contract360/index.tsx` (`:6`, `:20`) and both touch `handleUndo` (`:261-269`) ⇒ **one task, or different phases**. Both new wrappers (`getNegotiationSteps`, `putNegotiationSteps`) land in `client.ts`, which clause 25 makes a one-writer-per-phase file. | `amend ADR-001` (w16 clause 3) · `new ADR-028` §D3–D4 · `amend ADR-002` (w16 clause 1) · `amend ADR-003` (w16 clause 1) · `amend ADR-021` (w16) · `amend ADR-026` (w16 clause 5) · `amend ADR-009` (w16 clause 2) · `amend ADR-012` (w16 clauses 21, 25–26) | `reports/architecture/ADR-001-scope-r0-r4.md` · `reports/architecture/ADR-028-server-side-state.md` · `reports/architecture/ADR-002-dotnet-solution.md` · `reports/architecture/ADR-003-data-store.md` · `reports/architecture/ADR-021-schema-apply.md` · `reports/architecture/ADR-026-workspace-api-and-invitation-schema.md` · `reports/architecture/ADR-009-tenancy.md` · `reports/architecture/ADR-012-web-stack.md` |
| NW-21 — quote outcome updates Savings | software-architect, product-owner, client-architect | **PO**: scope IN, **not expanded** — OQ-w16-005 ruled: **A16-8 closes on the status move**, because no surface renders a realized money figure (the KPI row is Contracts analyzed · Upcoming renewals · Savings **identified**; realized is a **count**) and the per-opportunity `realizedAmount` is already on the wire, so no fact is dropped. **Fence**: no w16 task may render `SavingsKpiSummary.Realized` as a money amount. The amount fix is **not** a capability change but an unmet AC of the `active` story `E04/F03/US01` → **head of W17** (OQ-w16-po-01). An outcome may be **resolved** to an opportunity but **never guessed**: no unambiguous match ⇒ recorded **unlinked**, `savingsPropagated: null`, nothing moves. **SA**, and this **removes work the intake predicted**: NW-21 needs **no column, no migration, no contract delta and no client change** — the field is already on the wire, on the entity, typed in the client, and the propagation behind it is complete. Link precedence: (1) `savingsOpportunityId` in the body → used verbatim (this is A16-8's acceptance path, deterministic today); (2) absent → `Raffa.Api` **resolves through the product's own supplier-identity rule** — `SupplierNameNormalizer` + the `(tenant_id, normalized_name)` unique index (`SupplierConfiguration.cs:39-41`) — and links **only if exactly one** open opportunity matches; zero or two or more ⇒ **unlinked**, `savingsPropagated: null`, nothing moves. That is PO's "resolved, never guessed" implemented by a unique index rather than a heuristic. **`SupplierResolver` is never called** (it creates rows — recording an outcome must not mint a supplier). **Rejected**: `savings_opportunity.quote_id` (invents the R4 quote-originated opportunity; nothing would populate it) and any client-side match (ADR-012 w15 clause 4). **OQ-w16-ca-03 answered: server-side** ⇒ zero client edit ⇒ **ux-ui-designer stays unseated**. **PO (round 2, OQ-w16-sa-01 ruled — ADR-001 w16 clause 7)**: **ADR-028 §D5 clause 2 ships in w16.** With zero client change, clause 1 alone is unreachable from the UI (`quotes/index.tsx:274-282` omits the id), so an API-only close would leave the product in exactly the state NW-21 names — a recorded outcome that never moves Savings. Clause 2 adds no capability; it makes an already-built propagation reachable. **Fence 1**: A16-8's deterministic close is the **explicit-id** path — the acceptance walk never depends on the resolver firing, and a decline is a pass. **Fence 2**: when `savingsPropagated` is `null`, no surface may present the outcome as a realized saving and no total may absorb it (a *must not* — no screen, no designer). If the council drops clause 2, NW-21 is recorded **API-only closed with its UI path open**, at the head of W17 — never implicit. **SA (round 2, clause 7 accepted — ADR-028 w16 round-2 footer)**: the ruling is taken and **§D5 is unchanged by it** (it already shipped clause 2). Three things the footer adds. **(a) Fence 2 gets a mechanism**: when the resolver declines, `PropagateAsync` is **never invoked** — no opportunity row updates, `Status` stays `Identified`, no `RealizedSavings` row is inserted — so "no total may absorb it" holds **by construction**, not by a rendering promise; the task carries the test (a declining capture leaves the Savings KPI payload **byte-identical**). **(b) The phase plan does not move**: `savingsPropagated` is **already a required `["boolean","null"]` property with no `description`** (`raffa-api.v1.json:4356,4415-4419`; `schema.ts:504`), verified at the table, so clause 2 edits no JSON — **five contract paths, two contract tasks, no sixth writer**, constraint 1 unmoved. **(c) One stale record is created and must be closed in the same wave**: `backend/README.md:2296-2304` (and the doc comment `NegotiationsEndpointExtensions.cs:53`) assert the three fields are null together "whenever the caller supplied no `savingsOpportunityId`" — clause 2 makes that **false**. Corrected rule recorded in the footer; it is **prose inside NW-21's own single-writer file**, so no new task, writer or phase — and leaving it would re-create inside w16 exactly the stale-record defect **NW-31 deletes**. Existing green assertions (`NegotiationOutcomePropagationEndToEndTests.cs:65,114`, `R4EndToEndTests.cs:339`) are all explicit-id and stay valid. **CLIENT** (ADR-012 w16 footer clause 29): **C13 upheld — zero client change.** There is no derivable link (`NegotiationOutcome.cs:127-131`), so a client matching an outcome to an opportunity on `contractId`/`supplierId` would be **inferring a server fact** — prohibited by ADR-012 w15 clause 4 and §13.2. With server-side resolution `quotes/index.tsx:274-282` is **unchanged**, `client.ts:877` already types the field, NW-21's client cost is **zero**, there is no picker control and no new empty state, and **ux-ui-designer stays unseated** — the roster consequence this seat flagged **before** the table rather than at close (OQ-w16-ca-03). **Fence 2 holds structurally in the client, for the same reason software-architect gave server-side**: after NW-11's pseudo-row retirement (clause 30) Savings renders **only real `SavingsOpportunity` rows**, so an unlinked outcome has **no row to appear in and no total to enter**. Nothing is left to a rendering promise. **Product-owner's money fence is already satisfied — verified, not promised**: `savingsViewModel.ts:110` renders the realized KPI as `countOf(kpis.savingsRealized)` — a **count**, in a meta line beside the identified and in-progress counts — and no client surface renders `SavingsKpiSummary.Realized` as a money amount. **No w16 client task needs to change it; the fence's whole cost is that none may.** And the collision the intake predicted between NW-12 and NW-21 on `quotes/index.tsx` **dissolves**: NW-12 owns that file alone. | `amend ADR-001` (w16 clauses 4, 7) · `new ADR-028` §D5–D6 **+ w16 round-2 footer** · `amend ADR-002` (w16 clauses 2–3) · `amend ADR-012` (w16 clause 29) | `reports/architecture/ADR-001-scope-r0-r4.md` · `reports/architecture/ADR-028-server-side-state.md` · `reports/architecture/ADR-002-dotnet-solution.md` · `reports/architecture/ADR-012-web-stack.md` |
| W16-01 — delete the dead R0 in-process Worker queue | none | task only — **SA** records the deletion rather than leaving it to a task's judgement, because the two in-memory queues are one grep apart: delete `Raffa.Worker/Queue/{IQueueConsumer, InMemoryQueueConsumer, QueueConsumerHostedService}.cs` **and** the registration at `WorkerServiceCollectionExtensions.cs:70-72` (a second live `IHostedService` in every deployed Worker); **`InMemoryExtractionQueue` (`Raffa.Messaging`) stays** — it is the documented CI/local transport. | `amend ADR-027` (w16 clause 2) · `amend ADR-002` (w16 clause 5) | `reports/architecture/ADR-027-async-document-processing.md` · `reports/architecture/ADR-002-dotnet-solution.md` |

`Decision`: one or two sentences, or `task only` when no seat is involved.
`ADR action`: `new ADR-NNN <slug>` | `amend ADR-NNN` | `supersede ADR-NNN by
ADR-NNN` | `none — <why>`. `File`: the ADR path written or amended.

## What the table must settle (from `w16-requirements.md` §7)

These are the open questions the intake could not close from the code. Each
carries an assumption in force, so none gates a task — but a seat that leaves one
unaddressed is leaving the decomposer to guess. All eight are already recorded in
`reports/open-questions.md:140-147`; no duplicate was appended this run.

| OQ | Item | Seats | Question |
|---|---|---|---|
| OQ-w16-001 | NW-07 | product-owner, software-architect | A16-1 is vacuous (`oid` is invariant under UPN casing); what happens to conversations keyed by the pre-w15 email? — **RULED (PO, ADR-001 w16 clause 1)**: left in place, recorded not migrated; A16-1 reworded in four clauses; task DoD `:69` superseded. |
| OQ-w16-002 | NW-08 | security-architect, software-architect | Moving to `ICallerContext` changes the route's tenant derivation (claim → verified header) — a contract change, not a drop-in. — **RULED (SA, ADR-026 w16 clause 2; security S16-4 owns the ladder)**: adopt the standard shape, declare `X-Tenant-Id` **in the contract** (a route that gains a required header silently is a runtime discovery), and record that the "no `?tenantId=`" property survives as *selector, never an authorization input* — strictly stronger than the claim it replaces. |
| OQ-w16-003 | NW-08 | product-owner | Is a web audit surface in scope? If yes, ux-ui-designer joins the table. — **RULED (PO, ADR-001 w16 clause 5): NO.** A16-2 closes through `GET /api/audit` + a typed client; audit is absent from the pilot path; **ux-ui-designer stays unseated** and ADR-018/020 are untouched. |
| OQ-w16-004 | NW-31 | security-architect, delivery-manager, cloud-architect | Token-bearing identity (Terraform, infra phase) or ADR-027's worker-side re-enqueue (zero cloud delta)? **This decides whether cloud-architect's seat is active.** — **PREMISE WITHDRAWN (SA, ADR-027 w16 clause 1)**: the assumption in force rested on ADR-027 `:198`, which is a property of the **handler**, not a licence for a credential-free operator path — `:719` names "an admin inside that tenant" and `:720-723` forbids the sweep. Three seats converge from different evidence (SA on the ADR text, delivery D1 on the Service Bus role assignments, security on the identity plane): **neither option is taken, the API steps are deleted, and the wave's cloud delta is zero because of that — not because re-enqueue was free.** cloud-architect stays on `PASS`. |
| OQ-w16-005 | NW-21 | product-owner | Does "the Savings KPI moves" mean status → `Realized`, or must the realized amount replace the estimate? The second is an ADR-001 capability change. — **RULED (PO, ADR-001 w16 clause 4): the status move.** Plus a correction: the amount fix is **neither a capability change nor a new item** — it is `E04/F03/US01` AC-1, unmet on an `active` story, ruled **head of W17** (OQ-w16-po-01). Money-rendering fence binds w16. |
| OQ-w16-006 | NW-13 | software-architect, product-owner | Which module owns the step ticks (ADR-002 forbids `Raffa.Renewals` → contracts), and are rows keyed by the four **named** steps rather than by index? — **product half RULED (PO, ADR-001 w16 clause 3)**: four canonical named steps, **key not index, label stays client-side**, **per contract** not per cycle. The owning module remains software-architect's call. — **module half RULED (SA, ADR-002 w16 clause 1 + ADR-028 §D3)**: **`Raffa.Documents.Contracts`**, as a contract child resource; `Raffa.Renewals` cannot own it (allow-list enforced by `DependencyDirectionTests`). Table, keys and RLS: ADR-003 w16 clause 1. |
| OQ-w16-007 | NW-32 | software-architect, security-architect | A green test pins `"unattributed"`; two of fourteen write sites have no HTTP caller. — **RULED (SA + security S16-9/S16-10; ADR-002 w16 clause 4)**: the actor is a **required parameter with no default** on all nine methods, the two caller-less sites write a reserved **`system:<component>`** principal (the convention already live in this codebase), and `RagAnswerServiceTests.cs:70` is **rewritten to assert the resolved actor**, never deleted — it is the only evidence the behaviour changed. |
| OQ-w16-008 | — | delivery-manager | `reports/plan/gates/w15.hitl-ok` is missing (re-confirmed this run — the gates directory holds `e01…e13`, `readiness-gaps`, `w14.hitl-ok`) and `check_slice_prereqs.py:332` requires it — `run.ps1 -Slice w16` fails prereqs until the operator stamps it. |

## Constraints the table may not relax

1. **`web/openapi/raffa-api.v1.json` — one task owns it per phase** (ADR-012 §3).
   **Six** in-wave items touch it (NW-08, NW-31, NW-11, NW-12, NW-13, NW-21)
   against a 5-phase cap. The intake's recommendation is **two** contract tasks —
   one theme-A (NW-08 + NW-31), one theme-B (NW-11 + NW-12 + NW-13 + NW-21).
2. **Theme A before theme B** — NW-32 edits the same five services NW-11, NW-12
   and NW-21 modify; sharing a phase fails `check_single_writer.py`.
3. **NW-31 owns `.github/workflows/reprocess-tenant-documents.yml`** — the one
   wave allowed to open it (ADR-016 w15 clause 21).
4. **No w16 task may flip `Invitations__Mail__Enabled` or
   `guest_provisioning_enabled` on `demo`** (`docs/waves/w15-acceptance.md:300`).
5. **No item may be demoted or queued past W17**, and W16-01 (`could`,
   intake-originated) is the only item that may be cut — never displacing
   anything already queued for W17.

## Design refs carried into decisions

| Item | Oracle | Anchor |
|---|---|---|
| NW-13 | `inputs/design/prototypes/raffa-v2/screens-v2.md` | `:106-108` — the four **named, ordered** negotiation steps (*Notify · Request revised pricing · Counter with the market benchmark · Sign or send non-renewal notice*), which supply the stable row key the index-positional client store lacks |
| NW-11 | `inputs/design/prototypes/raffa-v2/screens-v2.md` · `app.jsx` | `:123-130` ("Status shared with the Contract 360 tracker (`racts`)") · `app.jsx:109,166` — `racts` is **one shared fact** across Renewals, Contract 360 and Savings |
| NW-21 | `inputs/design/prototypes/raffa-v2/screens-v2.md` | `:132-137` — the opportunities column is **"Estimate"**; the oracle shows a *status* move, not a realized amount replacing the estimate (bears on OQ-w16-005) |
| NW-12 | `inputs/design/prototypes/raffa-v2/screens-v2.md` | `:139-145` — "Target and negotiation levers are one step further", the boundary that keeps the history UX in NW-57 (W18) |
| NW-11 · NW-12 · NW-13 | `reports/architecture/ADR-018-web-information-architecture.md` *(IA-level contract, not a prototype export — added by ux-ui-designer, round 2)* | `:112-119` "Empty / error / loading (IA-level contract)" — every list/detail surface implements the three states of `design-system.md` §States; **`:116-117` error = 2px accent left rule + h4 + plain endpoint/job name + secondary Retry**. Recorded because **three decisions lean on this line and none of them listed it**: the §3 roster excuses the designer with it, ADR-012 w16 clause 26 binds NW-13's reverting tick to it, and clause 27 prices an OQ-w16-003 reversal with it. **Verified at this table** — and the path matters: the file is `ADR-018-**web**-information-architecture.md`, **not** `ADR-018-information-architecture.md`, which is what this seat tried first and got a "file does not exist". A task citing the shorter name finds nothing and may conclude the contract is imaginary. The anchor itself is exact. **Copy rule a task must not misread**: "plain endpoint/job name" is the surface's plain-language name ("negotiation steps", "the quote"), **never the raw path** — `GET /api/contracts/{id}/negotiation-steps` must never reach a procurement user. `:118-119` forbids treating empty/error as "nice to have". **No ADR action**: `:112-119` already says all of this — this row stops three tasks re-deriving it. |

## Votes (final table round)

| Seat | Vote |
|---|---|
| product-owner | **APPROVE** (round 2) — ADR-001 w16 footer now **clauses 0–8**, written and read back; rows NW-07/NW-12/NW-13/NW-21 filled, plus acceptance-wording addenda on NW-21 and NW-31; OQ-w16-001/003/005/006 ruled, **OQ-w16-sa-01 ruled** (§D5 clause 2 ships, with two fences), **A16-3 re-worded** and the bulk-reprocess deferral to W17 recorded; OQ-w16-po-01 recorded. No scope grows past §1.2, no paid market API, no `must` behind a `could`. |
| software-architect | **APPROVE** (round 2) — round 1: ADR-028 written; w16 footers on ADR-002, ADR-003, ADR-021, ADR-024, ADR-026, ADR-027; INDEX section + ADR-028 row appended; rows NW-07/08/31/32/11/12/13/21 and W16-01 filled; OQ-w16-002/006/007 ruled, OQ-w16-004's premise withdrawn, OQ-w16-ca-02/ca-03 answered; OQ-w16-sa-01/sa-02 opened. Round 2: **ADR-001 w16 clause 7 accepted** — **ADR-028 round-2 footer** written (§D5 unchanged; Fence 2 made structural with a byte-identical-KPI test; **clause 2 verified to cost no contract delta**, so five paths / two contract tasks / constraint 1 unmoved; the `README:2296-2304` + `NegotiationsEndpointExtensions.cs:53` staleness clause 2 creates is folded into NW-21's own single-writer file); **clause 8's acceptance path verified reachable with zero client work** (`client.ts:1943-1950` sends only `X-Tenant-Id` + bearer, so NW-31's header deletion cannot break it); OQ-w16-sa-01 closed, OQ-w16-po-02 checked, OQ-w16-sa-02 unchanged. All four files read back on disk. |
| cloud-architect | **PASS** (round 2) — **conditional seat resolved to *not involved***, which is what OQ-w16-004's ruling and delivery-manager's D1 both predict for this seat. w16 touches **no Azure resource, no SKU, no Terraform module, no container-app env key, no storage, no Foundry account or deployment and no apply path**; cost delta **€0.00/month**. Verified from this seat's own files, not taken on another seat's word: the Service Bus grants are workload-identity-only and topic-scoped (`modules/servicebus/main.tf:71-102`), the CI SP holds only `Key Vault Secrets User` (`modules/keyvault/main.tf:66-75`), the worker container declares no `Queue__*` key (W16-01 is infra-free, `containerapps/main.tf:348-461`), and every key A16-3's in-product path needs is already deployed on `dev` (`:387-450`). **This seat's own Path B recommendation is withdrawn** and its draft carries the withdrawal, so no infra task can be minted from it. Rows written: the NW-31 `CA` clause and its `none` ADR action above; four entries in `reports/open-questions.md` (OQ-w16-cloud-01 `closed`, OQ-w16-cloud-02 `moot`, OQ-w16-dm-03 answered-for-the-operator, and the `demo` promotion arithmetic — **12 in flight vs `dev`'s 20, 16.7k vs 15k TPM per document ⇒ no capacity or SKU change owed**). No ADR written, amended or superseded by this seat. |
| security-architect | **APPROVE** (round 2) — five w16 footers written and read back on disk: **ADR-010** (S16-1/S16-2/S16-3 — one comparison rule for the identity column; the email leg deleted from both authorization comparators; normalization fixed at the source, not per site; a w15 evidence line corrected), **ADR-011** (clauses 14–19 — who may read the audit trail, the actor as a required parameter, the reserved `system:<component>` principal and its collision property, the append-only consequence that makes existing rows permanent), **ADR-022** (clauses 1–6 — the schedule's last two rows fire, the capabilities alternative accepted **with the tenant-free rule it now depends on**, `roleGate` is presentation not authorization, OQ-w16-004 refused on identity-plane grounds, `X-Tenant-Id` explicitly does **not** retire), **ADR-025** (§K — §I's seam swap discharged, `WorkspacePrincipalAuthorization` deleted whole, the deletion proof a ladder test cannot give), **ADR-009** (the w15 §6 forward bullets discharged, **one of them corrected because this seat wrote it wrong**, and the wave's one new tenant table bound to the guard that actually fires). Rows NW-07/NW-08/NW-31/NW-32 filled, plus an ADR-009 clause on **NW-13** — not my item, but this wave's only new tenant table and my ADR. OQ-w16-002/003/004/007 answered; **OQ-w16-sec-01** opened (convention vs closed enumeration for the reserved principal). **No objection**: nothing at this table weakened RLS or authz-before-retrieval — I checked the three decisions that could have (the capabilities gate drop, the audit tenant-derivation swap, NW-21's resolver) and each is neutral or strictly stronger, for reasons now on disk rather than in a turn. |
| client-architect | **APPROVE** (round 2) — ADR-012's **fourth footer** written and read back (clauses **21–31**; body and §1–§20 untouched): §1's disposition table gains the three retiring stores with the "deleted **with** its read-back, never before" ordering; the wrapper rule **re-grounded against the precedent that cuts against it** (`getQuoteAssessment` is wrapped with no caller "for API-contract completeness", `client.ts:811-813`) and restated as a test — *a wrapper is added in the same task as its first caller*; **`web/src/api/client.ts` declared a one-writer-per-phase file**; NW-13's reverting tick and §D4's ratified ordering; NW-08's contract prose half; NW-31's paired grep; NW-21's Fence-2 client mechanism; **OQ-w16-ca-01 ruled**. Rows NW-08/NW-31/NW-11/NW-12/NW-13/NW-21 filled. **Two clauses correct this seat's own lane draft, and both were load-bearing.** (a) Contract 360 **does** already fetch `getRenewals` (`contract360/index.tsx:115`), so NW-11 needs **no new `ApiClient` method at all** and the standalone action GET stays unwrapped. (b) The quote screen's mount call is the **recalculate POST**, not the assessment GET my C10 assumed (`quotes/index.tsx:91`, `client.ts:805-813`) — so C10 would have shipped a read-back **the screen cannot see**, and software-architect's decline was right on stronger grounds than the ones it was given. **A third correction is against my draft's scheduling claim**: `client.ts` is **not** unwritten this wave — it gains three methods across NW-12 and NW-13 — and it is hand-written glue (`:1-8`), so it needs the contract file's treatment or `check_single_writer.py` rejects the slice; ADR-028's single-writer list is backend-only, correctly, so this file was mine to guard and the draft left it unguarded. Also named: NW-11 and NW-13 collide inside `contract360/index.tsx` (`:6`, `:20`, shared `handleUndo` `:261-269`) — one task or different phases. **ADR-018 `none`** (verified, not assumed: `/quotes/:quoteId` already exists, `WorkspaceShellApp.tsx:90`), **ADR-013 `none`**, **ADR-020 `none`** unless OQ-w16-003 or §30's product half is reversed — each of which seats the designer. No new ADR, none superseded, no body or earlier footer rewritten. **No objection**: every shape I asked for is adopted or declined with a reason I checked against the code, and the two declines improved the result. |
| ux-ui-designer | **PASS** (round 2) — **not seated** (`w16-requirements.md` §3), and **both conditions that would have seated me were tested and did not fire**: OQ-w16-003 **ruled NO** (PO, ADR-001 w16 clause 5 — no web audit surface) and OQ-w16-ca-03 **resolved server-side** (zero client edit, no picker control, no new empty state). No screen, state, copy, token or prototype alignment is touched by NW-07/08/31/32/11/12/13/21/W16-01. **ADR-018, ADR-019 and ADR-020 are `none`; no ADR written, amended or superseded by this seat; no INDEX row.** What this seat did do, because it is the only seat that could: **verified the five design anchors other seats carried into binding decisions** — `screens-v2.md:106-108` (the four named steps, and **"4-step checklist"** independently fixes PO's canonical count of four), `:130` (`racts` "Status shared with the Contract 360 tracker"), `:135` (the column is **"Estimate"**, and the KPI triple is *Contracts analyzed · Upcoming renewals · Savings **identified*** — **the oracle carries no realized-money KPI at all**, which supports PO's money fence from the design side rather than merely not contradicting it), `:143-144` (the W18 levers boundary), and **ADR-018 `:112-119`** — all five exact as cited. **One design-oracle ratification of a ruling made without a designer**: `screens-v2.md:135` fixes the opportunities columns as Supplier · Action · **Estimate** · Status, so an estimate-less pseudo-row is a row the oracle does not define — **OQ-w16-ca-01's retirement of `buildTrackedOpportunityRow` is correct on design grounds too**, independently of the tenant-scale flood argument that produced it. **Design refs table completed** with the missing ADR-018 row (above). **Pre-seated for W17, recorded by the seats that own them, not claimed here**: OQ-w16-ca-01's product half (a *designed* Savings section, never an estimate-less row), an OQ-w16-003 reversal (its own ADR-020 row + a security re-review), and OQ-w16-sa-02 (`roleGate` chip hiding — **presentation, per S16-11 never to be re-described as a security fix**). |
| delivery-manager | **APPROVE** — two w16 footers written and read back on disk: **ADR-016 clauses 27–35** (OQ-w16-004 answered "neither option" from the Terraform, so w16 buys CI **no new credential** and the zero cloud delta is a *consequence of deleting the API steps*, not evidence re-enqueue was free; the workflow's three broken axes; the delete/add disposition with **OQ-w16-dm-01 ruled for the rename and this seat's own fallback refused**; three shortcuts pre-empted; the W17 bulk console designed; the `demo` backlog and the baseline's unapplied infra delta with its four consequences; the final-integration list; the acceptance doc's known-gaps set) and **ADR-014 w16 clauses 1–5** (the two-file CI-YAML set; **one PR** — w15 clause 5's two-merge shape does not apply and must not be re-derived; the **wave-scoped close record** and OQ-w16-008; W16-A1 with new gate item **(g)**; the corrected wave order). **ADR-015 `none`** — no federated credential, GitHub secret, subject claim or Graph right changes, which is what "no new CI capability as a side effect" means in practice. NW-31 row filled; INDEX section appended; five entries in `reports/open-questions.md` (OQ-w16-008 and OQ-w16-dm-01/02 **ruled**, OQ-w16-dm-03 concurred and left open **for the operator**, **OQ-w16-dm-04** opened on the three banked `demo` promotions). **Reconciled, not assumed**: cloud-architect `PASS` with zero `infra/` delta and the apply confirmed as an operator action that cannot roll the image back; security-architect refusing the token on identity-plane grounds with ADR-010/ADR-015 unchanged and the W17 send-right question owed; product-owner re-wording A16-3 to the in-product path and requiring the bulk deferral in the known gaps. **Two corrections to this seat's own lane draft, both recorded rather than re-worded, because the decision record sends re-entering seats to that draft.** (a) It applied **ADR-016's continuous clause numbering to ADR-014**, which restarts per wave — "continue at 8" would have produced citations no reader could locate. (b) Its recommended skeleton put `NW-11 · NW-12 · NW-13` in one phase, which **fails `check_single_writer.py` twice** — `client.ts` (NW-12's wrapper vs NW-13's two) and `contract360/index.tsx` (both retire a store, both touch `handleUndo`). **Neither collision was in the intake's §5 constraint list; both came from client-architect at the table, and both sit inside the one artefact this seat owns.** A slice built on the draft would have been rejected before fan-out — the cheap failure; a decomposer "fixing" it by splitting the contract file would have shipped the expensive one. Corrected skeleton on disk. **No objection**: nothing at this table adds a CI capability, opens `infra/`, flips a `demo` flag, or asks a wave task to do an operator's apply. |

## Gate verdict

pending
