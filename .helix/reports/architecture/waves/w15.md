# Wave w15 — council decisions

Skeleton written by `next-intake`; rows completed by the involved seats at
`next-council-close`; verified by `next-council-gate`. Source:
`reports/context/waves/w15-requirements.md`.

Wave theme: **upload feels instant, and inviting a colleague works end to
end.** Baseline `3720543` (`helix/w15`); the wave base is `origin/main` @
`3c89d35` — see W15-01.

Rows below are the **ten in-wave items** of `w15-requirements.md` §5. The four
queued items (NW-07, NW-08, NW-31, NW-32) are **not** at this table: they are
the head of W16 and carry evidence only.

| Item | Seat(s) | Decision | ADR action | File |
|---|---|---|---|---|
| W15-01 — wave base is `origin/main` @ `3c89d35` | delivery-manager | **delivery-manager**: the base is `helix/w15` with `origin/main` **merged into it** by the operator at HITL, before `register_wave.py` and before fan-out. That much is not new — ADR-014's w14 clause 1 already rules that a wave base is produced by **merging**, never by branching afresh — so this row is an *application*, plus four things the intake's W15-A1 does not have. **(1) The SHA in this row's own title is already stale, and that is the finding.** `w15-requirements.md` is stamped `18:15Z` and names `3c89d35`; `../.git/refs/remotes/origin/main` held **`6ae21b9`**, and the reflog shows the ref advanced three times that day (`6e14b39 → adb9ef6 → 3c89d35 → 6ae21b9`), every update a fast-forward. The rule, not the number, is what binds: **the base is `origin/main` as read at the gate, and W15-A1 records the SHA actually merged.** Two reading traps, both verified and both handing an operator a wrong answer rather than an error: `../.git/packed-refs` is stale for **every branch ref this wave touches** (`origin/main` `3e1f359`, `integration` `333be648`, `origin/integration` `42f1c87a`) — **the loose ref wins**; tags, being immutable, are the one thing it may be trusted for. **(2) The merge must leave a zero product-tree delta.** Five files differ and **three are *shorter* on this branch** (`backend/README.md`, `web/README.md`, `web/e2e/day1.spec.ts`), two absent (`docs/waves/w14-acceptance.md`, `web/e2e/invite.spec.ts`). Resolving any of the three to the process-branch side **silently reverts w14's README sweep and its e2e work**, and no build, test or deploy notices — so the check is `git diff --stat origin/main..HEAD -- backend web infra .github docs scripts` is **empty**. "The two cited files resolve" proves two of five. **(3) Green means the `build + test` job at the base commit, not the deploy.** The deploy job is `needs: build` (`backend.yml:83`), so a red test job means the deploy never runs and the operator sees *no deploy* rather than *red tests* — which is exactly how w14 closed blind to a `main` that was red on two Testcontainers fixtures (fixed later in PR #97). **A red `main` is no `dev` deploy, and no `dev` deploy is no wave**; a `127.0.0.1:5432 refused` is a fixture gap, never a flake to re-run. **(4) `integration` is re-created from the wave base, never merged into** — it is wave-scoped and unprotected (ADR-014 w14 clause 2) and it has **already diverged** on this clone (local `8ed3af1a`, `origin/integration` `271c3ae1`, neither an ancestor of the other by inspection), so a wave starting on it either resurrects a previous wave's commits into its PR or loses its own. **W15-01 produces no fan-out task** — its enforcement *is* the gate, exactly as W14-01. Beside it in `reports/audit/w15-hitl.md` go four operator prerequisites this wave adds, none of them tasks: an **external mailbox the tenant has never seen** (A15-4/A15-5 cannot be walked without one), the **apply identity's directory rights** (ADR-015 w15 clause 2, verified *before* the first apply rather than discovered from a red run), `integration` re-created with confirmation that no second wave is live against it (`helix/w17-input` already exists in this clone), and the **`demo-v4` ruling** (ADR-016 w15 clause 18). Repo-root hygiene is unchanged and still not a wave item: `fed-cred-dev.json` / `fed-cred-demo.json` untracked, plus a stash — referenced by no workflow and no `infra/` file. | **`amend ADR-014`** — w15 footer (its **second**), clauses 1–7: the base SHA is read at the gate and never quoted from a wave document; the zero-product-delta merge test for any behind-base wave; green means the `build + test` job **at the base commit**; `integration` is re-created, not merged into; **a wave that changes `infra/` has two merges to `main`** (clause 5, the structural change, shared with NW-27 / NW-05 / NW-68); w15's planned CI-YAML set is **zero files**; and W15-A1 restated in six points. Body and w14 footer untouched, `Status: accepted` | **`reports/architecture/ADR-014-git-flow.md` (w15 footer — its second)** · `reports/architecture/INDEX.md` (w15 section, ADR-014 paragraph) |
| NW-27 — `POST /api/documents` returns once the file is stored; processing on the Worker | software-architect, cloud-architect, delivery-manager, product-owner, ux-ui-designer | **product-owner**: async pipeline restored as the product rule (spec §7.1 — an undelivered **P0**, so no new scope authority is granted). The admission gate **splits**: format/size in-request, content classification on the Worker. A refusal becomes a **persistent, visible, terminal record** whose *content Raffa does not keep*; never askable, **never counted** in "All documents" nor in the review queue; removable by the existing `DELETE`. Async classification **plus** a session-only refusal is not authorised. · **software-architect**: `201` once the blob and the three rows are durable; the AI pipeline runs on `Raffa.Worker`. **Publish before commit** — `extraction_job` carries `FORCE ROW LEVEL SECURITY` (`documents-contracts.sql:476-477`), so every poller *and* every sweeper is a cross-tenant read ADR-009 forbids; a lost commit leaves a harmless phantom message. The message is a **pointer**; the already-written `ExtractionJob` row is the work. Idempotency is a conditional `UPDATE` claim, never broker dedup; the **database owns the terminal state** (`max_delivery_count` > app `MaxAttempts`), which makes A15-2 a property of the schema. **The pipeline must replace, not append** — at-least-once otherwise duplicates every extracted fact. Gate splits; `Rejected` is terminal, blob deleted, **code-only** (`processing_status varchar(30)`, no CHECK — `:110`); `422` leaves the endpoint. Inline publisher keeps CI green **+ a startup log naming the transport**; A15-1/A15-2 are proven on deployed `dev`. New project **`Raffa.Storage`** — the worker cannot resolve `IDocumentStorage` today. `DELETE` needs no change (`DeleteIfExistsAsync`, verified). **Round 2 — cloud-architect's two corrections are adopted after re-verifying both at the source, and §D12 yields to their ADR-005 footer, which is the ruling §D12 asked for.** (a) **§D12's worker key list is false and would fail the apply, not review**: `containerapps/main.tf` already sets `AZURE_CLIENT_ID` (`:278`) and all three `AiGateway__*` keys (`:283`, `:288`, `:293`) on the worker, so a task acting on §D12 as written emits **duplicate `env` names**; the worker's one genuinely missing key is **`ConnectionStrings__Storage`** (`:138` is the API block), fail-fast while every `ServiceBus__*` binds optionally. (b) **`min_replicas = 1` is rejected** in favour of their KEDA scale rule, and **§D12 bullet 4 + Assumption 4 are superseded**; the subscription is **`document-processing`**, not the proposed `extraction-worker` — one name, or the Terraform and the consumer disagree about which subscription holds the messages; and **§D12 bullet 2 is superseded — NW-27 adds NO Key Vault secret** (topic-scoped RBAC), which is what makes ADR-011's "`acs-connection` is the wave's one new entry" exact rather than approximate. **§D10's predicate inherits**: the transport is Service Bus when the **namespace** is configured, inline otherwise, and the mandated startup log now names the namespace too — which matters more under RBAC, where a missing role assignment fails at first send, not at startup. **And one cross-item hazard no lane had named, which fails the BUILD**: RBAC means `DefaultAzureCredential`, so **`Azure.Identity` enters `Raffa.Worker`** as well as `Raffa.Api` — and `SdkAllowListTests` scans **every** csproj ("hosts and tests included", `:19`, `:31`) and forbids it outside `Raffa.AiGateway`. ADR-026's first w15 footer §2 amends that test for `Raffa.Api` **only**, so **NW-27 goes red on a file NW-67 already edits**: `SdkAllowListTests.cs` is a **single-writer file contended by NW-27 and NW-67**, like `Program.cs`. §D1–§D11 are unchanged (ADR-027 w15 round-2 footer §C2–§C4). · **cloud-architect**: **the resource set that exists today cannot pass A15-2, and for three independent reasons that produce the identical symptom** (201 in under 2 s, nothing ever progresses): `modules/servicebus` is 23 lines creating a namespace and a topic with **no subscription** — a topic with zero subscriptions **discards** every message, so a producer shipped against today's Terraform reports success and loses every document; the worker has `min_replicas = 0`, **no ingress** and **zero `scale_rule` matches in all of `infra/`**, so nothing can ever raise its replica count; and **no Service Bus RBAC and no authorization rule exist anywhere**. Fix: **exactly one** subscription `document-processing` (`max_delivery_count = 5` **strictly above** the app's `MaxAttempts`, `lock_duration = PT5M` — the Service Bus **maximum**, therefore coupled to `ServiceBus__MaxAutoLockRenewalMinutes = 30`; `default_message_ttl = P1D`; **sessions deliberately not enabled**, which would silently cap the worker at one effective replica), two **topic-scoped** role assignments (`Data Sender` + `Data Receiver`) on the existing workload identity — **so NW-27 adds no Key Vault secret at all** — a KEDA `azure-servicebus` scale rule, and `max_replicas` 1 → 3 on **both** apps (A15-1 drops 15 files at three in flight against one 0.25-vCPU replica). **`min_replicas = 1` is rejected**: ~$14/env/month ≈ $28 across both is a **new fixed monthly line** the lock forbids — recorded so it is not re-proposed as a simplification when the scale rule proves fiddly; if the scale rule is not expressible the item returns to this seat. **The quiet blocker: the worker cannot read the blob it is asked to OCR** — it holds `pg-cs` only and has **no `ConnectionStrings__Storage`**; `var.storage_connection_secret_id` is **already passed from both roots**, so the fix is a handle and an env var with **no new module variable and no root change**. That key stays **fail-fast** while all `ServiceBus__*` bind **optionally** — the shape `Raffa.Worker/Program.cs:44` already uses for `Market`, beside three `?? throw` guards. **Foundry needs nothing** and **this corrects software-architect's §11.3**: the worker already carries `AZURE_CLIENT_ID` (`:277-280`) and all three `AiGateway__*` keys plus the model map (`:282-306`), placed by task E10/F02/US01/T01 — acting on the uncorrected list would add duplicate `env` names, an **apply-time error**. · **ux-ui-designer**: the states contract, and **one ruling of this seat's own lane draft withdrawn**. A refusal becomes a **row with a terminal status and no next step**, because NW-27 removes the very premise the session-only card rests on — `UploadResultCard.tsx:16` states that premise itself (*"a rejected file was never stored, has no id"*), and with the gate on the Worker the file **is** stored and the refusal can arrive with the browser closed. The wire value is `Rejected`; **the screen says "Not added"** — no new word enters the product, since label and treatment are lifted from the card being retired (`UploadResultCard.tsx:24`, `.tag-outline`), and `.tag-accent` stays reserved for a Raffa.ai-side failure the user can retry. The three refusal sentences move **verbatim** into the existing hint slot (`DocumentStatusTable.tsx:138`) minus the `"Not added: "` lead-in, which is **embedded inside the two functions** (`uploadPipeline.ts:39-52`) and is therefore an edit to them, never a render-time strip; **`UploadResultCard.tsx` is deleted** — a card *and* a row for one file is two surfaces for one fact, and the card is the one that cannot survive a reload. **The counting ruling is corrected against product-owner and ADR-027 §D7, and it is better for it**: my lane §A4 had "All documents · N" counting refusals on the strength of its "Everything" hint — **withdrawn**. Every number now reads **exactly one server field**: `counts.needsAttention` (which must be defined *not `Completed` **and** not `Rejected`*), `counts.all`, and a **third filter chip `Not added · K`** reading `counts.rejected`, rendered only while > 0. That is the only arrangement in which product-owner's **"persistent, visible, terminal"** row is actually *reachable* — with two chips and `counts.all` excluding `Rejected`, the row exists on the server and **no filter on the screen shows it** — and `counts.rejected` had no consumer in §D7 until now. It also **dissolves OQ-w15-D1**. `stage: null` reads **"Queued…"**, and that costs nothing: `documentTable.ts:76-78` already states that only the stage text distinguishes queued from actively processing; the six stage strings are untouched. **The one hazard the compiler will not report**: `documentTable.ts:97-99` is an unchecked cast (`status as DocumentStatus`), so adding `"rejected"` to `RowStatus` and forgetting `semantics.ts:52` **compiles clean** and falls through an exhaustive switch to `undefined` — a two-file edit whose second file `tsc` will not ask for. **This seat did not supersede R-DOC-05 AC-1 and refused to from its own seat**; product-owner ratified it (OQ-w15-D3). · **delivery-manager** (the ordering, the apply and what proves it — the queue contract is software-architect's and the resources cloud-architect's): **one Terraform task owns every file under `infra/`, in phase 1, and it is one PR.** Not a preference: NW-27 alone writes `modules/servicebus`, `modules/containerapps` **and both environment roots**, and NW-68, NW-67 and NW-05 claim the same two of those — under `check_single_writer.py` that is either one task or four serialized phases, and four phases of Terraform in a five-phase wave leaves one phase for ten items. **That PR merges to `main` *before* the wave PR** (ADR-014 w15 clause 5), because cloud-architect's `ConnectionStrings__Storage` ordering is **HARD** and an HCP apply is triggered *by* the merge — a single merge event cannot put the apply first. It is safe by construction: `backend.yml` and `web.yml` are path-filtered to `backend/**` / `web/**` and **`infra/**` is in neither** (`backend.yml:16-23`, `web.yml:17-23`), so that merge deploys no image. **Inside the wave the Terraform task gets no dependents**: its effect is not observable by the wave that wrote it (ADR-016 w14 clause 3), so a `depends_on` pointing at it would be a lie — and under `require_delivery: true` with `task_failure_markers: ["HALTED:"]` it is a lie that fails real tasks. Its failure must therefore be caught by a human reading the slice, not by the graph. **The delivery finding this item turns on, and I adopt cloud-architect's version of it over my own**: `backend.yml`'s deploy is `az containerapp update --image` with **no revision-state assertion anywhere**, so a worker that crash-loops or never activates produces a **green run** — uploads return 201, nothing progresses, and a queue behind a dead consumer is indistinguishable from slow processing. Three independent Terraform gaps produce that identical symptom today (no subscription on the topic → silent discard; `min_replicas = 0` with no ingress and **zero `scale_rule` matches in all of `infra/`** → nothing ever starts; no Service Bus role assignment). So the gate gains **one post-deploy revision-state assertion per environment** — worker revision running and healthy, subscription present, scale rule present, both topic-scoped role assignments present — and **"the HCP run is `CURRENT`" proves none of them**. **A15-1 and A15-2 are walked on deployed `dev` with the worker scaled the way `demo` will run it, never in CI**, which has no Service Bus, no worker and no browser. **`backend.yml` needs no change and must not be opened**: the worker image is already built (`:138-144`) and deployed (`:203-208`) in the same job as the API, API first; the migration extends `documents-contracts.sql`, already in **both** ADR-021 arrays, so neither array moves — software-architect's `none — ADR-021` confirms it. If a new .NET module with its own migration script appears, **both** arrays change and `backend.yml` joins the planned set: a table decision, never a task-time discovery. **One retraction of my own lane**: §2.3 held the worker's scaling back from `demo` until promotion on cost grounds — **withdrawn**, because cloud-architect's answer keeps `min_replicas = 0` and scales on queue depth, so an idle `demo` worker costs nothing and there is nothing to hold back; only `max_replicas` moves, which is a ceiling, not a floor. Rollback is intact: the migration is **purely additive** (OQ-w15-012), so the previous image runs unchanged against the new schema and a code revert needs no schema rollback — and it does **not** un-apply Terraform, whose keys are inert to the old image. | **product-owner**: `amend ADR-001` (w15 footer, clauses 1–2 + 7) · **software-architect**: **`new ADR-027 async-document-processing`** + `amend ADR-002` (queue port becomes real; `Raffa.Storage`; the R0 "no messaging middleware" assumption retires) + `amend ADR-024` (supersede "classify before store" in its ordering half; A7 / R-DOC-05 AC-1 on the record; one definition of *validated*) · **`none — ADR-021`**: the new status needs **no DDL** and `documents-contracts.sql` is already in both `backend.yml` arrays · **`none — ADR-004 / ADR-017`**: the five roles and the gateway boundary do not change, only the host that calls `ocr`/`classify` · **`none — ADR-009`** from this seat: the worker's scope comes from the message's tenant id and no cross-tenant read is introduced — security-architect ratifies · **cloud-architect**: **`amend ADR-005`** (Service Bus provisioned → **wired**: the subscription and its five settings, the two topic-scoped role assignments, the worker `st-cs` handle, the scale rule with its **proved-not-asserted** prerequisite, the `min_replicas = 1` rejection with its arithmetic, both `max_replicas`, and the **$0.00** fixed-cost confirmation) · **`amend ADR-007`** (`servicebus → containerapps` and `identity → servicebus` become real edges; `modules/servicebus` takes `workload_principal_id`) · **`none — ADR-006`**: every resource here is regional and keeps the `northeurope` pin · **`none — ADR-008`**: no Foundry change — no role assignment, no deployment, no key (see the §11.3 correction) · **`none — ADR-021`**: concurs, no DDL from this seat's side either · **ux-ui-designer**: `amend ADR-020` (**its fifth** — §1, screen 3: the row, the label, the hint copy, the deleted card, the empty action cell, the third chip's copy, the "Queued…" reading and the restated state inventory; plus **§0**, the wave-wide rebrand rule) · `amend ADR-019` (**its second** — **one** semantic row, `Rejected → .tag-outline` "Not added", **derived from the shipped card** rather than invented, plus the unchecked-cast hazard; **no token, no component, no confidence row**) · `amend ADR-018` (**its fifth** — clause 2, what each Documents number counts, the third chip, and the rail-badge rule NW-10's task needs wherever it lands) · **`none — ADR-021 / ADR-024 / ADR-027`**: the wire value, the status vocabulary and the pipeline are other seats'; this seat rules only on what the screen says · **delivery-manager**: **`amend ADR-016`** (w15 footer — clause 13 the first per-environment API key and the apply barrier restated; clause 14 **one infrastructure PR merged before the wave PR**, with `demo` flag-gated at that merge; clause 15 required-vs-optional keys and the `ConnectionStrings__Storage` fail-fast row; clause 17 the **green-run-does-not-prove-the-worker-started** assertion; clause 19 the promotion sequence; clause 22 the final-integration checklist) · **`amend ADR-014`** (w15 clause 5 — the two-merge rule this item forces, recorded under W15-01) · **`none — ADR-005 / ADR-007 / ADR-027 / ADR-021`**: the resources, the layout, the queue contract and the schema are other seats' and are adopted as ruled; this seat rules only on the order they land in, the apply that carries them and the check that proves them | `reports/architecture/ADR-001-scope-r0-r4.md` (w15 footer) · **`reports/architecture/ADR-027-async-document-processing.md` (new)** · `reports/architecture/ADR-002-dotnet-solution.md` (w15 footer) · `reports/architecture/ADR-024-ask-raffa-v2.md` (w15 footer) · **`reports/architecture/ADR-005-azure-skus.md` (w15 footer §1–§2, §5–§8)** · **`reports/architecture/ADR-007-terraform-layout.md` (w15 footer — its first)** · `reports/architecture/INDEX.md` (w15 section + ADR-027 row + the ADR-005 / ADR-007 / ADR-018 / ADR-019 / ADR-020 paragraphs) · **`reports/architecture/ADR-020-web-screen-inventory.md` (w15 footer §0, §1 — its fifth)** · **`reports/architecture/ADR-019-web-design-system.md` (w15 footer — its second)** · **`reports/architecture/ADR-018-web-information-architecture.md` (w15 footer clause 2 — its fifth)** · **`reports/architecture/ADR-016-promotion-dev-demo.md` (w15 footer clauses 13–15, 17, 19, 22 — its third)** · `reports/architecture/ADR-014-git-flow.md` (w15 footer clause 5) |
| NW-61 — upload feels instant; details only when the document is ready | client-architect, ux-ui-designer, software-architect, product-owner | **product-owner**: reduced to the **completeness contract** — the client half already exists and two of its three defects vanish when NW-27 makes the server row appear. Product rule: **"still processing" outranks "empty"**; an empty Contract 360 during processing is a fabricated fact (spec §2). **Askable = validated, not merely `completed`** (`percorso-pilota-v1.md:55-56`). *Still processing* and *waiting for your review* must not collapse into one string. Screens are **gated, never blocked** (`:54`). No new copy — `screens-v2.md:28-29` already ships the sentence. · **software-architect**: the server must **say** "not ready yet", never let a screen infer it. `GET /api/documents` gains a server-computed **`counts { all, needsAttention, processing, rejected }`**, tenant-wide and never page-wide (one added `GroupBy`, not four round trips) — `all` excludes `Rejected`, which is what makes *never counted* a **server fact**; this field also closes **NW-10**. `GET /api/contracts/{id}` gains **`readiness { state, stage, documentCount, completedDocumentCount }`** — today a mid-pipeline contract returns **200 with a fully shaped, entirely empty tab tree**, indistinguishable from "extraction found nothing". `GET /api/contracts` gains `processingDocumentCount`. **Four incompatible definitions of "ready" exist; one survives** (ADR-026 §D2). The worst is already user-visible: `AskCopilotService.cs:294` renders the **unfiltered** count into *"Nothing in the N validated contract(s)…"*, so **the product states a fabricated fact in shipped copy before this wave changes anything** — A15-3's exact prohibition. Generator limits bind: `readiness.stage` is nullable so it **must not** carry an `enum` (`enum` is checked before the nullable union, so a nullable enum loses its `null`). **Round 2 — this seat's own claim that `counts`/`readiness` "are nested objects rendered `unknown`" is WITHDRAWN, and client-architect's correction is adopted after re-verifying it at the source**: `renderSchemaType`'s object case **recurses** (`generate-api-client.mjs:109-111`) and returns `unknown` only for an absent `properties` map (`:107`) or a `$ref`/`oneOf` (`:114-118`). Both remedies this seat prescribed — flatten to `countsAll`/`countsProcessing`, or extend the generator — are **wrong work**, and the third and likeliest outcome in front of an `unknown` is the hand-written DTO ADR-012 `:53` forbids. **Land them inline, touch no generator, flatten nothing.** The trap is in the source and the task removes it: that case's doc comment (`:98-106`) still says *"flat property maps only … extend this case when a future endpoint needs a property whose value is itself an object"*, which its own next two lines falsify — the staleness class of OQ-w15-003 and `NullInvitationMailer.cs:28-29`, and w15 is the first wave to exercise the recursion. **And ux-ui-designer's ask is answered: `counts` does NOT gain `askable`.** `buildKbSummary` (`documentTable.ts:154-160`) is wrong twice — it counts the **fetched page** and equates askable with `processingStatus == "Completed"`, a **document**-level test where §D2's *validated* is **contract**-level. Putting it on `counts` would mint a **fifth** definition of ready in the wave whose §D8 collapses four into one. Instead: `N documents` → `counts.all`; `M askable` → §D2's existing `contractCount` on the `GET /api/workspaces` row, which `useValidatedContractCount.ts:66` already reads and `AppShell.tsx:59` already passes down the `Outlet` context — **zero new fields, zero new round trips, and the same number the rail and Ask show** — or the segment is removed, which is **copy and theirs**; `K waiting for your review` → **`counts` gains `needsReview`**, because `needsAttention` is `NeedsReview + Failed` and rendering the wider number under narrower words is the defect §D8 removes. It is free: the counts are projections of **one** grouped query. **§D7's object is therefore `{ all, needsAttention, needsReview, processing, rejected }`** (ADR-027 w15 round-2 footer §C1, §C5). · **client-architect**: the client half of R-DOC-09 is **already built and correct and must not be rebuilt** — the optimistic row exists before the request starts (`useDocumentsList.ts:131-134`), the bar renders the **server's** stage and never a client timer (`DocumentStatusTable.tsx:151`), the 2 s poll exists (`:110-119`) — and **no poll-predicate change is needed**: `hasNonTerminalRow` (`:105-108`) already reads the server array, so this seat **withdraws the intake's implied change**. Four decisions. **(1) The optimistic row is handed off on evidence, not on a timer — and this is a regression NW-27 *creates*.** `:140` drops the local entry and `:141` **then** calls `load()`. Today the POST takes tens of seconds and the gap is one invisible RTT; at a sub-2 s POST it becomes a **visible flicker on every file of a 15-file batch** — precisely A15-1's "all 15 rows exist" claim. The 201's server id is recorded on the local entry and the entry is dropped **only when a server row with that id is present**; if `load()` fails the row **stays** and ADR-018 `:117`'s error state shows. **(2) Both counters read the server.** `:123` `allCount = documents.length` is the **fetched page** (capped at 100, `:15-18`) and `:122` `attentionCount` is a client filter over that same page; both read ADR-027 §D7's `counts`, which ADR-012's w14 clause 7 already required ("a count has one definition and it is the server's"). **(3) The completeness gate reads a server fact on four surfaces, and no client derivation is proposed for any of them** — deriving "still processing" by fetching every row is the heuristic clause 7 deleted. Ask's off-state branches on the **not-yet-terminal** count, not on `page.totalCount > 0` (`ask/index.tsx:99-105`) which counts **any** state including `Failed` and makes `askViewModel.ts:250-261` tell a tenant whose only document **failed** that it "is still processing or waiting for review" — **a fabricated fact the product ships today, before this wave changes anything**. Ask must also **re-read while it is off**: `useValidatedContractCount.ts:42-50` fetches once per shell mount, so a user sitting on `/ask` while a document completes **never sees the gate flip** — it re-reads on the same 2 s cadence, reusing the existing `listDocuments(pageSize: 1)` at `ask/index.tsx:102`, not a new endpoint. Portfolio does the same instead of "Nothing to triage yet" (`contracts/index.tsx:130-139`). Contract 360 is **contract-scoped**, so the workspace envelope cannot answer it (it reads no `processingStatus` anywhere today) and it needs §D8's `readiness`. The **third Ask off-state** this creates — *documents exist, none in flight, none validated* — **must not be collapsed back into the "still processing" sentence**, which would re-ship the same lie under a server number; **its copy is ux-ui-designer's**. **(4) One client-owned upload deadline**, configured in one place and never per call site (`client.ts:1726` has no `signal`; zero `AbortController` in the path), mapping an abort to the existing `"failed"` branch (`uploadPipeline.ts:127-132`) so the user gets Raffa's retryable message rather than an opaque platform 502. It must exceed the time to **send** a 50 MB body and sit **below** the platform ceiling — and **the ceiling is still implicit**: `containerapps/main.tf:179-194` declares no `timeout` and ADR-005's w15 footer pins none (verified). **120 s in force**, derivation recorded in the task (OQ-w15-ca-01); it is a safety net, not the mechanism. **Correction to a peer, and it is the one that changes a task**: §D7–§D9 records that `counts`/`readiness` "are nested objects rendered `unknown`" — **they are not**. `renderSchemaType` recurses (`generate-api-client.mjs:110` → `:97`), the contract already nests **three deep** (`raffa-api.v1.json:3103-3105`, `:5253-5261`) and the rendered result is on disk (`schema.ts:646`). Only **`$ref`/`oneOf`** renders `unknown`, which the contract itself works around by duplicating shapes inline and says so at `:3910`. Left uncorrected the likeliest repair in front of an `unknown` is a **hand-written DTO in `client.ts`** — the "no hand-written divergent DTOs" prohibition of ADR-012 `:53`. **Land `counts`/`readiness` inline; touch no generator; hand-write nothing.** **NW-10 rides this item** (OQ-w15-008): `documentStore.ts` is **deleted outright** — its write half `rememberDocument` (`:77`) has **zero production call sites**, so the badge is not wrong, it is **always absent** — replaced by a `useDocumentCounts` hook over `listDocuments(pageSize: 1)` reading the same `counts`; `getDocumentsBadge`'s honest-absence rule is unchanged. · **Round 2 — four client mechanisms ux-ui-designer's states ruling needs, one of which stops a deletion (ADR-012 w15 §13.4–§13.8).** **(a) The refusal has FOUR producers and exactly ONE becomes a server row, so `UploadResultCard.tsx` may not simply be deleted.** Their ground for deleting it is that "with the gate on the Worker the file **is** stored" — true for `uploadPipeline.ts:117-120` (**422 + `rejection.reason`**, the content gate that moves to the Worker) and **false for two others that product-owner's split gate keeps pre-storage**: `:105-108` **`isOversized`, checked in the browser with no HTTP call at all**, and `:122-125` **413/415**. Neither is ever stored, so neither can be a row, in this wave or any later one; deleting the card as written makes an oversized file a **silent drop**, a worse defect than the two-surfaces one the deletion fixes. Mechanism that keeps their principle instead of trading it: **`LocalUploadEntry.phase` gains `"rejected"`** and those two render as a **local row** in the grid that already renders local entries (`DocumentStatusTable.tsx:86-110`, hint slot `:90`), no action, no delete cell — **one file, one row, on every path**, session-only *by construction* (no id, never in `documents`, never in `counts`). The card, `RejectedFileOutcome`, `rejected`, `dismissRejected` and `onDismissRejected` then leave together. **Copy and tag stay theirs**; only the surface is this seat's — and if they prefer the card for these two paths that is their call, but the deletion may not land with no replacement. **(b) The third chip cannot filter the page.** The grid fetches **one page** (`useDocumentsList.ts:79`, `LIST_PAGE_SIZE = 100`) and buckets client-side by design (`:15-18`); `counts.all` excludes `Rejected`, so a client bucket would need the page to carry rows "All documents" says it does not contain — two definitions of one list. `Not added · K` therefore fetches `listDocuments({ status: … })`, which is **§10's eighth contract site** (the `status` *query parameter*, invisible to the build and to `tsc`) — the chip is what turns that latent hazard into a shipped one. And `isAttentionStatus` (`documentTable.ts:141-143`, `!== "Completed"`) must mirror `counts.needsAttention`'s server definition *not `Completed` and not `Rejected`*: one definition with two implementations is how a number and a list drift apart. **(c) `buildKbSummary` (`:154-160`) is a third page-derived number** — their ask to software-architect; the client half either way is that **no page-derived number survives this screen**: it reads `counts.askable` or the segment is removed, never left counting `Completed` over ≤100 rows. **(d) "Queued…" is the *server* row's null stage** (`DocumentStatusTable.tsx:155`); the **local** row's "Uploading…" (`:105`) is a different fact and stays, or the one genuinely-uploading reading in the product ends up saying "Queued". **(e) Single-writer, tightened**: (a)–(d) put NW-27's states work and NW-61's counts work in the *same four files* (`uploadPipeline.ts`, `useDocumentsList.ts`, `DocumentStatusTable.tsx`, `documentTable.ts`) → **one task or different phases**; and the contract gains a **fourth** w15 edit (the declared 502 of §13.2) on top of §10's three. · **ux-ui-designer**: the states and the copy for the three surfaces that cannot tell "nothing here" from "still processing". **ADR-018's IA states contract gains two states** — **not ready yet** (something is in flight) and **nothing made it through** (Raffa.ai holds documents, none in flight, none validated). Both render in the *empty* block and **never** the error treatment (nothing has failed at the surface's level), both are **told by the server and never inferred**, and both exist to enforce one rule: **an empty state must never tell a user to do a thing they have already done**. Stated once at IA level it binds Ask, Portfolio and Contract 360 **and** Renewals and Quote check, which inherit it with no task and no second decision. **Ask (screen 2): the copy is already right and the *predicate* is wrong.** `buildOffCopy(hasAnyDocument: boolean)` (`askViewModel.ts:250-261`) is a **boolean ternary** over `totalCount > 0` in **any** state (`ask/index.tsx:99-105`), so the product today tells a tenant whose only document **failed** that it "is still processing or waiting for review" — A15-3's exact prohibition, shipped before this wave changes anything. **This answers OQ-w15-ca-04**, which client-architect routed here: the third variant is **"Raffa.ai could not finish processing your documents. Ask only answers from facts that passed validation — so it never guesses."** with CTA **Go to Documents**, its second clause verbatim from the shipped string. It honours their binding constraint exactly — it is **not** collapsed back into the "still processing" sentence. **And a tenant holding only refused files gets the *first* variant, not the third**: "Upload a contract first" is **true** for them, because the counting rule excludes `Rejected` from what Raffa.ai holds and they were already told per row why each file was refused — the counting rule doing real work rather than bookkeeping. `buildOffCopy`'s signature widens from `boolean` to a three-way state; fixed grammatical number is this surface's existing practice, so **no pluralisation logic is introduced**. **Portfolio (screen 6)** gains a third variant with **no new string at all** — the shipped sentence minus its "Upload one to start." clause, which is precisely the clause that tells the user to do what they have done. **Contract 360 (screen 5)** gains a fifth state with two readings, driven by ADR-027 §D8's `readiness` and **never inferred from an empty clause array**, since that array is also what a genuinely factless contract returns and the two must not share a screen. **Four new strings enter the product here and none has an oracle** — the export has no such state anywhere — so each is derived from a shipped sentence and each is on the owed-exports list. **A finding this item creates that no lane had named**: `kbSummary`'s "**M askable**" (`documentTable.ts:154-160`) is computed over the **fetched page** (≤100) *and* equates askable with **`Completed`**, which product-owner's own row and ADR-026 §D2 rule is **not** the definition — the same fabricated-fact class software-architect found at `AskCopilotService.cs:294`, one screen earlier. Ask to software-architect: **`counts` gains an `askable` member under §D2's single definition, or the segment is removed** rather than left reading a number the product does not mean. **Both asks are now read back against ADR-027 as promoted, and one is a correction that changes a task.** (i) §D7 lands `counts { all, needsAttention, processing, rejected }` with **no `askable`**, so the segment is **removed** — recorded as resolved (ADR-018 w15 clause 3) so the decomposer writes one task rather than choosing. (ii) **§D7's `needsAttention` is `NeedsReview` + `Failed`, justified as "the definition the client's attention filter already applies" — and that premise is false.** `isAttentionStatus` is `processingStatus !== "Completed"` (`documentTable.ts:141-143`), including `Uploaded` and `Processing`, derived in its own doc comment from the export's `attnDocs = docs.filter(d => d.status !== 'completed')` and R-DOC-06. Since `attention` is the **default** filter, shipping §D7 as written puts fifteen just-dropped rows under a chip reading **"Needs your attention · 0"** — NW-61's reported defect verbatim, re-created on the server where it looks authoritative, and A15-1 fails on the screen the user is actually looking at. **A chip's number and the rows it filters to are the same set**: `needsAttention` is *not `Completed` and not `Rejected`* (ADR-018 w15 clause 2b). Narrowing the *filter* to match the count instead is rejected and named — it would hide the fifteen rows from the default screen and contradict `getFilterHint`'s shipped "Completed documents are hidden — they are already askable." **Ask to software-architect, whose file it is**: §D7's `needsAttention` bullet takes that definition, or names which of the two surfaces it intends to be wrong; `all` / `processing` / `rejected` are correct and not re-opened. · | **product-owner**: `amend ADR-001` (w15 footer, clause 3) · **software-architect**: covered by **`ADR-027`** (§D7–§D9) + `amend ADR-024` (§4 — one definition of *validated*; the grounding guards and the no-tools `answer` contract are **untouched**) · **`none — ADR-012`**: client-architect's, and §D7–D9 supply the server fields their provenance rule needs · **client-architect**: `amend ADR-012` (w15 footer §4–§6 the provenance rule, the evidence hand-off, the counters, the deadline and NW-10; §9 the generator correction; §10 the eight-site contract rule and the single-writer rulings; **§13.4–§13.8 in round 2** — the four refusal producers and the local `"rejected"` row that lets the card be deleted safely, the third chip's server-side bucket, `isAttentionStatus`'s single definition, `buildKbSummary`, and the tightened single-writer ruling) · **`none — ADR-018`**: **no route is added, moved or removed this wave** — recorded explicitly so no task re-opens the router table; the three new screen states are ADR-018 `:112-119`'s states contract and their copy is ux-ui-designer's · **`none — ADR-013`**: mobile is a non-gating scaffold and no w15 item reaches it · **ux-ui-designer**: `amend ADR-018` (clause 1 — the two new states, their two binding rules and the five surfaces they bind; clause 3 — the `kbSummary` "M askable" finding and its ask) · `amend ADR-020` (§2 — screens 2, 5 and 6: Ask's third variant, Portfolio's third variant with no new string, Contract 360's fifth state and its two readings) · **`none — ADR-019`**: every element is the existing empty block, `.btn-secondary` and a paragraph — no token, no component, no semantic row · **`none — ADR-012 / ADR-027`**: the poll predicate, the deadline, the read-back contract and the server fields are client-architect's and software-architect's; this seat rules only on what the screen says once they arrive | `reports/architecture/ADR-001-scope-r0-r4.md` (w15 footer) · `reports/architecture/ADR-027-async-document-processing.md` (§D7–§D9) · `reports/architecture/ADR-024-ask-raffa-v2.md` (w15 footer §4) · **`reports/architecture/ADR-012-web-stack.md` (w15 footer §4–§6, §9–§10 — its third)** · `reports/architecture/INDEX.md` (w15 section, ADR-012 paragraph) · **`reports/architecture/ADR-018-web-information-architecture.md` (w15 footer clauses 1, 3)** · **`reports/architecture/ADR-020-web-screen-inventory.md` (w15 footer §2)** |
| NW-10 — rail Documents badge reads the server | none | task only | none — ADR-012's w14 footer already decided it ("a client store never stands in for a missing GET") | — |
| NW-67 — Raffa provisions the invitee's Entra B2B guest at invite time (Graph) | security-architect, cloud-architect, software-architect, client-architect, delivery-manager, product-owner | **product-owner**: IN — it **completes w14 clause 1**, it is not an expansion (an Azure-portal step the customer must perform is not a product). Scope statement: *inviting a colleague creates a guest identity for that address and never anything more*. Three fences: Raffa **creates a guest, never deletes or blocks one** (answers OQ-w15-007); **one guest per address, idempotent**; **no directory reads beyond provisioning**. Not §1.2 "supplier onboarding" — the invitee is a colleague (§20 Day 1, §3.1). **w14 clause 7 resolved: the domain restriction is NOT enforced** (a B2B guest is by definition off-domain; acceptance invites a personal address). Admin consent is a **one-time operator act, never a runtime prompt**. · **software-architect** (seam, placement, failure contract — the permission and consent are security-architect's, the identity and Terraform cloud-architect's): **synchronous, inside the invite request, before the mail** — one interactive action with no 2 s budget, and a mail whose link leads to a sign-in the invitee cannot complete is A15-4's failure, not a pass. **A provisioning failure aborts the invitation: no invitation row, no mail, no token** — forced by ADR-026 §D4's own partial unique index, which would otherwise let a failed invite **hold the address slot** and block the retry (the dead-slot trap ADR-020's second w14 footer already documented). One audit row + **502** with a machine-readable `reason` from a **closed set** (`consent_missing` | `provisioning_failed` | `directory_unavailable`) whose copy is ux-ui-designer's. `IGuestProvisioner` takes `IInvitationMailer`'s shape with a fourth **`NotConfigured`** value (default off) so the wave lands whether or not the directory permission ships. The 201 gains **`identityProvisioned`** — NW-69's pre-send fact, a server fact rather than a client inference. Adapter in `Raffa.Api/Infrastructure/`, never the module (ADR-002); `Azure.Identity` therefore enters `Raffa.Api` and **`SdkAllowListTests` must be amended in the same task** — part of the design, not a workaround. **Round 2 — security-architect's §J.1 correction is adopted and this seat's ADR-002 clause 4 is corrected in a second footer, because the difference is between a rule and a build failure.** `DependencyDirectionTests` builds its path as `src/{moduleName}/` from the **fixed ADR-002 domain-module list** (`:111`, list `:60-73`), so adding `Microsoft.Graph` to `ForbiddenSdkPrefixes` **cannot** enforce "one Graph call site" in a **host** — which is where the adapter lives. It is **not deleted** (nothing in that list — `"Azure."`, `"Microsoft.Azure."`, `"Microsoft.AI."`, `:78-83` — matches `Microsoft.Graph`, so a domain module could take a direct Graph dependency today and no test would object; that hole is real and closing it is right), only **narrowed to what it can do**. The host rule belongs to **`SdkAllowListTests`**, amended **package-scoped**: widening its single `AllowedProjectName` (`:25`, `:40-43`) is the one-word edit a task will reach for and would make **`Azure.AI.*` legal in `Raffa.Api`**, un-guarding the Foundry boundary. **The wave-wide map, stated once so two items do not each invent one**: `Azure.AI.*` → `Raffa.AiGateway`; `Azure.Identity` → `Raffa.AiGateway` + `Raffa.Api` + **`Raffa.Worker`** (NW-27's RBAC transport — see that row); `Microsoft.Graph` → `Raffa.Api`. **`SdkAllowListTests.cs` is a single-writer file contended by NW-27 and NW-67**, and ADR-026 §7 item 3's instruction is correct **and now known to be insufficient** (ADR-002 w15 round-2 footer; ADR-026 w15 round-2 footer §10). · **cloud-architect**: **no Azure resource, no SKU, no meter, $0.00.** The caller is the **existing per-environment workload managed identity** (`id-raffa-<env>-workload`), already attached to both apps and already published as `AZURE_CLIENT_ID` — **no secret, no app registration, no client credential**; `DefaultAzureCredential` against `https://graph.microsoft.com/.default`. The `azuread` provider is already required (`~> 3.0`) and configured in both roots — **no new provider**. `modules/identity` gains a Graph `data "azuread_service_principal"`, a **`count`-gated** `azuread_app_role_assignment` for `User.Invite.All`, `var.guest_provisioning_enabled` (default **`false`**) and a `tenant_id` output. **The operator act is right in substance and wrong in mechanism, and the difference decides whether the apply succeeds: for a managed identity there is no separate "Grant admin consent" click — the `azuread_app_role_assignment` *is* the consent.** What genuinely needs a one-time grant is **upstream**: the identity that runs the HCP apply must be able to write app role assignments and **cannot today** — it needs Graph `AppRoleAssignment.ReadWrite.All` + `Application.Read.All`, or **Privileged Role Administrator** / **Cloud Application Administrator**, granted **before** the apply. That is an **ADR-015** matter, not a runbook line, because `modules/identity/main.tf:41-42` records "the deploy job does not need Microsoft Graph (ADR-015)" and that stops being true for the **apply plane**. **The `count` gate is what lets all three applies ride one PR**: if the permission is absent the flag stays `false`, `count = 0`, **the whole w15 apply still succeeds**, and NW-67 lands on a later one-line flip — so a missing directory permission **degrades NW-67 instead of blocking Service Bus and mail**. Fallback if the operator declines to widen the apply identity permanently: a Global Administrator performs the single assignment out-of-band, the resource stays at `count = 0`, and the deviation is recorded with an `import` path. Keys: `Invitations__GuestProvisioning__Enabled` (`dev` `"true"` / `demo` `"false"`) and `__TenantId`, both optional, **no secret** — the Graph endpoint is a constant, not configuration. `User.Invite.All` vs the narrower **Guest Inviter** directory role is **security-architect's ruling**; one resource either way and this seat implements whichever they name. · **security-architect** (the permission, the consent, the ordering, the bind, and the acceptability ruling): **`User.Invite.All` as a Graph *application* permission on the existing per-environment workload identity — not the Guest Inviter directory role.** Cloud-architect asked and this is the ruling: both are narrow enough, so least privilege is decided by **auditability** — an app-role assignment is a fixed, named grant visible in the API's consented-permission surface and in Terraform state, and it widens only when our code changes, whereas a directory role is a **Microsoft-owned bundle that can widen without our Terraform changing** and does not appear where a security reviewer looks. One resource either way. **Rejected**: `User.ReadWrite.All` (read/update/**delete** of every user object, to create one guest); delegated OBO (better attribution, but needs a confidential-client credential ADR-010 deliberately does not have and the customer's Procurement Admin to hold Guest Inviter). **The single highest-value instruction in this wave: bind the Graph-returned guest object id into `workspace_user.ExternalSubjectId` at invite time.** Without it a B2B guest's UPN arrives mangled (`luca_gmail.com#EXT#@…`), ADR-025 Rule D.3b's case-insensitive email equality **fails**, and **A15-4 dies at its last step with the invitee signed in and locked out**; the de-mangling "fix" is ambiguous for addresses containing `_` and must never be a grant basis. **Ordering: guest first, row second** — and this seat and software-architect converged on it independently (they from D4's partial unique index and the dead-slot trap, this seat from "an inert artefact beats a live credential": row-first leaves a **live 256-bit token for an identity that cannot sign in**, which is A15-7's defect). **The Graph `inviteRedeemUrl` is credential-shaped** — it redeems a guest with no Raffa row, no token and no membership write — and is **never mailed, returned, stored, logged or audited**. **Exactly two externally-visible outcomes** (`provisioned` covering *both* "created" and "already present", and `failed` from a closed reason set), because a workspace Admin is not a directory admin and the invite form must not become a **directory-enumeration oracle**: A15-5's no-op must be indistinguishable in shape from a fresh provision. **OQ-w15-005(b) ruled** (see the OQ table): the shared directory is acceptable **because** directory presence is not a grant, and **S-T19 proves it rather than assuming it**. **Correction to ADR-002's w15 footer, and it is the difference between a rule and a build failure**: `DependencyDirectionTests.ForbiddenSdkPrefixes` covers only the fixed ADR-002 **domain-module** list — `SdkAllowListTests.cs:13-19` says so verbatim — so adding `Microsoft.Graph` there cannot enforce "one Graph call site" in a **host**, which is exactly where the adapter lives. It belongs in **`SdkAllowListTests`**, which scans every project "hosts and tests included"; and the amendment must be **package-scoped**, because widening its single `AllowedProjectName` skip (`:25`, `:40-43`) — the one-word edit a task will reach for — would make **`Azure.AI.*` legal in `Raffa.Api` too**, silently un-guarding the Foundry boundary and making `:62-77`'s non-vacuity check meaningless. **`sendInvitationMessage: false`**, one call site, invited address from the authenticated Admin's body only, and a **per-tenant cap of 100 live invitations** (the partial unique index bounds the *same* address; the cap bounds *distinct* ones, which is the directory-spam shape). A directory policy can still refuse an otherwise-granted call, which is why the reason set is a closed enumeration and why provisioning is **proved by one real invite on deployed `dev`, never asserted from Terraform state**. · **client-architect** (exactly two things: the accept completing without a second click, and what the popup's return lands on): **A15-4 dies today on a mandatory second click, and the code says so.** `handleContinueWithEntra` (`invite/accept/index.tsx:172-183`) calls `loginPopup` and, verbatim, *"**Success needs no handling here**"* — the `useMsal()` re-render merely flips the CTA between "Continue with Microsoft Entra ID" and "Join" (`:224-238`). Decision: **it awaits its own `loginPopup` promise and continues into the accept on resolution.** Deliberately **not** a `useEffect` on `accounts`, for three reasons: it is the *same* user gesture and the consent the button carries is consent to join; it fires only on the transition **this screen initiated**, so a visitor **already signed in** on arrival still reaches ADR-020 **state 3** with its explicit "Join {workspace}" button — an effect on `accounts` would auto-join them silently and remove the one consent step state 3 exists for; and an effect keyed on an MSAL account array can fire for reasons that are not this sign-in. Ordering inside the chain: if the GET has not resolved when the popup does, the accept **waits for the offer** — `handleJoin`'s guard (`:137-139`) must not be weakened into a race. Popup-blocked keeps its current landing (`:175-177` → ADR-020 state 5), which is why ADR-012's second w14 footer clause 4 still holds: **no `must` depends on the popup**. Nothing else on this route changes — not the 8-phase union (`:70-80`), not the fragment capture (`:57-68`, `:101`, a lazy `useState` initializer that clears the address bar **before first paint**, satisfying ADR-025 C10b's intra-route ordering), not the storage rule. **The client-architecture reason NW-05 must not slip behind NW-67, and it is not in the intake.** The accept binds identity from `X-User-Id` (`client.ts:1093`), whose value is the MSAL account `username` (`main.tsx:70-73`), and the server matches that string case-insensitively against the invited email. For a freshly provisioned **B2B guest** the directory UPN is the `…#EXT#@<tenant>.onmicrosoft.com` form and what MSAL surfaces is the identity provider's `preferred_username` — **provider-defined for an email-OTP guest and not assertable from this tree**. If it is not the plain invited address the match yields **403 → ADR-020 state 6 "wrong account"**: the exact failure A15-4 exists to remove, arriving *after* the mail, the guest and the passcode all worked. **This seat does not guess which it is** — the decision that removes the guess is that the accept binds to a **server-verified identity**, not a client-supplied string, which is NW-05. **security-architect's `ExternalSubjectId` bind at invite time (§J.3b) is the other half and this seat adopts it as stated** — the two findings are the same hazard reached from opposite ends, and together they close it. **If NW-05 slips inside the wave, A15-4 must be walked against a real B2B guest before the wave is called done**: a passing walk with an existing member's account proves nothing about this path (OQ-w15-ca-02). **No new generated surface on the accept path** — `acceptInvitation` (`client.ts:1653`) sends no body and needs none; the provisioning outcome is NW-69's field. · **delivery-manager** (the apply, the operator act and the ordering — the permission is security-architect's and the resource cloud-architect's): **the danger here was never the permission; it is that one errored HCP run takes down the wave's other two features.** Both environment roots are a single state each and w15's one infrastructure PR carries Service Bus (NW-27) and ACS (NW-68) alongside the Graph assignment, so a missing directory right would have failed three features on one resource. **Cloud-architect's `count` gate resolves it and it is better than what this seat proposed**: my lane recommended keeping the grant out of Terraform entirely; with `var.guest_provisioning_enabled` defaulting `false` and `count = 0`, **the whole w15 apply still succeeds** and NW-67 degrades to a later one-line flip, while the assignment stays in Terraform state rather than becoming an untracked portal act. My option (ii) is **withdrawn** as the default and kept only as the named fallback (a Global Administrator assigns out of band, the resource stays at `count = 0`, the deviation recorded with an `import` path). **Second correction adopted, and it is the one that decides whether the apply succeeds**: my lane scheduled a one-time "Grant admin consent" click — for a managed identity **there is no such click; the `azuread_app_role_assignment` *is* the consent**. What genuinely needs a one-time grant is **upstream and different**: the identity that runs the HCP apply must be able to write app role assignments and cannot today. That is **ADR-015's** change, not an ADR-016 ordering line — cloud-architect asked for exactly that placement and they are right, because `identity/main.tf:41-42` records "the deploy job does not need Microsoft Graph (ADR-015)" and that sentence stops being true for the **apply** plane while staying true for the **deploy** plane. It is **operator prerequisite 2 at the ADR-014 w15 gate, verified before the first apply, never discovered from a red run.** **CI gains nothing**: no Graph permission on either service principal, no new federated credential, no new subject claim, no new secret — and **no workflow mentions Graph or `User.Invite`** (zero occurrences across all ten files, surveyed 2026-09-13) and none should. With `dev = true` / `demo = false` only `dev` can write to the directory in w15, which is the delivery half of security-architect's shared-directory ruling. **What proves it**: provisioning is proved by **one real invite on deployed `dev`**, never asserted from Terraform state, and A15-7 (consent missing → named error on the pane + audit row, no invitation claiming "sent") is a numbered acceptance step **precisely because** it is also the absent-key behaviour — the failure contract and the fallback are the same thing here, so option (ii) costs nothing A15-7 does not already buy. And client-architect's residual is scheduled rather than assumed: **if NW-05 slips inside the wave, A15-4 is walked against a real B2B guest before the wave is called done** — a walk with an existing member's account proves nothing about this path, and it is a numbered step in `docs/waves/w15-acceptance.md`, not a note. | **product-owner**: `amend ADR-001` (w15 footer, clauses 4–5) · **software-architect**: `amend ADR-026` (w15 footer §1–§3, §7) · **`none — ADR-002`** beyond the w15 footer's `Microsoft.Graph` prefix clause · **cloud-architect**: `amend ADR-005` (w15 footer §4 — the Azure delta is **one `azuread` role assignment and nothing else**, recorded so the wave's $0.00 claim is complete rather than silent) · `amend ADR-007` (`modules/identity` gains a data source, a gated resource and an output — inside the existing module, no boundary moves) · **`amend ADR-015` is delivery-manager's file, and this seat supplies the text**: the apply identity's Graph rights, the supersession of `identity/main.tf:41-42` **for the apply plane only**, and the out-of-band fallback — **the only change to a deploy identity's rights in this wave**, so it must not be folded into an ADR-016 ordering clause · **`none — ADR-011`**: no secret, confirming the intake's OQ-w15-005 assumption · **security-architect**: **`amend ADR-025` — a new §J** (§J.1 the permission and its blast radius + the `SdkAllowListTests` correction; §J.2 ordering and re-issue-by-replacement; §J.3 directory presence is not a grant, the `oid` bind, the shared-directory ruling; §J.4 the failure contract; §J.5 removal; §J.7 audit; §J.9 tests) · `amend ADR-010` (w15 §2.3–§2.4 — the `#EXT#` UPN is never parsed for an authorization decision, and the **`email` optional claim must be requested in Terraform**: verified, **no `optional_claims` block exists anywhere** in `modules/identity`) · `amend ADR-011` (w15 §1 — **NW-67 adds no Key Vault entry**, recorded as a *confirmation* so a later task does not invent one) · **`none — ADR-015`**: the Graph permission sits on the **runtime** workload identity, not a deploy service principal, so **CI gains no credential**; the separate question of the *apply* identity's directory rights is cloud-architect's text in delivery-manager's file and this seat does not contest it · **`none — ADR-026`** from this seat: the 201's **shape** is software-architect's; §J.4a constrains only its **values** · **`none — ADR-009`**: no new table and no policy change — `workspace_user.ExternalSubjectId` is merely populated **earlier**, on a table that already has its policy · **client-architect**: `amend ADR-012` (w15 footer §8 — the auto-continue rule, the state-3 preservation, and the `preferred_username` finding as input to the security seat's decisions) · **`none` on ADR-012's route and storage clauses**: nothing about the fragment, the memory-only invitation token or T15 changes — recorded so no task "hardens" a rule that is already correct · **`none — ADR-010 / ADR-025`**: security-architect's files; this seat supplies the client finding above as **input** to theirs and does not write them · **delivery-manager**: **`amend ADR-015`** — **its first amendment since 2026-09-01**, written to cloud-architect's text: clause 1 the runtime workload identity gains a *directory* permission (the body's `:82-83` sentence extended for the first time), clause 2 **the apply identity's Graph rights and the supersession of `identity/main.tf:41-42` for the apply plane only**, clause 3 the assignment **is** the consent, clause 4 the `count` gate so a missing right degrades NW-67 instead of blocking Service Bus and mail, clause 5 **CI gains nothing** — no permission on either service principal, no federated credential, no subject claim, no secret · **`amend ADR-016`** (w15 clause 14's per-environment flag table and clause 19 steps 2 and 8 — the `dev`-only directory write and the `demo` re-walk) · **`none — ADR-025 / ADR-010 / ADR-026`**: the permission's blast radius, the `oid` bind and the 201's shape are security-architect's and software-architect's, adopted as ruled · **`none — ADR-005 / ADR-007`**: the resource is cloud-architect's, and their `$0.00` claim is confirmed from this seat — no runner, no workflow, no environment and no GitHub secret is added | `reports/architecture/ADR-001-scope-r0-r4.md` (w15 footer) · `reports/architecture/ADR-026-workspace-api-and-invitation-schema.md` (w15 footer) · **`reports/architecture/ADR-005-azure-skus.md` (w15 footer §4)** · **`reports/architecture/ADR-007-terraform-layout.md` (w15 footer §2)** · **`reports/architecture/ADR-025-workspace-membership-and-invitations.md` (w15 footer — new §J)** · **`reports/architecture/ADR-010-entra-oidc.md` (w15 footer §2)** · **`reports/architecture/ADR-011-secrets-and-rag.md` (w15 footer §1)** · **`reports/architecture/ADR-012-web-stack.md` (w15 footer §8)** · **`reports/architecture/ADR-015-ci-azure-auth.md` (w15 footer — its FIRST amendment)** · `reports/architecture/ADR-016-promotion-dev-demo.md` (w15 footer clauses 14, 19) |
| NW-68 — Raffa sends the invitation email (ACS Email) | cloud-architect, delivery-manager, software-architect, security-architect, ux-ui-designer, product-owner | **product-owner**: IN — **the w14 clause-2 deferral ends; scope never changed** (answers OQ-w15-002, and the pilot doc refutes the objection in its own words: `:142` vs `:152`/`:153`). Conditions: **exactly one mail type** (the invitation — no notification framework); the **pilot script is unchanged and gains no inbox step**; **`mailDelivered` is a server fact**, never a default `true`, and `false` stays fully functional; **a site-relative accept link means NW-68 does not ship** — the one item to cut rather than narrow. Priority note on the record: §16.1's `P1` ("Email **alerts**") is not this item; NW-68 inherits §20 Day-1's weight. · **software-architect** (code side only — the ACS design is pre-decided in the ADR-005 w14 footer and **not re-opened**): `AcsInvitationMailer` replaces `NullInvitationMailer` at **one line** (`ServiceCollectionExtensions.cs:61`), but `TryAdd` means **first registration wins**, so it must be registered *before* `AddIdentityWorkspaceModule`; config binds in the host's `GetSection(...).Bind(...)` shape, **never `IOptions<T>`** (absent from `backend/src`). **`acceptUrl` becomes absolute** when `Invitations__AcceptUrlBase` is set — closing the ADR-026 §D5 gap the ADR-005 second w14 footer raised **for this seat** — fragment form unchanged (ADR-025 C9). Per ADR-026 §D6 the binding **must not `?? throw`**, so "mail enabled, no accept-url base" is handled by **`TrySendAsync` returning `false`**, not a startup crash: the pane shows the designed "could not be sent" state with the copyable link. `mailDelivered` is **unchanged** — literally `TrySendAsync`'s return, already `required` in the contract; **no wire change**. **Finding: the server cannot re-send the original link** (token stored only as a SHA-256 hash), so "Try sending again" is necessarily a **re-issue**; I propose invite-by-replacement in one transaction and **defer to ADR-025's owner**. **Round 2 — the ruling came back and it is the proposal: ADR-025 §J.2b orders revoke + issue in ONE transaction, so the client-composed `DELETE`+`POST` fallback is WITHDRAWN** (two requests with a window in which the address holds no invitation at all, and if the second fails the Admin has destroyed a working link and created nothing). Contract consequence, which is this seat's: **`POST …/invites` becomes replace-on-live-invitation — no new endpoint, no new verb, no schema change**; `InviteAsync`'s `alreadyInvited` pre-check (`WorkspaceMembershipService.cs:142-151`) stops returning `Conflict` and instead revokes and re-issues inside the same `SaveChangesAsync` with two existing audit verbs. **Two things must not move with it and both are easy to lose in that edit**: the `alreadyMember` branch (`:136-140`) keeps its `Conflict` — a live **membership** is not a re-issue case and replacement must never re-grant a role someone already holds — and the `IsUniqueViolation` catch (`:174-182`) **stays**, because it is the backstop for two concurrent invites and still owes that racer the clean 409 the pre-check no longer produces. This is what finally makes ADR-020's **"Send a new invitation"** legal against D4's partial unique index, which is itself **unchanged** — replacement satisfies it by revoking first, so no predicate had to be widened (and none could be: "expired" is a clock comparison and a partial-index predicate must be immutable). **And the field client-architect and ux-ui-designer both routed here is named: `deliveryOutcome`, a non-nullable string with an `enum` of exactly `"sent" | "mail_failed" | "no_transport"`** — three values because a provisioning failure is a **502**, not a fourth success state, which is what makes *a link renders only when it is usable* true by construction rather than by a branch; non-nullable is forced, not preferred (the generator checks `enum` before the nullable union, so a nullable enum loses its `null`); `snake_case` to match §3's 502 `reason` set, one vocabulary per endpoint. **`mailDelivered` is KEPT** — `required` in the shipped contract and three seats ruled on its meaning this wave — under a **normative biconditional: `deliveryOutcome == "sent"` iff `mailDelivered == true`**, both computed at one place, so the pane branches on **one string** and never combines two booleans, which is what client-architect's rule actually forbids; a vitest case over the three outcomes is the only thing that keeps `mail_failed`-with-`mailDelivered: true` from being expressible. `identityProvisioned` is unchanged (ADR-026 w15 round-2 footer §8–§9). · **cloud-architect** (this seat owns the transport; the ADR-005 w14 footer's design is **adopted unchanged and not re-opened** — w15 **applies** it): new module **`infra/modules/communication`**, four resources per environment **never shared** — `azurerm_communication_service` `acs-raffa-<env>`, `azurerm_email_communication_service` `acsemail-raffa-<env>`, an **Azure Managed Domain** and the association. **All four are SKU-less and metered per message with no idle charge**, so no third fixed monthly line joins Service Bus Standard and ACR Basic. The ACS resource is a **global type** (no `location`) with `data_location = "Europe"` — **not a second region**: ADR-006's w14 footer `:69-83` ruled this case in advance, so **ADR-006 needs no footer**. Path: `acs-connection` in Key Vault (with the **same `depends_on` RBAC pair** as `postgres-connection`/`storage-connection` — that is what stops the first apply racing RBAC propagation on an `rbac_authorization_enabled` vault) → handle **`acs-cs`** → env var, **API app only** (`:198-200`). **No new role assignment** — the workload identity already holds `Key Vault Secrets User` on its own vault; **ADR-011 gains a secret, not a permission**. Keys: `Invitations__Mail__Enabled` (`dev` `"true"` / `demo` `"false"`), `__SenderAddress` (**from the module output, never hand-composed**), `__ConnectionString` (`secret_name = "acs-cs"`), `Invitations__AcceptUrlBase` = `"https://${var.spa_host_name}"` **with no trailing slash** — a **reuse of an existing wire** (`var.spa_host_name` already reaches the module and is consumed only by the CORS block), which is what makes software-architect's absolute `acceptUrl` free on this side; the `#` fragment is **not a style choice** (ADR-025 C9 + the ADR-005 second w14 footer: SWA rewrites every non-asset path to `/index.html` and **logs the accept path**, so a query-string token would land a live single-use token in platform logs on infrastructure this seat owns). All four bind **optionally**, so the ordering stays soft. **`Invitations__Mail__Enabled` is a product switch, not a provisioning gate** — unlike `ai_gateway_wired`, publishing a *working* connection string behind `Enabled = false` is harmless, so resources, secret and all four keys land in **one** apply per environment: **no two-phase apply for mail**, which would buy a second operator-confirmed HCP gate for nothing. **Ruling on security-architect's PROPOSE (S15-26): the connection string stands for w15; the `TokenCredential` form is recorded as the preferred later migration** — they are right that it removes the only secret w15 adds and they called either form acceptable, but the Key Vault path is proven twice in this module, satisfies the lock verbatim, and swapping mid-wave adds a role assignment on an unprobed resource type **plus** an SDK auth path with no coverage against a wave already carrying three applies' worth of Terraform; when it lands it is an **ADR-011 amendment and one role assignment**, not a redesign. **Deliverability caveat stands** (ADR-005 `:217-219`): a managed domain sends from `…azurecomm.net`, which corporate filters treat harshly — **which is exactly why NW-69's `false` branch must stay reachable and why A15-6 exists**. · **security-architect** (the secret, the link, the logs — the ACS transport design is cloud-architect's and is **not re-opened**): **`Invitations__AcceptUrlBase` is a security control, not a convenience.** It is `https://` + this environment's own SPA host, supplied by Terraform, and **never** derived from `Host`, `Origin`, `X-Forwarded-Host` or any other request value — a host-header-injected accept base means Raffa **mails a live invitation token to an attacker-controlled origin** and the invitee hands it over by clicking a Raffa-branded mail. That is the single reason the base cannot be "computed". **Fail closed at startup**: mail enabled + base absent / non-`https` / not a well-formed absolute URI → **the API does not start**; it never falls back to the site-relative constant and mails a fragment with no origin, and there is no third state where a mailer is registered but unconfigured. **Reconciled with software-architect's ADR-026 §D6 rule that the binding must not `?? throw`** — compatible: the refusal is a **validated startup check on the composed pair**, not an unbound-key crash, and "enabled with no usable base" is the one combination that must never run. **ADR-025 Rule C9's fragment survives the transport and is now load-bearing for a second reason**: a fragment is never transmitted to a server, which is exactly what makes this link safe in an **email**, where it passes through mail gateways, archivers and click-protection services that fetch and rewrite URLs server-side — a query-string token would be handed to every one of them, and cloud-architect supplies the platform half (SWA rewrites every non-asset path to `/index.html` and **logs the accept path**). The absolute base changes the **origin and nothing else**; **C10 / C10a / C10b / T15 are unchanged**, since C10 governs client-side persistence, not channel count. **One recipient** — no CC, no BCC, no distribution list, and no `Reply-To` at a Raffa-operated mailbox that could receive a forwarded token. **`mailDelivered: true` means *accepted for delivery*, not *delivered*** — it stays `TrySendAsync`'s return, never a receipt, never inferred, never optimistically `true`; and cloud-architect's deliverability caveat (a managed domain sends from `…azurecomm.net`, which corporate filters treat harshly) makes the **`false` branch likely rather than rare**, which is why A15-6 exists and why that branch must stay fully functional. **Answering cloud-architect's ask that no recipient address reach a log *or* an audit row: agreed for the log, refused for the audit row, and the line between them is tenant scoping.** The audit table is tenant-scoped and RLS-protected and ADR-011's w14 clause 4 already admits the invited email as a membership fact inside that tenant — an audit trail that cannot name who was invited is not an audit trail. The **application log is a cross-tenant sink**, so it carries the invitation id, the ACS operation id and a named outcome, **not the address**; `NullInvitationMailer.cs:25-29` already models the discipline exactly, with `acceptUrl` in the signature and deliberately never interpolated. Never anywhere: the rendered body, the accept URL, the token or its hash, a raw ACS error payload. **On the credential: this seat's `TokenCredential` proposal was a `PROPOSE`, never an `OBJECT`, and cloud-architect's ruling is accepted** — the connection-string form stands for w15, `acs-connection` is the **one** new Key Vault entry in the wave, reached by the existing `Key Vault Secrets User` grant, and the managed-identity migration is recorded as an ADR-011 amendment plus one role assignment when it lands. **One shipped comment becomes false**: `NullInvitationMailer.cs:28-29` asserts "the 201 response body is the link's **only** channel" — the NW-68 task retires it in the same edit, the same staleness class as OQ-w15-003's. **And the re-issue question software-architect deferred to this seat is ruled in §J.2b**: re-issue **by replacement** — revoke the live invitation and create the new one **in one transaction**, so two live tokens for one address never coexist (a client-composed `DELETE`+`POST` is two requests with a window between them); both halves are already-existing audit verbs, and "the link you already shared stops working" is a consequence ADR-020's w14 copy already states. · **ux-ui-designer**: **the pane's delivery copy is confirmed unchanged and re-opened by nothing** — 10.1 pre-decided the whole set "so it is not re-opened" (`ADR-020:280-287`) — and **the absolute accept link needs no copy change at all**, because `composeAcceptLink` (`memberViewModel.ts:175-179`) already takes a site-relative **or** an absolute value unchanged, by design; recorded so **no task is written for it**. **What has no owner is the mail itself, and that is exactly how a task invents one.** There is **no email in any export**: `inputs/design/` holds screens, `markup.html`, `styles.css` and `app.jsx` — nothing that leaves the browser — and **none of ADR-019's mechanism survives an email client**: no external stylesheet, no CSS custom property, and `--font-heading/--font-body` is **Archivo**, which no mail client will load. Left to the implementing task, the first message Raffa.ai ever sends to a person **outside the tenant** would be branded HTML nobody reviewed. **Decision: the invitation email is *surface 12* of ADR-020** — **plain text is the body of record** (an HTML part may be added but must carry the same words in the same order and read correctly when stripped); one column, left-aligned, **no image, no logo file, no web font, no external stylesheet**; any colour an **inline literal of an ADR-019 token value** (`#201e1d`, `#ec3013`, `#ae1800`), never a variable reference and never a new value; and the accept link rendered as a **visible absolute URL** as well as an anchor, so a forwarded or text-only copy still works. Its two middle lines are **screen 11 state 2 verbatim** (`ADR-020:397`) — the mail and the landing page must say the same thing or the invitee believes they clicked the wrong link. Three rules, each closing a real failure: it carries **the workspace name, the offered role and the expiry and nothing else** (the leak guard of `:422-425`, and stronger, since a mail can be forwarded by anyone forever — the copy half of security-architect's one-recipient rule); the **one-time-code sentence is not decoration** but the thing that turns Microsoft's unexpected second message into an expected step; and **it never claims an account was created**, which is both alarming and, from the invitee's side, untrue. `NullInvitationMailer.cs:28-29`'s comment — "the 201 response body is the link's **only** channel" — **becomes false this wave and is retired in the same edit**; security-architect found the same staleness independently from the log side. · **delivery-manager** (the apply ordering and the promotion — the transport is cloud-architect's, the mailer software-architect's, the copy ux-ui-designer's, and none is re-opened): **this is the exact case ADR-016's w14 clause 2 was written to pre-empt** — `backend.yml` deploys `--image` only, so `Invitations__Mail__*` and `Invitations__AcceptUrlBase` can arrive **only** through Terraform plus an operator-confirmed HCP apply. Three decisions from this seat. **(1) `Invitations__AcceptUrlBase` composes inside `modules/containerapps` from `var.spa_host_name`**, which already reaches that module and feeds only the CORS block — no new root variable, no value a human types, and **structurally impossible to point `demo` at `dev`'s host**, since each root already wires its own `module.staticwebapp.default_host_name`. Hardcoding a hostname in either root is the cross-environment leak the body's `:79-80` exists to prevent, and here it mails a `dev` link into a `demo` invitation — an email you cannot recall. Cloud-architect implemented exactly this, trailing slash and all, and security-architect's "never derived from `Host` / `Origin` / `X-Forwarded-Host`" is the same control from the other side. **(2) No secret is ever promoted**: `acs-connection` follows the existing Key Vault → handle pattern and **each environment's secret is created by its own apply** (`demo-promote.yml:43-50` carries code and artifacts only) — recorded so nobody proposes copying it. **(3) `mailDelivered` stays a server fact, and the delivery reason is that it is what makes the promotion checkable**: if the pane could infer "sent", a `demo` whose flag is still `false` would *claim* delivery and step 5 of the promotion would have nothing to assert against. **Cloud-architect's one-apply ruling is adopted, and it does not weaken the `demo` gate — the two are different objects.** The resources, the secret and all four keys land in **one** apply per environment (no two-phase apply for mail, which would buy a second operator-confirmed gate for nothing); what stays `false` on the `demo` root is the **product switch** `Invitations__Mail__Enabled`, flipped by the one-line PR **before** the tag — the same way `ai_gateway_wired` is flipped with the promotion that carries the code. That flip is clause 19 step 5 and **it is the step people will skip**, because everything else in the promotion is a workflow and this one is a PR. **Email is the one w15 deliverable that leaves the building**, so it gets a walk of its own on `demo` with a **second** external address (clause 19 step 8): a `dev` pass proves nothing about `demo`'s sender domain, its flag or its accept-URL base. And because a managed domain sends from `…azurecomm.net`, which corporate filters treat harshly, **A15-6 is walked rather than assumed** — the `false` branch is likely, not rare, and it must stay fully functional. **Workflow decisions: none.** No workflow mentions `Invitations__` or ACS today (zero occurrences across all ten files) and none needs to; `--set-env-vars` stays drift, and the next apply would revert it anyway. | **product-owner**: `amend ADR-001` (w15 footer, clause 6) · **`none` for ADR-020** — delivery + failure copy pre-decided in its w14 footer, not reopened · **software-architect**: `amend ADR-026` (w15 footer §4–§6) · **`none — ADR-005`**: cloud-architect's; the transport design is adopted unchanged · **cloud-architect**: **`amend ADR-005`** (w15 footer §3 — the four ACS rows move from *decided* to **applied**: resource and SKU-less confirmation, the secret and handle names, the four keys with their per-env flag values, the **one-apply** ruling, and the `TokenCredential` deferral) · **`amend ADR-007`** (`infra/modules/communication/` joins the layout — and the block is corrected from **nine to twelve**, since `staticwebapp/` and `foundry/` landed without a footer; `infra/README.md:18-29` is a **second layout tree** that gains the same directory in the same task) · **`none — ADR-006`**: its w14 footer `:82` already ruled `data_location = "Europe"`; w15 applies it as written and re-opens nothing · **security-architect**: `amend ADR-011` (w15 §1 — the secret ledger for the whole wave stated exhaustively: **`acs-connection` is the one new entry, and no new permission**; §4 — two new audit verbs and the **log/audit split**) · `amend ADR-025` (§J.6 the accept link's integrity, §J.7 audit; the invitation lifecycle owns the link even when the transport does not) · **`none — ADR-005`**: cloud-architect's, and their transport design is adopted unchanged — this seat rules on the secret, the link and the sink, not the resource · **`none — ADR-026`**: the absolute `acceptUrl` is software-architect's shape, with §J.6a binding its **source** · **`none — ADR-020`**: the delivery and failure copy is pre-decided in its w14 footer and is ux-ui-designer's · **ux-ui-designer**: `amend ADR-020` (**§4 — surface 12, the invitation email**: its form, its copy and its three rules; and **§0**, the wave-wide rebrand rule, since the subject line is one of the strings it governs) · **`none` for the pane's delivery copy** — pre-decided in the w14 footer 10.1, **confirmed verbatim here and not re-opened** · **`none — ADR-019`**: the email consumes token **values** inline precisely *because* it cannot consume the system; no token, no component, no semantic row · **`none — ADR-018`**: a mail is neither a route nor an IA surface · **delivery-manager**: **`amend ADR-016`** (w15 footer — clause 13 the first per-environment API key, which is this item; clause 14 the `demo` flag table and the one infrastructure PR; clause 16 `Invitations__AcceptUrlBase` composed from `var.spa_host_name` plus the **three** named shortcuts that stay drift; clause 19 steps 5, 6 and 8 — the flip PR before the tag, `promote-backend` only after that apply is `CURRENT`, and the `demo` invitation walk with a second external address) · **`none — ADR-005 / ADR-006 / ADR-007`**: the transport, the region ruling and the module layout are cloud-architect's and are adopted unchanged · **`none — ADR-011 / ADR-026 / ADR-020`**: the secret, the absolute `acceptUrl` and the copy are other seats'; this seat rules only on which apply carries them and on which side of the promotion each lands | `reports/architecture/ADR-001-scope-r0-r4.md` (w15 footer) · `reports/architecture/ADR-026-workspace-api-and-invitation-schema.md` (w15 footer §4–§6) · **`reports/architecture/ADR-005-azure-skus.md` (w15 footer §3, §5)** · **`reports/architecture/ADR-007-terraform-layout.md` (w15 footer §1)** · **`reports/architecture/ADR-011-secrets-and-rag.md` (w15 footer §1, §4)** · **`reports/architecture/ADR-025-workspace-membership-and-invitations.md` (w15 footer §J.6–§J.7)** · **`reports/architecture/ADR-020-web-screen-inventory.md` (w15 footer §4 — surface 12)** · **`reports/architecture/ADR-016-promotion-dev-demo.md` (w15 footer clauses 13, 14, 16, 19)** |
| NW-69 — invite pane: honest delivery and identity state | ux-ui-designer, client-architect | **client-architect**: **the code names this wave as its owner.** `memberViewModel.ts:166-173` is a discriminated union on the server boolean `mailDelivered`, and its own doc comment reads verbatim — *"not a third, invented state; **the transport wave that needs a third value owns that change**, not this one"*; ADR-020's w14 footer `:283` settled the same point from the copy side ("a third value, not a second boolean"). Decision: **the discriminant becomes the server's outcome string on the invite 201** — not a second boolean, not two booleans the client combines, and **never a client inference from a status code**, which `members/index.tsx:118-120` already records the reason for in its own comment (*"this fallback used to claim a mail had gone out on this same failure path"*). Four outcomes become expressible: sent · mail failed · guest provisioning failed · created-with-no-transport-configured. **The link renders only when it is *usable*** — the `acceptUrl`/`expiresAt` arm is keyed to "the link is the way in", **not** to "mail did not go out", because A15-7's whole point is never a "ready" link that cannot be used: **mail failed → link shown** (it is the only way in) with ADR-020's pre-decided copy; **guest provisioning failed → link suppressed**, named error only, since a link to a sign-in the invitee cannot complete is the defect rather than the remedy; sent → no link. **This is the same split security-architect ruled from the security side** (their ask to ux-ui-designer) and this seat concurs exactly — arrived at independently, from A15-7 rather than from the credential. Generated client: the outcome is a **non-nullable `string` with `enum`** — unlike `role`, which ADR-026's w14 clause 3 keeps a bare string because role names are per-tenant rows, this is a closed server-side vocabulary, so a literal union is correct and is what makes the pane's branch exhaustive under `tsc --noEmit`; **non-nullable is not a preference** — a nullable `enum` silently loses its `null` in this generator (ADR-012 w15 §9). Field name and values are software-architect's (ADR-026). **NW-68's absolute accept link costs this seat zero**: `composeAcceptLink` (`:175-179`) is `new URL(acceptUrl, origin).toString()` and its own comment already states it accepts a site-relative **and** an absolute value unchanged — **recorded so no task is written for it**. The four outcomes are **vitest** cases (`web.yml` runs `npm test` today, so they are a real gate this wave); the browser case is N3b, which is NW-58r and not this seat's. **Reconcile with ux-ui-designer**: if their outcome vocabulary differs from the wire values, the mapping lives in `memberViewModel.ts` and is theirs to name — the pane still renders **one server fact**. · **Round 2 — two corrections adopted and one mechanism they need (ADR-012 w15 §13.1–§13.3).** **(a) Four outcomes become three.** ux-ui-designer is right and their mechanism is strictly stronger than this seat's: a provisioning failure **aborts the invitation** (no row, no mail, no token, 502), so no 201 can carry "guest provisioning failed" and the vocabulary is **sent · mail failed · created-with-no-transport-configured**. The rule *the link renders only when it is usable* is unchanged and now holds **by construction**; the task writes **three** arms and no fourth — a dead branch keyed to the link is exactly the one a later reader re-enables. **(b) The 502's `reason` cannot reach the pane today, and the shortest repair is the forbidden one.** `InviteWorkspaceMemberResult` (`client.ts:160-170`) carries only `error: string | null`, its own comment calling it a *"plain-language failure reason"*, and `members/index.tsx:121` pushes that prose straight into the pre-creation error slot (`InvitePane.tsx:110-114`) — so ux-ui-designer's three reason strings and their catch-all row would be selected by **string-matching server prose**, a client inference of a server fact that breaks on any copy edit. The envelope gains a typed **`failureReason`** anchored to `…["responses"][502]["content"]["application/json"]`, exactly as `InvitedMemberBody` is anchored to `[201]` (`:150-151`); `error` stays the fallback for 400/401/403/409. **This binds the contract**: `renderOperation` renders **every** status key under `responses` (`generate-api-client.mjs:135-143`), so a **declared** 502 is typed for free and an **undeclared** one forces the hand-written DTO `:53` prohibits — software-architect's contract task must declare it. **And the catch-all row is mandatory even though `tsc` will call it dead code**: a literal union describes the contract, not the wire. **(c) `identityProvisioned` is a non-nullable boolean** — with the abort rule its only two states are *provisioned* and *not configured*, so `false` means **not configured, never failed**, and the pane must not invent a warning from it; a nullable field would lose its `null` in this generator (§9). · **ux-ui-designer**: **the state set is corrected against what the backend seats actually promoted, and the correction retires this seat's own lane design.** My lane §D2 designed a fourth outcome — "Invitation created, but {email} cannot sign in yet", link suppressed — and client-architect's cell above lists the same as one of four 201 outcomes. **Neither can occur**: software-architect's NW-67 row rules that a provisioning failure **aborts the invitation — no invitation row, no mail, no token** — and returns **502**, and security-architect concurs from the credential side ("an inert artefact beats a live credential"). There is therefore nothing to show a link *for*. **My reasoning reached "suppress the link"; their mechanism reached "there is no link", which is strictly stronger, and this seat adopts it** — the rule *a link renders only when it is usable* now holds **by construction** rather than by a branch a task could get wrong. So the 201 carries **three** outcomes and **10.1 gains one value, not two**: "Invitation sent to {email}." (unchanged) · "Invitation created, but the email could not be sent." (**10.1's pre-decided third value, verbatim**, + the link block) · "Invitation ready for {email}." (unchanged, + the link block). **The pre-decided "Try sending again" affordance does not ship**, and the ground is this ADR's own precedent: software-architect establishes that the server **cannot re-send the original link** (the token is stored only as a SHA-256 hash), so any retry is necessarily a **re-issue** that mints a new token and **kills the link the Admin is looking at** — and `ADR-020:284-287` already forbids telling the Admin to invite again from this pane. **The link block is the remedy**, and cloud-architect's deliverability caveat (a `…azurecomm.net` sender that corporate filters treat harshly) makes that outcome **common rather than rare**, which is exactly why it must stay fully functional; the only re-issue path remains the roster row's **"Send a new invitation"** with its confirm and its "The link you already shared stops working." line. **This closes my lane §8.2 rather than re-opening it** — an affordance with no legal mechanism is not shipped, which is the same ruling this ADR's w14 §2 made for the other button. Copy is supplied for software-architect's **closed 502 reason set** (`consent_missing` · `provisioning_failed` · `directory_unavailable`) in the existing pre-creation error slot (`InvitePane.tsx:110-114`), each with the shared `.micro-meta` **"No invitation was created."** so the Admin never has to wonder whether a half-invitation exists — **plus a fourth, catch-all row, so an unrecognised or proxy-mangled value can never put a raw wire enum on screen**. `consent_missing` names "a tenant administrator", not "you", because a Raffa.ai workspace Admin is usually **not** the Entra tenant admin, and telling them to fix a permission they do not hold is worse than telling them nothing. **No new affordance** — the invite form is itself the retry. **One sentence about identity, on three surfaces** — *"They will get a one-time code from Microsoft the first time they sign in."* — keyed to `identityProvisioned === true`; it satisfies security-architect's **no-directory-enumeration-oracle** rule because it says the identical thing whether the guest was created or already existed, and it renders **nothing at all** when provisioning is not configured, leaking no configuration fact. **There is deliberately no pre-send identity state**: with NW-67 the product creates the account, so a warning that the invitee "has no account yet" would be false the instant it rendered. **No fifth member status** — the roster answers "who can get in"; a delivery outcome is a *pane* fact about the last action, not a property of the row. **Vocabulary mapping, which client-architect asked be named: it does not differ** — three outcomes, one per row, plus one boolean; the pane branches on **one server string**, renders **one server boolean**, and infers neither. | **client-architect**: `amend ADR-012` (w15 footer §7, **corrected in round 2 by §13.1–§13.3** — three outcomes not four, the typed `failureReason` off a **declared** 502, and `identityProvisioned` as a non-nullable boolean) · **`none — ADR-020` from this seat**: the states and the copy are ux-ui-designer's and the delivery-failure copy is **pre-decided** in their w14 footer — not re-opened · **`none — ADR-026`**: the field's name and values are software-architect's; this seat constrains only its **nullability and `enum`-ness**, which is a generator fact (§9) · **ux-ui-designer**: `amend ADR-020` (**§3 — screen 10**: the corrected three-outcome set, the withdrawn fourth state, the **non-shipping** "Try sending again" affordance with its precedent, the 502 reason copy with its catch-all row, the identity `.micro-meta` on three surfaces, and the confirmation that no member status is added) · **`none — ADR-019`**: `.btn-secondary`, `.micro-meta`, `.input` and paragraphs are all catalogued and no status treatment the semantic map lacks is needed, so **NW-69 adds no row** · **`none — ADR-018`**: no route and no IA state — the pane is one screen's affordance · **`none — ADR-026`**: the outcome field's name, values and nullability are software-architect's and client-architect's; this seat names only what each one renders | **`reports/architecture/ADR-012-web-stack.md` (w15 footer §7)** · **`reports/architecture/ADR-020-web-screen-inventory.md` (w15 footer §3)** |
| NW-58r — invitation e2e (N3b) runs because the flow creates the second account | delivery-manager, security-architect | **security-architect**: **no test-only authentication seam ships, on any environment, in any wave.** A code path that mints, reads or short-circuits an authentication credential, gated only by configuration, is present in the shipped image **everywhere**; three facts make it worse here rather than better. (1) `dev` and `demo` **share one Entra directory** (`identity/main.tf:63`, `:118`), so a seam "only on `dev`" is a seam against the directory `demo` trusts. (2) There is **no Playwright runner at all** — `web.yml` runs vitest only, and wiring one is **NW-50, W18** — so the seam would ship with **nothing executing it**: a bypass with no compensating check, which is the worst possible trade. (3) **NW-05 lands in this same wave**, so this would add an impersonation path to the wave that finally closes impersonation, and a reviewer should refuse it on sight. **Confirms OQ-w15-006's assumption in force**: NW-58r introduces no passcode seam; the spec's skip reason is rewritten to name the **passcode** (the second account stops being the blocker, because NW-67 creates it) and **N3b is walked by hand in the acceptance runbook** — the intake's pre-authorised narrowing #1, so it costs the wave nothing. **The acceptable future shape, recorded so W18 does not invent another**: a **`dev` mail-catcher** is test infrastructure rather than a product code path, and if NW-50 builds it — `dev` only and never provisioned in `demo`; only addresses on a dedicated test alias/domain, never a real person's mailbox; not reachable from the API and holding no Raffa credential; its contents treated as secrets in CI, with no artifact upload of a mailbox dump. **And if e2e directory credentials ever exist they are bounded (binds NW-50 in advance, so it is not re-argued)**: GitHub **environment** secrets scoped to `dev`, never repository-wide, never a workflow literal; a dedicated test principal whose only workspace grant is the test tenant, never an Admin of a real pilot workspace; and because **Playwright traces, videos and screenshots capture typed input**, the sign-in step runs with tracing off or the field masked and no trace artifact from a signed-in run is uploaded — **a leaked trace is a leaked directory password**. **S-T23 is the deliverable of this ruling**: a negative test asserting no configuration key, environment variable or code path skips, mints or reads an authentication credential for test purposes. It is what stops W18 adding one quietly, and it cannot be reconstructed later from the code. The "where it runs" half is delivery-manager's and this seat concurs with a manual runbook walk this wave; this seat objects only to a **product** seam. · **delivery-manager** (the "where it runs" half): **three refusals, three separate reasons, and only one of them is security's.** (1) **A Playwright runner is NW-50's job and NW-50 is W18.** ADR-016's w14 clause 10 already ruled the reasoning and the premise is unchanged — a grep of all ten workflow files for `playwright|e2e|test:e2e|spec.ts` returns **zero** hits, `web.yml:79-81` is `npm test` (vitest) only. A wave does not acquire a new CI capability as a side effect of a test it wanted. (2) **A test-only passcode seam** is security-architect's refusal and this seat concurs, adding the delivery half: it would ship with **no consumer**, because there is no runner to call it — a bypass with no compensating check. (3) **A `dev` mail-catcher is a fourth apply**: a new resource, a new module, and a fourth thing that can error the single HCP run already carrying Service Bus, ACS and Graph (ADR-015 w15 clause 4), for a test that still could not be executed. In the tightest budget this process has cut, that is the clearest "no" in the wave. **So NW-58r reduces to the intake's pre-authorised narrowing #1 and costs the wave one small task**: rewrite `web/e2e/invite.spec.ts`'s skip reason to name the **one-time passcode** — its current `SECOND_ACCOUNT_READY` premise is **removed by NW-67**, so leaving it would make the file state something false — and make **N3b a numbered step in `docs/waves/w15-acceptance.md`**, walked by hand against the operator's external mailbox. ADR-016's w14 clause 9 already fixes why that is not a dodge: *a task that writes only a spec file has not delivered the check*, and browser-expressed assertions land in the acceptance doc. **Two ordering facts the decomposer needs**: the spec file **exists only on `origin/main`**, so this task is unstartable until W15-01 lands (it would otherwise create a new file with no history and no four tests), and it comes **after NW-67**, whose landing is what makes the new skip reason true. **security-architect's S-T23 is adopted and it is the only part of this item that runs in CI** — a negative test asserting no key, env var or code path mints, reads or skips an authentication credential for test purposes. That is what makes the ruling durable instead of advisory, and it is why this seat is content to close the item on a runbook step. | **security-architect**: `amend ADR-025` (§J.8 — the ruling, the mail-catcher shape and the credential bounds; §J.9 — S-T23, and T14/T15 activation) · **`none — ADR-016`**: what proves a browser-expressed test and where it runs is delivery-manager's file · **delivery-manager**: **`amend ADR-016`** (w15 footer clause 20 — no Playwright runner and no `dev` mail-catcher in w15, w14 clause 10 reaffirmed with its premise re-verified, N3b as a numbered acceptance step, and the skip reason rewritten to name the passcode; clause 22 puts the acceptance doc and its known-gaps table in the final-integration task) · **`none — ADR-025 §H / §J.8–§J.9`**: the seam refusal, the mail-catcher bounds, the credential bounds and **S-T23** are security-architect's file and are adopted as written — this seat proposed the wording and does not write it · **`none — ADR-014`**: the spec file is not CI YAML, so w15's zero-workflow-file assertion is untouched | **`reports/architecture/ADR-025-workspace-membership-and-invitations.md` (w15 footer §J.8–§J.9)** · **`reports/architecture/ADR-016-promotion-dev-demo.md` (w15 footer clauses 20, 22)** |
| NW-05 — API JWT (ADR-010) replaces spoofable headers | security-architect, software-architect, client-architect, cloud-architect, delivery-manager | **software-architect** (the software half of security-architect's amendment; **this seat co-signs, it does not write ADR-010**): one seam, one middleware, one `Program.cs` line. `ICallerIdentity` is already the only reader of `X-User-Id` (8 consumers, all mapping absence to 401) and `Program.cs:53-59` already names it as the edit point; the tenant half converges on the same seam as a request-scoped `ICallerContext`. **Middleware, not a per-route filter** — `Program.cs` is contended by three items this wave and there is currently **no `app.Use*` call at all** between `builder.Build()` (`:230`) and the first map (`:232`), so nothing is reordered. It **resolves and never rejects**; rejection happens at the point of use, because the public routes are real (`/health`, `GET /api/invites`, `POST /api/invites/accept`, `POST /api/workspaces` bootstrap, and `GET /api/workspaces` which must keep taking no tenant input — ADR-026 §D1). **Contradiction with the intake I record deliberately: `X-Tenant-Id` is *demoted, not deleted*.** A caller may belong to several workspaces, so the token subject alone names no tenant, and only **6 of 36** routes carry `{tenantId}`; it becomes an **authorized selector** verified against live membership on every request, **404** on failure (ADR-025's rejection contract, never 403 — which would be a tenant-existence oracle). **Sizing correction for the decomposer: the sweep is 19 parse sites across 12 files and 72 `X-Tenant-Id` occurrences across 15 files — not "~12 blocks"; one task, or two identity regimes run live at once.** Two breakages named: `reprocess-tenant-documents.yml` authenticates with `X-User-Id` + role headers (`:203-206`, `:254-257`) and returns 401 after NW-05; and **web must deploy before backend**, or `dev` locks every user out. · **cloud-architect**: **NW-05's Azure delta is four non-secret env vars on the API app and nothing else — no resource, no registration, no scope, no SKU, no meter, $0.00.** The finding: **the Terraform half is already written and entirely unconsumed.** `modules/identity` already outputs `api_client_id` (`outputs.tf:12-15`), `issuer` (`:25-28`) and `api_identifier_uri` (`:17-23`), and **each has zero consumers in the repo**; `modules/containerapps` declares no authority, audience, tenant or client-id input and publishes no `AzureAd__*`. The registration is already shaped for it — `requested_access_token_version = 2` (`main.tf:74`). So NW-05's infrastructure cost is **wiring, not building**: `AzureAd__Authority` ← `module.identity.issuer`, `AzureAd__TenantId` ← a **new** `tenant_id` output, `AzureAd__ClientId` ← `api_client_id`, `AzureAd__Audience` ← `api_identifier_uri`. **`ClientId` and `Audience` are both published on purpose**: at token version 2 the `aud` claim is the **client id** while the SPA requests scopes against the **identifier URI** — publishing both removes an entire class of "token acquired, then 401". Values, sources and the per-env split are this seat's; the binding **prefix** (`AzureAd__` vs `Authentication__`) is software-architect's to confirm and nothing here changes under either. **Worker: none of the four** — it has no ingress and validates no token; not "for symmetry". **This seat does not propose an `Authentication__Enabled` kill-switch**, and that is the safer answer: a key that turns authentication off is a security control in the hands of whoever can run an apply, and flipping it back needs **another operator-confirmed HCP apply** — the slowest rollback in the repo — whereas reverting the image SHA is one `az containerapp update --image` and **no apply**. The four keys are **inert to any image that does not read them**, so the apply can land at the very start of the wave with zero observable effect, which is what makes the HARD ordering cheap rather than risky. **Recorded rename risk** (delivery-manager's surface, the Terraform side of the equality is mine): `web.yml:201-205` hardcodes `api://raffa-<env>-api/Raffa.Read|Write`, which must stay equal to `api_identifier_uri` plus the two `oauth2_permission_scope` values — they match on this tree, and a mismatch fails **at token acquisition in the browser, after CI is green**. · **security-architect** (owner of ADR-010 and ADR-022; software-architect's §5 is co-signed as the software half): **`ValidateAudience = true` and the value is `api_client_id`, never the `api://` identifier URI.** `identity/main.tf:75` sets `requested_access_token_version = 2`, so `aud` **is** the client id — `outputs.tf:13` says so verbatim ("the default `aud` claim on v2 access tokens"), while `:18` calls `api_identifier_uri` the **alternate** identifier a client *requests*. A task that wires the URI as the audience sees **every** token rejected, and the tempting repair is `ValidateAudience = false`; it is forbidden, and **S-T18 asserts the flags are `true`** so a later "temporary" flip fails a test rather than a pen test. This is the same hazard cloud-architect defuses from the Terraform side by publishing both keys. **`ValidateIssuer = true`** pinned to the concrete tenant issuer (`outputs.tf:25-28`) — **`common` / `organizations` / `consumers` are never accepted** — and defence in depth is mandatory rather than optional here, because the SPA's runtime-config validator (`write_web_runtime_config.py:80-83`) checks the **prefix only**, so `…/common` passes it today. `ClockSkew` **≤ 2 minutes** (the 5-minute default is five free minutes of stale authorization). **A missing scope denies (403); a present scope grants nothing** — `Raffa.Read`/`Raffa.Write` say the browser was allowed to ask, never that the caller may touch a tenant. **`oid` is the identity, `email` only a first-bind aid**: an address is mutable and re-assignable inside a directory, so matching a *standing* membership on email lets a re-assigned alias inherit someone's grant; `WorkspaceRoleResolver.cs:129` already matches `ExternalSubjectId`, so the swap needs **no query change**. **`tid` is the Entra directory, never the Raffa tenant** — both environments share one directory, so it is identical for `dev`, `demo` and every customer workspace and can never select a tenant; **no Raffa code reads it for any purpose**. **The ruling both software-architect and client-architect asked for: `X-Tenant-Id` is demoted, not deleted, and it stays a header.** A caller may belong to several workspaces, so the token subject alone names no tenant, and only 6 of 36 routes carry `{tenantId}`; it becomes an **authorized selector** — membership of the **token subject** verified before `BeginScope`, **404** on failure, never 403 (a 403 there is a tenant-existence oracle). That preserves client-architect's w14 hint and the `v2.spec.ts` e2e seam; moving it into the path is a different task size and no rule requires it. **Sizing, measured: 74 occurrences across 17 files** — one task, or two identity regimes run live at once. **`email` needs Terraform**: verified, **no `optional_claims` block exists anywhere** in `modules/identity`; the design does not depend on it (the `oid` is bound at invite time by NW-67), so its absence degrades to a 403 and a re-invite, **never to a silent grant**. **Revocation stays immediate because nothing caches authorization** — no membership cache, no role cache, no session cookie, no claims transformation that materializes a role; a removed member's token still authenticates and gets 404 everywhere, which is the correct answer rather than a gap. **No `Authentication__Enabled` kill-switch, in any environment**: this seat co-signs cloud-architect's ruling from the security side — a key that turns authentication off is a security control in the hands of whoever can run an apply, and it is precisely the "temporary" fallback that never gets removed. **NW-05 fails closed** — a bounded 401 window on `dev` is the correct failure and **no header fallback is re-added to shorten it**; delivery-manager's web-before-backend ordering is concurred with, not negotiated. And nothing about a token's shape is proven until **one interactive sign-in on deployed `dev`** names the claims actually observed (`ver`, `aud`, `iss`, `oid`, `scp`, `email`) — ADR-010's w14 clause 4, because this class of failure lands in the browser after CI is green. · **client-architect** (the client half; the validation parameters and the authorization rule are security-architect's and are taken as ruled): **this is not a find-and-replace, and it is the one structural client change in the wave.** `client.ts:48` declares the identity accessor **synchronous** (`GetUserId = () => string | null`) and its header literal `userIdHeaders` (`:55-58`) is spread inline into **37 separate `await fetch(` call sites** — verified, exactly 37, one per operation. Token acquisition is asynchronous, so 37 literals are **37 chances to omit one**, and after NW-05 an omitted header is a **401 with no fallback**. Decision: `createApiClient`'s identity parameter becomes an **async token accessor** and **one** internal helper attaches `Authorization: Bearer` to every request; `userIdHeaders`, the `X-User-Id` header, `GetUserId` and `main.tsx:70-73`'s closure over `getAllAccounts()[0]?.username` are **deleted, not made conditional** — ADR-010's w14 clause 3 requires the header to stop being *read*, and this is the client half, that it stops being *sent*. **This supersedes exactly one sentence of ADR-012's w14 clause 4** — *"Headers are attached per method in `client.ts`, never by a global wrapper"* — which described a **mechanism, not a rule**; clause 4's two actual rules are re-affirmed unchanged: **the SPA declares no role to the API, ever** (`X-Role|X-Workspace-Role`: still **zero** occurrences in `web/src`) and **a `{tenantId}` route gets no `X-Tenant-Id`**. Acquisition is `acquireTokenSilent({ scopes: appConfig.oidcApiScopes, account })` → `InteractionRequiredAuthError` → `acquireTokenPopup`; **never a redirect from inside a request**, because a redirect unloads the page and on `/invite/accept` destroys the in-memory invitation token (ADR-012 w14 clause 6; ADR-020 state 5). A **401 after a successful silent acquisition is a server rejection, not a stale token** — surfaced, never retried silently. **The SPA never stores the access token**: read from MSAL's cache per request, never copied into React state, a module variable, `sessionStorage` or `localStorage` — and, recorded so a task does not harden the wrong thing, **ADR-025 T15 is about the *invitation* token**, so MSAL's own `sessionStorage` cache (`msalConfig.ts:36`, chosen for a narrower XSS blast radius) is not a violation of it. **The web half needs no Terraform, no CI change and no contract edit** — and this is worth stating plainly so the decomposer does **not** place the client task behind the infra phase: `buildLoginRequest` (`msalConfig.ts:80-84`) **already requests the API scopes** and the SPA throws the token away (verified: **zero** `acquireTokenSilent|Popup|Redirect` and **zero** `Bearer` in all of `web/src`); `oidcApiScopes` is already declared, validated and assigned (`appConfig.ts:34`, `:75-82`, `:91`); and the generator parses **only `responses`**, so headers are hand-written by construction and NW-05 **does not contend for the contract file** — the one piece of slack in this wave's budget. **`X-Tenant-Id` stays a header: security-architect ruled it and this seat's position is adopted, not merely concurred with** — a membership-verified **selector**, 404 on failure. The client consequence now recorded as decided: the `raffa.signin.currentWorkspace` **hint survives unchanged** with its four-way resolution order, and the `v2.spec.ts:68-85` **e2e seam survives unchanged**; moving tenant selection into the route path would have been a change to **every tenant-scoped method plus every call site**, a different task size, and no task should re-open it. **The check that actually runs**: a **vitest** case enumerating the `ApiClient` surface (`client.ts:1021`, 36 methods) and asserting **every** method attaches `Authorization` — `web.yml` runs `npm test` today, so the 37-call-site risk gets a real gate this wave rather than a deferred one. A15-8's forged-header assertion belongs to the backend seats: **a browser cannot forge a header it does not send**. **Ordering, stated as a risk not a preference**: this is the change that can lock every user out of `dev`, so **web deploys before backend** (concurring with software-architect and delivery-manager), and `web.yml:201-205`'s hardcoded `api://raffa-<env>-api/Raffa.Read|Write` must stay equal to `api_identifier_uri` — a mismatch fails **at token acquisition in the browser, after CI is green**. · **delivery-manager** (the ordering, the CI surface and what proves it — the validation parameters are security-architect's, the values cloud-architect's, the client half client-architect's, all adopted as ruled): **five decisions, and one of them refuses what three seats asked for.** **(1) No scope rename and no new scope, which is what keeps w15's CI-YAML set at zero.** `web.yml:204-205` hardcodes `api://raffa-<env>-api/Raffa.Read` and `…/Raffa.Write` into the SPA's `config.json` and the SPA already requests them; NW-05 reuses them **verbatim**, so the web half needs no config key, no workflow edit and no contract edit. The rename risk client-architect and cloud-architect both flag is real but it is an **equality to preserve, not an edit to make** — and the check that makes it visible is cheap now and catastrophic later: **W15-A1 point (f), one interactive sign-in on deployed `dev` returning a token that carries the scope, before the wave starts.** Today a mismatch is invisible because the SPA discards the token; from NW-05 it is a total `dev` outage. If security-architect ever needs a different audience or an extra scope, `web.yml` joins the planned set and ADR-014's w15 clause 6 becomes "exactly one file" — a table decision, never a task-time discovery. **(2) Fail closed, never crash closed**, co-signed by security-architect: with the keys absent the authenticated routes answer 401 and **no header fallback is re-added, not even "temporarily"** — that fallback *is* the vulnerability NW-05 removes — but the API must still **boot**, because a boot-crash loop costs the wave its only acceptance environment and is indistinguishable from a bad image, while a 401 window self-heals the moment Terraform rolls the revision. **(3) The HARD ordering is satisfied by the infrastructure PR landing first, not by hope** (ADR-014 w15 clause 5): cloud-architect's own property — the four keys are **inert to any image that does not read them** — is exactly what makes that early merge free, so the apply is `CURRENT` and confirmed before the image that reads it exists. **(4) "Web deploys before backend" is refused, and the reason matters more than the refusal.** The mechanism does not exist — `backend.yml` and `web.yml` are separate workflows on the same push with independent path filters and no cross-workflow dependency — and creating one would gate **every future wave's** backend deploy on the web build, a permanent cost for a one-wave concern, in a wave whose workflow set is zero files. And it would not buy what it is meant to buy: **the window is symmetric**, opening at the first deploy and closing at the second whichever lands first, because after NW-05 the SPA sends only `Authorization` and the API accepts only `Authorization` — a new SPA against the **old** API is locked out at workspace discovery too (`WorkspaceEndpointExtensions.cs:68-71` 401s with no `X-User-Id`). The one asymmetry that exists argues the **other** way: a new SPA against the old API still reaches the nine service paths that default the actor to `"unattributed"` (`DocumentsEndpointExtensions.cs:569-573` never rejects), so *that* ordering can land **writes attributed to nobody** — the exact rows NW-32 exists to delete — while backend-first produces clean 401s and no writes. So no ordering is added, the window is named and bounded, backend-first is the cleaner direction if the operator has any influence at all, and **the acceptance walk starts only when both deploys are green**. **(5) The rollback is rehearsed as a numbered step, because this is the one w15 change whose failure mode is "nobody can use `dev`"**: revert PR to `main` → `dev` redeploys the previous image; the env vars stay and are harmless, being unread by that image; no apply, no HCP wait. **A15-8 is walked on deployed `dev` in both halves before the `demo-v*` tag** — forged `X-User-Id`/`X-Tenant-Id` with no token → **401**, *and* one interactive sign-in → **200** on a real tenant-scoped route. The second half is not ceremony: wiring `api_identifier_uri` where the client id belongs applies cleanly, deploys cleanly and then 401s every request in the browser, and no CI check can see it. **The breakage software-architect raised is mine and it is confirmed**: `reprocess-tenant-documents.yml` authenticates with `X-User-Id` + both role headers, so from the w15 code merge its first authenticated call — `GET /api/documents` (`:202-207`) — returns 401 and the step exits 1 with `::error::` (`:208-211`) **before any write**. No partial reprocess, no unattributed audit rows, no half-run tenant; the file predicted it itself at `:48`. w15 **neither repairs nor edits it**: a deploy-time credential able to call the API under the token regime is an identity-plane change (security-architect's, not a side effect of this wave), and **NW-31 owns that file in W16**, so one wave opens it once. It is a row in the acceptance doc's known-gaps table and W16's to design. Client-architect's two packing asks are adopted as hard constraints for the decomposer: **NW-05's client task shares its phase with no other `client.ts` writer**, and at most one task per phase regenerates `raffa-api.v1.json` + `web/src/api/generated/**`. | **software-architect**: **`none`** — **ADR-010 / ADR-022 are security-architect's**; §5 of this seat's lane draft is offered as the software half of their amendment and is co-signed, not written here · **cloud-architect**: **`amend ADR-005`** (w15 footer §4–§5 — the four non-secret keys, their Terraform sources, the per-environment split, and the **no-kill-switch** ruling with the image-revert rollback, so the wave's fixed-cost claim is complete rather than silent on its largest item) · **`amend ADR-007`** (`modules/identity` gains a `tenant_id` output; `modules/containerapps` gains four inputs — inside the existing layout) · **`none — ADR-010 / ADR-022`**: security-architect's files; this seat supplies the **values and their sources** and co-signs · **security-architect**: **`amend ADR-010`** (w15 footer §1 the validation parameters, §2 which claim is the identity, §3 the claims-branch deletion, §4 the rejection contract and the no-kill-switch co-sign) · **`amend ADR-022`** (w15 footer — the interim identity is **retired**: `X-User-Id` deleted, `X-Tenant-Id` demoted to a selector, the measured sizing, the updated retirement schedule, and the w14 "recorded limitation" **closed**) · **`none — ADR-009`** for the policies: only the *source* of `app.identity_subject` changes and Rule F.2c already guarantees `identity_self` needs no edit — but the ADR-009 w15 footer §5 records the one change this wave must **not** make (the GUC stays **parameter-bound**; an `oid` is a GUID and therefore *looks* safe to interpolate, which is exactly the reasoning that would remove the binding) · **`none — ADR-025`**: Rule B1's 404-for-a-non-member and §I's forward binding are applied, not amended — §I governs where it and §E touch one mechanism · **client-architect**: `amend ADR-012` (w15 footer §1 the choke point and the superseded sentence, §2 the never-stored token and the no-role-claim guard, §3 the tenant selector) · **`none — ADR-010 / ADR-022 / ADR-025`**: security-architect's files; this seat supplies the client half and adopts their rulings · **`none — ADR-018`**: no route changes · **`none — ADR-013`**: mobile untouched · **delivery-manager**: **`amend ADR-016`** (w15 footer — clause 15's **fail-closed-never-crash-closed** row and the no-kill-switch co-sign; clause 19's sequence and the rollback rehearsal; **clause 20** the web/backend ordering refused with its evidence and the `web.yml` scope equality recorded as a standing rename risk; **clause 21** `reprocess-tenant-documents.yml` out of service, failing on a read before any write, neither repaired nor edited in w15) · **`amend ADR-014`** (w15 clauses 5–7 — the infrastructure PR first, the zero-CI-YAML set with the two temptations named, and W15-A1 point (f), the pre-wave token check this item makes load-bearing) · **`none — ADR-015`**: NW-05 adds **no CI identity and no credential** — the four keys are non-secret configuration wired by Terraform, so this wave's only deploy-plane change stays NW-67's · **`none — ADR-010 / ADR-022 / ADR-012 / ADR-005`**: the validation parameters, the retirement of the interim posture, the client choke point and the key values are security-architect's, client-architect's and cloud-architect's, adopted as ruled | `reports/architecture/draft/next/software-architect/w15.md` §5 (offered to ADR-010's owner) · **`reports/architecture/ADR-005-azure-skus.md` (w15 footer §4–§5, §8)** · **`reports/architecture/ADR-007-terraform-layout.md` (w15 footer §2)** · **`reports/architecture/ADR-010-entra-oidc.md` (w15 footer — its second)** · **`reports/architecture/ADR-022-day1-demo-auth-and-fixture.md` (w15 footer — its second)** · **`reports/architecture/ADR-009-tenancy.md` (w15 footer §5)** · **`reports/architecture/ADR-012-web-stack.md` (w15 footer §1–§3)** · **`reports/architecture/ADR-016-promotion-dev-demo.md` (w15 footer clauses 15, 19–21)** · **`reports/architecture/ADR-014-git-flow.md` (w15 footer clauses 5–7)** |
| NW-06 — workspace role from membership / claims, never a client assertion | security-architect, client-architect | **security-architect**: **NW-06 is a deletion, not an enablement, and NW-05 is what makes it urgent.** `WorkspaceRoleResolver.ResolveAsync(httpContext, tenantId, …)` has two sources — a claims branch (`:60-65`) and the tenant-scoped membership read (`:70`, `:104-137`) — and **the claims branch returns `claimRole` without ever referencing its `tenantId` parameter**; only `:70` is scoped. Today that is harmless: this host has no authenticated principal at all (verified — a grep for `AddAuthentication\|AddJwtBearer\|UseAuthentication\|JwtBearer\|Microsoft.Identity.Web` over `backend/src` returns **two hits, both doc comments**). **The moment NW-05 wires JWT, one Entra app role assigned once in the directory resolves to that role in every workspace the caller can name, bypassing `workspace_membership` entirely** — and `IsAdminAsync` (`:74-76`) gates `DELETE /api/documents/{id}` and `POST …/reprocess`, so the failure is cross-tenant **destructive** access, not merely visibility. Ruling: `ResolveAsync` collapses to `ResolveMembershipRoleAsync`; the two header constants (`:48-49`) and `TryResolveHeaderRole` (`:89-102`, **zero call sites**) go in the same edit; `WorkspaceRoleClaimResolver` keeps only its non-authorization caller (`GET /api/capabilities`) until NW-31. Deleting costs six lines; tenant-scoping a claims path costs a design, a Terraform app role, an assignment story and a **permanent second authorization source**. **The deletion must take the doc comment with it** — the instruction to keep the branch is in the *source*, not an ADR: `WorkspaceRoleResolver.cs:20-22` calls the claims branch *"the ADR-010 end state, and the only source that survives past this wave"*, so an implementer opening the file to wire NW-05 reads a comment telling them to enable the very branch that is the escalation, and if it survives the next wave restores the branch from its own documentation. **§E / §I reconciliation, recorded so a reviewer reading one section does not conclude the opposite**: ADR-025 §E (`:395`) is about a **client-declared role** (headers); §I (`:658-677`) was written specifically to bind this wave and governs — *"the token carries identity only … a `tenant_id` or `roles` claim is **never** the authorization source"*. **S-T17(c) is the test that proves the branch is gone rather than merely unreachable** and it cannot be reconstructed later from the code. It rides NW-05's task; it is not worth its own. · **client-architect**: **nothing changes on the client, and that is the decision** — recorded rather than left silent, because a wave that rewires identity is exactly when someone "improves" a role parse. `workspaceRole.ts:37-39` is already the accepted parse (`wire === "Admin" ? "admin" : "procurement"`) with the least-privilege rule stated verbatim at `:26-30` and the deliberate split at `:41-53`: **permissions degrade to least privilege; the label does not** (`workspaceRoleLabel`, `:55-59`, which the accept screen already calls at `invite/accept/index.tsx:223`). ADR-026's w14 clause 3 makes that parse **mandatory rather than defensive**, because `role` is a bare wire string — role names are per-tenant rows. Both halves are unchanged by NW-05. **One guard for the decomposer, and it is the only way this item can go wrong on the client: NW-05 must not introduce a `roles` claim read in the SPA.** ADR-010's w14 clause 1 forbids a `roles`/`tenant_id` claim as an authorization source server-side; the client mirror is that the SPA keeps reading `role` from the **`GET /api/workspaces` row** (ADR-026 §D1 — `useValidatedContractCount.ts:65` already reads that row) and **never from the token it is now holding**. **A token is trivially decoded in a browser**, which is exactly why this must be said before the task exists rather than caught in review; security-architect co-signs. NW-06's residues (`WorkspaceRoleResolver.cs:48-49`, `:89-102`) are backend, and `X-Role`/`X-Workspace-Role` have **zero occurrences in `web/src`** (ADR-012 w14 clause 4, re-verified this wave) — so NW-31, when it lands, is a backend + contract item that touches nothing of this seat's. · | **security-architect**: `amend ADR-010` (w15 footer §3, including §3.1 the doc comment and §3.2 the §E/§I reconciliation) · `amend ADR-022` (w15 footer clause 4 — the `X-Role` row of the retirement schedule; the reader goes now, the `GET /api/capabilities` alias with NW-31) · **`none — ADR-025`**: §E and §I both stand as written; the reconciliation is recorded in ADR-010, and this ADR's body is not rewritten · **client-architect**: **`none` — ADR-012's w14 clause 5 (the client's role is presentation-only, server-derived, least-privilege by default) and ADR-026's w14 clause 3 (the parse is mandatory because `role` is a bare wire string) already govern, and the client parse is unchanged.** The no-role-claim guard rides the ADR-012 w15 footer §2, where NW-05's token handling already lives, rather than becoming a fourth ADR action for a zero-line client change · | **`reports/architecture/ADR-010-entra-oidc.md` (w15 footer §3)** · **`reports/architecture/ADR-022-day1-demo-auth-and-fixture.md` (w15 footer §4)** · **`reports/architecture/ADR-012-web-stack.md` (w15 footer §2 — the guard; no client change)** |

`Decision`: one or two sentences, or `task only` when no seat is involved.
`ADR action`: `new ADR-NNN <slug>` | `amend ADR-NNN` | `supersede ADR-NNN by
ADR-NNN` | `none — <why>`. `File`: the ADR path written or amended.
Next free ADR number at the time of this skeleton: **ADR-027**.

## Open questions the table must answer

Carried from `w15-requirements.md` §7. Each already has an assumption in force
and none gates a task; the table either confirms the assumption or replaces it.

| OQ | Question | Owning seat(s) | Status |
|---|---|---|---|
| OQ-w15-001 | Which commit is the wave base (this checkout is five commits behind `origin/main`; two cited oracles exist only there)? | delivery-manager (operator act at HITL) | **resolved — and the answer is a rule, because the number in the question is already wrong.** The base is `helix/w15` with `origin/main` **merged in** at HITL before `register_wave.py` — ADR-014's w14 clause 1 governs, so this is an application, not a new decision. But `origin/main` was **`6ae21b9`** on disk (`../.git/refs/remotes/origin/main`) while the requirements, stamped seven minutes later, name `3c89d35`; the ref advanced three times that day, every update a fast-forward. **So the base is `origin/main` as read at the gate and W15-A1 records the SHA actually merged** — and it is read from the **loose ref**, never from `../.git/packed-refs`, which is stale for every branch ref this wave touches (three wrong answers, not one). The assumption in force was right in kind and wrong in number, and it was missing three checks that all bit the last wave: the **zero product-tree delta** (three of the five differing files are *shorter* on this branch, so a careless merge silently reverts w14's README and e2e work), **green on the `build + test` job at the base commit** (a red `main` means the deploy never runs, which reads as "no deploy" rather than "red tests" — how w14 closed blind to two red fixtures), and **`integration` re-created rather than merged into** (it has diverged: `8ed3af1a` vs `271c3ae1`). → ADR-014 w15 footer clauses 1–4, 7. |
| OQ-w15-002 | Is a real email transport in scope, given `percorso-pilota-v1.md` excludes email from the pilot and the stakeholder marks NW-68 `must`? | product-owner | **resolved — IN, and no stakeholder override is needed.** The pilot doc never excluded mail from the *product*: §7's heading is "Fuori da questo pilota **anche se restano nello spec V1 pieno**" (`:142`) and its table separates "non servono a mostrare il processo" (`:152`, email) from "**Non-goal prodotto**" (`:153`, outreach/RFQ/CLM/signature — which maps onto §1.2). ADR-001 w14 clause 2 pre-authorised the pickup. The pilot script is unchanged and gains no inbox step. → ADR-001 w15 footer clause 6. |
| OQ-w15-003 | `requirements.md` A7 / `OQ-askv2-007` ("upload stays synchronous") is superseded by NW-27 — confirm and record. | product-owner, software-architect | **resolved — confirmed superseded.** A7 (`:827`) and `OQ-askv2-007` are `assumed-wrong` from this wave on. The authority they cite must not survive in code comments (`DocumentProcessingPipeline.cs:65-77`) or in the two work-item files named in "Work-item instructions" below. → ADR-001 w15 footer clause 1; software-architect records the mechanism half. **software-architect — mechanism half recorded**: the supersession is carried by the **ADR-024 w15 footer §3** (A7 / `OQ-askv2-007` and R-DOC-05 AC-1 marked `assumed-wrong` **on the record**, since `inputs/**` is never edited by this process). The authority must not survive in code either: the NW-27 task retires the synchronous-upload comment at `DocumentProcessingPipeline.cs:65-77`, and the two work-item files named in "Work-item instructions" take their partial banners. |
| OQ-w15-004 | Does the admission gate stay inside the 2 s budget, or move to the Worker with a terminal `Rejected` row (a new status value, a migration, and an amendment to the design oracle's *session-only* "Not added" rule)? | product-owner, software-architect, ux-ui-designer | **resolved — the gate SPLITS; the intake's assumption is refined, not adopted.** (a) format/size stays **in the request** (nothing stored for a non-document); (b) content classification moves to the **Worker**, and a refusal becomes a **persistent, visible, terminal record** whose content Raffa does not keep. **Never askable, never counted** in "All documents" or the review queue. Supersedes `requirements.md` **R-DOC-05 AC-1** and **R-DOC-04's *session-only / not stored* clause**; **R-DOC-04's *never counted* clause stands** (this corrects the product-owner lane draft, which had said the row is counted — ux-ui-designer §A4 and R-DOC-04 are right). `screens-v2.md:75` is overridden in its *session-only* half only. → ADR-001 w15 footer clause 2; ADR-024 amendment is software-architect's. **software-architect — written, and the split has a second, independent ground the requirements did not have**: `AdmissionDecision.Pages` carries the parsed text in memory (`AdmissionDecision.cs:67`) and feeds the pipeline's pages overload (`DocumentProcessingPipeline.cs:174-182`), so today the product parses **once**. A 256 KB message cannot carry pages — therefore a gate left in the request makes the worker **OCR every upload a second time**, permanently more expensive *and* slower. Keeping it synchronous is worse on both axes. **The intake's "a new status value is a migration" is false on this schema**: `processing_status` is `character varying(30)` with no CHECK, no enum type and no default (`documents-contracts.sql:110`, its only occurrence) — `Rejected` is **code-only**, so **ADR-021's action is `none`**. Carried by **ADR-024 w15 footer §1–§2** and **ADR-027 §D1, §D6**. |
| OQ-w15-005 | Which identity calls Graph, does Terraform manage the permission and consent, and is a guest provisioned from `dev` acceptable in `demo` (same Entra tenant)? | cloud-architect, security-architect | **(a) resolved by cloud-architect; (b) analysed here, acceptability ruling is security-architect's.** **(a)** The caller is the **existing per-environment workload managed identity**, already attached to both apps and already published as `AZURE_CLIENT_ID` — **no secret, no app registration, no client credential**, so **ADR-011 gains nothing for NW-67** and the intake's assumption holds. Terraform manages the permission: the `azuread_app_role_assignment` **is** the consent — **for a managed identity there is no separate "Grant admin consent" click**. The genuine one-time operator act is **upstream and different**: the **HCP apply identity** must gain Graph `AppRoleAssignment.ReadWrite.All` + `Application.Read.All` (or Privileged Role Administrator / Cloud Application Administrator) **before** the apply, which is an **ADR-015** change because `identity/main.tf:41-42` records the opposite. Gated by `guest_provisioning_enabled` (default `false`) so its absence **degrades NW-67 rather than blocking the wave**. **(b)** **The fact, stated plainly rather than wished away**: both environments authenticate against the **same single Entra tenant** — `sign_in_audience = "AzureADMyOrg"` on both registrations (`identity/main.tf:63`, `:118`) and the same authority GUID on both SWAs. A B2B guest is a **tenant object**, and **there is no Azure control that makes a guest environment-scoped**; claiming otherwise would fail at acceptance. **Why it is nonetheless contained**: what *is* environment-scoped is workspace membership — per-env PostgreSQL, per-env RLS (ADR-009), never shared (locked). A guest provisioned from `dev` who signs in to `demo` gets ADR-026's two-phase discovery over **`demo`'s** database, finds **zero workspaces**, and receives ADR-025's **404** for any tenant id they name. **Blast radius: can authenticate to `demo`, can see nothing** — the same posture every employee of the tenant already has, and **not new surface created by NW-67**. **And the capability stays per-environment**: the role assignment sits on the per-env workload identity and is flag-gated, so with `dev = true` / `demo = false` only `dev` can write to the directory in w15. **The directory is shared; the right to write to it is not.** · **security-architect — (a) confirmed, (b) ruled: acceptable, and here is the reason rather than the hope.** (a) concurs exactly: the existing per-environment workload managed identity, **no secret, no app registration, no client credential**, so **ADR-011 gains no Key Vault entry for NW-67**; and the permission is **`User.Invite.All` as a Graph *application* permission, not the Guest Inviter directory role** — both are narrow, so least privilege is decided by **auditability**: an app-role assignment is a fixed, named grant visible in the consented-permission surface and in Terraform state, while a directory role is a Microsoft-owned bundle that can widen **without our Terraform changing**. Cloud-architect's correction of the consent mechanism is accepted as stated. **(b) Acceptable — and cloud-architect's analysis is right in every fact, so this seat supplies only the ruling and its proof.** A B2B guest is a tenant object and **no Azure control makes it environment-scoped**; what *is* environment-scoped is **membership**. A `dev`-provisioned guest who signs in to `demo` gets ADR-026's two-phase discovery over **`demo`'s** database, finds **zero** workspaces, and receives **404** for any tenant id they name. **Blast radius: can authenticate to `demo`, can see nothing** — the posture every employee of that directory already has, and **not new surface created by NW-67**. It is acceptable **only because ADR-025 §J.3a holds** (directory presence is not a grant), which is why the ruling ships with **S-T19** — a test run for a guest provisioned against a `dev` tenant and asserted against a `demo` tenant. **Assumed-safe would not be good enough here; proved-safe is.** Per-environment gating of provisioning was considered and **rejected** for cloud-architect's reason (same directory, same object — it would stop a `demo` invite provisioning at all and buy nothing §J.3a does not already give). **Recorded residual, accepted**: the invitee's address becomes visible to whoever can read the company directory — the customer's own admins — **never to another customer and never to another workspace**. → ADR-025 w15 footer §J.1a, §J.3a, §J.3c; ADR-011 w15 footer §1. |
| OQ-w15-006 | How does N3b read the invitee's one-time passcode, given no Playwright runner exists and NW-50 is W18? | delivery-manager, security-architect | **security-architect half — assumption confirmed: it does not, this wave, and no seam is built to make it.** A test-only passcode seam is an **authentication bypass shipped in every image**, on a directory `dev` and `demo` **share**, with **no Playwright runner to even execute it** (`web.yml` runs vitest only; wiring one is NW-50, W18), in the wave that finally closes impersonation. Rejected. **N3b is a runbook walk** and the spec's skip reason is rewritten to name the **passcode** — the second account stops being the blocker, because NW-67 creates it. The only acceptable future shape is a **`dev` mail-catcher** (test infrastructure, not a product path), bounded in ADR-025 §J.8c, and any e2e directory credential is bounded in §J.8d — environment-scoped GitHub secrets, a dedicated test principal, and **no trace artifact from a signed-in run**, because Playwright captures typed input and a leaked trace is a leaked directory password. **S-T23** makes the ruling enforceable rather than advisory. The **"where it runs"** half is delivery-manager's and this seat concurs with the manual walk. → ADR-025 w15 footer §J.8. · **delivery-manager half — resolved, and the third refusal is mine alone.** No runner (NW-50 is W18; **zero** `playwright|e2e|test:e2e|spec.ts` hits across all ten workflow files, re-verified), no passcode seam (security-architect's refusal, which I concur with because it would ship with **no consumer**), and **no `dev` mail-catcher** — that one is a **fourth apply**: a new resource and module, a fourth thing that can error the single HCP run already carrying Service Bus, ACS and Graph, for a test nothing can execute. N3b becomes a numbered step in `docs/waves/w15-acceptance.md` walked against the operator's external mailbox, and the spec's skip reason is rewritten to name the **passcode** — its current `SECOND_ACCOUNT_READY` premise is removed by NW-67, so leaving it would make the file assert something false. Two ordering facts: the spec exists **only on `origin/main`**, so the task is unstartable before W15-01, and it lands **after NW-67**. → ADR-016 w15 footer clauses 20, 22. |
| OQ-w15-007 | Does removing a member ever delete or block the Entra guest? | security-architect, product-owner | **resolved — never.** The directory object is the **customer's**, the same person may belong to other workspaces, and Raffa is not an identity-lifecycle manager — becoming one is adjacent to §1.2's "supplier onboarding" and no oracle asks for it. Membership removal stays immediate (ADR-025). **No directory-deletion path is built** without a new ADR. security-architect concurs (S15-18, S15-23). → ADR-001 w15 footer clause 4. · **security-architect — promoted to disk, so the "never" has an enforceable home**: **ADR-025 w15 footer §J.5a**. Three grounds, and the second is the one that makes this cheap rather than a sacrifice: the directory object is the **customer's** and the same person may belong to other workspaces; it is **unnecessary**, because Rule D.5b already makes removal immediate (nothing caches authorization) and §J.3a makes the surviving guest **inert** — 404 everywhere, empty workspace list; and building an identity-lifecycle manager is adjacent to §1.2's "supplier onboarding". **No directory-deletion *or directory-block* path is built, in this wave or any later one, without a new ADR.** Rule D.5c (removal revokes that email's live invitations in the same transaction) is unchanged and still carries the whole revocation. |
| OQ-w15-008 | Does NW-10 ride NW-61's task, or return to the head of W16? | decomposer (recorded here for visibility) | **answered by client-architect — it rides NW-61, and by then it is nearly free.** The scheduling call is the decomposer's; the *reason* is this seat's, because NW-10's rule is ADR-012's w14 clause 1 and its replacement reads the field NW-61 is already adding. Once `counts` exists on `listDocuments`, NW-10 is: delete `documentStore.ts`, add a `useDocumentCounts` hook over `listDocuments(pageSize: 1)`, pass two numbers through `AppShell` → `RailNav`. It also **shares `useDocumentsList.ts` and the shell files with NW-61**, so splitting it across phases buys a single-writer conflict for no benefit. **If the 20-task cap binds, NW-10 is the one release valve this seat offers** — and product-owner independently named the same one. Scheduled apart, it is the **head of W16, never later**: the badge is *always absent* today (`rememberDocument`, `documentStore.ts:77`, has **zero production call sites**), so nothing regresses by waiting, but nothing improves either. |
| OQ-w15-009 *(raised by product-owner in lane)* | Does ADR-001's w14 footer **clause 7** (the deferred workspace-domain invite restriction) fire now that NW-05 lands ADR-010? | product-owner | **resolved — no; it stays helper text and is not enforced.** A B2B guest is by definition off-domain, and A15-4 requires inviting a personal address to succeed, so enforcing clause 7 would fail this wave's own acceptance on the first click. **A deferral does not convert into an obligation when its trigger fires.** Recorded explicitly so the next implementer or reviewer does not read clause 7 as a standing instruction. → ADR-001 w15 footer clause 5. |
| OQ-w15-D3 *(raised by ux-ui-designer §8.1)* | `requirements.md` **R-DOC-05 AC-1** ("no `Rejected` value server-side") is contradicted by NW-27's worker-side gate — who supersedes it? | product-owner ratifies; software-architect records | **resolved — ratified by product-owner.** ux-ui-designer was right not to supersede an accepted requirement from its own seat. Superseded **on the record** (ADR-001 w15 footer clause 2 + the ADR-024 amendment), never edited in place — `inputs/**` is never written by this process. `assumed-wrong` from this wave on, on the same grounds as A7. If OQ-w15-004 had resolved the other way, AC-1 would simply still be correct. |
| OQ-w15-cl-01 *(raised by cloud-architect in lane)* | Can a Container Apps `custom_scale_rule` authenticate its Service Bus queue-length probe with the **workload identity** under the pinned `azurerm ~> 4.0`, or is a `listen`-only credential required? | cloud-architect, resolved **inside the infra PR** | **Assumption in force: yes, workload-identity auth.** If `terraform validate` says otherwise, take the bounded fallback — `azurerm_servicebus_namespace_authorization_rule` `keda-listen` (`listen = true, send = false, manage = false`) → Key Vault `servicebus-listen-connection` → handle `sb-listen-cs`, referenced **only** by `custom_scale_rule.authentication`, **never** by an `env {}` block and never by application code, so the application data plane stays secret-free either way. A `manage = true` or namespace-wide send rule is rejected outright. **`min_replicas = 1` is not a fallback** — it creates a fixed monthly line the lock forbids; if neither form is expressible the item returns to this seat. **Does not gate a task**: the answer is proved by `terraform validate` in the PR job, which is where identifiers are proved rather than asserted (ADR-005 `:211-219`). · **security-architect — the fallback is accepted, with three conditions, and one of them names why it is acceptable at all.** This seat's standing rule is *no shared-access-key connection string for Service Bus, anywhere*; the scaler is the one place it can be bounded to a **control plane** without touching the data plane, so the fallback is admitted rather than refused. (i) It is **`listen = true, send = false, manage = false`**; a `manage` rule or a namespace-wide **send** rule is rejected outright. (ii) It **never appears in an `env {}` block** and is never read by application code — referenced only by `custom_scale_rule.authentication` — so the application data plane stays secret-free and the key cannot surface in a process-environment dump. (iii) **Namespace-wide `listen` grants read on every entity including the dead-letter queue, which retains message bodies for operator inspection** — so the fallback is acceptable **only because ADR-009's w15 footer §3 keeps the message to ids alone**: a leaked listen key would disclose identifiers, not contract content. That is a dependency, not a coincidence, and it is recorded in both files so neither can be relaxed alone. The rule and its secret are **removed** when the identity form becomes expressible. → ADR-011 w15 footer §2c. |
| OQ-w15-cl-02 *(raised by cloud-architect in lane)* | Should the single per-env workload identity split into api- and worker-identities so Service Bus **Sender** and **Receiver** separate? | security-architect, a later wave | **Assumption in force: no, not this wave** — one identity holding both **topic-scoped** roles. It is a genuine least-privilege improvement and it is out of this wave's budget: splitting re-keys four role assignments per environment across `keyvault`, `acr`, `foundry` and `servicebus`, in a wave already carrying three applies' worth of Terraform. Recorded rather than silently deferred, so the next wave inherits a named option instead of re-deriving it. **Does not gate a task.** · **security-architect — concur, deferred, with the residual named rather than left implicit.** One identity holding both roles means a compromised API host can also **drain** the queue, not just fill it. Accepted for w15 because both hosts run at the **same trust level**, in the same environment, against the same database and the same Key Vault — so the split buys separation between two components that already share every other credential — and because the roles are **topic-scoped rather than namespace-scoped**, which bounds the blast radius to one topic. It is a genuine least-privilege improvement and it is the right *later* change; this seat will not spend a wave already carrying three applies' worth of Terraform on re-keying four role assignments per environment. **Condition on the deferral: `RootManageSharedAccessKey` is never the shortcut that makes the split look unnecessary** — the deferral is one identity with two narrow roles, not one identity with a broad key. → ADR-011 w15 footer §2a, §2d. |
| OQ-w15-012 *(raised by delivery-manager in lane, addressed to this seat)* | NW-27 needs **work-queue** semantics on infrastructure that is currently a topic with **no subscription** — and is the migration purely additive, since the rollback story depends on it? | software-architect (answers), cloud-architect (provisions) | **resolved — both halves.** (a) **A topic with exactly one subscription *is* a work queue**: Service Bus delivers each message once **per subscription**, and competing receivers on one subscription share it. So no resource-type change is needed — `extraction-events` + **one** `extraction-worker` subscription gives the required semantics, and raising `max_replicas` stays a pure cost decision. **"Exactly one subscription" is a design constraint, not an accident**: a second subscription would silently process every document twice, which ADR-027 §D5's replace step would mask as *lost work* rather than as an error. **Zero** subscriptions — today's state — means the broker accepts every message and **discards it**, so the producer reports success and every document stalls (cloud-architect §12.3, this seat's §1.7: the same failure found from two directions). → ADR-027 §D12. (b) **The migration is purely additive**: three nullable columns on `document`, three on `extraction_job` (`attempt_count` defaulted, `claimed_at`/`claimed_by` nullable), and `Rejected` needs **no DDL at all**. No column is dropped, no type changed, no `NOT NULL` added to an existing column — **so the previous image runs unchanged against the new schema, and rollback is a container revert with no schema rollback**. |
| OQ-w15-sec-01 *(raised by security-architect in lane)* | Does the access token carry an `email` claim for a **B2B guest**, given the resource-directory UPN is mangled (`luca_gmail.com#EXT#@…`)? | security-architect, cloud-architect (the Terraform half) | **Assumption in force: only if the optional claim is requested — and the design deliberately does not depend on it.** Verified: **no `optional_claims` block exists anywhere** in `infra/modules/identity`, so today the claim is absent; adding `optional_claims { access_token { name = "email" } }` to `azuread_application.api` is a small addition to the Terraform change NW-05 already needs. The design's primary path is **the `oid` bound at invite time** (ADR-025 §J.3b), so `email` is a fallback, never the mechanism. **Proved by one interactive sign-in on deployed `dev` naming the claims observed — never asserted from Terraform state.** If `email` proves absent *and* the `oid` bind were somehow missed, accept answers **403** and the Admin re-invites — **never a silent grant**. **Does not gate a task.** → ADR-010 w15 footer §2.3–§2.5. |
| OQ-w15-sec-02 *(raised by security-architect in lane)* | ACS via managed identity (`TokenCredential`) or a Key Vault connection string? | cloud-architect (owner), security-architect | **resolved — connection string for w15, by cloud-architect's ruling, and this seat accepts it.** It was raised as a `PROPOSE`, never an `OBJECT`: the managed-identity form removes the only secret w15 adds, but the Key Vault path is proven twice in that module, satisfies the locked row verbatim, and swapping mid-wave would add a role assignment on an unprobed resource type plus an untested SDK auth path to a wave already carrying three applies. `acs-connection` is the **one** new Key Vault entry in the wave and needs **no new permission**. The migration is recorded as **an ADR-011 amendment and one role assignment** when it lands, not a redesign. → ADR-011 w15 footer §1b. |
| OQ-w15-sec-03 *(raised by security-architect in lane)* | What is the cap on live (unaccepted, unrevoked) invitations per tenant, now that an invite writes into the customer's directory? | security-architect raises; **product-owner may set the number** | **Assumption in force: 100**, audited as `workspace.invitation.cap_reached` when hit. ADR-025 §F.4's partial unique index already prevents accumulating links for the **same** address; the cap bounds **distinct** addresses, which is the directory-spam shape NW-67 newly creates. The product-owner may change the number; **the mechanism does not change**. Same pattern as OQ-sec-003's 7-day expiry. **Does not gate a task.** → ADR-025 w15 footer §J.1c.4. |
| OQ-w15-sec-04 *(raised by security-architect at the table — a contradiction between two ADRs written at this table)* | Does Service Bus authenticate with a **Key Vault connection string** (ADR-027 §D12) or **managed identity + RBAC** (ADR-005 w15 footer)? | security-architect **rules** (ADR-011 owns secret-vs-identity); software-architect to note | **resolved — identity and RBAC; no Service Bus secret.** Both instructions are on disk for the same environment variable: **ADR-027 `:347` and `:395`** specify "the connection as a Key Vault secret → container-app secret handle → env var, on both hosts, exactly as `pg-cs` and `st-cs` already do", while **ADR-005's w15 footer** specifies two **topic-scoped** role assignments and states that NW-27 adds no Key Vault secret at all. **A decomposer reads files, not reasoning, and would ship whichever it opened first** — which is why this is recorded rather than left to be noticed. Ruling: **`Azure Service Bus Data Sender` / `Data Receiver`, topic-scoped, no `Manage`, no `RootManageSharedAccessKey`, fully-qualified namespace as non-secret config.** ADR-027 §D12's own framing ("cloud-architect owns; stated here as a contract") makes this a narrowing of a hand-off rather than a contradiction of a decision, and its other requirements (one subscription, dead-lettering, `max_delivery_count` strictly above `MaxAttempts`) are untouched and correct. **No ADR body is edited.** → ADR-011 w15 footer §2a–§2b. |
| OQ-w15-ca-01 *(raised by client-architect in lane)* | What is the Container Apps request ceiling, and therefore the client's upload deadline? | cloud-architect (owns the ceiling), client-architect (derives from it) | **Assumption in force: a client-owned deadline of 120 s.** `infra/modules/containerapps/main.tf:179-194` declares no `timeout` and **ADR-005's w15 footer pins none** (verified this pass — the footer's ingress content is the CORS block and the scale rule, not a request timeout), so the ~240 s platform default is **implicit and undocumented**. The deadline must sit above the time to *send* a 50 MB body on a slow link (`MAX_FILE_BYTES`, `uploadPipeline.ts:19`; `fetch` + `AbortController` cannot express "time-to-first-byte after the body is sent", so it is necessarily absolute) and **below** the platform ceiling or it never fires. If cloud-architect pins the ingress for NW-27, the client number **derives from theirs**; if they do not, the task ships 120 s **with its derivation recorded**. **Does not gate a task** — an abort maps to the existing `"failed"` branch (`uploadPipeline.ts:127-132`) and this is a safety net, not the mechanism that makes upload feel instant. → ADR-012 w15 footer §5. |
| OQ-w15-ca-02 *(raised by client-architect in lane)* | What string does a freshly provisioned B2B guest present as its identity to the accept call? | security-architect + software-architect (own the bind), client-architect (raised it) | **Assumption in force: the question stops mattering, because the accept binds to a server-verified identity.** The MSAL `username` for an email-OTP guest is the provider's `preferred_username` and is **provider-defined** — not assertable from `web/src`, which is precisely why the decision is "bind to the validated token subject", not "assume a string". Two independent fixes now cover it and **both are on disk**: NW-05's validated token replaces `X-User-Id` as the identity the accept binds (ADR-010 w15 footer), and security-architect's **`ExternalSubjectId` bind at invite time** (ADR-025 §J.3b) removes the email-equality dependency altogether. **Residual, and it is the reason this stays on the register**: if NW-05 slips inside the wave, **A15-4 must be walked against a real B2B guest** before the wave is called done — a walk with an existing member's account proves nothing about this path, and the failure mode is a 403 (ADR-020 state 6 "wrong account") arriving *after* the mail, the guest and the passcode all worked. **Does not gate a task.** → ADR-012 w15 footer §8. |
| OQ-w15-ca-03 *(raised by client-architect in lane)* | Does the tenant stay a header after NW-05, or move into the route path? | security-architect (ruled), software-architect (concurs), client-architect (asked) | **resolved at this table — it stays a header, demoted to a membership-verified selector, 404 on failure.** Recorded as *answered* rather than left open so no task re-derives it: a caller may belong to several workspaces, so the token subject alone names no tenant, and only **6 of 36** routes carry `{tenantId}`. The client consequence this seat claimed is therefore decided, not assumed: `raffa.signin.currentWorkspace` **keeps its hint role** with the four-way resolution order of ADR-012's w14 clause 2, and the **`v2.spec.ts:68-85` e2e seam survives unchanged**. The rejected alternative is recorded with its cost so it is not re-proposed as a simplification: moving selection into the path is a change to **every tenant-scoped method in `client.ts` plus every call site** — a different task size, and no rule requires it. → ADR-010 w15 footer; ADR-012 w15 footer §3. |
| OQ-w15-ca-04 *(raised by client-architect in lane)* | Ask gains a **third** off-state — *documents exist, none in flight, none validated* — whose copy does not exist today. Who writes it? | ux-ui-designer (owns ADR-020 copy); client-architect makes the state distinguishable | **Assumption in force: the state ships and the ux-ui-designer owns its copy.** It is created, not invented: Ask's gate branches on `page.totalCount > 0` (`ask/index.tsx:99-105`) — **any** state including `Failed` — so `askViewModel.ts:250-261` currently tells a tenant whose only document failed that it "is still processing or waiting for review". Reading a server not-yet-terminal count makes the two cases **distinguishable for the first time**, which is what forces a third string. **The binding constraint, and it is the whole point**: it must **not** be collapsed back into the "still processing" sentence, which would re-ship today's fabricated fact under a server number and fail A15-3 while appearing to satisfy it. `screens-v2.md:25-30`'s `askOffReason` supplies the two existing strings; the third has no oracle. **Does not gate a task** — the state is expressible the moment the count exists. → ADR-012 w15 footer §4; ADR-020 is ux-ui-designer's. · **ux-ui-designer — resolved, and the binding constraint is honoured rather than argued with.** The third variant is **"Raffa.ai could not finish processing your documents. Ask only answers from facts that passed validation — so it never guesses."**, CTA **Go to Documents**. Its second clause is the shipped string **verbatim**, so exactly **one** new sentence enters the product, and it is **not** collapsed back into the "still processing" sentence. Two refinements the framing of the question did not have. **(i) The state is three-way, not "documents exist vs not": a tenant holding only *refused* files gets the *first* variant** ("Upload a contract first"), because the counting rule excludes `Rejected` from what Raffa.ai holds — and that is **true** for them, since they were already told per row why each file was refused. **(ii) The anchor a task must widen is a signature**: `buildOffCopy(hasAnyDocument: **boolean**)` (`askViewModel.ts:250`) is a boolean ternary, so the third state is a type change, not a third string in an existing switch. Portfolio takes the same reading with **no new string** (the shipped sentence minus its "Upload one to start." clause); Contract 360 takes it as the second of its two readings. → ADR-018 w15 clause 1; ADR-020 w15 §2.1–§2.3. |
| OQ-w15-D1 *(raised by ux-ui-designer in lane)* | `AttentionFilter`'s "All documents · N" counts refused files while `kbSummary`'s "N documents" does not — two numbers on one screen deliberately differing by the refusal count. Does that need an explanatory line? | ux-ui-designer | **Dissolved at this table, not answered.** The question presupposed my lane §A4, which is **withdrawn**: reconciled against product-owner's "never counted in 'All documents'" and ADR-027 §D7's `counts.all`, every number on screen 3 now reads **exactly one server field**, and refusals get their **own chip** (`Not added · K` ← `counts.rejected`). No two numbers claim to be the same thing, so there is nothing left to explain. Recorded rather than deleted, so the next reader can see that the two-number problem was removed rather than tolerated. → ADR-018 w15 clause 2. |
| OQ-w15-D2 *(raised by ux-ui-designer in lane)* | A `Rejected` row accumulates with no next step for a Procurement user — only an Admin can `DELETE` it. | ux-ui-designer; product-owner may overrule | **Assumption in force: acceptable for the pilot, and honest.** The record of a file that was dropped and **not kept** is worth more than a tidy list, and its reason now sits on the row instead of in a card that vanished on reload. Two things bound it: the **`Not added` chip renders only while `counts.rejected > 0`**, so a tenant that never has a refusal sees no change at all; and refusals are in **their own filter**, so they never dilute "All documents" or the attention default. **Does not gate a task.** Revisit only if a pilot user complains. |
| OQ-w15-dm-01 *(raised by delivery-manager in lane; numbered `dm-` to avoid the collision with product-owner's OQ-w15-009)* | Does the `raffa-demo` HCP workspace apply automatically on a push to `main` that touches `infra/`, or only through the promotion? | delivery-manager, cloud-architect | **Assumption in force: it does — `demo`'s infrastructure moves at the infrastructure merge, before `demo` has the code.** `infra/README.md:8-12` says **both** workspaces are VCS-connected with `trigger-prefixes: infra/`, while `:88-91` says the first `demo` apply is an HCP UI "New run"; the two sentences are not obviously consistent, and w15 is the first wave since that note whose Terraform is not empty. Assuming the riskier reading costs nothing and is the only safe order: **every w15 key whose feature can act on the world is flag-gated `false` on the `demo` root**, and the operator confirms the `raffa-demo` run state explicitly at promotion steps 2 and 5. If the workspace turns out not to auto-run, nothing breaks — the flip PR and the confirmation are the same act either way. **Does not gate a task.** → ADR-016 w15 footer clauses 14, 19. |
| OQ-w15-dm-02 *(raised by delivery-manager in lane)* | What wakes the worker, and how would anyone find out that nothing does? | cloud-architect (answered), delivery-manager (the detection half) | **Resolved on the resource side, and the detection half is this seat's and is new.** Cloud-architect's NW-27 cell supplies all three missing pieces — one `document-processing` subscription, a KEDA `azure-servicebus` scale rule with `min_replicas = 0` kept, and two topic-scoped role assignments — so the lane's `min_replicas = 1` alternative is off the table and the locked cost rule is intact. **What remains is that none of it is observable from CI**: `backend.yml` deploys `az containerapp update --image` with **no** revision-state, `runningState` or health assertion, so **a worker that cannot start produces a green run**, uploads keep returning 201, and a queue behind a dead consumer looks exactly like slow processing. **Assumption in force**: the promotion gate carries **one post-deploy revision-state assertion per environment** (worker revision running and healthy, subscription present, scale rule present, both role assignments present), and **A15-2 is walked on deployed `dev` with the worker scaled the way `demo` will run it** — the only acceptance step that catches any of the three gaps, and it catches all three. "The HCP run is `CURRENT`" proves none of them. **Does not gate a task.** → ADR-016 w15 footer clause 17. |
| OQ-w15-dm-03 *(raised by delivery-manager in lane — the one fork on this register that only the operator can close)* | **w14 was never promoted.** The highest tag is `demo-v3` (`2db5734`, 2026-09-04) and there is **no `demo-v4`**, so `demo` has been running w13's code for nine days and the w15 promotion would carry two waves. Cut `demo-v4` at the gate, or bundle? | delivery-manager; **operator decides at HITL** | **Assumption in force: `demo-v4` is cut on the current `main` at the gate, before w15 starts, and w15 promotes as `demo-v5`** — which is also the tag cloud-architect's runbook names, and it is correct **only** because `demo-v4` is cut first. Read from `packed-refs`, trustworthy here because tags are immutable: `demo-v1 a4e564c`, `demo-v2 22c474f`, `demo-v3 2db5734`, and nothing else. Grounds: it restores ADR-016's own invariant (`:41-48`, one promotion is one reviewable wave), it needs no extra verification because W15-A1 point (d) already proves that commit green, and it makes a w15 rollback land on **w14** instead of on 2026-09-04. If the operator declines, the bundled promotion is the fallback and it is not a disaster — it is more to walk, on the stakeholder environment, in one sitting. **Either way `demo` owes three data-plane steps that belong to the *previous* wave** (the w14+w15 schema apply, `seed-demo-fixture.yml`, `backfill-workspace-membership.yml`, then `docs/waves/w14-acceptance.md` N1–N9 + W14-A2), and the wave-close must record them so W16 does not inherit them a third time. **This was invisible on this tree because no `wave-close-w14.md` was ever written** — which is itself the process gap: *a wave that closes without a promotion must say so in its close report.* → ADR-016 w15 footer clauses 18, 19. |
| OQ-w15-dm-04 *(raised by software-architect at the table; answered here because the file is this seat's)* | NW-05 makes `reprocess-tenant-documents.yml` return 401 — does w15 repair it, edit it, or let it go out of service? | delivery-manager (rules), security-architect (owns any credential that would repair it) | **Resolved — out of service, named, neither repaired nor edited.** The job authenticates with `X-User-Id` + both role headers, so from the w15 code merge its first authenticated call, `GET /api/documents` (`:202-207`), returns 401 and the step exits 1 with `::error::` (`:208-211`) — **before any write**, so there is no partial reprocess and no unattributed audit row; the file predicted this itself at `:48`. It is not repaired, because a deploy-time credential able to call the API under the token regime is an identity-plane change and *a wave does not acquire a new CI capability as a side effect* (ADR-016 clause 10); it is not edited, because **NW-31 owns that file in W16** together with the role-header removal, and one wave should open it once. Recorded as a row in `docs/waves/w15-acceptance.md`'s known-gaps table. **W16 designs the repair** — a token-bearing operator identity, or an operator job that enqueues on the worker side now that NW-27 makes reprocess asynchronous. **Does not gate a task.** → ADR-016 w15 footer clause 21; ADR-014 w15 footer clause 6. |
| OQ-w15-ca-05 *(raised by client-architect at the re-entry round)* | When the Documents poll stops on its no-change budget (ADR-012 §17), what does the screen say, and what is the resume control called? | ux-ui-designer | **Answered — ADR-020 w15 round-3 §8.** It is a **list-level notice**, not a row state and not the error treatment: the rows stay exactly as the server last reported them, the bar keeps rendering the server's stage and the chips keep the server's last numbers. Copy: **"Nothing has changed for five minutes, so this page stopped checking for updates."** + **"Check again"**, as `.hint` + `.btn-secondary` — the pair `DocumentStatusTable.tsx:100-103` already ships, so **no component and no token** (ADR-019 w15 round-3 clause 5). Three things the sentence refuses to say: that Raffa.ai is still working (in §C6's stranded case nothing is — the fabricated fact A15-3 forbids), that anything failed (nothing has), and any cause at all — the only fact the client owns is what **the page** did, so the actor is the page and never Raffa.ai. **Widened beyond the question, because the question was scoped to one hook**: NW-61 turns Ask's one-shot gate fetch (`ask/index.tsx:100-105`, verified) into a 2 s poll, so this wave introduces the same unbounded wait on a surface with **no row grid** and whose whole promise in that state is *we will let you in when it is ready*. ADR-018 w15 round-3 clause 6 binds all five surfaces to one budget, one sentence and one label. **Does not gate a task** — it is a small edit in the file NW-61's counts task already owns. → ADR-020 w15 round-3 §8; ADR-018 w15 round-3 clause 6. |

## Design refs carried into decisions

| Item | Oracle | Anchor |
|---|---|---|
| NW-27 | `inputs/design/prototypes/raffa-v2/screens-v2.md` | `:59-79` — the six real stage strings; the **"Not added"** card marked *session-only, never counted*; state inventory including `rejected (not added)` |
| NW-61 | `inputs/design/prototypes/raffa-v2/screens-v2.md` | `:25-30` — `askOffReason` "Your document is still processing or waiting for review. Ask only answers from facts that passed validation — so it never guesses."; `:59-79` — list states and stages |
| NW-67 | `inputs/design/prototypes/raffa-v2/screens-v2.md` | `:147-154` — §10 Members table and invite form |
| NW-68 | ADR-020 w14 amendment footer | screen 10's two `mailDelivered` strings and the **pre-decided** delivery-failure copy ("Invitation created, but the email could not be sent." + "Try sending again") — not re-opened |
| NW-69 | `inputs/design/prototypes/raffa-v2/screens-v2.md` + ADR-020 w14 footer | `:147-154`; footer §10.3 (row actions, `Active`/`Invited`/`Expired`) |
| NW-68 — surface 12 | **no oracle exists**: `inputs/design/` contains **no email in any form** — it holds screens, `markup.html`, `styles.css`, `app.jsx`, and nothing that leaves the browser | decided from ADR-019's locked token **values** (`--color-text` `#201e1d`, `--color-accent` `#ec3013`, `--color-accent-700` `#ae1800`; `--font-heading/--font-body` is **Archivo**, which no mail client loads) + screen 11 state 2 **verbatim** (`ADR-020:397`). `HITL_CLAUDE_DESIGN` item (11); **does not block w15** |
| NW-27 — refusal states | `screens-v2.md:72-79` **vs** the shipped code | the export's "Not added" card is *session-only, never counted* (`:75`); the row/tag/label are lifted from `UploadResultCard.tsx:24` and `documentTable.ts:74-79`, which **states the queued-vs-processing rule itself** at `:76-78`. Divergence named and ratified as OQ-w15-D3 |
| **all w15 copy** | `inputs/design/prototypes/raffa-v2/` is **pre-rebrand** | `screens-v2.md:26`, `:73` say "**Raffa**"; every shipped string says "**Raffa.ai**" (`SignInScreen.tsx:52`, `askViewModel.ts:232`/`:258`, `uploadPipeline.ts:42`, `contracts/index.tsx:57`, …). **Any task retyping copy out of the export silently reverts the rebrand** — ADR-020 w15 §0 |
| NW-27 / NW-61 — the local refusal row, and updates that stop | **no oracle exists for either**: the export has a "Not added" **card** (`screens-v2.md:75`) and no local-row form of it, and it has **no stopped-updates state anywhere** — `app.jsx:46-48` advances a document a stage every 700 ms, so the prototype has no wait that can fail to end | decided from the shipped idiom instead: the hint slot (`DocumentStatusTable.tsx:90`) and the `.btn-secondary` beside it (`:100-103`), the retired card's own tag (`UploadResultCard.tsx:24`), and the second clause of the shipped `no_readable_text` sentence (`uploadPipeline.ts:44`) for the 415 wording. `HITL_CLAUDE_DESIGN` items (12) and (13); **does not block w15** — ADR-020 w15 round-3 §6, §8 |

## Work-item instructions — product-owner, correcting §6 of the intake

`w15-requirements.md` §6 (`:1032`) says *"**No status banner is written this
wave**"* and names `us-01-documents-v2` as *amended, not superseded*, because
*"the story's ACs are still exactly what the product wants"*. **That reason is
false under this table's resolution of OQ-w15-004**, and correcting it is this
seat's charter: a `status: active` story whose acceptance states the opposite
of what the wave ships cannot go unbannered — the next intake, implementer and
reviewer would all read it as the wanted rule, and a reviewer would be **right**
to reject NW-27's task against it.

| Work item | Instruction | Why |
|---|---|---|
| `epic-13-ask-v2/feature-04-documents-v2/us-01-documents-v2/us-01-documents-v2.md` | **Partial banner, not a supersede.** Append `## Superseded in part (2026-09-13, wave w15)` naming **AC-6 entirely** and **AC-1's 422 / nothing-persisted clause only** (the 415 format clause **stands**). Point at NW-27, this ADR-001 w15 footer, and the epic-16 story that replaces them. Record `OQ-askv2-007` as **`assumed-wrong`**. | AC-6 (`:44-46`) says verbatim *"**no `Rejected` status exists server-side**"* — negated word for word. AC-1 (`:22-26`) says *"no blob, no `document` row"* — half negated. AC-2–AC-5 stand, so the story is **mostly still wanted** and must not be superseded whole. |
| `.../tasks/task-01-documents-admission.md` | Same footer, naming its two required-test rows (`:63`, `:81`, "nothing persisted on 422") as superseded by the split gate, and its `OQ-askv2-007` line (`:85`) as retired. | The task carries the same premise as the story it serves. |
| `epic-15-workspace-invitations/feature-01-invitation-lifecycle/us-01-invite-accept-remove` | **No banner. The intake is right here and this seat agrees.** | NW-67 / NW-68 land its two *deferred* clauses; the delivered lifecycle stands unchanged. |

Append-only rules apply (`kb-contract-next`): frontmatter `status:` line plus an
appended banner; the body stays byte-identical. This seat does not write
work-item files — `next-decomposer` does.

## Votes (final table round)

| Seat | Vote |
|---|---|
| product-owner | **APPROVE** — ADR-001's w15 footer **and its addendum** written and read back, the `## Decision outcome` and all four earlier footers intact above them, `Status:` still `accepted`, **nothing superseded**. **INDEX's w15 section now carries the ADR-001 paragraph — that was the one gap in this seat's delivery at the previous round**, found by reconciling against the other seats rather than by re-reading myself: every involved seat had indexed its footer and this one had not, which would have left a decomposer reading six amendment paragraphs and no scope one (no row removed; **no new ADR from this seat** — scope, priority and acceptance already have a home, and a sixth file would fragment it). NW-27 / NW-61 / NW-67 / NW-68 rows filled; OQ-002, -003, -004, -007, -009, -D3 resolved, **-D2 confirmed unoverruled** and **-sec-03's number set at 100**; §6 of the intake corrected with the two partial banners the decomposer must write. **One withdrawal of my own work**: my lane draft's **A15-6b / A15-6c** required a working "Try sending again", and ADR-020's w15 §3.4 rules the affordance out — correctly, because the token is stored only as a SHA-256 hash, so any retry is a **re-issue** that kills the link the Admin is reading, and ADR-020's own w14 footer already forbids telling the Admin to invite again from that pane. **No capability is lost** (revoke + re-invite exists, and §J.2b makes it one transaction), so this is a correction, not a reduction — and **A15-6's binding wording is now addendum clause 8**, so a reviewer cannot reject NW-68 against an affordance the council deliberately refused. **One priority ruling on the record**: §5's order is **dependency**-driven, so two `should`s sit ahead of two `must`s — if the cap binds the valves are **NW-10, then NW-58r's runbook walk, then NW-69**, and **never** NW-27's durability, NW-67's failure contract or NW-68's absolute accept link. **No objection outstanding**: nothing in this wave expands past §1.2, no paid market API enters `demo` (NW-52 stays out, ADR-001's own rule), ACS Email and Microsoft Graph are first-party surfaces on the existing tenant at a **$0.00 fixed-cost delta**, and **no `must` is demoted behind a `should`**. `Invitations__Mail__Enabled = false` and `guest_provisioning_enabled = false` on `demo` are **confirmed consistent with clause 6** — the pilot gains no inbox step — and recorded so no later task "fixes" `demo` by turning mail on and adding one. **Re-entry round, 2026-09-13** — footer and addendum re-read on disk (`ADR-001:196-486`), the `## Decision outcome` and all four earlier footers intact above them, `Status:` still `accepted`, nothing superseded. **Addendum clause 12 added**, closing the one asymmetry left in this seat's own file: clause 8 ruled what `Invitations__Mail__Enabled = false` means for an invite, and **nothing ruled the provisioning flag — the path that aborts**. It is resolved by a peer's mechanism rather than by this seat's authority: ADR-026 §2's fourth value **`NotConfigured`** (`:536-539`) makes flag-off behave "exactly as it does on `main` today — link-only", so §3's "a failure leaves nothing behind" binds **`Failed`** only, and **`demo` loses nothing w14 delivered**. Clause 12 then fences the two things a reviewer would otherwise get wrong — **A15-4 / A15-5 / A15-7 are `dev` acceptance and are not walkable on `demo` this wave**, and no task may turn either flag on for `demo` to make them pass — and **ratifies delivery-manager's `demo-v4` cut as a product requirement**: `demo` stands at `demo-v3` of 2026-09-04 (`ADR-016:416-421`), so the workspace-and-invite half of `percorso-pilota-v1.md` has never existed on the environment the client demo is run from. |
| software-architect | **APPROVE** — `ADR-027` written; w15 footers appended to `ADR-002`, `ADR-024`, `ADR-026`; INDEX w15 section + ADR-027 row appended; NW-27 / NW-61 / NW-67 / NW-68 / NW-05 cells filled; OQ-003, -004 mechanism halves recorded and OQ-012 answered. `none` recorded with reasons for ADR-021, ADR-004/017, ADR-009, ADR-012, ADR-005. Asks outstanding to security-architect (the pre-verdict blob window; the worker's tenant scope; `X-Tenant-Id` demoted to an authorized selector; re-invite by replacement; Graph least privilege), cloud-architect (**one** subscription, `max_delivery_count` > `MaxAttempts`, the worker's missing `ConnectionStrings__Storage` / `AiGateway__*` / `AZURE_CLIENT_ID`, `min_replicas ≥ 1`) and delivery-manager (web before backend on NW-05; `reprocess-tenant-documents.yml` breaks; A15-1/A15-2 proven on deployed `dev`, never in CI). **Re-entry round, 2026-09-13** — ADR-027's **round-3 footer (C6–C11)** written and read back, the `## Decision outcome`, D1–D12 and the round-2 footer all intact above it, `Status:` still `accepted`, **nothing superseded**; INDEX's ADR-027 paragraph extended (no row removed, no new ADR). **The finding of this round is against my own design, not a peer's**: discharging cloud-architect's "the app-side number is software-architect's" (`ADR-005:358`) meant reading D2 against D3, and D3 disposes of a claim matching nothing by **completing the message** — merging three cases that are not one. A commit slower than D2's 2 s delay makes the row invisible at delivery; D3 completes; the commit lands; and because §0.1 forbids a sweeper under `FORCE` RLS there is **nothing left to redeliver** — the document sits at `Uploaded` **permanently, on a POST that returned 201**. That is A15-2 failing silently on the *success* path, which is the opposite of the property D3 exists to provide. **C6 splits the cases on `DeliveryCount`** — row present ⇒ complete, absent and `< 2` ⇒ abandon, absent and `≥ 2` ⇒ dead-letter `job-not-found` — capping abandons at two with no handler state. I record the limit honestly rather than claiming a fix: D2's "nothing is lost" is **withdrawn**, the residual stranding is bounded, alarmed and recoverable through D5's re-enqueue inside the tenant, and closing it completely would need exactly the cross-tenant sweep ADR-009 forbids. **Consequence handed to cloud-architect and delivery-manager: a non-empty DLQ now also means "an upload's commit failed", not only "a bug".** **C7 sets `MaxAttempts = 3`** — strictly-below is *necessary but not sufficient*, because deliveries and attempts do not advance together (a delivery is spent by any failure at or before the claim, including C6's abandons), so `4` would satisfy the inequality and still let the broker dead-letter **before** the handler wrote `Failed`; `MaxAutoLockRenewalMinutes = 30` confirmed against `lock_duration = PT5M`. **C8 repairs a stale constant my own round-2 footer left**: C3 renamed the subscription in prose but D2's code block still said `extraction-worker`, and D2 is the clause that names the constant the code carries — a receiver opened on a subscription Terraform never created, failing exactly where delivery-manager warned a broken worker still yields a **green** `backend.yml`. It is `document-processing`; the topic is re-verified correct and `modules/servicebus/main.tf` ends at `:24` with **no subscription resource of any name**. **C9 adopts ux-ui-designer's clause 2b into D7**, premise re-read at source rather than taken on their word (`isAttentionStatus` is `!== "Completed"`, oracle-derived and the **default** filter), so `needsAttention` becomes *not `Completed` and not `Rejected`* — plus two facts that are mine to state: `counts` is **five overlapping projections, not a partition** (no sum holds, and a test asserting one is wrong), and `all − needsAttention` must **never** be rendered as *askable* since it is the document-level fifth definition C5 refused. **C10 discharges delivery-manager's declaration**: `Raffa.Storage` carries **no `DbContext`, no migration, no `.sql`** — a provider adapter in the `Raffa.AiGateway` shape, not a domain module — so `backend.yml` is untouched and w15's CI-YAML set stays **ZERO**. **C11** makes security-architect's ids-only ask a **prohibition** (a path beside its own tenant id makes `EnsureWithinTenant` self-referential), and confirms `AzureAd__` is unclaimed anywhere in `backend/src`. **All three peer corrections to my files stand as adopted** — cloud-architect's worker key list, client-architect's generator recursion, security-architect's OQ-sec-04 and package-scoped `SdkAllowListTests`. **One ask of mine is refused and I do not hold it**: delivery-manager showed web-before-backend is unenforceable across two workflows and the window symmetric, and security-architect supplied the mechanism that makes the refusal safe — NW-05 **fails closed**, a bounded 401 window on `dev`, no header fallback re-added. Nothing I need is blocked. |
| cloud-architect | **APPROVE** — w15 footers appended to `ADR-005` (its third) and `ADR-007` (**its first**) and read back, both bodies intact above them; INDEX w15 section extended with the ADR-005 and ADR-007 paragraphs (no row removed, no new ADR from this seat); NW-27 / NW-67 / NW-68 / NW-05 cells filled; OQ-005 resolved in both halves; OQ-cl-01 and OQ-cl-02 added with assumptions in force. `none` recorded with reasons for **ADR-006** (the w14 footer already ruled `data_location`) and **ADR-008** (no Foundry change — no role assignment, no deployment, no key). **Fixed-cost delta $0.00 in both environments; no third fixed monthly line.** **One correction to a peer, verified line by line**: software-architect's §11.3 lists the worker as missing `AZURE_CLIENT_ID` and every `AiGateway__*` — it has all four (`containerapps/main.tf:277-306`, placed by task E10/F02/US01/T01); only `ConnectionStrings__Storage` is genuinely absent, and acting on the uncorrected list would add duplicate `env` names, an apply-time error. Ruling issued on security-architect's S15-26 PROPOSE (connection string stands for w15; `TokenCredential` recorded as the preferred later migration). Asks outstanding to security-architect (`User.Invite.All` vs the **Guest Inviter** directory role — one resource either way; the acceptability half of OQ-005(b); the `acs-connection` line in ADR-011; and that no mail body, accept token or recipient address reaches a log or an audit row), software-architect (confirm the `AzureAd__` prefix; keep **every** `ServiceBus__*` / `Invitations__*` binding optional and `ConnectionStrings__Storage` fail-fast; the handler's automatic lock renewal must match `lock_duration = PT5M`; the app's `MaxAttempts` **strictly below** `max_delivery_count = 5`) and delivery-manager (the **ADR-015** footer for the apply identity's Graph rights — the only deploy-identity change in the wave, not an ADR-016 ordering clause; **one** Terraform PR owns all of `infra/**`; the two HARD orderings; and the §7 finding that a worker which cannot start produces a **green** `backend.yml` run, so one post-deploy revision-state assertion is needed). **Re-entry round, 2026-09-13** — **ADR-005's second w15 footer (clauses 9–14)** written and read back at `:648-880` (the file's last line), with `## Decision outcome` (`:24`), the SKU table (`:34-57`), all three earlier amendments and the round-1 w15 footer (`:295-647`) intact **above** it, `Status: accepted` at `:3`, **nothing superseded**; INDEX's w15 section extended with the round-3 paragraph (no row removed, **no new ADR from this seat**). **ADR-007: `none` this round, with the reason** — `modules/identity` gains a *block*, not a module and not an edge, so its w15 footer is complete as written and a second one would say nothing. **The finding of this round is against this seat's own number, and it is arithmetic rather than opinion.** Discharging `MaxAttempts` produced ADR-027 §C7, whose sum closes *exactly* — 2 abandons + 3 attempts = 5 — while the sentence above it names a delivery that advances **no** attempt (a transient Postgres error at the claim), and **two more exist that this seat created and therefore must budget for**: **scale-in eviction** under `min_replicas = 0`, and the **30-minute renewal ceiling** against `lock_duration = PT5M`. Realistic worst case is **7 deliveries against a ceiling of 5**, so the broker dead-letters **before the handler writes `Failed`** — the exact failure §C7 exists to prevent, reached through the one path neither seat could see alone: software-architect owns the attempts, this seat owns the deliveries, and **only the pair is wrong**. **`max_delivery_count` moves `5` → `8`** with the inequality restated to carry slack (`≥ abandon cap + MaxAttempts + ≥2`); **`MaxAttempts = 3` stands untouched**, so no peer re-works anything and `3 < 8` holds *a fortiori*; the bound on a looping message is **time, not count** (`P1D` + dead-letter on expiry), so headroom cannot loop, and it costs **$0.00**. **Clause 11 fills a hole this seat left in its own §4**: NW-05's Azure delta was recorded as "four env vars and nothing else", but ADR-010 §2.4 assigns the **`email` optional claim** here — verified absent from **all** of `infra/` — and without it A15-4 dies at its last step on a 403 that reads as an auth bug. It is one ungated block on `azuread_application.api` (`identity/main.tf:57`), and **the plan must show an in-place update (`~`), never a replacement (`-/+`)**: replacing that registration mints a new client id and takes out `AzureAd__ClientId`, the `aud`, both SWAs and `web.yml`'s scope literals **behind a green CI run**. **Clause 10** accepts §C6's hand-off (a non-empty DLQ now also means *"an upload's commit failed"*) and lands it on **two operator checks**, recording that the dead-letter queue is **durable and swept by nothing** — which is what makes §C6's "recoverable" true rather than aspirational. **Clause 12** accepts security-architect's three conditions **verbatim** and offers one narrowing **against this seat's own draft**: a **topic-scoped** authorization rule in place of the namespace-wide one §1.3(2) named, which shrinks condition (iii)'s residual to the single DLQ ADR-009 §5 already bounds to ids — subject to proof, and if unprovable the namespace rule stands exactly as accepted. **Clause 13 answers OQ-w15-ca-01**: **no ingress ceiling is pinned and client-architect's 120 s stands**, because NW-27 makes the API's longest synchronous request *shorter*, and a request needing more than the platform default is a design defect, not a knob. **Clause 14** confirms delivery-manager's two asks (infra PR is **only** `infra/**`, which `infra/README.md` sits inside; `demo`'s flags default `false`) and **corrects the gate clause that answered my §7**: with `min_replicas = 0` a healthy worker has **zero replicas at rest**, so "a replica is running" fails on a healthy environment and "the revision exists" passes through a crash-loop — the assertion belongs against a worker **given work** (A15-2's walk), and the static half must state that zero replicas at rest is a **pass**. A gate check that fails when nothing is wrong gets waived, and the waiver is what the next silent worker death would hide behind. **Every ask this seat raised is discharged**: `User.Invite.All` ruled; the `AzureAd__` prefix confirmed unclaimed; every `ServiceBus__*` / `Invitations__*` key optional and `ConnectionStrings__Storage` fail-fast; `MaxAutoLockRenewalMinutes = 30` bound to `PT5M`; ADR-015 written to this seat's text; and §9.1's allow-list ask answered **project-complete** — the wave-wide map names **`Raffa.Worker`** (INDEX `:1005`), which no lane but this one would have needed, since it is this seat's RBAC ruling that puts `DefaultAzureCredential` in the worker. **Nothing is outstanding to me and I hold no objection.** |
| security-architect | **APPROVE** — five w15 footers written and read back, every body intact above them and **nothing superseded**: `ADR-010` (its second), `ADR-022` (its second), `ADR-025` (a new **§J**, §J.1–§J.10), `ADR-011` (its fourth), `ADR-009` (its second). INDEX w15 section extended with five paragraphs (no row removed, **no new ADR from this seat** — splitting tenancy, identity and secrets across a sixth file would fragment five ADRs that already own these questions). NW-67 / NW-68 / NW-05 / NW-06 / NW-58r cells filled; OQ-005(b) ruled, OQ-006 answered, OQ-007 given its ADR home, OQ-cl-01 and OQ-cl-02 answered for cloud-architect; OQ-sec-01, -02, -03, -04 added. `none` recorded **with reasons** for **ADR-015** (the Graph permission sits on the *runtime* workload identity, not a deploy SP, so CI gains no credential), **ADR-026** (the 201's shape is software-architect's; §J.4a constrains only its values), **ADR-016**, **ADR-020**, **ADR-005**, and **ADR-021**. **The finding that makes NW-06 urgent rather than tidy**: `WorkspaceRoleResolver`'s claims branch returns a role **without ever referencing its `tenantId` parameter**, so wiring JWT converts a dead branch into cross-tenant **destructive** access on the path that gates document delete and reprocess — and the instruction to keep it is in the **source** (`:20-22`), not an ADR, so the deletion takes the doc comment with it. **One correction to a peer, verified line by line**: `DependencyDirectionTests.ForbiddenSdkPrefixes` covers only the ADR-002 **domain-module** list (`SdkAllowListTests.cs:13-19`), so adding `Microsoft.Graph` there cannot enforce "one Graph call site" in a **host** — it belongs in `SdkAllowListTests`, package-scoped, because widening its single `AllowedProjectName` skip would make `Azure.AI.*` legal in `Raffa.Api` too. **One contradiction between two ADRs written at this table, ruled rather than left to the decomposer**: ADR-027 §D12 specifies a Key Vault connection string for Service Bus, ADR-005's w15 footer specifies RBAC with no secret — **identity and RBAC govern** (OQ-sec-04). **Two rejections I hold**: a test-only passcode seam (NW-58r), and the Graph `inviteRedeemUrl` in any response, mail, store, log or audit row. **Zero new Key Vault entries beyond `acs-connection`, and no new Key Vault permission.** Asks outstanding to software-architect (note the OQ-sec-04 reconciliation; the message carries **ids only and never a storage path**, because `EnsureWithinTenant` compares the path against the tenant *the caller passes*; the `SdkAllowListTests` amendment is **package-scoped**; re-issue **by replacement in one transaction** is ruled and accepted), cloud-architect (the `email` optional claim is a real Terraform addition — **no `optional_claims` block exists today**; the `listen`-only scaler fallback is accepted under three conditions), client-architect (**`X-Tenant-Id` stays a header, demoted to a membership-verified selector** — the answer to their question; and **no `roles` claim is read in the SPA**, which this seat co-signs), ux-ui-designer (**mail failed → show the link; identity provisioning failed → suppress the link**, because a link that cannot be redeemed is worse than none) and delivery-manager (NW-05 **fails closed** — a bounded 401 window on `dev` is correct and no header fallback is re-added; the Graph grant must not be able to error the shared run; **web before backend**). **Re-entry round, 2026-09-13** — **ADR-011's second w15 footer (clauses 6–13)** written and read back at `:355-521` (the file's last line), with the `## Decision outcome` (`:47`), the three earlier amendments (`:117`, `:128`, `:146`) and the first w15 footer's sections 1–5 (`:194-354`) intact **above** it, `Status: accepted` at `:3`, **nothing superseded**; INDEX's w15 section extended with the ADR-011 paragraph, appended before the `waves/w15.md` pointer line (no row removed, **no new ADR from this seat**). The other four footers (`ADR-010`, `ADR-022`, `ADR-025` §J, `ADR-009`) need nothing this round and no second footer is written for them — nothing at this round touched validation, the interim retirement, the invitation lifecycle or an RLS policy. **The finding of this round is against this seat's own ruling, and it is the one an ADR `none` hid.** At round one this seat recorded `none — ADR-015` because "the Graph permission sits on the *runtime* workload identity, not a deploy SP, so CI gains no credential" — both halves true, the conclusion wrong: it reasoned about the **deploy** identity and never about the **apply** identity, and creating an `azuread_app_role_assignment` **is itself a directory write**. Delivery-manager wrote the footer this seat called unnecessary and their clause 2 is right that it is needed; what no seat priced is **what the grant costs**, which is this lane's job alone. **`AppRoleAssignment.ReadWrite.All` is not a narrow right — it grants any application permission of any API, Graph's own `Directory.ReadWrite.All` and `RoleManagement.ReadWrite.Directory` included, to any service principal including itself.** Held by an *automation* identity triggered by a merge to `infra/**` (delivery-manager's own two-merge structure), it means whoever can merge Terraform can mint arbitrary directory privilege **in the customer's tenant**, inside a plan that reads as one assignment — for a need that is **one** assignment, of **one** permission, **once**. **And one of the three options at `ADR-015:126-128` cannot do the job at all**: Cloud Application Administrator excludes **Microsoft Graph application permissions**, and `User.Invite.All` is one — so the two that work are both tenant-wide escalation and the narrow-sounding one fails, routing an operator to a red apply and then, under time pressure, to an escalation. That is precisely the "discovered from a red run" outcome delivery-manager asked this seat to prevent. **The ruling costs nobody any rework**: `ADR-015:148-151` already carries the mechanism as a *degradation*, and clause 9 promotes it to the **default** — a Global Administrator makes the single assignment out of band, the resource stays `count = 0`, the later `import` leaves the apply identity needing only **read** (a read right cannot grant anything), and **revocation stays a human act** rather than something a merge can perform. The standing grant survives as the fallback under four conditions (never Privileged Role Administrator; exactly one assignment, and never one whose principal is the apply identity itself; re-reviewed each `infra/**` wave; recorded in the ADR-016 runbook with date and grantor). **Clause 10 makes it a gate check with a named expected outcome per shape** — a plan that errors on a directory right is the failure the check prevents, not its result — which **discharges delivery-manager's ask verbatim**. **Clause 11 co-signs cloud-architect's `~`-not-`-/+` rule as an *authorization* requirement**: `azuread_application.api`'s client id **is** the `aud` ADR-010 w15 §1.2 pins, so a replacement fails every token in flight at once and the repair a reviewer reaches for is the `ValidateAudience = false` §1.2 forbids — a destroy/recreate of that registration is a **security event**, and it would take the service principal (`:107`) and the pre-authorization (`:155`) with it. **One premise corrected at no cost to its conclusion**: ADR-005 clause 11 says A15-4 dies without the `email` claim; ADR-010 §2.4 already says the design does **not** depend on it because `oid` is bound at invite time, so it dies only if the §J.3 bind is *also* missed — recorded because the stronger reading invites a task to implement accept as an **email match** on a mutable, re-assignable identifier, which §1.2 forbids. The Terraform addition itself is this seat's own S15-9 and is **accepted unchanged**. **Clause 12 binds two peer clauses without objecting to either**: ADR-027 §C6's `DeliveryCount` split is evaluated **inside the message's own tenant scope** (a `job-not-found` that consulted every tenant would be the read primitive S15-43 denies; at `max_delivery_count = 8` a forged message buys eight scope entries, still ids-only, still no read, still bounded by `P1D`), and ADR-005 clause 12's **topic-scoped** narrowing of §2c(iii) is accepted as strictly stronger with the namespace rule standing if it proves inexpressible. **Nothing is outstanding to me, and the two rejections I hold are unchanged**: no test-only passcode seam, and the Graph `inviteRedeemUrl` in no response, mail, store, log or audit row. |
| client-architect | **APPROVE** — `ADR-012`'s **third** w15 footer written and read back (§1–§12), the `## Decision outcome` and **both** w14 footers intact above it, `Status:` still `accepted`, **nothing superseded**; INDEX w15 section extended with the ADR-012 paragraph (no row removed, **no new ADR from this seat** — the client half of this wave is one provenance rule extended and one mechanism replaced, not a new decision surface); NW-61 / NW-69 / NW-67 / NW-05 / NW-06 cells filled; OQ-008 answered and OQ-ca-01, -ca-02, -ca-03, -ca-04 added, one of them already resolved at this table. `none` recorded **with reasons** for **ADR-018** (no route is added, moved or removed this wave — recorded explicitly so no task re-opens the router table), **ADR-013** (mobile is a non-gating scaffold), **ADR-020** (states and copy are ux-ui-designer's; the failure copy is pre-decided in their w14 footer), **ADR-026** (the invite outcome's name and values are software-architect's; this seat constrains only its nullability), and **ADR-010 / ADR-022 / ADR-025** (security-architect's). **One correction to this ADR's own w14 clause 3, which is this seat's error and which a peer has already built on**: "a nested object renders `unknown`" is **false** — `renderSchemaType` recurses at `generate-api-client.mjs:110`, the contract already nests **three deep** (`raffa-api.v1.json:3103-3105`, `:5253-5261`) and the rendered result is on disk (`schema.ts:646`); only **`$ref`/`oneOf`** renders `unknown`, which the contract works around by duplicating shapes inline and says so at `:3910`. ADR-027 §D7–§D9 promoted `counts`/`readiness` on the false belief, whose likeliest repair in front of an `unknown` is a **hand-written DTO in `client.ts`** — the one prohibition ADR-012 `:53` has held since it was accepted. **Second finding**: `processingStatus` lives in **eight** contract sites, and the eighth (`:864-878`) is a **query parameter** the generator never parses, so a partial edit fails no build and no `tsc` check. **One withdrawal**: the intake's implied poll-predicate change — `hasNonTerminalRow` already reads the server array correctly. Asks outstanding to software-architect (land `counts`/`readiness` **inline**, no `$ref` at any depth; `readiness.stage` nullable ⇒ **no `enum`**; the invite outcome **non-nullable with `enum`**; the eight-site sweep is one task whose check is a grep, not a green build), ux-ui-designer (the **third Ask off-state**'s copy, OQ-ca-04 — it must not collapse into the "still processing" sentence; and the outcome-vocabulary mapping if theirs differs from the wire values), cloud-architect (pin the ingress ceiling or my 120 s stands, OQ-ca-01) and delivery-manager (**web deploys before backend** on NW-05 or `dev` locks everyone out; `web.yml:201-205`'s scope literal is a rename risk that fails **in the browser after CI is green**; NW-05's client task **does not share a phase** with any other task editing `client.ts`, and one task owns `raffa-api.v1.json` per phase). Nothing I need is blocked; the two rulings I asked for — `X-Tenant-Id` stays a header, and no `roles` claim in the SPA — were both given by security-architect. **Re-entry round, 2026-09-13** — **ADR-012's second w15 footer (§15–§20)** written and read back at `:814-1038`, the file's last content line, with the `## Decision outcome` (`:25`), **both** w14 footers (`:64`, `:189`) and the round-1 w15 footer (`:286-813`) intact **above** it, `- **Status**: accepted` at `:3`, **nothing superseded**; INDEX's w15 section extended with the round-3 paragraph, appended before the `waves/w15.md` pointer line (no row removed, **no new ADR from this seat**). **ADR-018, ADR-013, ADR-020, ADR-026: nothing this round and no second footer** — no route moved, mobile is untouched, and the states, the copy and the 201's shape are other seats'. **The finding of this round is the one nobody's lane could reach, because it needs the SPA's config path and the wave's merge structure at the same time.** ADR-005 clause 11 calls a replaced app registration *"an environment-wide sign-in outage behind a green CI run"*. Verified from this side it is worse: `web.yml:144-205` writes `dist/config.json` in the **deploy** job from four live Azure lookups plus two hardcoded scope literals, and `web.yml` fires only on `web/**`, `.github/workflows/web.yml`, `.github/actions/azure-login/**` and `scripts/write_web_runtime_config.py` (`:9-23`) — **no `workflow_dispatch`** — while `infra/**` sits in **neither** workflow's filter. So delivery-manager's infrastructure-only PR applies with **no web run at all**: the outage is behind **no run**, on `dev` there is **no manual redeploy**, and it is not loud at boot either, since `AppConfigError` (`appConfig.ts:44-50`) rejects only *missing or malformed* config and a stale-but-well-formed client id is neither — it validates, the app boots, and each user's sign-in fails in the browser. **§15's rule is a gate step, not a task**, and **w15's own answer is checked and negative**: the only registration this wave's apply touches is `azuread_application.api` (`identity/main.tf:57`), not the public client (`:116`), and a Container Apps FQDN is per app, not per revision — so no forced redeploy is owed, recorded as a negative result so the gate adds no step it does not need and the next `infra/**` wave asks rather than inherits. **§16 adopts clause 11's `~`-not-`-/+` rule and corrects its client-half collateral three times, none of which touches the rule, the ordering or the $0.00.** Both SWAs record the **public-client** id, not the API's (`web.yml:202` ← `tags.oidcPublicClientId` ← `identity/main.tf:43-44`; `appConfig.ts:25` names the field verbatim) — and clause 11's own next bullet (`:772-775`) says the public client is untouched, so that item contradicts the decision ten lines above it and is struck. The scope literals resolve against `identifier_uris` (`:70`), a **name-based** string re-created identically by a replacement; what a replacement actually takes out is the service principal (`:107`) and the pre-authorization (`:155-161`), turning every sign-in into a **consent prompt** rather than a clean failure. **And the inversion, which is the client-critical half**: the SPA binds to the **URI string**, so the shape clause 11 declares *safe* is the one that breaks it — an in-place `~` changing `identifier_uris` **passes** the check and silently breaks every login, because `web.yml:204-205` is a hand-copied duplicate of `identity/main.tf:70` with no test, no build step and no plan assertion comparing them. **The plan gains a second, different assertion: `identifier_uris` shows no diff at all.** The provenance of the error is in my own file and I name it rather than blame the peer: `appConfig.ts:30` still documents the scope as `api://<api-client-id>/…`, the form this product does not use. **§17 is a second finding, and it is against a property withdrawn at this very round.** ADR-027 §C6 honestly retires D3's *"the database owns the terminal state"* — which is exactly what made this ADR's 2 s poll terminate. `useDocumentsList.ts:105-108` polls while any row is `Uploaded`/`Processing` and `:110-119` clears the interval **only** when that boolean flips, so §C6's permanently-stranded row leaves an open tab polling `GET /api/documents` **every 2 s indefinitely** (~1,800 requests/hour/tab) beneath a *not ready yet* state that is **true and never resolves**; §C6's recovery is wholly server-side and the client is never told. The poll gains a **no-change budget** with an explicit resume, under three prohibitions that are each a repair a task would otherwise reach for — never re-label the row `Failed` (the server says `Uploaded`; that is the fabricated fact §D8 and A15-3 forbid), never a client timer feeding the bar, never a second definition of terminal. Assumption in force: five minutes, one constant, no endpoint and no contract change; the stopped state's sentence and the resume label are ux-ui-designer's — **OQ-w15-ca-05**, the one question I add this round. Cost stated so the table may decline it knowingly: it is a small edit in a file NW-61's counts task already owns (§13.8), and deferring it ships an unbounded poll against a stranding case this wave itself introduces — legitimate, but it must be recorded rather than silent. **§18 closes the counts clauses with nothing left to choose**: §C9 adopted my `isAttentionStatus` ask in full (third site, checked by grep not `tsc`), §C5 with ADR-018 clause 3 resolves my §13.6 to its second branch, and the one mechanism still missing was a **signature** — `buildKbSummary` (`documentTable.ts:154-160`) becomes a function of `counts` (`all`, `needsReview`), after which **no page-derived number survives screen 3**; §C9.1's "five overlapping projections, not a partition" lands as a client prohibition, so `all + rejected` is the only true total and no surface shows it, correctly. **All four of my open questions are discharged** — ca-01 by ADR-005 clause 13 (no ceiling pinned, 120 s stands, and their reason beats my ask: NW-27 makes the longest synchronous request *shorter*), ca-02 scheduled by delivery-manager as a numbered B2B walk, ca-03 ruled a membership-verified header, ca-04 answered by ux-ui-designer without collapsing the third variant. **One ask of mine is refused and I accept the refusal**: "web before backend" is unenforceable across two workflows on one push and the window is symmetric, and security-architect supplied what makes it safe (NW-05 fails closed, bounded 401, no header fallback). **Nothing is outstanding to me and I hold no objection.** |
| ux-ui-designer | **APPROVE** — three w15 footers written and read back, every body intact above them and **nothing superseded**: `ADR-020` (**its fifth** — §0 the wave-wide rebrand rule, §1 screen 3, §2 screens 2/5/6, §3 screen 10, §4 **surface 12, the invitation email**), `ADR-018` (**its fifth** — the states contract gains **not ready yet** and **nothing made it through**; what each Documents number counts; the rail-badge rule), `ADR-019` (**its second** — **one** semantic row, derived from the shipped card; no token, no component). INDEX w15 section extended with three paragraphs (no row removed, **no new ADR from this seat** — four items that are states, counts and copy do not earn a new decision surface, and ADR-018/019/020 already own every question asked). NW-27 / NW-61 / NW-68 / NW-69 cells filled; **OQ-w15-ca-04 answered** for client-architect; **OQ-w15-D1 dissolved** and OQ-w15-D2 brought onto the register; four rows added to "Design refs carried into decisions". `none` recorded **with reasons** for ADR-021/024/027, ADR-012, ADR-013, ADR-026 and — three times over — for ADR-018 and ADR-019 where an item needed nothing from them. **Two corrections of my own work, both found by reconciling rather than by re-reading myself.** (1) My lane §A4 ruled that "All documents · N" **counts** refused files; **withdrawn** against product-owner's row and ADR-027 §D7. The replacement is better than either position: every number reads **exactly one server field**, and a **third chip `Not added · K`** consumes `counts.rejected`, which had no consumer. It is also the only arrangement in which product-owner's *"persistent, visible, terminal"* row is **reachable at all** — with two chips and `counts.all` excluding `Rejected`, the row exists on the server and **no filter shows it**. (2) My lane §D2 designed a fourth invite outcome with the link suppressed; **retired**, because software-architect and security-architect ruled that a provisioning failure **aborts the invitation** — my reasoning reached "suppress the link", their mechanism reached "there is no link", and theirs is strictly stronger. **One affordance is refused**: 10.1's pre-decided **"Try sending again" does not ship**, because the server cannot re-send the original link (the token is a SHA-256 hash), so any retry is a re-issue that kills the link the Admin is looking at — the precedent is this ADR's own w14 §2, *an affordance with no legal mechanism is not shipped*. **Two findings no other lane had named**: `kbSummary`'s "**M askable**" is page-scoped *and* equates askable with `Completed`, which ADR-026 §D2 rules is not the definition — the same fabricated-fact class as `AskCopilotService.cs:294`, one screen earlier; and the V2 export is **pre-rebrand throughout**, so any task retyping copy out of it silently reverts "Raffa.ai" to "Raffa". **One hazard for the decomposer**: adding `"rejected"` to `RowStatus` while forgetting `semantics.ts:52` **compiles clean** — `documentTable.ts:97-99` is an unchecked cast — so that edit is two files and `tsc` will ask for only one. Asks outstanding to software-architect (`counts.needsAttention` must exclude `Rejected` as well as `Completed`; **`counts` gains `askable` under ADR-026 §D2, or screen 3's segment is removed**; `readiness` must distinguish *in flight* from *terminal and empty*, or Contract 360 cannot render its second reading) and product-owner (the OQ-w15-D2 assumption is theirs to overrule). **`HITL_CLAUDE_DESIGN`: four exports owed** — the pane's three outcome states plus its blocking-error state; a **Not added** row and a **Queued…** row and the third chip; Portfolio and Contract 360 in their new zero states; and **the invitation email**. **None blocks w15**: every state is decided from ADR-019's locked catalogue and the export's own idiom. **Re-entry round, 2026-09-13** — three footers re-read on disk (`ADR-020:573-931`, `ADR-018:301+`, `ADR-019:232+`), bodies intact above each, `Status:` still `accepted`, nothing superseded; INDEX paragraphs at `:815`, `:839`, `:855`. **Two of my three asks to software-architect are answered in ADR-027 as promoted and the third is a correction I have now written into my own file**: `readiness.state` distinguishes `ready` / `processing` / `unavailable` (`:311-312`), which is exactly what Contract 360's two readings need — **answered**; `counts` lands with no `askable`, so ADR-018 clause 3's **branch two is in force and the segment is removed** — **resolved, not left open**; and §D7's `needsAttention` = `NeedsReview` + `Failed` rests on a premise that is false in one line (`isAttentionStatus` is `!== "Completed"`, `documentTable.ts:141-143`), which under the **default** filter would put fifteen just-dropped rows beneath "Needs your attention · 0" — NW-61's own reported defect, re-created server-side. Recorded as **ADR-018 w15 clause 2b** with the rule that decides it (*a chip's number and the rows it filters to are the same set*) and the alternative reconciliation named and rejected; the edit is software-architect's, in their file. **I do not hold this as an objection**: the oracle is unambiguous (`app.jsx` `attnDocs`, the shipped filter, `getFilterHint`, R-DOC-06), my own ADR governs what the number on the screen means, and it is on disk. **Re-entry round, second pass, 2026-09-14** — **three second w15 footers written and read back**: `ADR-020:932-1115` (its sixth amendment), `ADR-018:456-504` (its sixth), `ADR-019:296-337` (its third); in each the `## Decision outcome` (`ADR-020:38`, `ADR-018:46`, `ADR-019:42`), both w14 footers and the round-1 w15 footer (`ADR-020:573-931`, `ADR-018:301-455`, `ADR-019:232-295`) are intact **above** the new one, `- **Status**: accepted` at `:3` on all three, **nothing superseded**; INDEX's w15 section extended with the round-3 paragraph at `:1222-1260`, appended before the `waves/w15.md` pointer line, still last at `:1262` (no row removed, **no new ADR from this seat**). OQ-w15-ca-05 is on the register as answered and one row is added to "Design refs carried into decisions". **Two of this round's three findings are against my own round-1 text, and the first stops a deletion I ordered.** §1.3 routed all three refusal sentences into the **server row's** hint slot (`DocumentStatusTable.tsx:138`) — and only one producer of four ever has a server row: the browser-side oversize check makes **no HTTP call at all** (`uploadPipeline.ts:105-108`) and 413/415 (`:122-125`) is kept **in-request** by product-owner's split gate. Client-architect's local-row mechanism is **adopted in full**, because it is what makes my own rule — *one file, one row on every path* — true rather than aspirational; their offer to keep the card for those two paths is **declined**, since a card *and* a grid for one class of outcome is precisely what §1.3 removed. Second correction: the oversize sentence drops its interpolated filename (the row has a filename cell), licensed by **provenance rather than taste** — it is app-authored, while `getRejectionReasonCopy`'s two sentences are requirements copy (`requirements.md:186`) and lose only the lead-in. **The finding no lane had named**: `:123` renders `result.error ?? "Not added: this file could not be added."` — a user-facing sentence **the API authors**, with no oracle behind it, on the one refusal path NW-27 does not remove. Two designed sentences replace it, and the rule generalises the convention NW-67 already uses: **a server field may *select* the sentence a user reads; it may never *supply* it.** **The second finding is a missing wire under a row this table has already ruled durable**: ADR-027 §D6 persists the rejection reason (`:217-218`), but §D7's `items[]` (`:246-256`) and §11's contract list (`:467`) carry **no reason field** — so on the very reload §1.1 exists to survive, "Not added" renders with an **empty hint**: persistent and visible, and no longer legible. Ask to software-architect (their file): a reason **code** from the closed set their own §D6 cites (`AdmissionDecision.cs:23-32`), never a sentence. Assumption in force is **tag and no hint** — never a client store remembering this session's 422, because a blank hint is a gap a user can ask about and a remembered one is a lie the next browser tells. **OQ-w15-ca-05 is answered and then widened.** The stopped poll is a **list-level notice**, not a row state and not an error treatment: *"Nothing has changed for five minutes, so this page stopped checking for updates."* + **"Check again"**, `.hint` + `.btn-secondary` (`DocumentStatusTable.tsx:100-103`), the actor being **the page** and never Raffa.ai, never implying failure, and never claiming work is in progress — in §C6's stranded case nothing is. The widening is the part §17 could not see from inside one hook: NW-61 turns Ask's **one-shot** gate fetch (`ask/index.tsx:100-105`, verified) into a 2 s poll, so this wave adds a second unbounded wait on the surface with no row grid and whose whole promise in that state is *we will let you in when it is ready*. ADR-018 clause 6 states it once at IA level and binds five surfaces to one budget, one sentence, one label. **One hazard handed to the decomposer, worse than clause 2's**: `DocumentStatusTable.tsx:94-96` maps a local entry with a ternary, so widening `LocalUploadEntry.phase` without a third branch **compiles clean** and renders a refused file as **Processing** forever — the refusal edit is a **three-site** edit and `tsc` asks for none of them. **Nothing is outstanding to me and I hold no objection**: my one ask this round is a field in software-architect's file, it gates no task, and its assumption is on disk. |
| delivery-manager | **APPROVE** — three w15 footers written and read back, every body and every earlier footer intact above them, `Status: accepted` on all three and **nothing superseded**: `ADR-014` (its **second**, clauses 1–7), `ADR-016` (its **third**, clauses **13–22**), and `ADR-015` (**its first amendment since 2026-09-01**, clauses 1–5, written to cloud-architect's text at their request). INDEX w15 section extended with three paragraphs (no row removed, **no new ADR from this seat** — a wave's order, its applies and its promotion are three existing decision surfaces, not a fourth). W15-01 / NW-27 / NW-67 / NW-68 / NW-05 / NW-58r cells filled; OQ-001 resolved, OQ-006's "where it runs" half answered, and **OQ-dm-01…dm-04 added** (numbered `dm-` because product-owner's lane already holds OQ-w15-009). **The structural decision of this seat's wave**: a wave that changes `infra/` has **two merges to `main`** — one infrastructure-only PR first, then the wave PR — because an HCP apply is triggered *by* the merge, so a single merge event cannot satisfy either of cloud-architect's HARD orderings; it is safe by construction, since `infra/**` is in neither `backend.yml`'s nor `web.yml`'s path filter, and it is what makes their "the apply can land days before the code" property actually reachable. **Two findings no requirements document carries.** (1) **w14 was never promoted**: the highest tag is `demo-v3` of **2026-09-04**, there is no `demo-v4`, so `demo` is nine days and one whole wave behind, and w15's promotion would silently carry two waves plus three data-plane steps that are w14's debt — `demo-v4` is cut at the gate and w15 promotes as `demo-v5`, which is also the tag cloud-architect's runbook assumed and is correct only because of it. (2) **The wave base's SHA was stale before the requirements were written** (`origin/main` was `6ae21b9` on disk against the document's `3c89d35`), and `packed-refs` is stale for **every** branch ref this wave touches — so the rule is *read the loose ref at the gate*, and the merge is proven by a **zero product-tree delta**, not by "the two cited files resolve". **One refusal, against three seats and with evidence rather than authority**: "web deploys before backend" is not enforceable (two workflows, one push, no cross-workflow dependency) and would not help — the outage window is **symmetric**, and the requested direction is the one that can land **writes attributed to nobody** through the nine service paths that still default the actor. Backend-first is the cleaner direction; the acceptance walk starts when both deploys are green. **Two corrections of my own lane, both adopted from peers**: the admin-consent step I scheduled does not exist for a managed identity (the `azuread_app_role_assignment` **is** the consent; what needs a one-time grant is the **apply** identity, which is why it belongs in ADR-015 and not in an ADR-016 ordering clause), and my hold-back of the worker's scaling from `demo` on cost grounds is **withdrawn** — scale-to-zero plus a queue-depth rule costs nothing idle, so there is nothing to hold back. `none` recorded **with reasons** for **ADR-005 / ADR-007** (resources and layout are cloud-architect's), **ADR-025 §H / §J.8–§J.9** (the seam refusal and S-T23 are security-architect's — I proposed the wording, they wrote it), **ADR-010 / ADR-022 / ADR-012 / ADR-026 / ADR-027 / ADR-021** (adopted as ruled), and **ADR-015 for NW-05** (four non-secret keys are configuration, not a CI credential — NW-67 is this wave's only deploy-plane change). **w15's planned CI-YAML set is ZERO files**, and the two temptations are named so they are refused rather than discovered: `backend.yml`'s stale "eight"/"six" strings stay parked, and `reprocess-tenant-documents.yml` — which **NW-05 takes out of service**, failing on a *read* before any write, so there is no partial run — is neither repaired nor edited, because NW-31 owns it in W16. Asks outstanding: to **cloud-architect**, that the infrastructure PR contain **only** `infra/**` and that the `demo` root's flags default `false`; to **security-architect**, that the apply identity's Graph rights be verified at the gate rather than discovered from a red run; to **software-architect**, that a new .NET module with its own migration script be declared **at this table** (it is the one thing that moves the CI-YAML set from zero to one); to **the decomposer**, one Terraform task with **no dependents**, NW-05's client task alone in its phase among `client.ts` writers, and NW-58r after both W15-01 and NW-67. Nothing I need is blocked. **Re-entry round, 2026-09-14** — **ADR-015's second w15 footer (clauses 6–8)** and **ADR-016's second w15 footer (clauses 23–26)** written and read back at `ADR-015:165` and `ADR-016:558`, each the file's last section; above them `- **Status**: accepted` at `:3`, the `## Decision outcome` at `:39` on both, ADR-016's **two** w14 footers (`:93`, `:177`) and both round-1 w15 footers (`ADR-015:97`, `ADR-016:294`) **intact**; **nothing superseded**. INDEX's w15 section extended with the round-3 paragraph, appended before the `waves/w15.md` pointer line (no row removed, **no new ADR from this seat**). **ADR-014: nothing this round and no second footer, with the reason** — clauses 1–7 govern the base, the merge test and the two-merge structure, and nothing at this round touched any of them; a second footer would restate them. **Three of this round's four findings are against this seat's own promoted clauses, and the two that matter most were found by peers reading my files rather than by me re-reading them** — which is the argument for the re-entry round itself. (1) **ADR-015 clause 2 offered three options for the apply identity's rights and one of them cannot perform the assignment**: Cloud Application Administrator excludes Microsoft Graph *application* permissions and `User.Invite.All` is one (`ADR-011` §8). It is the narrowest-sounding entry, so it is the one a least-privilege operator reaches for first — and it fails **at the apply**, producing precisely the red HCP run my own clause 2 closes by forbidding. Struck. (2) **The default inverts** (clause 7): security-architect priced what I had only scheduled — `AppRoleAssignment.ReadWrite.All` grants any application permission of any API to any service principal **including itself**, so on an automation identity triggered by a merge to `infra/` — **my own two-merge structure** — whoever can merge Terraform can mint arbitrary directory privilege in the customer's tenant, for a need that is one assignment of one permission once. The out-of-band grant becomes the default, the standing grant the conditioned fallback, and clause 8 gives the gate a named expected outcome **per shape** so it proves the answer either way. Neither costs any seat rework: both options were already on my page and this decides which. (3) **ADR-016 clause 17's assertion failed on a healthy environment.** With `min_replicas = 0` — kept, because `min_replicas = 1` is a fixed ~$14/env/month line the lock forbids — a healthy worker has **zero replicas at rest**, so *"a replica is running"* fails when nothing is wrong while *"the revision exists"* passes through a crash-loop. Cloud-architect supplied the constraint and asked this seat for the wording; clause 23 splits the check, and **the static half now states that zero replicas at rest is a PASS**. The rule is the reusable part: **a gate check that fails when nothing is wrong gets waived, and the waiver is what the next silent worker death hides behind** — which would have undone the whole point of clause 17. (4) **Two additions that are not corrections**: clause 24 makes the dead-letter queue a **standing condition to be read** — empty before the acceptance walk, afterwards read with ADR-027 §C6's two meanings (`job-not-found` ⇒ an upload's commit failed; anything else ⇒ a defect) and **routed, never drained**, because nothing sweeps it and ADR-009 forbids a cross-tenant sweep; and clause 25 adds **two different** plan assertions on one resource — `azuread_application.api` shows `~` never `-/+`, **and** `identifier_uris` shows no diff at all, the second being the inversion of the first, since the SPA binds by URI *string* and the shape the first declares safe is the one that silently breaks every login. **One finding of my own this round, recorded as a negative result rather than omitted** (clause 26): client-architect showed that `web.yml` builds `dist/config.json` in its deploy job and has **no `workflow_dispatch`** — I verified it on this tree, the triggers are `pull_request` (`:10`), `push` (`:17`) and `workflow_call` (`:28`) only — so the inertness that makes my infrastructure-only PR safe has an edge: a PR 1 that ever changed a value the SPA's config is built from would leave the outage **behind no run at all**, with no manual redeploy on `dev` and no loud failure at boot. **For w15 it is checked and negative** — this apply touches `azuread_application.api`, while both SWAs record the public-client id — so the gate adds no step it does not need, and it becomes a question the next wave that changes `infra/` **asks rather than inherits**. **Every ask I carried is discharged**: cloud-architect confirmed the infrastructure PR is `infra/` only and `demo`'s flags default `false` (`ADR-005` §14); security-architect verified the apply identity's rights at the gate and went further by re-pricing the grant; software-architect declared `Raffa.Storage` carries **no `DbContext`, no migration, no `.sql`** (`ADR-027` §C10), which is the one declaration that could have moved the CI-YAML set off zero — **it stays ZERO files**; and my refusal of "web before backend" was accepted on evidence by both seats that asked for it. **Nothing is outstanding to me and I hold no objection.** |

## Gate verdict

pending
