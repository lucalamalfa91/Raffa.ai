# Wave w19 — architecture decision record

Generated by `next-intake` (skeleton); completed by the operator from the five
lane drafts after `next-council-close` truncated on DeepSeek (tool-role 400).
ADR actions are owned by the seat named; files cited exist on disk.

Wave theme: **Ask Raffa wow answers** — evidence-based savings, a bound notice
date, and a negotiation strategy that writes Renewals. 22 `must` items; five
seats involved, cloud-architect and delivery-manager `PASS`. The nine
product-owner locks (2026-09-16) and the "still as written" table A–E are
binding and are recorded, not re-decided.

| Item | Seats | Decision | ADR action | File |
|---|---|---|---|---|
| NW-76 — engine consumes `scopeContractId` | sw, client | **software-architect:** pass `Conversation.ScopeContractId` into `AskAsync`; deixis = that id; scoped id wins over same-name; unseen id → 404/N11. **Lock 4:** `PortfolioMarketPosition` still ranks the workspace portfolio even when scoped. **client-architect:** leave the existing create plumbing alone — do not re-derive scope from the URL on resume. | `none` — ADR-024 already owns one engine / scoped entry (w18 NW-56); this is the wiring of that rule. | — |
| NW-77 — 360 bar passes origin scope | client, ux | **ux-ui-designer:** IN — no new chrome on the originating bar; the visible binding is NW-78's chip in chat. **client-architect:** from `/contracts/:contractId` the bar navigates `/ask?scope=<id>` (query string, reuses w18 AskRoute parse), not a new nav-state field. | **client-architect:** amend ADR-012 (cl. 49). **ux-ui-designer:** amend ADR-020 (37.1). | ADR-012-web-stack.md, ADR-020-web-screen-inventory.md |
| NW-78 — persistent binding chip | client, ux | **ux-ui-designer:** IN — `.tag-neutral` `{supplierName} · {type}` in the scope line, linking `/contracts/{id}`; never a guid; unrendered while the contract is not yet resolvable. **client-architect:** chip reads the conversation's persisted `scopeContractId`, never the transient `?scope=` query. | amend ADR-012 (cl. 49) + ADR-020 (37.2) | ADR-012-web-stack.md, ADR-020-web-screen-inventory.md |
| NW-79 — intent lexicon | sw | Planner test table (IT+EN) for the three screenshot sentences. New `AskIntent.PortfolioMarketPosition`; `risparm*` reaches portfolio before `mercato`→QuoteRoute; notice/preavviso structured; `contrattare`/`rinnovo` hit `RenewalStrategyPattern`. No phrasing falls to unscoped Clause RAG when facts exist. | amend ADR-024 | ADR-024-ask-raffa-v2.md |
| NW-80 — supplier/contract resolution | sw | Exact → normalized/contains; multi-contract uses scoped id else soonest deadline; named pack item, never merge. Host composition (`Raffa.Api`); `Raffa.Insights` stays fenced. | amend ADR-024 | ADR-024-ask-raffa-v2.md |
| NW-81 — tenant RAG filters to contract | sw | `EmbeddingSearchQuery` with `contractId`: "this contract" slice + labelled "similar types" peer slice (same type/category, lower K). Market stays on `IMarketKnowledgeRetrieval`. RLS remains the backstop (ADR-009, not re-decided). | amend ADR-024 | ADR-024-ask-raffa-v2.md |
| NW-82 — priced-lines parity | sw | Ask uses async `ToPricedLines` + `BenchmarkKeyResolution` (same numbers as 360 `/strategy`). Missing band → "insufficient market data", never a fabricated cross-line delta. | amend ADR-024 | ADR-024-ask-raffa-v2.md |
| NW-83 — citations carry ids | sw, client | **software-architect:** pack stamps real `contractId`+`documentId`+`page`+span/clause; no citation without a pack source. **client-architect:** two-CTA card (lock 5) — 360 + viewer-at-span; stop synthesising `href` from a bare `page`. | **software-architect:** `none` — ADR-024 already owns "no citation without a pack source". **client-architect:** amend ADR-012 (cl. 50). | ADR-012-web-stack.md |
| NW-84 — Renewals honours `?select=` | client | Read `useSearchParams().get("select")`; matching listed id becomes initial selection; invalid/absent → default top-priority row, never a 500. Prerequisite for NW-90/97 deep-links. | amend ADR-012 (cl. 51) | ADR-012-web-stack.md |
| NW-85 — Renewals negotiation TODO list | sw, client, ux, sec | **software-architect:** new entity `renewal_negotiation_todo` keyed `(tenant_id, contract_id, point_key)`; host upsert after rank before answer; idempotent (same key updates, never un-ticks Done, vanished→Superseded); GET + tick PUT. Do not reuse `RenewalAction` or `ContractNegotiationStep`. **security-architect:** ordinary tenant table (ENABLE+FORCE RLS, `USING`+`WITH CHECK`); actor = token subject, never the model; audit `renewal.negotiation_todos_written` post-durable; tick PUT = Procurement/Admin, authz before retrieval. **ux-ui-designer:** `.table` sub-surface, Mark-done `.btn-secondary`, Open/Done tags. **client-architect:** read-back + tick; never invent a point. | amend ADR-003 + ADR-009 + ADR-011 + ADR-028 + ADR-012 (cl. 52) + ADR-020 (41) | ADR-003-data-store.md, ADR-009-tenancy.md, ADR-011-secrets-and-rag.md, ADR-028-server-side-state.md, ADR-012-web-stack.md, ADR-020-web-screen-inventory.md |
| NW-86 — portfolio market not Quote check | sw, po | **product-owner:** IN — R-SYS-02 narrowed (lock 6): Quote check is a *new market proposal*; portfolio mal-position / 2026-savings is answered in Ask, never routed to Quote check. **software-architect:** new `AskIntent.PortfolioMarketPosition`; planner reorder; QuoteRoute short-circuit removed for this sentence. Single-supplier "is X above market?" stays `MarketCompare`. | **product-owner:** `none` — scope already locked 2026-09-16. **software-architect:** amend ADR-024 (the routing mechanism). | ADR-024-ask-raffa-v2.md |
| NW-87 — candidate set option 3 | sw | Validated **and** `Status="Active"` **and option 3 (lock 2)**: all Active impacting 2026 costs (paying in 2026 / driving spend), not "renewal ∈ 2026 only". Ended 2025-12-31 out; 2025–2028 term in; starts 2026-06-01 in. Bound N×lines, batched. ADR-024 w15 cl. 4's validated-only stands and is **not** the whole set. | amend ADR-024 | ADR-024-ask-raffa-v2.md |
| NW-88 — mal-position % calculator | sw | `linePct = (unitPrice − media)/media × 100` (media = P50). Listed iff any line >0 **or** worse condition. Contract % = spend-weighted avg of positive linePcts. `HasSufficientData=false` → omit %. Never a model float; NumericGuard binds it. Do not reuse Renewals `DetermineMarketPosition`. | amend ADR-024 | ADR-024-ask-raffa-v2.md |
| NW-89 — two-bucket list + cards | sw, client, ux | **Lock 2 buckets, not "have data / no data".** (a) actionable-in-2026 → TODOs/Renewals + follow-up chip per supplier; (b) impacts-2026-but-locked → 360 link only, no 2026-save TODO. **ux-ui-designer:** two labelled `.card` groups in one `answer`. **client-architect:** render server facts, never compute % or bucket. Honesty locks 3/9. | **software-architect:** amend ADR-024. **ux-ui-designer:** amend ADR-020 (38). **client-architect:** `none` — ADR-020 owns cards. | ADR-024-ask-raffa-v2.md, ADR-020-web-screen-inventory.md |
| NW-90 — follow-up writes TODOs | sw, client | Resolve X (NW-80); multiple X spelled out, never merged. Top-3 in chat (lock 7), persist **all** TODOs (`persistTodos=true`); server-injected `/renewals?select={id}`. Locked rows never get a 2026-save TODO. **client-architect:** render follow-up chip + injected action, never model `actionKeys`. | **software-architect:** amend ADR-024 + ADR-028. **client-architect:** `none` — ADR-028/024 own write-back. | ADR-024-ask-raffa-v2.md, ADR-028-server-side-state.md |
| NW-91 — notice structured pack | sw | notice/preavviso/disdetta → structured pack (endDate/cancellationDeadline/autoRenewal/renewalTermMonths + `WhenYouMustMove` + evidence). RAG only as fallback filtered to the contract (NW-81). | amend ADR-024 | ADR-024-ask-raffa-v2.md |
| NW-92 — notice days date+days-if-in-span | sw | Answer the **date**. "N days" only if N is in the cited clause; never model-subtract. No `cancellationNoticeDays` column this wave (R5a → W20). | amend ADR-024 | ADR-024-ask-raffa-v2.md |
| NW-93 — two-CTA card | client, ux, sw | **ux-ui-designer:** one `reply.actions[]` of two — `.btn-primary` "Open contract" + `.btn-secondary` "Open at this span" (viewer route ADR-018 w17 cl. 9), never two nested buttons. **client-architect:** same two-CTA kind as NW-83; span is the notice clause. **software-architect:** payload carries the ids; no new reply `kind`. | **client-architect:** amend ADR-012 (cl. 50). **ux-ui-designer:** amend ADR-020 (39). **software-architect:** `none` — ADR-024 reply contract already on disk. | ADR-012-web-stack.md, ADR-020-web-screen-inventory.md |
| NW-94 — notice fallbacks | sw, client | Five server-decided cases: deadline+evidence → answer+deep-link; deadline no-span → date + Review CTA; no deadline, clause text → quote + Review; neither → abstain naming this contract + 360 Review; unscoped deictic → abstain + Portfolio. Scoped answers never ask "which supplier". **client-architect:** render verbatim, never synthesise a recovery. | **software-architect:** amend ADR-024. **client-architect:** `none` — ADR-024 owns fallback semantics. | ADR-024-ask-raffa-v2.md |
| NW-95 — Q3 plans `RenewalStrategy` | sw | Q3 → `RenewalStrategy` named (AsterCloud in the fixture); corpora calc+tenant(+market+raffa Renewals); missing deadline = named point, not abstain. `kind=answer` ranked, not abstain. | amend ADR-024 | ADR-024-ask-raffa-v2.md |
| NW-96 — `NegotiationPointRanker` | sw | Point = topic/rank/current/target/whyItMatters/citationKeys/strength; **only grounded points**. Shared `BuildNegotiationPointsPackAsync(contractId, includeRenewalUrgency, persistTodos)`. Insights stays `[SharedKernel, Benchmark]`. Chat cap top 3; persist all. | amend ADR-024 | ADR-024-ask-raffa-v2.md |
| NW-97 — Q3 persist + deep-link | sw, client, ux | After rank, upsert all TODOs; answer = top-3 markdown + citations + server-injected `/renewals?select={guid}` (never model `actionKeys`). **ux-ui-designer:** ranked list + honesty line "N more points saved in Renewals." **client-architect:** render the injected action verbatim. Re-ask: no duplicate Open, Done preserved (server). | amend ADR-024 + ADR-028 + ADR-012 (cl. 53/55) + ADR-020 (40) | ADR-024-ask-raffa-v2.md, ADR-028-server-side-state.md, ADR-012-web-stack.md, ADR-020-web-screen-inventory.md |

`Decision`: go/no-go + a one-sentence ruling per item, or `task only` where no
seat decides beyond the already-locked spec.
`ADR action`: `amend ADR-NNN` | `none — <why>`. `File`: the ADR path written
or amended.

## Design refs carried into decisions

| Item | Oracle | Anchor |
|---|---|---|
| — | none (this wave's Ask wow answers are acceptance-specified, not export-specified; HITL_CLAUDE_DESIGN recorded in ADR-020 w19, blocks nothing) | — |

## Votes (final table round)

Operator-recorded from the lane drafts after the live table truncated. Involved
seats' drafts are on disk under `reports/architecture/draft/next/<seat>/w19.md`.

| Seat | Vote |
|---|---|
| product-owner | APPROVE |
| software-architect | APPROVE |
| cloud-architect | PASS |
| security-architect | APPROVE |
| client-architect | APPROVE |
| ux-ui-designer | APPROVE |
| delivery-manager | PASS |
| council-gate | operator close (table truncated; five checks held on disk — see Gate verdict) |

## Gate verdict

**Operator-closed 2026-09-16.** `next-council-close` on DeepSeek ended
`truncated` (OpenAI-compat tool-role 400) before emitting the close markers.
The five gate checks of `council-protocol-next` are satisfied on disk:

1. **Votes** — involved seats APPROVE; uninvolved PASS (roster §3).
2. **Decision record** — every involved-seat item has a Decision and an ADR
   action (none unfinished); every `amend` names a file that exists and carries
   `## Amendment (2026-09-16, wave w19`.
3. **INDEX** — no new ADR; no existing INDEX row removed; w19 section appended.
4. **Bodies** — original `## Decision outcome` intact above each new footer.
5. **Locked files** — nothing under `reports/architecture/draft/<seat>/`
   (initial council), no earlier wave file, no `inputs/**` rewritten.

```
COUNCIL_FILES_WRITTEN: waves/w19.md + 7 ADR files
COUNCIL_APPROVED: w19 — 22 decisions, 7 ADRs amended, 0 new, 5 no-change
```
