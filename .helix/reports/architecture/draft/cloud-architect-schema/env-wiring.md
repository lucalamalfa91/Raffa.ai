# cloud-architect-schema — draft

Add `ConnectionStrings__Savings` and `ConnectionStrings__Quotes` → `pg-cs`.
HCP apply only. Key Vault `postgres-connection` is the single connection
string CI and apps share.
