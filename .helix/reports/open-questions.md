# Open questions

Decisions this run needed and did not have. Every entry carries an **assumption
in force**, so the pipeline can keep moving. This file is not a halt.

Status: `open` · `answered` · `assumed-confirmed` · `assumed-wrong`

---

## How to use this file

**Agents**: never invent a locked decision. Add an entry, state the assumption,
build against it, and reference it from the ADR or task. An assumption you
record is a result; an assumption you absorb silently is a defect.

Mark a wave-spec task `status: gated` only when **no** assumption is defensible.

---

## Client-architect lane (independent)

- **OQ-client-001** — Azure Static Web Apps free tier is available and sufficient in the chosen region. **Status**: `assumed-confirmed`. **Assumption in force**: SWA free tier (TLS + CDN) hosts the static web bundle. Confirm SKU/region with cloud-architect at council-close. Ref: ADR-web-stack.
- **OQ-client-002** — Entra ID (OIDC) supports the Authorization Code + PKCE public-client flow for `dev`/`demo` tenants. **Status**: `assumed-confirmed`. **Assumption in force**: clients are public clients; no client secret in bundle. Confirm with security-architect. Ref: ADR-web-stack, ADR-mobile-stack, api-consumption.
- **OQ-client-003** — No BFF/API-proxy is required for V1; the SPA calls the API origin directly with CORS scoped to front-end origins. **Status**: `assumed-confirmed`. Ref: api-consumption.
- **OQ-client-004** — A mobile store release is genuinely out of scope for V1 `dev`/`demo` (no store-release dependency in spec §16/§20). **Status**: `assumed-confirmed`. Ref: ADR-mobile-stack.
- **OQ-client-005** — Mobile CI lanes can be configured as non-blocking in the council git flow/CI. **Status**: `assumed-confirmed`. Confirm with delivery-manager. Ref: ADR-mobile-stack.
- **OQ-client-006** — Expo/React Native supports OIDC Authorization Code + PKCE against Entra ID for public clients. **Status**: `assumed-confirmed`. Ref: ADR-mobile-stack.
- **OQ-client-007** — OpenAPI codegen tool and URL versioning scheme (e.g. `/v1/...`) are owned by software-architect; clients consume one generated TypeScript client. **Status**: `open` — resolve with software-architect at council-close. Ref: api-consumption.

## Delivery-manager lane (independent)

- **OQ-DM-001** — GitHub Environments with required reviewers are available on the `lucalamalfa91/raffa` public-repo plan; if not, `demo` promotion falls back to a protected tag + a PR to a `demo/*` pointer (still explicit/manual). **Status**: `assumed-confirmed`. Ref: ADR-git-flow, ADR-promotion-dev-demo.
- **OQ-DM-002** — The reviewers for the `demo` environment approval gate are product-owner + security-architect during V1; authority is council-owned. **Status**: `assumed-confirmed`. Ref: ADR-git-flow, ADR-promotion-dev-demo.
- **OQ-DM-003** — GitHub OIDC federation to the customer's Entra ID tenant is permitted on the org plan; fallback is a short-lived SP certificate in Key Vault (never a plaintext GitHub secret). **Status**: `assumed-confirmed`. Ref: ADR-ci-azure-auth.
- **OQ-DM-004** — OIDC subject-claim pinning for `demo` (environment + tag) is sufficient and the org does not allow unrestricted fork-triggered OIDC; deploy jobs run with `pull_request: false`. **Status**: `assumed-confirmed`. Ref: ADR-ci-azure-auth.
- **OQ-DM-005** — Tag naming `demo-v*` and environment name `demo` are council-owned and may be renamed; only the *mechanism* (tag + gated environment) is fixed. **Status**: `assumed-confirmed`. Ref: ADR-promotion-dev-demo.
- **OQ-DM-006** — No absolute start date exists in the brief; the wave calendar uses sequential weeks from an unspecified kickoff, not named calendar dates. **Status**: `assumed-confirmed`. Ref: wave-calendar.
- **OQ-DM-007** — Wave durations (~18 weeks R0–R4 + 2 weeks S0) are first-pass planning estimates owned downstream by the decomposer; this lane commits no hours/dates. **Status**: `assumed-confirmed`. Ref: wave-calendar.

## Security-architect lane (independent)

- **OQ-sec-001** — The chosen relational store (PostgreSQL + pgvector) supports `FORCE ROW LEVEL SECURITY` on the cheapest managed SKU that meets product constraints. **Status**: `assumed-confirmed`. **Assumption in force**: RLS is available; if the software-architect's SKU choice drops RLS, tenancy must force a SKU that keeps it (RLS is non-optional). Ref: ADR-tenancy.
- **OQ-sec-002** — Microsoft Foundry in the chosen region offers a no-training model endpoint (or an opt-out) for the contract-content models. **Status**: `assumed-confirmed`. **Assumption in force**: the AI Gateway selects a no-training endpoint; final model IDs confirmed jointly with software-architect + cloud-architect in the Foundry model-ID ADR. Ref: ADR-secrets-and-rag.

## Software-architect / council (OCR)

- **OQ-ocr-001** — OCR vs native document parse (brief §8, former CQ-008 sub-item). **Status**: `answered`. OCR is in V1: hybrid native-text + Azure AI Document Intelligence (`prebuilt-read` / `prebuilt-layout`) behind the AI Gateway; full document; no 2-page cap. Ref: ADR-017, ADR-004, ADR-005, ADR-008.

## Implementer — E01/F03/US02/T01 (per-folder workflows)

- **OQ-impl-001** — Task E01/F03/US02/T01's own "Files to create or modify" table names `workspace/raffa-infra/.github/workflows/*.yml`. ADR-014 itself fixes the product tree as `infra/`, `backend/`, `web/`, `mobile/`, `.helix/` at the repo root and explicitly rejects `workspace/<repo>/` as "not a stand-in"; `scripts/hcp_vcs_wiring.py` (already merged) also asserts HCP's trigger-prefix as root-relative `infra/`. **Status**: `assumed-confirmed`. **Assumption in force**: the four workflows were written to `.github/workflows/{infra,backend,web,mobile}.yml` at the worktree root — the only location GitHub Actions can actually discover them — not under `workspace/raffa-infra/`. Ref: ADR-014, task E01/F01/US01/T01 (root folder bootstrap).
- **OQ-impl-002** — Task E01/F03/US01/T01 (ci-azure-auth) left its composite action at `.helix/workspace/raffa-infra/.github/actions/azure-login/action.yml` (same wrong-prefix pattern as OQ-impl-001) instead of the repo root. **Status**: `assumed-confirmed`. **Assumption in force**: the action.yml content was copied verbatim (not rewritten) to `.github/actions/azure-login/action.yml` at the repo root, since `backend.yml`/`web.yml` reference it via `uses: ./.github/actions/azure-login`, which only resolves at the real repo root. The stale copy under `.helix/workspace/` was left in place (not this task's file scope to delete). Ref: ADR-014, ADR-015.
- **OQ-impl-003** — Whether `infra.yml`'s plan/apply job also needs an `azure/login` OIDC step. **Status**: `assumed-confirmed`. **Assumption in force**: no — `scripts/hcp_vcs_wiring.py` confirms both `raffa-dev`/`raffa-demo` HCP Terraform workspaces run `execution-mode=remote` (ADR-007's operating model: HCP itself runs plan/apply), so the GitHub Actions runner only ever talks to the HCP Terraform API (via `TF_API_TOKEN`, a non-Azure secret outside ADR-015's scope) — Azure credentials for the `azurerm`/`azuread` providers are supplied as that HCP workspace's own workspace variables, not via this workflow. Ref: ADR-007, ADR-015, `scripts/hcp_vcs_wiring.py`.
- **OQ-impl-004** — `web.yml` deploys to an Azure Static Web App resource; no `infra/modules/staticwebapp` exists yet (only `acr`, `containerapps`, `identity`, `keyvault`, `monitor`, `network`, `postgres`, `servicebus`, `storage` are provisioned). **Status**: `open` — needs a web-hosting infra task (feature-02-shaped) to provision the per-environment Static Web App and set `vars.AZURE_STATIC_WEB_APP_NAME` at the `dev`/`demo` GitHub Environment scope. `web.yml`'s deploy step fails fast with a named error if that variable is unset, rather than silently no-op-ing. Ref: ADR-012.
- **OQ-impl-005** — `backend.yml` builds `backend/src/Raffa.Api/Dockerfile` and `backend/src/Raffa.Worker/Dockerfile` via `az acr build`; neither Dockerfile exists yet anywhere in the repo. **Status**: `open` — needs a backend containerization task to add both Dockerfiles at those exact paths (the CI contract fixed here). Ref: ADR-002, ADR-005.
- **OQ-impl-006** — `infra.yml`/`backend.yml`/`web.yml` (not `mobile.yml`, which has no deploy target) declare `on.workflow_call` with a `target_environment` input, and their deploy/apply job's `environment:` and resource names key off it, so task E01/F03/US03/T01's `demo-promote.yml` can call `uses: ./.github/workflows/backend.yml` / `web.yml` / `infra.yml` with `target_environment: demo` per its own task text ("reuses the per-folder deploy jobs") instead of duplicating them. **Status**: `assumed-confirmed`. **Assumption in force**: this is additive only — direct `push`/`pull_request` triggers are unchanged and default to `dev`; us-03 still owns `demo-promote.yml`, the `demo` GitHub Environment/reviewers, and the `demo-v*` tag trigger untouched by this task. Ref: ADR-016, task E01/F03/US03/T01.
## Implementer / E01/F04/US02/T01 (EF Core + pgvector wiring)

- **OQ-impl-001** — ADR-004 fixes the embed-role model as "Foundry embedding model (e.g.
  `text-embedding-3-small` or `text-embedding-3-large`)... dimension fixed at schema time; small
  dimension preferred" but does not pin an exact integer. **Status**: `assumed-confirmed`.
  **Assumption in force**: `Embedding.Vector` is a fixed `vector(1536)` Postgres column
  (`Embedding.VectorDimensions` constant), matching `text-embedding-3-small`'s native output size
  — the smaller of the two named candidates, per ADR-004's "small dimension preferred for
  cost/size." If the council/AI Gateway later selects a different embed model with a different
  native dimension, this column width is a migration, not a redesign. Ref: ADR-003, ADR-004,
  `backend/src/Raffa.Documents.Contracts/Domain/Embedding.cs`.
- **OQ-impl-002** — ADR-003/ADR-009 write every table/column name in lowercase snake_case
  (`tenant_id`, `document`, `contract`, `embedding`, `clause`, ...), but neither ADR names an EF
  Core naming convention mechanism. **Status**: `assumed-confirmed`. **Assumption in force**: the
  Documents/Contracts `DbContext` calls `UseSnakeCaseNamingConvention()` (`EFCore.NamingConventions`
  package) so the physical schema matches the ADRs' own naming verbatim — without it, Npgsql/EF
  Core would emit quoted PascalCase identifiers instead. This convention is now load-bearing for
  every future migration in this module; us-03's RLS policies (`CREATE POLICY ... USING
  (tenant_id = ...)`) can rely on the lowercase column names existing as written. Ref: ADR-003,
  ADR-009, `backend/src/Raffa.Documents.Contracts/Infrastructure/DocumentsContractsDbContextOptions.cs`.

## Ask V2 lane (epic-13 / ADR-024, 2026-09-08)

Source: `inputs/requirements.md` §13. Every entry has an assumption in force; none gates a task.

- **OQ-askv2-001** — The mock market-intelligence record shape (`MarketDeal`, R-MKT-01) is Raffa's own normalized contract; the third-party API will be mapped onto it. **Status**: `assumed-confirmed`. **Assumption in force**: build `IMarketIntelligenceProvider` around `MarketDeal`; the live client maps into it. Ref: ADR-024.
- **OQ-askv2-002** — Admission threshold (0.6) and minimum readable text (200 chars) are right for the golden set. **Status**: `assumed-confirmed`. **Assumption in force**: both are configuration (`Documents:AdmissionThreshold`, `Documents:MinReadableChars`), tuned on `Raffa.AiEval`. Ref: R-DOC-03.
- **OQ-askv2-003** — Savings KPIs / opportunities are not a rail item in V2. **Status**: `assumed-confirmed`. **Assumption in force**: route `/savings` (renamed from Home), reached from Ask actions, Renewals and Contract 360. Ref: R-WEB-02, `raffa-v2/ia-v2.md`.
- **OQ-askv2-004** — Conversation retention. **Status**: `assumed-confirmed`. **Assumption in force**: unlimited in V2; deletion by the owner only. Ref: R-CONV-01.
- **OQ-askv2-005** — Per-user identity for conversations under the ADR-022 header posture. **Status**: `assumed-confirmed`. **Assumption in force**: `X-User-Id` = MSAL account username, non-authoritative; the task that lands the API JWT (ADR-010) replaces it with the token subject. Ref: R-CONV-03.
- **OQ-askv2-006** — Answer language. **Status**: `assumed-confirmed`. **Assumption in force**: follows the question's language; fixtures and golden set cover Italian and English. Ref: ADR-024.
- **OQ-askv2-007** — Upload processing stays synchronous in the request. **Status**: `assumed-confirmed`. **Assumption in force**: bounded by 50 MB / file and the OCR page budget; worker-queued upload is a later task. Ref: R-DOC-01, A7.
- **OQ-askv2-008** — A Quote admitted in Documents. **Status**: `assumed-confirmed`. **Assumption in force**: no automatic `Quote` record; the result card and Ask route to Quote check (`/quotes`) where the user uploads the quote. Ref: R-DOC-03 AC-4.
- **OQ-askv2-009** — Live Foundry on `dev` / `demo`. **Status**: `assumed-confirmed`. **Assumption in force**: acceptance A2–A8 runs against the Foundry-backed gateway (Container Apps inject `AiGateway__*`); CI proves the same paths on the fixture gateway. Ref: R-AI-01.

## Wave w14 — workspace is real (next-wave process, 2026-09-10)

Source: `inputs/next/next-waves-todo.md` → `reports/context/waves/w14-requirements.md` §7. Every entry has an assumption in force; none gates a task.

- **OQ-w14-001** — Which identity keys `workspace_membership` in w14, given ADR-010's JWT (NW-05) is queued for W15 and the raw file's own order puts W15 first? **Status**: `open`. **Assumption in force**: w14 uses the interim `X-User-Id` (MSAL account username, ADR-022 / OQ-askv2-005) as the membership key, behind **one** resolver seam (`WorkspaceRoleResolver`'s membership branch, `backend/src/Raffa.Api/Infrastructure/WorkspaceRoleResolver.cs:85-119`), so W15 swaps in the token `sub`/`oid` without touching callers. `X-Role` / `X-Workspace-Role` must never become the product answer for an Admin-only action. Ref: NW-02, NW-01, NW-14; ADR-009, ADR-010, ADR-022.
- **OQ-w14-002** — Is a real email transport in scope for w14? `inputs/percorso-pilota-v1.md` §2 lists **"email"** under *"Non è onboarding"*, while `inputs/product-spec.md` §20 Day 1 requires "invite Procurement users" and NW-58 requires a mail with a link. A transport is a new Azure resource (none in ADR-005), a Terraform module (ADR-007) and a per-env secret (ADR-011). **Status**: `open` — product-owner + cloud-architect at the w14 table. **Assumption in force**: w14 lands the full **token → accept → join → remove → re-invite** path behind an `IInvitationMailer` seam; if no transport lands, the Members UI shows the **copyable accept link** and must **not** say "Invitation sent." (NW-58 must #1), and acceptance N3b is read with that substitution. Ref: NW-58, ADR-001, ADR-005, ADR-007, ADR-011, ADR-020.
- **OQ-w14-003** — Which commit does the wave branch from? `origin/main` @ `25b10da` renamed every `Raffa.*` module to `Raffa.*` (PR #77, `87b7976`: 1477 files, 840 path renames, `.helix` included); the audited checkout `1650213` (`helix/next-wave-process`) is pre-rebrand and carries the next-wave process `origin/main` does not have. **Status**: `open` — operator decision at HITL. **Assumption in force**: the operator rebases/merges `helix/next-wave-process` onto `origin/main` **before** fan-out; every path in `w14-requirements.md` is then read with `Raffa.X` → `Raffa.X` and `inputs/design/prototypes/raffa-v2/` → `raffa-v2/`. Every w14 gap was re-verified against `origin/main` and is unchanged by the rename. Ref: W14-01, ADR-014.
- **OQ-w14-004** — Which workspace profile fields (NW-24)? `percorso-pilota-v1.md` §2 step 1 asks "nome / industria / paese"; `raffa-v2/markup.html:63` shows "CHF · eu-west". **Status**: `open` — the raw file itself marks NW-24 `(HITL)`. **Assumption in force**: `country` and `currency` become first-class columns on `workspace` and drive the picker row; `industry` is optional free text; "region" is a business region and does **not** touch ADR-006 (`northeurope` stays); a workspace currency is a display default, never an override of a contract's own stored currency. Ref: NW-24, ADR-003, ADR-006, ADR-020, ADR-021.
- **OQ-w14-005** — Who may list and remove members (NW-04, NW-58)? **Status**: `open`. **Assumption in force**: any member may read the roster (Procurement read-only, with the "request access" state of `raffa-v2/screens-v2.md` §10); only an Admin may invite or remove; an Admin may not remove the last Admin; a removed member loses access on the next request (NW-01 / NW-03 revalidation), not at token expiry. Ref: NW-04, NW-58, ADR-009.

## Wave w14 — decomposition (next-decomposer, 2026-09-11)

- **OQ-w14-dec-001** — Which email address does the seeded `demo` Admin membership bind to? The delivery-manager's w14 row requires `backend/scripts/demo-fixture-seed.sql` to gain an idempotent Admin `workspace_user` + `workspace_membership` for the fixture tenant `00000000-0000-0000-0000-000000000001`, because that tenant is seeded by SQL and can never receive a membership row from `POST /api/workspaces`. The record does not say which identity that membership belongs to, and the file is checked into git, so a real address cannot be hardcoded. **Status**: `open` — operator decision, per environment. **Assumption in force**: the script takes an optional `psql` variable `demo_admin_email` and falls back to a documented, inert placeholder (`demo-admin@raffa.invalid`) that grants nobody real access; `.github/workflows/seed-demo-fixture.yml` passes `-v demo_admin_email="${{ vars.DEMO_ADMIN_EMAIL }}"` only when that GitHub Environment variable is set. The operator sets it once per environment before the first post-w14 seed. Real addresses on existing tenants arrive through `.github/workflows/backfill-workspace-membership.yml`, which takes `(workspace id, admin email)` pairs at dispatch. This is a **CI variable, not an Azure or Terraform change** — w14's cloud delta stays zero. Ref: NW-01, NW-02, NW-03; ADR-022, ADR-016, ADR-025 §D.2/§G; task `E14/F05/US01/T01`; `reports/audit/w14-hitl.md` §8.

## Wave w15 — upload feels instant, invite works end to end (next-wave process, 2026-09-13)

Source: `inputs/next/w15-todo.md` → `reports/context/waves/w15-requirements.md` §7. Every entry has an assumption in force; none gates a task.

- **OQ-w15-001** — Which commit is the wave base? This checkout (`3720543`, `helix/w15`) is **five commits behind `origin/main` (`3c89d35`)**, and two oracles the raw file cites — `docs/waves/w14-acceptance.md` and `web/e2e/invite.spec.ts` — exist only on `origin/main` (`Glob ../docs/waves/*` returns nothing here; `../web/e2e/` holds only `day1.spec.ts` and `v2.spec.ts`). **Status**: `open` — operator act at HITL. **Assumption in force**: the operator merges `origin/main` into the wave base **before** fan-out; every path in the requirements document then resolves unchanged, because `git diff --stat origin/main..HEAD -- backend web infra .github docs scripts` is exactly five files (`backend/README.md`, `docs/waves/w14-acceptance.md`, `web/README.md`, `web/e2e/day1.spec.ts`, `web/e2e/invite.spec.ts`) and **none** is under `backend/src`, `web/src`, `infra/` or `.github/workflows/` — so every code citation is already true on `origin/main`. Ref: W15-01, ADR-014 w14 footer, OQ-w14-003 (same class).
- **OQ-w15-002** — Is a real email transport in scope, given the pilot path excludes it? `inputs/percorso-pilota-v1.md:36` lists **email** under *"Non è onboarding"*, `:110` "Niente email nel pilota", `:152` repeats it; `inputs/product-spec.md:619`/`:626`/`:756` rank email **P1 / V1**, and the ADR-001 w14 footer already ruled the transport "**deferred, not a §1.2 non-goal**". The stakeholder ruling of 2026-09-13 marks NW-68 `must` ("dovevi già farlo ora"). **Status**: `open` — product-owner at the w15 table. **Assumption in force**: the stakeholder ruling governs; the pilot doc's exclusion is read as "email is not a *pilot demo step*", not "email is out of the product", so NW-68 ships and the pilot script is unchanged — nobody is asked to demo an inbox. Ref: NW-68, ADR-001, ADR-005 w14 footer; supersedes the open half of OQ-w14-002.
- **OQ-w15-003** — `inputs/requirements.md` A7 (`:827`, "upload stays synchronous in the request for V2 … moving it to the worker queue is a later task") and **OQ-askv2-007** are superseded by NW-27, which the raw file states outright. **Status**: `open` — recorded so the decomposer does not build to A7. **Assumption in force**: **OQ-askv2-007 is `assumed-wrong` from wave w15 on**; the asynchronous worker path is the product rule, and R-DOC-01 / R-DOC-03 / R-DOC-09 are read with the admission gate's placement decided at the w15 table (OQ-w15-004). The `backend/src/Raffa.Documents.Contracts/Application/Extraction/DocumentProcessingPipeline.cs:65-77` comment citing OQ-askv2-007 as authority becomes stale in the same task. Ref: NW-27, ADR-002, ADR-024.
- **OQ-w15-004** — Does the admission gate (R-DOC-03) stay synchronous inside the 2 s budget, or move to the Worker? The answer changes the data model: today a refusal is a 422 with **nothing persisted** (`backend/src/Raffa.Api/DocumentsEndpointExtensions.cs:242-252`), the design oracle calls the "Not added" card *session-only, never counted* (`inputs/design/prototypes/raffa-v2/screens-v2.md:72-75`), and `DocumentProcessingStatus` has **no `Rejected` value** (`backend/src/Raffa.Documents.Contracts/Domain/DocumentProcessingStatus.cs:4-11`). **Status**: `open` — product-owner + software-architect + ux-ui-designer. **Assumption in force**: the gate **moves to the Worker** and a refused file becomes a terminal `Rejected` row, because the gate parses/OCRs and calls the Foundry `classify` role (`Admission/DocumentAdmissionGate.cs:110-112,136-138`) and cannot be guaranteed inside 2 s against live Foundry — which puts a new `processing_status` value, a migration (ADR-021) and an amendment to the design oracle's session-only rule inside NW-27's scope. Ref: NW-27, R-DOC-01/03/09, ADR-017, ADR-021, ADR-024.
- **OQ-w15-005** — Which identity calls Microsoft Graph, and does Terraform manage the permission? A per-environment user-assigned managed identity already exists and is attached to both container apps (`infra/modules/identity/main.tf:36-46`; `infra/modules/containerapps/main.tf:34-37,214-217`), but `infra/modules/identity/main.tf:41-42` records that the current design deliberately **avoids** Graph. **Status**: `open` — cloud-architect + security-architect. **Assumption in force**: the existing workload managed identity is granted the least-privilege Graph application permission (`User.Invite.All` or narrower) through Terraform, with **admin consent granted once by the tenant admin as an operator step at the ADR-016 gate**, and **no new secret** — so ADR-011 gains no Key Vault entry for NW-67. Both environments authenticate against the **same Entra tenant** (`infra/modules/identity/main.tf:63,118`; `tests/test_check_demo_swa_config.py:32,42`), so a guest provisioned from `dev` is visible to `demo` — the council must say whether that is acceptable or whether provisioning is gated per environment. Ref: NW-67, ADR-010, ADR-011, ADR-015.
- **OQ-w15-006** — How does N3b read the invitee's one-time passcode? No workflow runs Playwright at all (`.github/workflows/web.yml` runs vitest only), and wiring one is **NW-50, queued W18**. **Status**: `open` — delivery-manager + security-architect. **Assumption in force**: NW-58r does **not** introduce a passcode seam in the product this wave; the spec's skip reason is rewritten to name the passcode (not the missing second account, which NW-67 removes) and N3b is walked by hand in the acceptance runbook. A `dev`-only mail-catcher or a test-only seam is the council's call; if neither is approved, the item closes on the runbook step. Ref: NW-58r, ADR-016 w14 footer, ADR-025 §H (T14/T15).
- **OQ-w15-007** — What happens to the Entra guest when a member is removed? **Status**: `open` — security-architect + product-owner. **Assumption in force**: removing a workspace membership **never** deletes or blocks the Entra guest — the guest may belong to other workspaces, and ADR-025 already rules removal immediate because nothing caches authorization. The rule is recorded in an ADR-025 amendment; no directory-deletion path is built. Ref: NW-67, ADR-025.
- **OQ-w15-008** — Does NW-10 ride NW-61's task, or return to W16? The raw file makes it conditional ("take NW-10 into W15 if the same task touches it"). **Status**: `open` — decided by the decomposer, not by a seat. **Assumption in force**: NW-10 is **in wave**, co-located with NW-61's server-row work, because the fix is one component reading an endpoint that already exists (`web/src/components/shell/RailNav.tsx:67` → `backend/src/Raffa.Api/DocumentsEndpointExtensions.cs:82`). If the decomposer cannot co-locate it, it becomes the **head of W16**, never later. Ref: NW-10, NW-61, ADR-012 w14 footer.

### Delivery-manager, added at the w15 table (2026-09-13)

Numbered `dm-` because OQ-w15-009 is already product-owner's. All four are recorded in `reports/architecture/waves/w15.md`; none gates a task.

- **OQ-w15-dm-01** — Does the `raffa-demo` HCP workspace apply automatically on a push to `main` that touches `infra/`? `infra/README.md:8-12` says **both** workspaces are VCS-connected with `trigger-prefixes: infra/`; `:88-91` says the first `demo` apply is an HCP UI "New run". w15 is the first wave since that note whose Terraform is not empty. **Status**: `open` — delivery-manager + cloud-architect. **Assumption in force**: `demo`'s infrastructure **does** move at the infrastructure merge, so every w15 key whose feature can act on the world is flag-gated `false` on the `demo` root and the operator confirms the `raffa-demo` run state explicitly before and after the flip PR. If it does not auto-run, nothing breaks — the flip PR and the confirmation are the same act either way. Ref: NW-27, NW-68, ADR-016 w15 clauses 14, 19.
- **OQ-w15-dm-02** — How would anyone find out that nothing wakes the worker? `backend.yml` deploys `az containerapp update --image` with no revision-state, `runningState` or health assertion, so **a worker that cannot start produces a green run** and a queue behind a dead consumer looks like slow processing. **Status**: `open` on the detection half (the resource half is resolved by cloud-architect: one subscription, a KEDA queue-depth rule with `min_replicas = 0` kept, two topic-scoped role assignments). **Assumption in force**: the promotion gate carries one **post-deploy revision-state assertion per environment**, and **A15-2 is walked on deployed `dev` with the worker scaled the way `demo` will run it** — "the HCP run is `CURRENT`" proves none of it. Ref: NW-27, ADR-005, ADR-016 w15 clause 17.
- **OQ-w15-dm-03** — **w14 was never promoted.** The complete tag set on disk is `demo-v1 a4e564c`, `demo-v2 22c474f`, `demo-v3 2db5734` — **no `demo-v4`** — so `demo` has been running the same commit since **2026-09-04** and w15's promotion would carry two waves. **Status**: `open` — the operator decides at HITL. **Assumption in force**: `demo-v4` is cut on the current `main` **at the gate, before w15 starts** (W15-A1 already proves that commit green), restoring ADR-016's one-promotion-one-wave invariant and making a w15 rollback land on w14 instead of on 2026-09-04; w15 then promotes as `demo-v5`. If declined, the bundled promotion is the fallback. Either way `demo` owes three data-plane steps belonging to the **previous** wave — the w14+w15 schema apply, `seed-demo-fixture.yml`, `backfill-workspace-membership.yml`, then `docs/waves/w14-acceptance.md` N1–N9 + W14-A2 — and the wave-close must record them. **Process gap behind it**: no `wave-close-w14.md` was written, so a wave that closed without a promotion said nothing about it. Ref: W15-01, ADR-016 w15 clauses 18–19, ADR-014.
- **OQ-w15-dm-04** — NW-05 makes `reprocess-tenant-documents.yml` return 401: repair, edit, or out of service? **Status**: `resolved` — out of service and named. Its first authenticated call (`:202-207`) fails and the step exits 1 (`:208-211`) **before any write**, so no partial reprocess and no unattributed audit rows; the file predicted it at `:48`. w15 does not repair it (a token-bearing deploy identity is an identity-plane change, security-architect's) and does not edit it (**NW-31 owns that file in W16**). Recorded in `docs/waves/w15-acceptance.md`'s known-gaps table; W16 designs the repair. Ref: NW-05, NW-31, ADR-016 w15 clause 21.
- **Operator assumptions this wave runs on** (recorded because each is a way the wave fails quietly): one operator drives every HITL gate, and `demo`'s only required reviewer is the repo owner; **no second wave runs concurrently against this `integration`** — `helix/w17-input` already exists in this clone, so the e13 rule applies; the operator has an **external mailbox the tenant has never seen** (A15-4/A15-5 cannot be walked without one) and the **tenant-admin rights** the Graph grant needs (ADR-015 w15 clause 2); and `integration` is **re-created** from the wave base rather than merged into, since it has diverged (`8ed3af1a` vs `271c3ae1`). Ref: W15-01, ADR-014 w15 clauses 4, 7.

## Wave w16 — nothing the product knows lives only in a browser tab (next-wave process, 2026-09-14)

Source: `inputs/next/w16-todo.md` → `reports/context/waves/w16-requirements.md` §7.
Every entry has an assumption in force; none gates a task.

Context note for every later phase: **`reports/execution/wave-close.md` is stale.**
It is stamped `2026-09-14T02:53:30Z` and lists 9 of 11 w15 tasks as *undelivered*;
it was written before PR #103 (`e52f663`) merged `integration` into `main` and
before the follow-ups #111–#117. All six w15 areas were re-verified as delivered
on `ff66ee6` (`w16-requirements.md` §1). Do not re-open a w15 item on its word.

- **OQ-w16-001** — NW-07: the raw file's acceptance A16-1 ("sign in as the same user with different UPN casing") is **unsatisfiable as written** — the conversation key is `oid` (`backend/src/Raffa.Api/Infrastructure/CallerIdentity.cs:80-81`), which is invariant under UPN casing, and the `TryResolveUserId` helper the raw file describes **no longer exists** (deleted in w15; only a dangling `<see cref="TryResolveUserId"/>` survives at `ConversationsEndpointExtensions.cs:24`). The real residual is the raw file's own third `must`: no migration or backfill exists for conversation rows written under the pre-w15 email key (`backend/src/Raffa.Chat/Migrations/` holds only `Initial` and the RLS policy), so those rows are silently orphaned — an empty rail and a 404 from `ConversationService.GetAsync`. **Status**: `open` — product-owner + software-architect. **Assumption in force**: the orphaned rows are **left in place and not backfilled** (mapping an email to an `oid` needs a directory lookup the product has no batch path for) and the decision is *recorded* rather than executed; the acceptance becomes "a post-w15 conversation is readable by the same `oid` and by no other member of the workspace" (R-CONV-01 AC-1). **`OQ-askv2-005` retires with this item.** Ref: NW-07, ADR-010, ADR-024, ADR-009.
- **OQ-w16-002** — NW-08: swapping the audit guard to `ICallerContext` changes how the route derives its tenant — a `tenant_id` **claim** today (`WorkspacePrincipalAuthorization.cs:62-67`), an `X-Tenant-Id` **header verified against membership** after — and the route currently *deliberately forbids* a `?tenantId=` query (`AuditEndpointExtensions.cs:25-29`). That is a contract change for this route, not a drop-in. **Status**: `open` — security-architect + software-architect. **Assumption in force**: `/api/audit` adopts the shape every other tenant-scoped route already uses (`DocumentsEndpointExtensions.cs:492-509`) — 401 → 404 (non-member) → 403 (member, not Admin) — and the "no `?tenantId=`" rule survives as "the header is a selector, never an authorization input" (ADR-022's retirement wording). Ref: NW-08, ADR-025, ADR-010, ADR-026.
- **OQ-w16-003** — NW-08: is a **web audit surface** in scope? There is no audit route, page or fetch in `web/src` (`web/src/App.tsx:222-236` registers none) and no audit operation in the generated client. **Status**: `open` — product-owner. **Assumption in force**: **no** — the raw file's A16-2 reads "Admin opens audit (**or calls** `GET /api/audit`)", so the item closes through the API plus a typed client; ADR-020's screen inventory is unchanged this wave and **ux-ui-designer is not seated**. If product-owner rules a screen in scope, ux-ui-designer joins for that one screen. Ref: NW-08, ADR-020, ADR-018.
- **OQ-w16-004** — NW-31: repair `.github/workflows/reprocess-tenant-documents.yml` with a **token-bearing identity** (a registration or federated credential, Terraform, ADR-010/ADR-015) or convert it to **ADR-027's worker-side re-enqueue**? The workflow authenticates to Azure only (`:124-129`) and sends no bearer token at all (zero `Authorization|Bearer` matches), so after NW-05 it fails **401 at the first API step** and its own 403 diagnostic at `:274` is unreachable dead code. **Status**: `open` — security-architect + delivery-manager + cloud-architect. **Assumption in force**: the **re-enqueue path**, because ADR-027 `:198` already rules that "reprocess collapses into re-enqueue" on the same publisher, and it needs **no new identity, no secret and no Terraform** — keeping the wave's cloud delta at zero and cloud-architect on `PASS`. If the token is preferred instead, cloud-architect's seat becomes active and an infrastructure phase must precede the workflow task. Ref: NW-31, ADR-016 w15 clause 21, `OQ-w15-dm-04`, ADR-027.
- **OQ-w16-005** — NW-21: does "the Savings KPI moves" mean the opportunity's **status** becomes `Realized` (already implemented once propagation fires — `SavingsOpportunityService.cs:292,300-308`), or must the **realized amount** replace the estimate in the KPI? The Realized bucket sums `EstimatedSavingsLow/High` and never the realized amount (`SavingsKpiCalculator.cs:103-104`, a gap documented at `:46-54`), and the design oracle's column is **"Estimate"** (`inputs/design/prototypes/raffa-v2/screens-v2.md:135`). **Status**: `open` — product-owner. **Assumption in force**: W16 delivers the **link and the status move only**; making the KPI report realized amounts is a separate product decision, is not in this wave's text, and if ruled in becomes an ADR-001 capability change and the head of W17. Ref: NW-21, ADR-001, ADR-003.
- **OQ-w16-006** — NW-13: which module owns the negotiation-step ticks, and what keys the row? `Raffa.Renewals`'s ADR-002 allow-list is `[SharedKernel, Benchmark]` (`RenewalActionService.cs:23`) so it cannot reference `Raffa.Documents.Contracts`; and the client store is an **index-positional** `boolean[4]` (`negotiationStepsStore.ts:11,18,24,31`) whose labels live elsewhere and are **parameterized** in two of four cases (`contract360ViewModel.ts:211-218`). **Status**: `open` — software-architect + product-owner. **Assumption in force**: the ticks are a **contract child resource** owned by `Raffa.Documents.Contracts`, following the shipped `GET /api/contracts/{id}/corrections` shape (`ContractsEndpointExtensions.cs:66,431-470`), keyed by the **four named steps** of `screens-v2.md:106-108` — never by array index. Ref: NW-13, ADR-002, ADR-003, ADR-021, ADR-026.
- **OQ-w16-007** — NW-32: `backend/tests/Raffa.Chat.Tests/RagAnswerServiceTests.cs:70` asserts `Assert.Equal("unattributed", entry.Actor)` — **a green test pinning the defect** — and two of the fourteen runtime write sites have no HTTP caller at all (`RagAnswerService.cs:140`; `SavingsOpportunityService.cs:165` `CreateAsync`). **Status**: `open` — software-architect + security-architect. **Assumption in force**: the test is **rewritten** to assert the resolved actor (never deleted silently), and the two caller-less sites take the actor as a **required parameter** rather than keeping a default, so the constant can be deleted outright. **Reframing on the record**: since PR #117 (`949004e`) every endpoint reaching these nine services is behind `ICallerContext` and 401s first, so `"unattributed"` is no longer an *absent-identity* branch — it is an unconditional hardcode that **falsifies the audit trail of an authenticated caller** (an ADR-011 violation), not an authentication bypass. The raw file's `should` therefore stands; it is not promoted to `must`. Ref: NW-32, ADR-011, ADR-022.
- **OQ-w16-008** — **process, not product**: `reports/plan/gates/w15.hitl-ok` **does not exist** — the gates directory holds `e01…e13`, `readiness-gaps` and `w14.hitl-ok` only — while `scripts/check_slice_prereqs.py:332` requires `<previous>.hitl-ok` for the `hitl_previous` check. With `previous: w15`, `run.ps1 -Slice w16` will **fail its prerequisites before fan-out**. **Status**: `open` — operator + delivery-manager. **Assumption in force**: the operator stamps it at the w16 HITL gate (`python scripts/check_slice_prereqs.py --record-hitl w15`), which is accurate because w15 is confirmed terminated and merged to `main` (PR #103 + #111–#117). **No task is created for this.** Ref: `scripts/check_slice_prereqs.py:122-125,329-338`, ADR-016.

### Rulings from the w16 council table (product-owner, 2026-09-14)

Appended rather than edited in place: the four entries above that name
product-owner are ruled below, and one new question is opened. Authority:
`reports/architecture/ADR-001-scope-r0-r4.md` `## Amendment (2026-09-14, wave w16)`;
rows in `reports/architecture/waves/w16.md`.

- **OQ-w16-001 → `resolved`** (NW-07). Assumption in force **ratified**: pre-w15 conversation rows are left in place, never remapped, never deleted; the retirement is *recorded*, not executed (a wrong email→`oid` remap would hand one user's threads to another — a breach of R-CONV-01 AC-1 strictly worse than the loss it repairs). **A16-1 is reworded in four clauses** (ADR-001 w16 clause 1): post-w15 conversation readable by the same `oid`; no other member can list or read it; a pre-w15 conversation is not listed, not counted, and opens as a clean 404; the disposition is recorded in the PR. **Added finding**: the promoted task's DoD line `:69` ("readable by the same subject presenting `user@example.com`") **must not be implemented** — it would re-introduce the normalization `CallerIdentity.cs:60-68` deliberately rejects. Standing constraint: no surface may count a conversation it cannot open. `OQ-askv2-005` retires with the item (ADR-024, software-architect).
- **OQ-w16-003 → `resolved`, ruled NO** (NW-08). No web audit surface in W16: A16-2 closes through `GET /api/audit` plus a typed client, audit appears nowhere in `inputs/percorso-pilota-v1.md`, and a screen would seat ux-ui-designer and add ADR-018/ADR-020 work the 5-phase cap has no room for. **ux-ui-designer stays unseated**; ADR-020's screen inventory is unchanged. An audit screen returns as its own item in a later wave if wanted.
- **OQ-w16-005 → `resolved`, ruled "status move"** (NW-21), **with a correction to the question itself**. A16-8 closes on the status move because **no surface renders a realized money figure** (the KPI row is Contracts analyzed · Upcoming renewals · Savings *identified*; realized appears only as a **count**) and the per-opportunity `realizedAmount` is already stored and already on the wire — so w15 clause 7 ("never silently drop a fact the user is entitled to") is not engaged and the estimate-summed `Realized` range is a latent **API-shape** defect reaching no screen. The question's framing of the amount fix as "an ADR-001 capability change and the head of W17" is **half wrong**: it is **not** a capability change and **not** a new item — see OQ-w16-po-01. **Fence binding this wave**: no w16 task may render `SavingsKpiSummary.Realized` as a money amount (w14 clause 3). **Second half ruled**: an outcome may be *resolved* to an opportunity from a fact the system holds, but **never guessed** — with no unambiguous opportunity it is recorded **unlinked**, `savingsPropagated` stays `null`, nothing moves.
- **OQ-w16-006 → product half `resolved`** (NW-13); the owning module stays software-architect's. The **four named steps** of `screens-v2.md:106-108` are canonical (four, no more); rows are keyed by **stable step key, never array index**, and the rendered label stays client-side because two of four labels are parameterized (w14 clause 3 — a persisted row must not freeze a fact that later changes); ticks are **per contract**, matching `renewal_action`'s unique `(tenant_id, contract_id)`, **not** per renewal cycle (per-cycle tick history would be a new capability, out for V1 and asked for by no oracle).
- **OQ-w16-po-01** — **new, product-owner + software-architect.** `E04/F03/US01` AC-1 (the Savings **Realized** KPI reporting *verified* amounts — spec `:447`) is an **unmet AC of a story that is still `active`**, not a new capability and not a new item: its implementing code documents the gap (`SavingsKpiCalculator.cs:46-54`) and names the record task that did ship (`E04/F02/US02/T02`, `status: live`). It therefore needs **no ADR-001 amendment and no new epic** — it needs a priority ruling, which this seat gives: **head of W17**, recorded so it is not lost a third time. **Status**: `open` — no task this wave. **Assumption in force**: w16 delivers the link and the status move only, under the money-rendering fence of OQ-w16-005; W17's head picks up the amount. Ref: NW-21, ADR-001 w16 clause 4, `reports/workitems/epic-04/**`.

### Rulings from the w16 council table (software-architect, 2026-09-14)

Appended, not edited in place. Authority: `reports/architecture/ADR-028-server-side-state.md`
and the `## Amendment (2026-09-14, wave w16)` footers on ADR-002, ADR-003,
ADR-021, ADR-024, ADR-026 and ADR-027; rows in `reports/architecture/waves/w16.md`.

- **OQ-w16-002 → `resolved`** (NW-08). The swap to `ICallerContext` is adopted **and named as a contract change on a live route**: the tenant moves from a `tenant_id` claim to an `X-Tenant-Id` **verified against membership**, so the header is **declared in `web/openapi/raffa-api.v1.json`** rather than discovered at runtime by the client (ADR-026 w16 clause 2). The route's "no `?tenantId=`" property survives in the stronger form *authorized selector, never an authorization input*; the 401→400→404→403→200 ladder is security-architect's S16-4 and `/api/audit` gets no bespoke version of it. `WorkspacePrincipalAuthorization` is **deleted whole**, not just stripped of its constant (S16-5, ADR-002 w16 clause 5).
- **OQ-w16-004 → `resolved` by withdrawal of its premise** (NW-31). The assumption in force ("the re-enqueue path needs no new identity, no secret and no Terraform, per ADR-027 `:198`") **does not follow from that ADR**: `:198` describes the *handler* reached by `POST /api/documents/{id}/reprocess` inside an authenticated tenant scope; `:719` names the operator it sanctions — "an admin inside that tenant" — and `:720-723` rules that any other closure needs the cross-tenant sweep ADR-009 forbids (ADR-027 w16 clause 1). Three seats converge from independent evidence: software-architect on the ADR text, delivery-manager's **D1** on the Service Bus role assignments (`infra/modules/servicebus/main.tf:73-74,91-92`), security-architect on the identity plane. **Neither option is taken**: the workflow's API steps are deleted (delivery-manager's disposition), so the wave's cloud delta is zero **because of that**, not because re-enqueue was free. cloud-architect stays on `PASS`.
- **OQ-w16-006 → `resolved` in full** (NW-13); product-owner ruled the product half, this seat rules the module: **`Raffa.Documents.Contracts`** owns the ticks as a contract child resource (`GET`/`PUT /api/contracts/{id}/negotiation-steps`, the `/corrections` shape verbatim). `Raffa.Renewals` was the tempting owner and is structurally impossible — allow-list `[SharedKernel, Benchmark]`, enforced by `DependencyDirectionTests`. One new tenant table, unique `(tenant_id, contract_id, step)`, step stored **by name**, the row's **presence is the tick**, RLS in the same migration (ADR-002 w16 clause 1, ADR-003 w16 clause 1, ADR-028 §D3).
- **OQ-w16-007 → `resolved`** (NW-32). The actor is a **required parameter with no default** on all nine methods (an ambient accessor would let the two caller-less sites compile and write nothing); the caller-less sites write a reserved **`system:<component>`** principal — the convention already live at `NegotiationOutcomePropagationService.cs:97` — so A16-4's "grep returns nothing" is reachable without inventing a vocabulary; `RagAnswerServiceTests.cs:70` is **rewritten to assert the resolved actor, never deleted** (security-architect S16-8/S16-9/S16-10, ADR-002 w16 clause 4).
- **OQ-w16-ca-02 → `answered`** (NW-13, client-architect's question). **Undo is two idempotent writes — the ticks `PUT` first, then the action `POST` — not one composed call.** Folding the tick-clear into the renewals POST would make a write named "set the renewal action" silently erase another module's rows through `Raffa.Api`, so any future non-UI caller posting `NotStarted` (a script, a test, W17's operator console) would destroy a negotiation record it never named. The client's real requirement — invent no partial-failure state — is met by **re-reading**, not by compensating: both writes are idempotent and both surfaces already re-read on mount (ADR-028 §D4).
- **OQ-w16-ca-03 → `answered`** (NW-21, client-architect's question). **Server-side resolution**, in the deterministic form only: the outcome's named `savingsOpportunityId` wins; absent, `Raffa.Api` resolves through the product's own supplier-identity rule (`SupplierNameNormalizer` + the `(tenant_id, normalized_name)` unique index) and links **only if exactly one** open opportunity matches, otherwise records the outcome **unlinked** with `savingsPropagated: null`. That is product-owner's "resolved, never guessed" enforced by a unique index rather than a heuristic. `SupplierResolver` is never called on this path — it *creates* rows, and recording an outcome must not mint a supplier. Client cost is **zero**, so **ux-ui-designer stays unseated** (ADR-028 §D5).
- **OQ-w16-sa-01** — **new, software-architect + product-owner + client-architect.** Should ADR-028 §D5's *clause 2* (the deterministic supplier-name resolution) ship in w16 at all, or only clause 1 (the explicit id)? **Assumption in force**: it ships — clause 1 alone closes A16-8 by `curl` but leaves NW-21 **unreachable from the UI**, which is the state the item exists to fix, and clause 2 costs one read-only lookup plus a composition in `Raffa.Api` with no schema, contract or client change. If the council prefers the minimum, clause 2 moves to W17 together with the picker question, and w16's NW-21 is an API-only close. **Risk named, not hidden**: on the pilot corpus the resolution may never fire (a quote's supplier is free text typed at upload), which is harmless — it declines — but it means the *acceptance walk must drive the explicit-id path*, never the resolver. Status `open`; no task turns on it.
- **OQ-w16-sa-02** — **new, client-architect + ux-ui-designer (W17).** With the capability catalog served whole (ADR-024 w16 clause 2), an admin-gated capability's example questions can surface as a suggestion chip for a non-Admin. **Assumption in force**: acceptable for w16 — it is a **label, never an authorization** (every action behind it is enforced server-side by membership), the catalog is static and tenant-free, and nothing in `web/src` reads `roleGate` today. Hiding them is **client presentation work**, not a server gate, and is deferred to W17. Status `open`; no task this wave.

### Round-2 rulings from the w16 council table (product-owner, 2026-09-14)

Appended, not edited in place. Two questions reached this seat after its first turn. Authority: `reports/architecture/ADR-001-scope-r0-r4.md` `## Amendment (2026-09-14, wave w16)` **clauses 7–8**; rows NW-21 and NW-31 in `reports/architecture/waves/w16.md`.

- **OQ-w16-sa-01 → `resolved`, ruled "clause 2 ships"** (NW-21). The assumption in force is ratified, with the reason corrected: it is **not** a judgement call about cost, it follows from ADR-001 w16 clause 4 — a lookup against the `(tenant_id, normalized_name)` unique index that links only on exactly one match and otherwise declines **is** "resolved, never guessed", adds no capability, and touches no §1.2 non-goal. The decisive ground is reachability: with software-architect's zero-client-change decision, the one production call site omits `savingsOpportunityId` (`web/src/routes/quotes/index.tsx:274-282`), so clause 1 alone closes A16-8 **by `curl` and by nothing else** and leaves the product in exactly the state NW-21 names. **Fence 1**: A16-8's deterministic close is the **explicit-id** path — no acceptance step may depend on the resolver firing, and a decline is a pass (software-architect's pilot-corpus risk is accepted openly, not hidden). **Fence 2**: when `savingsPropagated` is `null`, no surface may present the outcome as a realized saving and no total may absorb it — a *must not*, so it grows no screen and **ux-ui-designer stays unseated**. **If the council drops clause 2**, NW-21 is recorded as **API-only closed with its UI path still open** and that residual is an item at the head of W17, never implicit. `SupplierResolver` is never called on this path (software-architect's, cited not re-decided).
- **OQ-w16-po-02** — **new, product-owner (acceptance wording) + delivery-manager (mechanism).** Both published branches of **A16-3** were declined by the table (OQ-w16-004's premise withdrawn by three seats; the workflow's API steps deleted), so the acceptance is **re-worded rather than quietly narrowed** (ADR-001 w16 clause 8): *no `X-Role`/`X-Workspace-Role`/`X-User-Id` survives in `.github/`, in `web/openapi/raffa-api.v1.json` or in the capabilities endpoint; Admin catalog rows come from membership; an **Admin resubmits a document on deployed `dev` through the product's own path** and the Worker drives it to a terminal state; the surviving verification job runs green on that tenant.* Nothing the product *does* is removed — the deleted CI path is 401 at its first API step today — and `POST /api/documents/{id}/reprocess` (membership-derived Admin, 202) is a **stronger** close than the CI job was. **Status**: `resolved` for the wording; **`open` for the residual it names** — w16 does **not** restore the **bulk whole-tenant** reprocess. **Assumption in force**: the bulk job is **W17**, its shape is already designed (delivery-manager D5), and its absence is written into this wave's known-gaps record so a `demo` walker does not read it as a w16 regression (w15 clause 7 — a loss is acceptable only when recorded and bounded). No `must` is postponed behind a `could`: NW-31 is `should`. Ref: NW-31, ADR-001 w16 clause 8, `reports/architecture/draft/next/delivery-manager/w16.md` D3–D5.

### Round-2 rulings from the w16 council table (software-architect, 2026-09-14)

Appended, not edited in place. Authority: `reports/architecture/ADR-028-server-side-state.md`
`## Amendment (2026-09-14, wave w16 — table round 2)`; row NW-21 in
`reports/architecture/waves/w16.md`.

- **OQ-w16-sa-01 → `closed`** (NW-21). Product-owner's ruling (ADR-001 w16 clause 7) is **accepted as given**, and this seat confirms it costs what the question claimed: **§D5 needs no edit** — the ADR already shipped clause 2 — so the round-2 footer adds mechanism rather than a decision. **Fence 2 is made structural**: when the resolver declines (zero matches, or two or more), `PropagateAsync` is **never invoked**, so no `savings_opportunity` row updates, `Status` stays `Identified`, no `RealizedSavings` row is inserted, and `SavingsKpiCalculator` — which reads opportunity rows — **cannot** absorb the outcome. "No total absorbs it" is therefore true by construction, with the NW-21 task carrying the proof (a declining capture leaves the Savings KPI payload **byte-identical** before and after). Two checks run at the table: (a) clause 2 adds **no contract delta** — `savingsPropagated` is already a **required** `["boolean","null"]` property carrying **no `description`** (`web/openapi/raffa-api.v1.json:4356,4415-4419`; `web/src/api/generated/schema.ts:504`) — so the wave holds at **five contract paths, two contract tasks, no sixth writer** and constraint 1 is unmoved; (b) clause 2 **falsifies** `backend/README.md:2296-2304` and the doc comment `NegotiationsEndpointExtensions.cs:53`, which both assert the three fields are null together "whenever the caller supplied no `savingsOpportunityId`". Corrected rule: *`savingsPropagated` is `null` exactly when no opportunity was linked — none named **and** none resolved unambiguously*. That correction sits in NW-21's **own single-writer file**, so it adds no task, writer or phase; recorded rather than left to judgement because leaving it would re-create, inside w16, the stale-record defect **NW-31 deletes this wave**. The pilot-corpus risk stays accepted and openly recorded as **Fence 1**: a resolver that never fires is a **pass**, never a failed acceptance.
- **OQ-w16-po-02 → noted, wording accepted; one reachability check supplied** (NW-31). The acceptance wording is product-owner's and is **not re-decided here**; what this seat owes it is whether the path it names exists. It does, **with zero client work**: `web/src/routes/documents/useDocumentsList.ts:240` calls `apiClient.reprocessDocument(workspace.id, documentId)` → `web/src/api/client.ts:1943-1950`, which sends **only `X-Tenant-Id` and the bearer** — **no `X-Role`, no `X-Workspace-Role`, no `X-User-Id`** — and already renders the membership-derived refusal on 403 (`:1967`) and the 202 queued path (`:1961-1963`, ADR-027 §D1). **NW-31's header deletion therefore cannot break the acceptance path clause 8 names**, so the wave's two round-2 rulings are compatible and the re-wording smuggles in no client task. The deferred **bulk whole-tenant** reprocess (W17) is delivery-manager's shape and this seat adds no claim on it beyond ADR-027 w16 clause 1 (no credential-free operator path, no worker-side sweep).
- **OQ-w16-sa-02 → unchanged, `open`** (W17). Nothing in round 2 touched it: the catalog is still served whole, `roleGate` is still presentation-only, and hiding an admin-gated chip from a non-Admin remains **client work**, never a server gate. No task this wave.

### Rulings from the w16 council table (cloud-architect, 2026-09-14)

Appended, not edited in place. This seat is **`yes (conditional)`** in the w16
roster (`w16-requirements.md` §3, NW-31 only) and the condition **resolved to
not involved**, so no ADR is written or amended by this seat. Authority: the
**NW-31 row and the cloud-architect vote row** in
`reports/architecture/waves/w16.md`, plus the `infra/` files cited inline.

- **OQ-w16-cloud-01 → `closed`; this seat's own recommendation withdrawn** (NW-31). The lane draft (`reports/architecture/draft/next/cloud-architect/w16.md`) argued the council must pick A/B/C/D explicitly and put **Path B** in force — a third topic-scoped `Azure Service Bus Data Sender` for the CI deploy SP. The table took **Path D** (the workflow's API steps are deleted; no operator publisher is created), which that same draft named as the only branch leaving the cloud delta at zero. **The draft's factual half is upheld**: there is no credential-free repair, because both Service Bus role assignments are scoped to `var.workload_principal_id` — the container-apps managed identity — at `infra/modules/servicebus/main.tf:71-102`, the module accepts **no CI input at all**, and `raffa-sp-<env>` holds exactly one Terraform-granted role in this product, `Key Vault Secrets User` (`infra/modules/keyvault/main.tf:66-75`). **The draft's recommendation is withdrawn**: it was conditional on a runner-side publisher the table did not take. **Binding consequence — no w16 task may open `infra/`, and no decomposer may mint a role-assignment task from that draft.** The send-right question returns in **W17** with the bulk operator console (delivery-manager D5), where security-architect owes the ruling on whether a CI principal may hold a topic-scoped send right at all. **Status**: `closed`.
- **OQ-w16-cloud-02 → `moot`** (NW-31). It asked what audit actor a **CI-published** message would carry, since a runner-published message has no user actor and NW-32 lands in the same wave. With no CI publisher created, **there is no such message and no such actor**: the only reprocess path in w16 is `POST /api/documents/{id}/reprocess`, which already writes `document.reprocessed` with the Admin's resolved subject. NW-32's sweep is therefore untouched by this lane, and `OPERATOR_USER_ID` is **deleted with the API steps** (delivery-manager D3) rather than needing a provenance vocabulary invented for it. Re-opens in W17 only if the operator console is built. **Status**: `moot` — no task, no decision owed.
- **OQ-w16-dm-03 → answered as far as this tree permits; stays `open` for the operator** (process, delivery-manager's question to this seat). HCP run state is **not readable from this checkout** — this seat reads `.tf` files, not cloud state — so it is asked, not asserted, exactly as delivery-manager framed it. What this seat adds: **(a)** the pending apply's content is `worker_max_replicas 3→5` (`infra/environments/dev/main.tf:164`) plus `ServiceBus__MaxConcurrentCalls = 4` and the KEDA `messageCount = 4` rule (`infra/modules/containerapps/main.tf:424-427,473-483`); **(b)** it is **already authorised** by ADR-005's w15 footer §2 and needs no new decision — nothing for this council to rule; **(c)** it is an **operator action** (W16-A1 gate item (g)), never a wave task, and `git diff origin/main..HEAD -- infra` stays empty; **(d)** it **cannot roll the running image back** — `lifecycle { ignore_changes = [template[0].container[0].image] }` on the worker (`:489-491`), verified at the table, so the natural fear that stalls such an apply is unfounded and must not be re-derived; **(e)** **it does not gate A16-3** — one document reaches a terminal state with or without it; the apply only changes how fast twenty do. A **throughput** judgement taken before the apply measures the previous config and reads as a regression that does not exist. **Assumption in force**: unapplied until the operator confirms the HCP run on `raffa-dev`. operator + cloud-architect.
- **Recorded finding (cloud-architect, no question and no task) — what `demo` inherits at its next promotion.** Written down because it is arithmetic only this seat holds, and because `demo` currently owes three banked promotions (delivery-manager D10). `demo` keeps `worker_max_replicas = 3` (`infra/environments/demo/main.tf:182`) but consumes the **same module**, so it inherits `ServiceBus__MaxConcurrentCalls = 4` ⇒ **12 documents in flight**, against `gpt-5.4` @ **200k TPM** (`demo/main.tf:222`) which serves **both** `extract` and `answer` (`:229-230`) = **≈16.7k TPM per in-flight document** — compared with `dev`'s 20 in flight on `gpt-5.4-nano` @ 300k = **15k**. **The promotion therefore does not under-provision `demo`, and w16 owes it no capacity, SKU or replica change.** Residual named rather than hidden: on `demo`, extraction shares one deployment with interactive Ask, so a large batch during a live pilot walk can throttle **answers**. A 429 on the extraction path is absorbed — `ExtractionTransientException` → abandon → redelivery, bounded by `max_delivery_count = 8` and `default_message_ttl = P1D` with a durable DLQ swept by nothing (`infra/modules/servicebus/main.tf:53-62`) — so the failure mode is **delay, never a lost document**. If it ever bites, the remedy is a `demo`-root `gpt-5.4` capacity bump (200→300) costing **€0.00 in fixed monthly spend**: `DataZoneStandard` capacity is a **rate limit, not a reservation**, and ADR-005's cost lock rejects **provisioned** SKUs, which is a different thing. Ref: ADR-005, ADR-004 amendment 2026-09-09, ADR-016 clause 19.

### Rulings from the w16 council table (security-architect, 2026-09-14)

Appended, not edited in place. Seat is **`yes`** in the w16 roster
(`w16-requirements.md` §3) on **NW-07, NW-08, NW-31, NW-32**. Authority: the
`## Amendment (2026-09-14, wave w16 …)` footers on **ADR-009, ADR-010, ADR-011,
ADR-022** and **ADR-025 §K**, plus the rows in `reports/architecture/waves/w16.md`.

- **OQ-w16-002 → `resolved` (security half)** (NW-08). Software-architect ruled the contract shape; this seat rules the ladder and the property. **The swap is not a weakening and the record must say why**: the "no `?tenantId=`" comment (`AuditEndpointExtensions.cs:25-29`) defends against a *cross-tenant read*, and under `ICallerContext` the tenant is still never trusted — it is a **candidate verified against the token subject's membership before the scope opens** (`CallerContext.cs:135-152`). Enforcement moves from *the absence of a parameter* to *a membership fact*, which is **strictly stronger**; `X-Tenant-Id` is an authorized selector, never an assertion. **S16-4's ladder is verbatim and `/api/audit` gets no bespoke version**: 401 → 400 (missing/non-GUID header) → **404** (well-formed tenant, no live membership — never 403, which is a tenant-existence oracle) → 403 (member, wrong role) → 200, that tenant's rows only. **S16-5**: `WorkspacePrincipalAuthorization` is deleted **whole**. Membership is the gate; `audit_event`'s `ENABLE`+`FORCE`+`tenant_isolation` (`audit.sql:54-56`) is the backstop, and the read runs inside the verified scope (ADR-009 w16 clause 3a). Ref: ADR-011 w16 clause 14, ADR-025 w16 §K.1–§K.2.
- **OQ-w16-003 → concur, `no` — and the reversal is bounded, not free** (NW-08). Product-owner ruled no web audit surface; this seat agrees and records the condition on any later reversal, because the body of that response is **a whole tenant's audit trail, the most sensitive read in the product**. If an audit screen is ever ruled in, it is not "just another screen": it needs its own ADR-020 row and a re-review from this seat covering **pagination bounds** and the rule that it discloses **no actor identifier beyond what the member list already shows**. Recorded now so the reversal cannot land as a routine addition. Ref: ADR-011 w16 clause 14c.
- **OQ-w16-004 → `resolved` by withdrawal of its premise; this seat refuses the token option on its own ground** (NW-31). Three seats converged from independent evidence (SA on ADR-027's text, delivery D1 on the Service Bus role assignments, cloud-architect on `infra/` itself). **The identity-plane answer does not depend on any of it**: an app role on `azuread_application.api` plus an app-only token is precisely the token **ADR-010 w15 §1.1 rejects by design** — it carries **no `oid`**, therefore no membership, therefore no tenant, so it would *also* require a bypass of `CallerContext`'s gate. That is **a second authorization source built for a CI job**. Refused. The alternative — a `workspace_membership` row making a service principal a workspace Admin — is worse: it seats a non-human principal in the table that grants humans, where ADR-025's last-Admin guard and the member list both count it. **→ no new identity, federated credential, app role or secret in w16**; ADR-010 and ADR-015 unchanged, ADR-011 w16 clause 19. **Owed to W17** with the bulk operator console: may a CI principal hold a **topic-scoped Send** right? Inclination **yes — Send only, topic-scoped, never `Manage`, never a SAS key**; the actor such a path writes is already ruled (ADR-011 w16 clause 16). Ref: ADR-022 w16 clause 4.
- **OQ-w16-007 → `resolved` (security half)** (NW-32). **S16-8**: the actor is a **required parameter with no default** on all nine methods, so the placeholder cannot return by omission; `AuditEvent.Actor` stays `required string` — **the column was never the problem, the placeholder exists precisely because null is impossible**. **S16-9**: the two caller-less sites write a reserved **`system:<component>`** principal — the convention already live at `NegotiationOutcomePropagationService.cs:97` — required to (a) contain a character no token subject can produce (`:`), so it can **never collide with an `oid`**, with the reserved prefix **never accepted as a resolved token subject**, and (b) be greppable. **A reserved actor is a fact; `"unattributed"` is a lie.** **S16-10**: `RagAnswerServiceTests.cs:70` is **rewritten, never deleted** — it is the only evidence the behaviour changed. **Recorded consequence nobody had named**: `audit.sql:76-88` rejects UPDATE and DELETE on `audit_event`, so the `"unattributed"` rows already written are **permanent and uncorrectable**. That is correct for an audit trail — **the trigger is never dropped**, not for a backfill, not inside a migration, not temporarily. NW-32 stops the bleeding; it does not clean history. Ref: ADR-011 w16 clauses 15–18.
- **Recorded ruling (no question was open) — the wave's only new tenant table.** NW-13 creates `contract_negotiation_step`. This seat is **not seated on NW-13**, but ADR-009 is this seat's ADR and "RLS on every tenant table" is its rule, so the consequence is written rather than left to a task's judgement. Software-architect's "RLS policy in the same migration" is **ratified and made checkable**: `ENABLE` **+ `FORCE`** + `tenant_isolation` with **both** `USING` and `WITH CHECK` (template three lines away at `documents-contracts.sql:421-423`; a table shipping `ENABLE` without `FORCE` would be the only one in the product, and `FORCE` is what binds the table owner). **The part that is not automatic**: ADR-009 w15 clause 4c's CI guard discovers its tables from `TenantScopedEntity` subclasses of **`DocumentsContractsDbContext` alone** — which is exactly where the module ruling puts this table, **so the free branch is available and w14's `workspace_invitation` escape hatch is not needed**, but only if the entity actually subclasses `TenantScopedEntity` there. Proof of coverage is the check's table count going up. Placed anywhere else it owes a hand-written RLS test carrying clause 4d's trap (**its own unprivileged Postgres role** — Testcontainers hands you a superuser, and RLS constrains neither a superuser nor a table owner). Plus the negative: **a second tenant's ticks never appear**. Ref: ADR-009 w16 clause 2, ADR-028 §D3.
- **Recorded correction (this seat's own error, on the record rather than quietly re-worded).** ADR-009's w15 §6 NW-07 bullet asserts *"`ConversationsEndpointExtensions.TryResolveUserId` does not lower-case, so one person splits across two keys."* **`TryResolveUserId` does not exist anywhere under `backend/src`** — deleted in w15; only a dangling `<see cref>` survives at `ConversationsEndpointExtensions.cs:24`. The **conclusion stands** and is re-derived in ADR-010's w16 footer from the four sites that do exist; only the evidence line was stale. Recorded because a W16 task sent to `TryResolveUserId` finds nothing, concludes the defect is imaginary and closes the item — **the same failure mode as the stale doc comments NW-31 deletes, one level up, in our own records**. Ref: ADR-009 w16 clause 1.
- **OQ-w16-sec-01** — **new, security-architect.** Does the reserved non-human actor string need a **closed enumeration** in ADR-011, or is the documented `system:<component>` convention enough? **Status**: `open` — no task turns on it. **Assumption in force**: the **convention**, with the collision property (S16-9a: contains `:`, never a valid token subject, never accepted as one) as the binding rule — a closed list would need an ADR amendment per new background writer, which is the kind of friction that gets routed around by reusing an existing string wrongly. **Revisit in W17** if the operator console adds a second non-human writer, since two is where a vocabulary starts to drift. Ref: NW-32, ADR-011 w16 clause 16.

### Rulings from the w16 council table (client-architect, 2026-09-14)

Appended, not edited in place. Seat is **`yes`** in the w16 roster
(`w16-requirements.md` §3) on **NW-08, NW-31, NW-11, NW-12, NW-13, NW-21**.
Authority: `reports/architecture/ADR-012-web-stack.md`
`## Amendment (2026-09-14, wave w16 …)` **clauses 21–31**, plus the rows in
`reports/architecture/waves/w16.md`. **ADR-013, ADR-018 and ADR-020 are `none`**
this wave.

- **OQ-w16-ca-01 → `resolved`, ruled by this seat because no other seat can** (NW-11). Product-owner is seated on NW-12/NW-13/NW-21 but **not on NW-11**, so leaving this open means a task decides it by accident. `buildOpportunityRows` (`web/src/routes/savings/savingsViewModel.ts:236-245`) **prepends every tracked renewal action** to the opportunities table as a pseudo-opportunity row with `estimate: NOT_YET_AVAILABLE` (`:223`), the code conceding its own premise at `:210-214`. Today that set is **one session's** actions — a handful. Read back from the server it becomes **every renewal action in the tenant, permanently**, filling the table with rows that are not opportunities and carry no estimate. **A naive store→GET swap ships that flood**, as a side effect of a read-back nobody asked to change Savings. **Ruled**: `buildTrackedOpportunityRow` is **retired** and Savings renders **only real `SavingsOpportunity` rows** — NW-21 supplies the real link the pseudo-row stood in for, and this is also what makes product-owner's **Fence 2** true by construction rather than by a rendering promise (an unlinked outcome has no row to appear in). **The product half is deferred, not answered**: whether Savings should show tracked renewal actions at all is product-owner's, and if the answer is yes it is a **designed section** of that screen — not an estimate-less pseudo-row — which seats ux-ui-designer in **W17**. Ref: ADR-012 w16 clause 30, ADR-028 §D1.
- **OQ-w16-ca-02 → `answered`, accepted, and my own ask withdrawn** (NW-13). I asked for Undo to be **one** composed server call so the client would have no partial-failure state to invent. ADR-028 §D4 ruled **two idempotent writes** and its reason is better than the convenience it refuses: folding the tick-clear into the renewals POST would make a write named "set the renewal action" silently erase another module's rows for any future non-UI caller. **Ratified from the client side, including the ordering**: **ticks `PUT` first, then the action `POST`** — an action still in progress with no ticks is an honest state a user can reach, while `NotStarted` with four ticks is a contradiction no flow produces, so the order is load-bearing rather than arbitrary. The client invents nothing because **both writes are idempotent and both facts are already re-read on mount** (`contract360/index.tsx:93,115`): recovery from a partial failure is a **re-read, never a compensating write**. Ref: ADR-012 w16 clause 26, ADR-028 §D4.
- **OQ-w16-ca-03 → `answered`, accepted** (NW-21). Server-side resolution, so `web/src/routes/quotes/index.tsx:274-282` is **unchanged**, NW-21's client cost is **zero**, there is no picker control and no new empty state, and **ux-ui-designer stays unseated** — the roster consequence this seat flagged before the table rather than at close. The alternative I named (a user-picked candidate list) would have been a new control **and** a new empty state on a screen whose designer is not at this table. Ref: ADR-012 w16 clause 29, ADR-028 §D5.
- **OQ-w16-003 → concur, `no`** (NW-08), and the client half of any reversal is priced so it cannot land as a routine addition: a route, a rail entry in `navItems.ts`, an `ApiClient` wrapper, its `client.test.ts` surface-test row, a loading state and an error state (ADR-018 `:117`) — plus the designer security-architect already requires for a whole tenant's audit trail. That is a **wave item**, never a task addendum. Recorded with the same intent as the security seat's bounded-reversal note, from the client side. Ref: ADR-012 w16 clause 27.
- **OQ-w16-sa-02 → accepted as this seat's, stays `open` for W17** (NW-31). With the capability catalog served whole, an admin-gated capability's example question can surface as a suggestion chip for a non-Admin. **Assumption in force ratified**: acceptable for w16 — per security **S16-11** `roleGate` is **presentation, never authorization**, every action behind it is enforced server-side by membership, and nothing in `web/src` reads `roleGate` today. Hiding the chip is **client presentation work**, deferred; **it must never be re-described as a security fix**, in the W17 item or in a PR. Ref: ADR-012 w16 clause 28, ADR-024 w16 clause 2.
- **Recorded correction — this seat's own lane draft was wrong twice, and both errors were load-bearing.** Written down rather than quietly re-worded, because a task reads the draft. **(a)** The draft's NW-11 shape ask said Contract 360 "does not fetch that list"; it does — `contract360/index.tsx:115` calls `apiClient.getRenewals(workspace.id)` in the same `Promise.all` as `getRenewalPriority` (`:116`), deriving its row at `:227`. So the embedded `savedAction` reaches **all three** surfaces through the **existing** wrapper and **NW-11 adds no `ApiClient` method at all** — the draft would have bought a per-contract read that is already covered. **(b)** The draft's C10 asked the recorded outcome to ride `GET /api/quotes/{id}/assessment`, "which the screen already calls on mount". **The screen never calls it**: the mount path is `recalculateQuoteAssessment` (`quotes/index.tsx:91`), and `client.ts:805-813` records why — the recalculate response is a **superset** of the assessment GET. So C10 would have shipped **a read-back the screen cannot see**, and ADR-028 §D2's decline is correct on **stronger** grounds than the ones it was given (the hazard shape it names is the very call the screen makes). Ref: ADR-012 w16 clauses 22–23.
- **Recorded constraint (no question was open) — `web/src/api/client.ts` is a one-writer-per-phase file.** The draft recorded it as having **no writer** this wave ("no new `ApiClient` method"). That is false: **three** hand-written methods land across two items — `getQuote` (NW-12) and `getNegotiationSteps` + `putNegotiationSteps` (NW-13) — and the file is **hand-written glue**, not generated (`client.ts:1-8`). Two tasks editing it inside one phase is exactly the defect ADR-012 §3 / constraint 1 prevents for `web/openapi/raffa-api.v1.json`, and **`check_single_writer.py` would reject the slice**. **Cheapest resolution for the decomposer**: the theme-B **contract task also owns the wrapper additions**, or NW-12's and NW-13's web halves land in **different phases**. A second collision, also absent from the intake's constraint list: **NW-11 and NW-13 both retire a store inside `contract360/index.tsx`** (`:6`, `:20`) and both touch `handleUndo` (`:261-269`) ⇒ **one task, or different phases**. ADR-028's single-writer list (`:231-237`) is backend-only — correctly, that is software-architect's lane — so these two were this seat's to name. **Still not writers**: `navItems.ts`, `App.tsx`'s router (`:222-236`), `WorkspaceShellApp.tsx`'s route table (`:81-94`). Ref: ADR-012 w16 clause 25, `reports/architecture/waves/w16.md` constraints 1–2.
- **Recorded finding (no question, no task) — product-owner's money fence costs nothing because it is already satisfied.** ADR-001 w16 clause 4 fences w16 against rendering `SavingsKpiSummary.Realized` as a money amount. Verified rather than promised: `savingsViewModel.ts:110` renders it as `countOf(kpis.savingsRealized)` — a **count**, in a meta line beside the identified and in-progress counts — and no client surface renders a realized money figure anywhere. **No w16 client task needs to change it; the fence's whole cost is that none may.** Recorded so the W17 amount item (OQ-w16-po-01) knows the rendering side is untouched ground rather than a fix already half-applied. Ref: ADR-012 w16 clause 29, ADR-001 w16 clause 4.

### Rulings from the w16 council table (delivery-manager, 2026-09-15)

Appended, not edited in place. Seat is **`yes`** in the w16 roster
(`w16-requirements.md` §3) on **NW-31**, plus the process question
**OQ-w16-008**. Authority: the `## Amendment (2026-09-15, wave w16)` footers on
**ADR-016** (clauses 27–35) and **ADR-014** (w16 clauses 1–5), plus the NW-31
row and the delivery-manager vote row in `reports/architecture/waves/w16.md`.
**ADR-015 is `none`** this wave.

- **OQ-w16-008 → `resolved`, ruled; no task** (process). `reports/plan/gates/` holds `e01…e13`, `readiness-gaps` and `w14` — **no `w15.hitl-ok`** — while `scripts/check_slice_prereqs.py:332` requires `<previous>.hitl-ok`, so with `previous: w15` **`run.ps1 -Slice w16` fails its prerequisites before fan-out**. **Ruled**: the operator stamps it at the w16 HITL gate — `python scripts/check_slice_prereqs.py --record-hitl w15` — which is **accurate rather than a rubber stamp** (w15 merged as PR #103 plus #111–#117, and every w15 area was re-verified against this checkout). It is an **operator prerequisite** recorded in `reports/audit/w16-hitl.md`, **never a wave task**. **Root cause is shared with the close-record defect below**, which is why both are fixed by one rule rather than two patches. Ref: ADR-014 w16 clause 3.
- **OQ-w16-dm-01 → `resolved`, ruled: the rename — and this seat's own published fallback is refused** (NW-31). The draft offered a fallback: keep the filename `reprocess-tenant-documents.yml` and rewrite the body, for a cheaper diff. **Refused**, by product-owner (ADR-001 w16 clause 8) and on its own merits: **a file named `reprocess-*` that never reprocesses is a new instance of the stale-record defect NW-31 exists to delete** — the same class as the doc comments and README paragraphs this wave spends itself sweeping, created fresh, in the wave that swept them. **Ruled**: `reprocess-tenant-documents.yml` **deleted**, `verify-tenant-corpus.yml` **added**; one file-set change, one writer, one task. Mechanically safe — nothing `uses:` the old file and its `workflow_call` trigger (`:68`) has **zero callers**. Ref: ADR-016 w16 clause 29, ADR-014 w16 clause 1.
- **OQ-w16-dm-02 → `resolved`; the deferral is recorded as a bounded known gap, not a scope change** (NW-31). R-DOC-07's text names "an operator job for the whole tenant", and the disposition leaves that capability unavailable until W17. Product-owner is **not seated on NW-31** but ruled on A16-3's wording in round 2 and **required the deferral explicitly**: what w16 does not restore — the **bulk whole-tenant** reprocess — **must appear in this wave's known-gaps record**, so a `demo` walker does not read its absence as a regression. The requirement is satisfied in substance by the **live Admin reprocess path** (`useDocumentsList.ts:240` → `POST /api/documents/{id}/reprocess` → membership-derived Admin → 202, working today with a real token) plus the **designed** W17 console. **No `must` sits behind a `could`**; the loss is acceptable because it is recorded and bounded (ADR-016 w15 clause 7). Ref: ADR-016 w16 clauses 29, 31, 35.
- **OQ-w16-dm-03 → concur with cloud-architect; stays `open` for the operator** (process). This seat asked it and **did not assert it**, because HCP run state is not readable from this checkout — only `.tf` files are. Cloud-architect answered it from their own files and this seat adopts the answer unchanged: the pending apply is `worker_max_replicas 3→5` + `ServiceBus__MaxConcurrentCalls = 4`; it is **already authorised** by ADR-005's w15 footer §2 and needs no decision from this council; it is an **operator action** (W16-A1 gate item (g)), never a wave task; it **cannot roll the running image back** (`ignore_changes = [template[0].container[0].image]` on both container apps); and **it does not gate A16-3** — one document reaches a terminal state with or without it, the apply only changes how fast twenty do. **What this seat adds**: the acceptance doc states the applied-or-not fact **as a fact**, and a **throughput** judgement taken before the apply measures the previous config and reads as a regression that does not exist. **Assumption in force**: unapplied until the operator confirms the HCP run on `raffa-dev`. operator + cloud-architect.
- **OQ-w16-dm-04 — new, delivery-manager.** `demo` has run **`demo-v3` since 2026-09-04** and now owes **three banked promotions**: w14's three data-plane steps (ADR-016 clause 19 step 9), w15's flag-flip PR and the invitation walk (step 5, "the one people will skip"), and w16. **When does it clear, and in what order?** **Status**: `open` — no task turns on it, and w16's promotion is deferrable. **Assumption in force**: nothing is promoted until the w16 acceptance walk passes on deployed `dev`; then the operator clears the backlog with **one promotion plus one HCP apply** — the apply being newly owed because the baseline's `containerapps` module change reaches `demo` on promotion, making this **the first of the three that is not flag-only**. Two binding constraints survive any order chosen: **the `demo-v*` number is read at the gate (`git tag -l "demo-v*"`), never assumed**, and **no w16 task may flip `Invitations__Mail__Enabled` or `guest_provisioning_enabled` on `demo`**. operator + delivery-manager + cloud-architect. Ref: ADR-016 w16 clauses 32–33.
- **Recorded correction — this seat's own lane draft was wrong twice, and the second error was load-bearing.** Written down rather than quietly re-worded, because the decomposer reads the draft (the w16 decision record explicitly directs re-entering seats to it). **(a) Clause numbering.** The draft proposed continuing ADR-014's footer at **8**. ADR-016 numbers **continuously** and says so in its own text; **ADR-014 restarts per wave** (w14 clauses 1–5, w15 clauses 1–7, every cross-reference wave-qualified). A footer numbered 8–12 would have produced citations no reader could locate by wave. **(b) The wave order — inside the one artefact this seat owns.** The draft's recommended skeleton put `NW-11 · NW-12 · NW-13` in one phase. **It fails `check_single_writer.py` twice**: on `web/src/api/client.ts` (NW-12's `getQuote` against NW-13's `getNegotiationSteps` + `putNegotiationSteps` — hand-written glue, not generated) and on `web/src/routes/contracts/contract360/index.tsx` (NW-11 and NW-13 both retire a store there, `:6` and `:20`, and both touch `handleUndo` `:261-269`). **Neither collision is in the intake's §5 constraint list**; both came from client-architect at the table. A slice built on the draft would have been rejected by the prereq check **before fan-out**, which is the cheap failure — but a decomposer that "fixed" it by splitting the contract file instead would have shipped the expensive one. Corrected skeleton on disk. Ref: ADR-014 w16 clauses 1, 5; ADR-012 w16 clauses 25, 30.
- **Recorded finding (no question, no task) — the generic close-record filename is the defect, not the stale content.** `reports/execution/wave-close.md` was stamped **before** the merge that delivered w15 and lists **9 of 11 delivered tasks as undelivered**. Content goes stale everywhere; what made this one dangerous is that **it is permanently the newest file with that name**, so it reads as current forever and cost this wave a seven-line banner at the top of `w16-requirements.md` to neutralise. w15's gate stamp (OQ-w16-008) was never written for the same reason — nothing wave-scoped existed to notice its absence. **Rule**: `reports/execution/wave-close-<w>.md`, written after the `integration → main` PR merges, stating the promotion outcome even when there is none. Ref: ADR-014 w16 clause 3, ADR-016 clause 18.

### Opened by the w17 intake (next-intake, 2026-09-15)

Appended, not edited in place. Source: `inputs/next/w17-todo.md`
(sha256 `8afa124…`), audited against `origin/main` @ **`d3d2d24`**. Full text and
evidence in `reports/context/waves/w17-requirements.md` §7; the rows owed a
ruling are listed in `reports/architecture/waves/w17.md`. Every item below
carries an **assumption in force** so no task is blocked. **Only OQ-w17-001 can
change the wave's shape.**

- **OQ-w17-001 — NW-63: does the document viewer ship whole this wave?** `open`. Four pieces are missing and **none** exists on this checkout: a real page render (NW-26 — `PlaceholderDocumentPreviewRenderer` is the only `IDocumentPreviewRenderer`, `Infrastructure/ServiceCollectionExtensions.cs:127`); page **geometry**, which the AI gateway **drops at the wire** (`Raffa.AiGateway/Foundry/Wire/DocumentIntelligenceContracts.cs:24-26` keeps only `PageNumber` + offset `Spans` — no `words`, no `polygon` — and ADR-017 `:103` records `prebuilt-layout` as "available on the account and **not called in V1**"); evidence **columns** for boxes (`ExtractionEvidence.cs:46-48` is `SourceSpan` string + `SourcePage` int; a repo-wide grep of every `*.sql` for `bbox|bounding|polygon` returns **zero** — a migration is required); a **viewer library** (`web/package.json:19-25` lists five runtime deps, none PDF-related — this would be the **first new runtime dependency since ADR-012 was set**); and a **write target for phrase edits** (`PATCH /api/contracts/{id}` is field-name→scalar only, `ContractsEndpointExtensions.cs:78`; there is **no document PATCH/PUT at all**, `DocumentsEndpointExtensions.cs:90-96`). **Assumption in force**: NW-63 **splits** — W17 ships the viewer over **real pages** (NW-26) with jump-to-page anchored on the **existing** `SourcePage` + text-level `SourceSpan` (the mechanism `ClauseHighlight.tsx:28-36` and `EvidencePane.tsx:141-160` already use), plus clause→page navigation from 360 and Review; **true bounding-box overlays and editable OCR phrases become the head of W18**, ahead of this run's four overflow items, because a `must`'s remainder outranks a `should`/`could` overflow. If the council rejects the split it must cut two `should` items instead and name them. client-architect + software-architect + ux-ui-designer + product-owner.
- **OQ-w17-002 — NW-71: raw `≥ 0.90` or rounded `≥ 90 %`?** `open`. The operator stash that carries the web half (**`stash@{2}`**, *"wip: review auto-accept and next-waves-todo (not w14)"* — verified present, touching `web/src/styles/semantics.ts`, `ReviewHeader.tsx`, `ReviewFieldList.tsx`, `reviewViewModel.ts` and three test files) introduces `AUTO_ACCEPT_MIN_PCT = 90` and compares the **rounded** percentage, so `0.895` auto-accepts on the web; the raw file's server rule says `≥ 0.90`. Unreconciled, the screen and the audit row disagree at the boundary. **Assumption in force**: the **server** decides with raw `>= 0.90` and persists the decision; the web **renders the server's decision and never recomputes it** (the raw file's own OpenAPI clause requires exactly that), so the stash's band function becomes presentation-only for the confidence *label*. Seed the web half from the stash rather than re-deriving it. software-architect + client-architect.
- **OQ-w17-003 — NW-71: which thresholds actually retire?** `open`. `0.6`/`0.8` are duplicated across **≥ 5** sites in three services, **deliberately** (`DocumentProcessingPipeline.cs:107-113` and `DocumentQueryService.cs:28-31` both document the duplication as intentional): `StagedExtractionService.cs:78,88,509-510,664,720,772,826,878`; `DocumentProcessingPipeline.cs:114,195,378`; `DocumentQueryService.cs:33,42,46-52`. **The raw file is wrong about which pair matters**: `DocumentQueryService`'s bars are **display-only** (they feed `WeakFactCount` → the "Review N fields" badge) and never touch `DocumentProcessingStatus`; `needs_review` is decided by the **separate** pair in `StagedExtractionService.DetermineDocumentStatus` (`:863-884`, per-stage `:419-422`, applied `:236`) and `DocumentProcessingPipeline.cs:195,:378`. **Changing one pair silently desyncs the badge from the status.** **Assumption in force**: NW-71 retires the review bars as **one** definition consumed by both the badge and the status decision; `DocumentAdmissionOptions.AdmissionThreshold` (`:33`) is a **different decision** (admission — ADR-027 / ADR-024) and is **out of scope**; `Raffa.Quotes/.../QuoteLineExtractionService.cs:48,87` is a separate domain and out of scope. **Sharp edge to record**: `FixtureContractFactExtractor.cs:49` `GoodConfidence = 0.9` renders **Flagged** today and flips to **Accepted** under the new band — demo fixtures change behaviour the moment the threshold moves. software-architect + product-owner.
- **OQ-w17-004 — NW-62: where does benchmark *geography* come from?** `open`. `BenchmarkQuery` requires a non-null supplier **name** and **geography** (`InsightsEndpointExtensions.cs:40-53`, which states the gap verbatim), and `Contract` has neither field. **The name half is now solved** — `ISupplierNameLookup` exists (`Raffa.SharedKernel/Suppliers/ISupplierNameLookup.cs:10`), is implemented (`Raffa.Suppliers.Products/Application/SupplierNameLookup.cs:15`) and is registered (`Infrastructure/ServiceCollectionExtensions.cs:41`). Geography is the remaining gap. **Assumption in force**: geography is derived from the **workspace country** (the w14 profile field), used as the contract's market and **labelled representative**, never invented per contract and never a paid lookup (ADR-001 §1.2 and the R3/R4 fixture-adapter gate). If the council prefers a per-contract geography, that is a schema change and NW-62 grows a migration. software-architect + product-owner.
- **OQ-w17-005 — NW-62: consume the existing `/strategy` endpoint, or compute a second answer?** `open`. `GET /api/contracts/{id}/strategy` (`InsightsEndpointExtensions.cs:64`) already returns a `StrategyPack` with **`WhenYouMustMove`** and **`WhereYouCanPush`** sections (`Raffa.Insights/Contracts/StrategyPack.cs:26-31`, `:51-54`), and it is already in the generated client (`schema.ts:928-929`, `getContractStrategy` at `:724`) — **no Contract 360 component imports it** (`contract360/index.tsx` references only `buildAnswers`/`AnswersBand`/`WhyClauses`). **Assumption in force**: NW-62 **consumes the existing endpoint** rather than computing a parallel answer in the view model — two answer sources on one screen is exactly the divergence ADR-012 forbids, and the unconsumed endpoint is the cheaper half of a `must`. software-architect + client-architect.
- **OQ-w17-006 — NW-72: which amount is "verified"?** `open`. **The raw file (and `SavingsKpiCalculator.cs:50-52`'s own comment) is stale**: `Domain.RealizedSavings` is no longer missing — it exists (`Raffa.Savings/Domain/RealizedSavings.cs:26`, `Amount` `:45`, `Currency` `:51`), is persisted (`Infrastructure/SavingsDbContext.cs:29`) and is written (`SavingsOpportunityService.cs:325`) — but it is **write-only**, with exactly one `.Add` and no reader anywhere, which is why the KPI still sums the opportunity's own estimate (`SavingsKpiCalculator.cs:86`, `:103-104`). `SavingsOpportunityResult.RealizedAmount` is non-null only on the PATCH that just recorded it (`:10-19`) and has **zero** web consumers. **Assumption in force**: the KPI reads **`RealizedSavings` rows grouped by currency** (the audit-tracked record), `RealizedAmount` stays PATCH-only, and the stale comments at `SavingsKpiCalculator.cs:50-52` and `Domain/SavingsOpportunityStatus.cs:37-44` are swept by the same task. ADR-001 w16 clause 4's money fence is **lifted** here rather than violated. software-architect + product-owner.
- **OQ-w17-007 — NW-23: supplier category, contract type, or neither?** `open`, and **travels with the item to W18** (NW-23 overflows this wave). `PortfolioFilter.cs:11-18`'s deferral comment is **partly stale**: it says Suppliers/Products "is still an empty scaffold project with no domain types", but that context is now populated and **already has a `Category`** (`Raffa.Suppliers.Products/Domain/Supplier.cs:36`, persisted `SupplierConfiguration.cs:32`). What still holds is the architecture boundary — `DependencyDirectionTests.cs:63` pins `Raffa.Documents.Contracts` to `[Raffa.SharedKernel, Raffa.AiGateway]`, so the join must happen in `Raffa.Api`, the way `supplierName` already does (`PortfolioEndpointExtensions.cs:133-146`). `Contract` itself has **no** category, only `ContractDocumentType` (`Contract.cs:21`), a document-kind axis explicitly not a spend category. **Assumption in force**: if NW-23 lands, it filters by **supplier category** joined in the host, and screens-v2 §6's "Type" column stays the document type; no fake enum ships. product-owner + software-architect.
- **OQ-w17-008 — NW-73: may a CI principal hold a Service Bus Send right?** `open`, pre-answered with an **inclination, not a ruling** (ADR-016 w16 clause 31 `:770-784`, ADR-022 `:245-251`): *yes — Send only, topic-scoped, never `Manage`, never a SAS key*. Verified that the grant is genuinely new: `infra/modules/servicebus/main.tf:71-87` grants `Azure Service Bus Data Sender` **topic-scoped** but to `var.workload_principal_id`, which is the **Container Apps managed identity** (`modules/identity/main.tf:36-37`, wired `environments/dev/main.tf:115`); the CI principal is a **different** principal looked up by literal client id (`environments/dev/main.tf:223-225`, ADR-015 note `:215-222`) whose only data-plane grant today is **Key Vault Secrets User** (`modules/keyvault/variables.tf:35`). **No `azurerm_role_assignment` anywhere in `infra/` gives `raffa-sp-<env>` any Service Bus role.** **Assumption in force**: the inclination is adopted, the assignment is one topic-scoped Sender role on the CI principal, and the console writes actor `system:<component>` (ADR-011 w16 clause 16 / NW-32). **Also recorded**: both Sender and Receiver currently land on the *same* workload identity despite `Raffa.Api.csproj`/`Raffa.Messaging.csproj` prose saying "Sender on the API and Receiver on the Worker" — harmless today, misleading when reasoning about least privilege. security-architect + cloud-architect + delivery-manager.
- **Operator prerequisite, not a question and not a task — `reports/plan/gates/w16.hitl-ok` does not exist.** `reports/plan/gates/` holds `e01…e13`, `readiness-gaps`, `w14` and `w15`; `scripts/check_slice_prereqs.py:329-338` requires `<previous>.hitl-ok`. With `previous: w16`, **`run.ps1 -Slice w17` fails its prerequisites before fan-out**. This is the **exact recurrence of OQ-w16-008**, which was raised for the missing `w15.hitl-ok` and closed by an operator stamp rather than by a rule — so the class of defect ADR-014 w16 clause 3 named has now fired twice. **Assumption in force**: the operator stamps it at the w17 HITL gate — `python scripts/check_slice_prereqs.py --record-hitl w16` — which is accurate rather than a rubber stamp, since w16 merged as PR #129 (`d3d2d24`) and every w16 area was re-verified against this checkout. operator + delivery-manager.

### Opened at the w17 table, assigned by the w17 decomposition (next-remediator, 2026-09-15)

Appended, not edited in place. The full row is
`reports/architecture/waves/w17.md:417`; this entry records only **who
discharges it and how**, because the decomposition check found the clause had a
code consequence and no implementer.

- **OQ-w17-cl-03 — NW-63, NW-66: does the post-login return URL preserve a query string?** `open`, and **now assigned**: both halves belong to `E22/F03/US01/T01` (`task-01-document-viewer-route.md`, phase 3), steps 12–13 and AC-11/AC-12. A citation deep-link (`/documents/:documentId/viewer?page=3&clause=…`) is the **first** route in this product whose query string carries meaning a user can arrive on **cold**; client-architect refused to assert either way and owed a **check**, and ux-ui-designer ruled the screen state **whether or not the check passes**. **Assumption in force**: the return URL's behaviour is **unknown on this base** and is **not assumed** — the task runs the signed-out deep-link + OIDC round trip and records the landed URL in its PR body. Two rules bind regardless: **(a)** the viewer never renders page 1 *as though it were the citation* — it shows page 1 in the ordinary page state and says the **citation could not be restored** (owed unconditionally, since a truncated paste loses a query too); **(b)** if the query is dropped, the repair is in the **auth landing**, amending **ADR-012's w14 footer** (`:189-285`) in a later wave — **never** in the viewer, and **never** by stashing the query in `sessionStorage`/`localStorage`, which is the wrong layer *and* the persistence rule's own prohibition. The operator closes this row from that PR body. client-architect + ux-ui-designer + operator. Ref: ADR-020 w17 §16; ADR-018 w17 clause 12 (the *wrong destination with no error* class).
