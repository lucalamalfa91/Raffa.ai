---
id: E18/F03/US01/T01
type: task
story: us-01-role-headers-retired
wave: w15
status: queued
target_repo: raffa-backend
---

# task-01-role-headers-retired — Delete the capabilities reader, the contract parameter and the workflow headers

> **Queued for W16.** No entry in `reports/plan/slices/w15.yaml`; not fanned out
> this wave.

## Coding objective

Finish the retirement ADR-022's w14 footer scheduled. Delete `X-Role` from
`CapabilitiesEndpointExtensions.cs` and resolve the catalog's Admin rows from the
membership role the token regime supplies. Remove the header **parameter** from
`web/openapi/raffa-api.v1.json` (`:5128`, `:5131` — the only header parameter in
the document) and correct the stale description at `:1142`, which still tells
readers that `DELETE /api/documents/{id}` "carries the role as
`X-Workspace-Role`" when that endpoint's gate has been membership-based since
w14. Drop both headers from `reprocess-tenant-documents.yml` and, in the same
change, design how that job authenticates at all — w15 left it **out of service**
precisely so this wave opens the file once.

## Parent story AC covered

- AC-1 … AC-6 (all of them — this is the story's only task)

## Files to create or modify

| Path | Change |
|------|--------|
| `backend/src/Raffa.Api/CapabilitiesEndpointExtensions.cs` | delete `RoleHeaderName` (`:54`) and `CallerIsAdmin` (`:77-80`); `:64-67`'s gate reads the membership role |
| `web/openapi/raffa-api.v1.json` | remove the `X-Role` header parameter (`:5128`, `:5131`); correct the stale `:1142` description |
| `web/src/api/generated/schema.ts` | regenerated by `npm run generate:api` |
| `.github/workflows/reprocess-tenant-documents.yml` | drop `X-Role` / `X-Workspace-Role` (`:42-43`, `:205-206`, `:256-257`, `:274`) and give the job a working authentication path — or convert it to a worker-side enqueue, which NW-27 makes possible |
| `docs/ask-v2-acceptance.md` | `:71`, `:357`, `:600` stop describing the headers |
| `backend/tests/Raffa.Api.Tests/` | the tests below |

Passata 2 cwd is the per-task git worktree of the product clone.

## Context the implementer needs

`Closes: NW-31`.

Not at the w15 table — queued, with **`seats: none`**: ADR-022's w14 footer
already decided it. Evidence:
`reports/context/waves/w15-requirements.md` §2, NW-31 row. The binding w15
decision is `reports/architecture/waves/w15.md` **NW-05**, delivery-manager cell,
and **OQ-w15-dm-04**.

- **Architecture decisions in force**: **ADR-022** w14 footer (this is the
  retirement, not a new decision); **ADR-026**; **ADR-016** w15 clause 21.
- **This wave opens `reprocess-tenant-documents.yml` once.** From the w15 code
  merge its first authenticated call, `GET /api/documents` (`:202-207`), returns
  401 and the step exits 1 with `::error::` (`:208-211`) **before any write** —
  so there is no partial reprocess and no unattributed audit row, and the file
  predicted this itself at `:48`. Do not patch it twice.
- **This task changes a CI YAML file.** That is expected here and was explicitly
  excluded from w15, whose planned CI-YAML set was **zero**.

## Definition of done

- [ ] `dotnet build backend/Raffa.slnx` exit 0
- [ ] `dotnet test backend/tests/Raffa.Api.Tests` exit 0 — `-H "X-Role: Admin"` changes **no** response; the capability catalog's Admin rows follow membership
- [ ] `grep -rn "X-Role\|X-Workspace-Role" backend/src/ web/openapi/ .github/ docs/` returns **no match**
- [ ] `cd web && npm run build` exit 0
- [ ] The reprocess workflow runs end to end against `dev` (or is demonstrably converted to an enqueue) — a task that only deletes headers leaves the job still broken

## Tests required

| Level | What it proves | Where |
|-------|----------------|-------|
| API | no role header changes any response, on capabilities or anywhere else | `backend/tests/Raffa.Api.Tests/` |
| API | the capability catalog's Admin rows follow the membership role | `backend/tests/Raffa.Api.Tests/` |
| manual | the reprocess job works again, or its replacement does | the W16 acceptance doc |

## Open questions blocking this task

- How the reprocess job authenticates after NW-05. **Assumption in force**: W16 designs it; the worker-side enqueue is the cheaper option and adds no credential. This one **does** gate the task's fifth DoD box.

## Wave-spec entry

**None — `status: queued`.** When W16 schedules it:

```yaml
- id: E18/F03/US01/T01
  prompt: reports/workitems/epic-18-api-authentication/feature-03-interim-posture-retirement/us-01-role-headers-retired/tasks/task-01-role-headers-retired.md
  produces: [role-headers-retired]
  depends_on: []
  effort: M
  layer: backend
  status: live
```
